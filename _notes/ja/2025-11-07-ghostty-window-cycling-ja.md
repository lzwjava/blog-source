---
audio: false
generated: true
image: false
lang: ja
layout: post
model: x-ai/grok-4-fast
title: Ghostty ウィンドウ切り替えショートカット
translated: true
---

確かにその通りで、GhosttyでCmd + `（チルダ/バッククォート）を使ってウィンドウを循環させる操作は、スナップ感があって直感的ですよね。特にタブに全てを詰め込むのではなく、軽量で独立したセッションとして扱う場合には。タブバーの煩雑さを気にせず、循環する流れが良いです。デフォルトのCmd + Shift + [ / ] でのタブ移動（またはCmd + [ / ] でのペイン移動）は、Macでは確かに使いづらく、Cmdに加えてShiftも押す必要があり、ブラケットキーは親指が届きにくいです。

おっしゃる通り、単一のCmd + キーの組み合わせの方が、ほとんどの人にとってより洗練されています——エルゴノミックで、素早く押せ、複数の修飾キーを組み合わせるよりもミスが少ないです。Ghosttyのデフォルト設定では、タブ選択（Cmd + 1–8で特定のタブに直接移動）に既にその思想が取り入れられていますが、タブよりもウィンドウをメインで使うのであれば、循環操作をさらにシンプルなもの、例えばCmd + J/K（Vimスタイル）やCmd + ←/→（前/次のウィンドウ）に再マップすることもできます。設定ファイルでカスタマイズはとても簡単で、以下のような行を追加するだけです:

```
keybind = "cmd-j", "window:previous"
keybind = "cmd-k", "window:next"
```

これなら、修飾キー1つ + キー1つで、面倒な操作はありません。主にどのような設定で使っていますか——複数のスペースにまたがる複数のGhosttyウィンドウですか、それとも1つのデスクトップに全て固定していますか？もし特定のアクション（例えば素早いペイン切り替えなど）をバインドしたいのであれば、さらに調整を考えられます。