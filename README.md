# lzwjava.github.io

```shell
gem install jekyll bundler

jekyll new myblog

cd myblog

bundle install

bundle exec jekyll serve

bundle add webrick

jekyll serve

# others
rougify style github > assets/css/syntax.css


```