---
audio: false
generated: true
image: false
lang: ar
layout: post
title: مقارنة لقطات الشاشة بين PIL وPyAutoGUI
translated: true
type: note
---

كلا أداتي **`ImageGrab` من مكتبة PIL** وأدوات **لقطة الشاشة في PyAutoGUI** تتيح لك التقاط الشاشة، لكنهما تختلفان في الاستخدام، والميزات، والدعم عبر المنصات.

---

## 1. PIL (Pillow) – `ImageGrab`

`ImageGrab` هو جزء من **Pillow (النسخة الحديثة من PIL)**. يسمح لك بالتقاط الشاشة أو جزء منها.

### آلية العمل

* في **أنظمة Windows وmacOS**، يستخدم واجهات برمجة التطبيقات الأصلية (GDI على Windows، وQuartz على macOS) لالتقاط البكسل.
* في **Linux**، لا يتم دعمه افتراضيًا (يحتاج إلى مكتبات إضافية مثل `xwd`).

### مثال للاستخدام

```python
from PIL import ImageGrab

# التقاط الشاشة بالكامل
screenshot = ImageGrab.grab()
screenshot.save("screenshot.png")

# التقاط منطقة محددة (يسار، أعلى، يمين، أسفل)
region = (100, 100, 500, 500)
cropped_screenshot = ImageGrab.grab(bbox=region)
cropped_screenshot.show()
```

### الإيجابيات

* بسيط، وهو جزء من Pillow (لا توجد تبعيات إضافية).
* جيد عندما تكون تعمل بالفعل مع Pillow لمعالجة الصور.

### السلبيات

* لا يوجد دعم لنظام Linux افتراضيًا.
* مرونة أقل (لا يوجد وعي بشاشات متعددة).

---

## 2. PyAutoGUI – `screenshot()`

PyAutoGUI هو أكثر عمومية لأتمتة المهام (التحكم بالماوس/لوحة المفاتيح + التقاط الشاشة).

### آلية العمل

* يستخدم **Pillow داخليًا**.
* يستخدم واجهات برمجة التطبيقات الخاصة بنظام التشغيل (عبر المنصات: Windows، macOS، Linux).
* يمكنه إرجاع كائن `Image` من Pillow مباشرة أو حفظه في ملف.

### مثال للاستخدام

```python
import pyautogui

# التقاط الشاشة بالكامل
screenshot = pyautogui.screenshot()
screenshot.save("screenshot.png")

# التقاط منطقة محددة (يسار، أعلى، عرض، ارتفاع)
region_screenshot = pyautogui.screenshot(region=(100, 100, 400, 400))
region_screenshot.show()
```

### الإيجابيات

* يعمل عبر مختلف المنصات (Windows، macOS، Linux).
* سهل التكامل مع نصوص الأتمتة (التحكم بالماوس/لوحة المفاتيح).
* يمكنه التقاط مناطق محددة مباشرة.

### السلبيات

* أبطأ قليلاً من `ImageGrab` لأنه يضيف طبقة تجريد أعلى.
* تبعية إضافية (`pip install pyautogui`).

---

## أيّها يجب أن تستخدم؟

* **إذا كنت تحتاج فقط لقطات الشاشة وأنت تستخدم Windows/macOS** → `ImageGrab` خفيف الوزن وسريع.
* **إذا كنت تحتاج أيضًا للأتمتة أو الدعم على نظام Linux** → `pyautogui` أفضل.

---

👉 هل تريد مني أيضًا أن أوضح لك **كيفية قياس الأداء** للفرق بين `ImageGrab` و `pyautogui.screenshot()` باستخدام كود Python؟