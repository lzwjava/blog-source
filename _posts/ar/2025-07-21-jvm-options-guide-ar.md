---
audio: false
generated: true
image: false
lang: ar
layout: post
title: دليل سريع مصنف لخيارات آلة جافا الافتراضية
translated: true
type: note
---

في عالم JVM، هناك **خيارات عديدة** يمكنك ضبطها، وتعتمد المجموعة الدقيقة على تنفيذ JVM (مثل HotSpot، OpenJ9، GraalVM، إلخ) والإصدار. بشكل عام، تنقسم خيارات JVM إلى هذه الفئات:

---

## 1. الخيارات القياسية

هذه خيارات مستقرة وعابرة للإصدارات، موثقة رسمياً ومضمونة العمل. أمثلة:

* `-classpath` / `-cp` → تحديد مسار الفئات (classpath).
* `-Dproperty=value` → تعيين خصائص النظام.
* `-version` → عرض إصدار JVM.
* `-Xms` / `-Xmx` → حجم الكومة (heap) الابتدائي والحد الأقصى.
* `-ea` → تمكين التأكيدات (assertions).

---

## 2. الخيارات غير القياسية (`-X`)

هذه خاصة بـ JVM، وغير مضمونة أن تبقى مستقرة. أمثلة (لـ HotSpot):

* `-Xint` → تفسير فقط (بدون JIT).
* `-Xcomp` → ترجمة جميع الطرق عند أول استخدام.
* `-Xbatch` → تعطيل الترجمة في الخلفية.
* `-Xss512k` → تعيين حجم كومة المهام (thread stack).

---

## 3. الخيارات المتقدمة (`-XX`)

توفر هذه الخيارات ضبطاً دقيقاً لعامل جمع القمامة (GC)، وـ JIT، وسلوك وقت التشغيل. أمثلة:

* `-XX:+UseG1GC` → تمكين جامع القمامة G1.
* `-XX:+PrintGCDetails` → تسجيل نشاط GC.
* `-XX:MaxMetaspaceSize=256m` → تحديد الحد الأقصى لمساحة ما بعد البيانات (Metaspace).
* `-XX:+HeapDumpOnOutOfMemoryError` → تصوير الكومة (dump) عند حدوث خطأ نفاد الذاكرة (OOM).
* `-XX:+UnlockExperimentalVMOptions` → السماح بالخيارات التجريبية.
* `-XX:+UseStringDeduplication` → تقليل استخدام الذاكرة من خلال إزالة تكرار السلاسل النصية.

---

## 4. التشخيصية والتجريبية

بعض الأعلام مخفية وتتطلب فتح القفل:

* `-XX:+UnlockDiagnosticVMOptions` → تمكين أعلام التشخيص.
* `-XX:+UnlockExperimentalVMOptions` → السماح بالضبط التجريبي.

ثم يمكنك استخدام خيارات مثل:

* `-XX:+PrintFlagsFinal` → سرد جميع خيارات JVM وقيمها.
* `-XX:+LogCompilation` → تسجيل نشاط مترجم JIT.

---

## 5. أدوات للعمل مع خيارات JVM

* **`java -X`** → يعرض الخيارات غير القياسية لبناء JVM الخاص بك.
* **`java -XX:+PrintFlagsFinal`** → يطبع جميع الخيارات المعترف بها مع القيم الافتراضية والحالية.
* **`jcmd <pid> VM.flags`** → عرض أعلام JVM لعملية قيد التشغيل.
* **`jcmd <pid> VM.uptime`**, `jcmd <pid> VM.system_properties` → فحص وقت التشغيل.
* **`jps` + `jinfo`** → سرد عمليات Java وطباعة خيارات JVM.
* **VisualVM / JConsole** → مراقبة الكومة، وـ GC، ومعاملات وقت التشغيل.

---

👉 طريقة عملية لاستكشاف بيئتك هي تشغيل:

```bash
java -XX:+PrintFlagsFinal | grep GC
```

لمشاهدة جميع الخيارات المتعلقة بـ GC، أو ببساطة:

```bash
java -X
```

للتحقق من الأعلام غير القياسية المتاحة.

---

هل تريد مني أن أعطيك **ورقة غش مصنفة لأكثر خيارات JVM فائدة** (خاصة بـ GC، الذاكرة، التصحيح، ضبط الأداء) حتى لا تضطر لتحليل القائمة الضخمة لـ `PrintFlagsFinal`؟