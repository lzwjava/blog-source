---
audio: false
generated: true
image: false
lang: ar
layout: post
title: تحويل ماركداون إلى شرائح في دقائق
translated: true
type: note
---

يتيح لك Marp CLI تحويل ملفات Markdown إلى شرائح (HTML، PDF، PPTX). وهو مفيد بشكل خاص إذا كنت تريد كتابة العروض التقديمية بـ Markdown عادي بأقل جهد في التنسيق.

إليك دليلًا خطوة بخطوة:

---

### 1. تثبيت Marp CLI

أنت بحاجة إلى Node.js أولاً.

```bash
npm install -g @marp-team/marp-cli
```

تحقق من التثبيت:

```bash
marp --version
```

---

### 2. اكتب عرضًا تقديميًا بـ Markdown

أنشئ ملفًا، على سبيل المثال `slides.md`.

```markdown
---
marp: true
theme: default
paginate: true
---

# مرحبًا، Marp!

---

## الشريحة 2

- نقطة 1
- نقطة 2
```

النقاط الرئيسية:

* `---` (YAML frontmatter) يُفعِّل Marp والإعدادات.
* `---` (بين المحتوى) يُفصل بين الشرائح.

---

### 3. معاينة في المتصفح

شغّل خادمًا محليًا مع إعادة تحميل تلقائية:

```bash
marp -p slides.md
```

افتح [http://localhost:8080](http://localhost:8080) في متصفحك.

---

### 4. تصدير الشرائح

أنتِج صيغًا مختلفة:

* HTML (مستقل):

  ```bash
  marp slides.md -o slides.html
  ```

* PDF:

  ```bash
  marp slides.md -o slides.pdf
  ```

* PowerPoint (PPTX):

  ```bash
  marp slides.md -o slides.pptx
  ```

---

### 5. خيارات مفيدة

* **مراقبة التغييرات** (إعادة بناء تلقائية):

  ```bash
  marp -w slides.md
  ```

* **تعيين السمة** (default، gaia، uncover، أو CSS المخصص الخاص بك):

  ```bash
  marp --theme-set ./my-theme.css slides.md -o slides.html
  ```

* **تضمين ملاحظات المتحدث**:

  ```markdown
  ---
  marp: true
  ---

  # العنوان

  <!--
  هذه هي ملاحظات المتحدث
  -->
  ```

---

### 6. التكامل مع المحرر

* VSCode لديه [امتداد Marp](https://marketplace.visualstudio.com/items?itemName=marp-team.marp-vscode).
* يعرض معاينات حية أثناء تحرير Markdown.

---

👉 مع هذا الإعداد، يمكنك الانتقال من ملف `.md` عادي إلى عرض تقديمي مصقول في دقائق.

هل تريد مني أن أعطيك **قالب ملف Markdown جاهز للاستخدام** (يحتوي على شريحة عنوان، وجدول أعمال، ومحتوى، وشريحة ختامية) حتى تتمكن من البدء على الفور؟