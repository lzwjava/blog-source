---
audio: false
generated: true
image: false
lang: ar
layout: post
title: العبارة مقابل العبارة القابلة للاستدعاء في جي دي بي سي
translated: true
type: note
---

في جافا JDBC (والإطارات الأخرى المبنية عليه مثل Hibernate أو Spring)، فإن مفاهيم **`Statement`** و **`CallableStatement`** هما واجهتان تستخدمان لإرسال أوامر SQL إلى قاعدة البيانات، لكنهما تخدم أغراضًا مختلفة.

---

### 1. `Statement`

* **الغرض**: يُستخدم لتنفيذ استعلامات SQL بسيطة بدون معاملات.
* **طريقة الاستخدام**: تنشئ كائن `Statement` من `Connection` وتستخدمه لتنفيذ نصوص SQL.
* **الطرق النموذجية**:

  * `executeQuery(sql)` → لاستعلامات `SELECT`، تُرجع `ResultSet`.
  * `executeUpdate(sql)` → لـ `INSERT`، `UPDATE`، `DELETE`.
  * `execute(sql)` → عامة، تُرجع `true` إذا كانت النتيجة `ResultSet`.

**مثال**:

```java
Connection conn = DriverManager.getConnection(url, user, pass);
Statement stmt = conn.createStatement();
ResultSet rs = stmt.executeQuery("SELECT id, name FROM employees");

while (rs.next()) {
    System.out.println(rs.getInt("id") + " " + rs.getString("name"));
}
```

* **متى تُستخدم**: لعبارات SQL الثابتة التي لا تحتوي على معاملات.
* **العيب**: يمكن أن تؤدي إلى ثغرة حقن SQL إذا استخدمت مع مدخلات مستخدم ديناميكية.

---

### 2. `CallableStatement`

* **الغرض**: يُستخدم لتنفيذ **الإجراءات المخزنة** في قاعدة البيانات.
* **طريقة الاستخدام**: تُنشأ باستخدام `Connection.prepareCall()` وتسمح باستدعاء روتينات قاعدة البيانات باستخدام معاملات **IN** و **OUT** و **INOUT**.
* **الطرق النموذجية**:

  * `setInt`، `setString`، إلخ → لتعيين معاملات الإدخال.
  * `registerOutParameter` → لتسجيل معاملات الإخراج.
  * `execute()` أو `executeQuery()` → لتشغيل الإجراء المخزن.
  * `getInt`، `getString`، إلخ → لاسترداد قيم الإخراج.

**مثال**:

```java
Connection conn = DriverManager.getConnection(url, user, pass);
CallableStatement cstmt = conn.prepareCall("{call getEmployeeName(?, ?)}");

// تعيين معامل IN
cstmt.setInt(1, 101);

// تسجيل معامل OUT
cstmt.registerOutParameter(2, java.sql.Types.VARCHAR);

// التنفيذ
cstmt.execute();

// الحصول على معامل OUT
String empName = cstmt.getString(2);
System.out.println("Employee name: " + empName);
```

* **متى تُستخدم**: للإجراءات/الدوال المخزنة التي تغلف المنطق داخل قاعدة البيانات.
* **المزايا**: تدعم المنطق المعقد، النتائج المتعددة، ومعاملات الإدخال/الإخراج.

---

### الاختلافات الرئيسية (بدون جدول)

* `Statement` مخصص لـ **SQL الثابت** (استعلامات مخصصة).
* `CallableStatement` مخصص لـ **الإجراءات/الدوال المخزنة**.
* `Statement` ينفذ SQL مباشرة، بينما `CallableStatement` يتفاعل مع روتينات قاعدة البيانات التي قد تُرجع مجموعات نتائج ومعاملات متعددة.
* `Statement` بسيط لكنه أقل أمانًا مع المدخلات الديناميكية، بينما `CallableStatement` يوفر طريقة منظمة لاستدعاء المنطق القابل لإعادة الاستخدام المخزن في قاعدة البيانات.

---

👉 إذا كنت ترغب، يمكنني أيضًا شرح كيف يتناسب **`PreparedStatement`** بين هذين الاثنين (SQL آمن ومعامل، لكن ليس للإجراءات المخزنة). هل تريد مني إضافة ذلك؟