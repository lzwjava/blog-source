---
audio: false
generated: true
image: false
lang: ar
layout: post
title: البنية متعددة اللغات في ويندوز
translated: true
type: note
---

لم يُبنى نظام Windows 10 Enterprise بلغة واحدة فقط — فهو نظام تشغيل كبير يتكون من العديد من المكونات، كُتب كل منها بلغات برمجة مختلفة تم اختيارها بناءً على نقاط قوتها. إليك التفصيل:

**النظام الأساسي والنواة**

* مكتوبة بشكل أساسي بلغة **C** و **C++**.
* الأجزاء الحساسة للأداء (مثل نواة Windows NT، ومدير الذاكرة، ونظام الإدخال/الإخراج) هي في الغالب بلغة C، مع استخدام بعض لغة التجميع للعمليات منخفضة المستوى جدًا.

**خدمات النظام وواجهات برمجة التطبيقات (APIs)**

* لغتي **C و C++** تهيمن هنا أيضًا (مثل Win32 API، وDirectX، ومكتبات النظام).
* بعض أجزاء إطار عمل .NET وواجهات برمجة التطبيقات الأحدث مكتوبة بلغة **C#**.

**واجهة المستخدم (Shell، Explorer، إلخ)**

* تاريخيًا، كانت تُستخدم **C++ مع COM (نموذج كائن المكون)**.
* طبقات الواجهة الحديثة، مثل أجزاء من تطبيقات UWP (المنصة العالمية لنظام Windows) وإعدادات Windows 10، مكتوبة بلغة **C#، وXAML، وJavaScript**.
* عناصر Fluent Design وواجهة مستخدم XAML تعمل بواسطة C++ في الأساس ولكن يتم عرضها عبر لغات عالية المستوى.

**الإدارة، البرمجة النصية، والأتمتة**

* **PowerShell** (مبني على .NET ومكتوب بشكل كبير بلغة **C#**).
* العديد من أدوات النظام تعرض أيضًا واجهات برمجة نصية.

**الأمان وبرامج التشغيل**

* برامج تشغيل وضع النواة: في الغالب **C**، وبعض **C++**، ولغة التجميع.
* برامج تشغيل وضع المستخدم: غالبًا **C++**، وأحيانًا **C#** للأدوات عالية المستوى.

لذا، للتلخيص:

* **C و C++** هما الأساس.
* **C# ولغات .NET** تُستخدم على نطاق واسع في أدوات الإدارة والتطبيقات.
* **لغة التجميع** لا تزال موجودة للإقلاع منخفض المستوى والروتينيات الحساسة للأداء.
* **XAML/JavaScript** لأجزاء من مجموعة واجهة المستخدم الحديثة.

👉 هل تريد مني أن أعطيك **خريطة طبقية** لنظام Windows 10 Enterprise (النواة، برامج التشغيل، خدمات النظام، واجهة المستخدم، أدوات الإدارة) توضح اللغات المسيطرة على كل طبقة؟ قد يجعل ذلك الصورة أكثر وضوحًا.