---
audio: false
date: 2025-09-09
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: إنشاء ملفات خريطة الموقع لجيكل
translated: true
type: note
---

ملف **sitemap.xml** هو ملف XML يخبر محركات البحث مثل Google و Bing بالصفحات الموجودة على موقعك الإلكتروني. فهو يساعد العناكب على اكتشاف وهيكلة موقعك بشكل أكثر كفاءة. بالنسبة لمدونة Jekyll، هذا يعني أن محركات البحث يمكنها العثور على جميع مقالاتك وصفحاتك وترجماتك (مثل `index-zh.html`، `index-fr.html`، إلخ) حتى لو لم تكن مرتبطة في كل مكان.

### ما الذي يفعله خريطة الموقع

*   يسرد جميع عناوين URL المهمة لموقعك.
*   يوفر بيانات وصفية مثل تاريخ آخر تعديل (`<lastmod>`)، وتكرار التغيير (`<changefreq>`)، والأولوية (`<priority>`).
*   يحسن SEO من خلال ضمان عدم تفويت العناكب لأي صفحات.

### كيفية إنشائه لـ Jekyll

لديك خياران:

#### 1. استخدام الإضافة `jekyll-sitemap` (موصى به)

1.  في ملف `Gemfile` لمشروع Jekyll الخاص بك، أضف:

    ```ruby
    gem 'jekyll-sitemap'
    ```
2.  في ملف `_config.yml` الخاص بك، أضف:

    ```yaml
    plugins:
      - jekyll-sitemap
    ```

    (إذا كنت تستخدم GitHub Pages، فإن `jekyll-sitemap` مسموح بها مسبقًا، لذا فهي تعمل مباشرة).
3.  عند التنفيذ (`jekyll build` أو عبر `make` إذا كنت قد قمت بإعداده)، سيولد Jekyll تلقائيًا ملف `sitemap.xml` في مجلد `_site/`.

#### 2. إنشاء قالب خريطة موقع يدويًا

إذا كنت تفضل تحكمًا أكبر:

1.  أنشئ ملفًا في المجلد الجذري للمشروع: `/sitemap.xml`.
2.  أضف قالب Liquid هذا:

    ```xml
    ---
    layout: nil
    ---
    <?xml version="1.0" encoding="UTF-8"?>
    <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
      {% for page in site.pages %}
        {% if page.sitemap != false %}
          <url>
            <loc>{{ site.url }}{{ page.url | replace:'index.html','' }}</loc>
            <lastmod>{{ page.date | date_to_xmlschema }}</lastmod>
          </url>
        {% endif %}
      {% endfor %}
      {% for post in site.posts %}
        <url>
          <loc>{{ site.url }}{{ post.url }}</loc>
          <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
        </url>
      {% endfor %}
    </urlset>
    ```
3.  تأكد من أن ملف `_config.yml` الخاص بك يحتوي على `url: "https://yourdomain.com"` حتى يتم إنشاء الروابط بشكل صحيح.

#### 3. التحقق

*   بعد التنفيذ، تحقق من ملف `_site/sitemap.xml`.
*   قدّمه إلى Google Search Console لفهرسة أفضل.

---

👉 بما أن لديك بالفعل `_config.yml` و `_plugins/`، فإن أسهل طريقة هي تمكين `jekyll-sitemap` فقط. بهذه الطريقة، لن تحتاج إلى الحفاظ على ملف XML يدويًا.

هل تريد مني صياغة الإضافة الدقيقة لملف `_config.yml` لمستودعك (بناءً على ملفاتك الحالية)، حتى تتمكن من لصقها مباشرة؟