---
audio: false
generated: true
lang: ar
layout: post
title: خادم لغة جافا في فيجوال ستوديو كود
translated: true
type: note
---

مرحبًا! إعداد `java.jdt.ls.java.home` في Visual Studio Code (VSCode) هو خيار تكوين محدد تستخدمه خادم لغة Java (وهو جزء من امتداد "Language Support for Java(TM) by Red Hat" المضمن في Java Extension Pack). دعنا نشرحه بالتفصيل:

---

### ماذا يعني
- **JDT**: ترمز إلى "أدوات تطوير Java"، وكانت في الأصل جزءًا من بيئة التطوير Eclipse. خادم لغة Java في VSCode مبني على JDT الخاص بـ Eclipse، والذي يوفر ميزات مثل إكمال الرمز، التصحيح، والترجمة.
- **LS**: ترمز إلى "خادم اللغة"، وهي عملية خلفية تزود دعم Java في VSCode (مثل IntelliSense، فحص الأخطاء).
- **`java.jdt.ls.java.home`**: هذا الإعداد يخبر خادم لغة Java بشكل محدد بأي Java Development Kit (JDK) يجب استخدامه لعملياته. إنه منفصل عن الإعداد العام `java.home`، على الرغم من أنهما مرتبطان.

باختصار، `java.jdt.ls.java.home` يحدد مسار JDK الذي يستخدمه خادم لغة Java من أجل:
- تحليل كود Java الخاص بك.
- توفير ميزات اللغة (مثل الإكمال التلقائي، الانتقال إلى التعريف).
- ترجمة وتشغيل الكود في بعض الحالات (على الرغم من أن الترجمة تعتمد غالبًا على إعدادات أخرى أو أدوات بناء).

---

### الاختلافات عن `java.home`
- **`java.home`**: إعداد عام في VSCode يشير إلى JDK لجميع الامتدادات والمهام المتعلقة بـ Java في VSCode. يتم استخدامه ما لم يتم تجاوزه بواسطة إعدادات أكثر تحديدًا.
- **`java.jdt.ls.java.home`**: إعداد أكثر تحديدًا يتجاوز `java.home` فقط لخادم لغة Java. إذا لم يتم تعيين هذا، فإن خادم اللغة يعود إلى استخدام `java.home`.

لذا، إذا قمت بتعيين `java.jdt.ls.java.home`، فإنه يأخذ الأولوية لعمليات خادم اللغة، مما يسمح لك باستخدام JDK مختلف لميزات اللغة عن، على سبيل المثال، مهام التشغيل أو التصحيح.

---

### كيفية تكوينه
بما أنك تستخدم Windows و VSCode 1.96.4 مع Java Extension Pack، إليك كيفية تعيينه:

1. **افتح الإعدادات:**
   - اضغط `Ctrl + ,` لواجهة مستخدم الإعدادات، أو استخدم `Ctrl + Shift + P` واكتب **"Open Settings (JSON)"** لتحرير `settings.json`.

2. **عيّن المسار:**
   - **في واجهة مستخدم الإعدادات:**
     - ابحث عن **"java.jdt.ls.java.home"**.
     - أدخل المسار الكامل لـ JDK الخاص بك (مثال: `C:\Program Files\Java\jdk-17.0.1`).
   - **في settings.json:**
     - أضف هذا السطر (استبدل بمسار JDK الخاص بك):
       ```json
       "java.jdt.ls.java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
       ```
     - مثال `settings.json`:
       ```json
       {
           "java.jdt.ls.java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
       }
       ```

3. **تحقق من مسار JDK:**
   - تأكد من أن المسار يشير إلى مجلد JDK يحتوي على دليل `bin` به `javac.exe` و `java.exe`.

4. **أعد تحميل VSCode:**
   - أعد تحميل النافذة (`Ctrl + Shift + P` > "Reload Window") أو أعد تشغيل VSCode لتطبيق التغيير.

---

### متى تستخدمه
عادةً ما تقوم بتعيين `java.jdt.ls.java.home` إذا:
- كان لديك عدة إصدارات من JDK مثبتة وتريد أن يستخدم خادم اللغة واحدًا محددًا (مثل JDK 17) بينما قد تستخدم مهام Java الأخرى (مثل تشغيل الكود) JDK مختلفًا عبر `java.home`.
- كنت تحاول حل مشاكل خادم اللغة (على سبيل المثال، لا يلتقط JDK الصحيح تلقائيًا).
- كان لمشروعك متطلبات إصدار JDK محددة لميزات اللغة.

إذا لم تقم بتعيينه، فسيستخدم خادم اللغة JDK من `java.home` (أو متغير البيئة `JAVA_HOME` في النظام كحل أخير).

---

### مثال عملي
لنفترض أن لديك JDK 8 و JDK 17 مثبتين:
- عيّن `"java.home": "C:\\Program Files\\Java\\jdk1.8.0_291"` للاستخدام العام.
- عيّن `"java.jdt.ls.java.home": "C:\\Program Files\\Java\\jdk-17.0.1"` لضمان استخدام خادم اللغة لـ JDK 17 لدعم بناء جملة Java الحديث.

بهذه الطريقة، يمكنك التصحيح/التشغيل باستخدام JDK 8 ولكن تحصل على دعم اللغة لميزات JDK 17.

---

هل هذا يوضح الأمر؟ إذا كنت تتساءل عن كيفية تناسب هذا مع إعداداتك أو تحتاج إلى مساعدة في تعديله أكثر، فقط أعلمني! 😊