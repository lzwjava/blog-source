---
audio: false
generated: false
lang: ar
layout: post
title: مخدم لغة جاڤا في فيجوال ستوديو كود
translated: true
---

مرحبًا! الإعداد `java.jdt.ls.java.home` في Visual Studio Code (VSCode) هو خيار تهيئة محدد يستخدم من قبل خادم لغة Java (جزء من امتداد "Language Support for Java(TM) by Red Hat" الذي يُشمل في Java Extension Pack). دعونا نحلل ذلك:

---

### ما يعنيه
- **JDT**: تعني "Java Development Tools"، وهي جزء من Eclipse IDE أصلاً. خادم لغة Java في VSCode يعتمد على JDT من Eclipse، والذي يوفر ميزات مثل إكمال الكود، التصفح، والتجميع.
- **LS**: تعني "Language Server"، وهي عملية خلفية تفعّل دعم Java في VSCode (مثل IntelliSense، التحقق من الأخطاء).
- **`java.jdt.ls.java.home`**: هذا الإعداد يحدد خادم لغة Java بالضبط أي Java Development Kit (JDK) يجب أن يستخدمه في عملياته. إنه مختلف عن الإعداد العام `java.home`، رغم أنهما مرتبطان.

في الختام، `java.jdt.ls.java.home` يحدد مسار JDK الذي يستخدمه خادم لغة Java:
- لتحليل كود Java الخاص بك.
- لتوفير ميزات اللغة (مثل إكمال الكود، الانتقال إلى التعريف).
- لتجميع و تشغيل الكود في بعض الحالات (ولكن التجميع يعتمد غالبًا على إعدادات أخرى أو أدوات بناء).

---

### الفرق بين `java.home`
- **`java.home`**: إعداد عام في VSCode يشير إلى JDK لجميع الإضافات والمهام المتعلقة بJava في VSCode. يستخدمه ما لم يُغلب عليه بواسطة إعدادات أكثر تحديدًا.
- **`java.jdt.ls.java.home`**: إعداد أكثر تحديدًا يُغلب على `java.home` فقط للخدمة اللغوية. إذا لم يُحدد، فإن الخدمة اللغوية تعود إلى `java.home`.

فإذا قمت بتحديد `java.jdt.ls.java.home`، فإنه يأخذ الأولوية في عمليات الخدمة اللغوية، مما يسمح لك باستخدام JDK مختلف للميزات اللغوية مقارنة بمهام تشغيل أو تصحيح الأخطاء.

---

### كيفية تهيئته
منذ أنك على Windows وتستخدم VSCode 1.96.4 مع Java Extension Pack، إليك كيفية تعيينه:

1. **فتح الإعدادات:**
   - اضغط على `Ctrl + ,` للوصول إلى واجهة الإعدادات، أو استخدم `Ctrl + Shift + P` واكتب **"Open Settings (JSON)"** لتحرير `settings.json`.

2. **تعيين المسار:**
   - **في واجهة الإعدادات:**
     - ابحث عن **"java.jdt.ls.java.home"**.
     - أدخل المسار الكامل إلى JDK الخاص بك (مثل `C:\Program Files\Java\jdk-17.0.1`).
   - **في settings.json:**
     - أضف هذا السطر (استبدل بمسار JDK الخاص بك):
       ```json
       "java.jdt.ls.java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
       ```
     - مثال على `settings.json`:
       ```json
       {
           "java.jdt.ls.java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
       }
       ```

3. **تحقق من مسار JDK:**
   - تأكد من أن المسار يشير إلى مجلد JDK يحتوي على مجلد `bin` يحتوي على `javac.exe` و `java.exe`.

4. **إعادة تحميل VSCode:**
   - اعد تحميل النافذة (`Ctrl + Shift + P` > "Reload Window") أو اعد تشغيل VSCode لتطبيق التغيير.

---

### متى استخدامه
عادةً، ستحدد `java.jdt.ls.java.home` إذا:
- لديك JDKات متعددة ومريد أن تستخدم الخدمة اللغوية JDK معينًا (مثل JDK 17) بينما قد تستخدم JDK آخر لمهام Java الأخرى (مثل تشغيل الكود) عبر `java.home`.
- أنت في عملية تصحيح أخطاء للخدمة اللغوية (مثل أنها لا تحدد JDK الصحيح تلقائيًا).
- مشروعك لديه متطلبات محددة لنسخة JDK للميزات اللغوية.

إذا لم تحددها، فإن الخدمة اللغوية ستستخدم JDK من `java.home` (أو متغير البيئة `JAVA_HOME` للنظام كخيار آخر).

---

### مثال عملي
فرض أن لديك JDK 8 و JDK 17:
- قم بتحديد `"java.home": "C:\\Program Files\\Java\\jdk1.8.0_291"` للاستخدام العام.
- قم بتحديد `"java.jdt.ls.java.home": "C:\\Program Files\\Java\\jdk-17.0.1"` لضمان أن تستخدم الخدمة اللغوية JDK 17 لدعم السنتاكس الحديث لJava.

بهذه الطريقة، يمكنك تصحيح/ تشغيل باستخدام JDK 8 ولكن الحصول على دعم لغة لخصائص JDK 17.

---

هل هذا يوضح الأمر؟ إذا كنت تسأل كيف يتوافق هذا مع إعداداتك أو تحتاج إلى مساعدة في تعديلها أكثر، فلا تتردد في إخبارني! 😊