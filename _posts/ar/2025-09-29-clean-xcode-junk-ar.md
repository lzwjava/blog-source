---
audio: false
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: تحرير مساحة القرص بتنظيف ملفات إكس كود غير الضرورية
translated: true
type: note
---

سؤال جيد 👍 يمكن لـ Xcode (ومحاكي iOS) أن يتراكمان عليهما الكثير من المخلفات بمرور الوقت — ذاكرة التخزين المؤقت للبناء، البيانات المشتقة، أجهزة المحاكاة، ملفات دعم الأجهزة، السجلات، إلخ. تنظيفها يُحرر غيغابايت. إليك نهج منظم:

---

### 1. مسح البيانات المشتقة (ذاكرة التخزين المؤقت للبناء)

هذا يزيل منتجات البناء القديمة، والبيانات الوسيطة، والفهارس.

* من **واجهة Xcode**:
  `Xcode → Settings → Locations → Derived Data → انقر على السهم → احذف المجلدات.`
* من **الطرفية**:

  ```bash
  rm -rf ~/Library/Developer/Xcode/DerivedData/*
  ```

---

### 2. تنظيف بيانات المحاكي

* إعادة ضبط أو حذف محتويات المحاكي:

  ```bash
  xcrun simctl erase all
  ```

  (يمسح التطبيقات والبيانات، لكن يحتفظ بتعريفات الجهاز)

* لإزالة جميع أجهزة المحاكاة غير المستخدمة (إذا تراكمت لديك إصدارات iOS قديمة):

  ```bash
  xcrun simctl delete unavailable
  ```

* لحذف جميع أجهزة المحاكاة (كن حذرًا، سيُزيل كل شيء):

  ```bash
  xcrun simctl delete all
  ```

* حذف المجلدات الثقيلة يدويًا:

  ```bash
  rm -rf ~/Library/Developer/CoreSimulator/Devices/*
  ```

---

### 3. إزالة ملفات دعم الأجهزة القديمة

عندما تصل أجهزة حقيقية، يقوم Xcode بتنزيل رموز التصحيح لكل إصدار iOS. تتراكم هذه الملفات.

```bash
rm -rf ~/Library/Developer/Xcode/iOS\ DeviceSupport/*
```

---

### 4. تنظيف الأرشيفات

أرشيفات التطبيقات من “Product → Archive” تستهلك مساحة كبيرة.

* من Organizer في Xcode → Archives → احذف الأرشيفات القديمة.
* أو عبر الطرفية:

  ```bash
  rm -rf ~/Library/Developer/Xcode/Archives/*
  ```

---

### 5. تنظيف السجلات

```bash
rm -rf ~/Library/Developer/Xcode/DocumentationCache/*
rm -rf ~/Library/Developer/Xcode/ios device logs/*
rm -rf ~/Library/Logs/CoreSimulator/*
```

---

### 6. تنظيف الفهارس المشتقة وذاكرة التخزين المؤقت للوحدات

```bash
rm -rf ~/Library/Developer/Xcode/DerivedDataModuleCache/*
```

---

### 7. اختياري: إعادة تعيين كاملة

إذا كنت تريد إعادة تعيين ذاكرات التخزين المؤقت المتعلقة بـ Xcode تمامًا:

```bash
rm -rf ~/Library/Developer/Xcode/*
rm -rf ~/Library/Developer/CoreSimulator/*
```

(هذا يزيل كل شيء: أجهزة المحاكاة، الأرشيفات، البيانات المشتقة، إلخ. التشغيل التالي لـ Xcode سيشعر وكأنه "تثبيت جديد.")

---

⚠️ **نصائح**

* دائمًا أغلق Xcode والمحاكي قبل تشغيل هذه الأوامر.
* استخدم `du -sh ~/Library/Developer/*` لترى أي المجلدات الأكبر حجمًا.
* أحيانًا `Clean Build Folder` (`⇧⌘K`) في Xcode يكون كافيًا، لكن التنظيف العميق يوفر مساحة أكبر.

---

هل تريد مني أن أعطيك **سكريبت واحد** يمكنك تشغيله لتنظيف كل المخلفات الشائعة دفعة واحدة (مع خيارات اختيارية)؟