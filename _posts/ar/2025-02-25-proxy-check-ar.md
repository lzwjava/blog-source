---
audio: false
generated: true
lang: ar
layout: post
title: تنفيذ فحص الوكيل في ويندوز
translated: true
type: note
---

لتنفيذ فحص الوكيل في Windows باستخدام Git-Bash و PowerShell، تحتاج إلى إعداد آليات في كل shell لاكتشاف وقت تنفيذ الأوامر المتعلقة بالشبكة وعرض إعدادات الوكيل إذا كانت مضبوطة. فيما يلي الخطوات والكود لكل من Git-Bash و PowerShell. من المرجح أن "المحطة الطرفية" المذكورة تشير إلى Windows Terminal الذي يستضيف هذه الأصداف، لذلك سنركز على تنفيذ Git-Bash و PowerShell.

---

### **لـ Git-Bash**

Git-Bash هو محاكاة لـ Bash على Windows، ويمكننا استخدام فخ `DEBUG` لتشغيل دالة قبل كل تنفيذ أمر. الهدف هو التحقق مما إذا كان الأمر متعلقًا بالشبكة وما إذا كانت إعدادات الوكيل مضبوطة، ثم عرضها.

#### **الخطوات:**

1. **تحديد قائمة الأوامر المتعلقة بالشبكة.**
2. **إنشاء دالة لعرض إعدادات الوكيل.**
3. **إنشاء دالة للتحقق من الأمر وإعدادات الوكيل.**
4. **إعداد فخ `DEBUG` لتشغيل الفحص قبل كل أمر.**
5. **تحديد دالة يدوية `checkproxy` لعرض إعدادات الوكيل عند الطلب.**
6. **إضافة جميع التكوينات إلى ملف `.bashrc`.**

#### **التنفيذ:**

أضف الكود التالي إلى ملف `~/.bashrc` الخاص بك (قم بإنشائه إذا لم يكن موجودًا):

```bash
# قائمة الأوامر المتعلقة بالشبكة
network_commands=(
    "gpa"
    "git"
    "ssh"
    "scp"
    "sftp"
    "rsync"
    "curl"
    "wget"
    "apt"
    "yum"
    "dnf"
    "npm"
    "yarn"
    "pip"
    "pip3"
    "gem"
    "cargo"
    "docker"
    "kubectl"
    "ping"
    "traceroute"
    "netstat"
    "ss"
    "ip"
    "ifconfig"
    "dig"
    "nslookup"
    "nmap"
    "telnet"
    "ftp"
    "nc"
    "tcpdump"
    "adb"
    "bundle"
    "brew"
    "cpanm"
    "bundle exec jekyll"
    "make"
    "python"
    "glcoud"
)

# دالة لعرض إعدادات الوكيل
display_proxy() {
    echo -e "🚀 **تم اكتشاف إعدادات الوكيل:**"
    [ -n "$HTTP_PROXY" ] && echo "   - HTTP_PROXY: $HTTP_PROXY"
    [ -n "$http_proxy" ] && echo "   - http_proxy: $http_proxy"
    [ -n "$HTTPS_PROXY" ] && echo "   - HTTPS_PROXY: $HTTPS_PROXY"
    [ -n "$https_proxy" ] && echo "   - https_proxy: $https_proxy"
    [ -n "$ALL_PROXY" ] && echo "   - ALL_PROXY: $ALL_PROXY"
    [ -n "$all_proxy" ] && echo "   - all_proxy: $all_proxy"
    echo ""
}

# دالة للتحقق مما إذا كان الأمر متعلقًا بالشبكة وإعدادات الوكيل مضبوطة
proxy_check() {
    local cmd
    # استخراج الكلمة الأولى من الأمر
    cmd=$(echo "$BASH_COMMAND" | awk '{print $1}')
    
    for network_cmd in "${network_commands[@]}"; do
        if [[ "$cmd" == "$network_cmd" ]]; then
            # التحقق مما إذا كانت أي متغيرات بيئة للوكيل مضبوطة
            if [ -n "$HTTP_PROXY" ] || [ -n "$http_proxy" ] || \
               [ -n "$HTTPS_PROXY" ] || [ -n "$https_proxy" ] || \
               [ -n "$ALL_PROXY" ] || [ -n "$all_proxy" ]; then
                display_proxy
            fi
            break
        fi
    done
}

# ضبط فخ DEBUG لتشغيل proxy_check قبل كل أمر
trap 'proxy_check' DEBUG

# دالة للفحص اليدوي لإعدادات الوكيل
checkproxy() {
    echo "HTTP_PROXY: $HTTP_PROXY"
    echo "HTTPS_PROXY: $HTTPS_PROXY"
    echo "Git HTTP Proxy:"
    git config --get http.proxy
    echo "Git HTTPS Proxy:"
    git config --get https.proxy
}
```

#### **كيفية العمل:**

- مصفوفة `network_commands` تسرد الأوامر المتعلقة بالشبكة.
- `display_proxy` تعرض جميع متغيرات بيئة الوكيل ذات الصلة إذا كانت مضبوطة.
- `proxy_check` تستخدم `BASH_COMMAND` (المتاح في فخ `DEBUG`) للحصول على الأمر الذي يتم تنفيذه، وتستخرج الكلمة الأولى، وتتحقق مما إذا كانت تطابق أي أمر شبكة. إذا كانت متغيرات الوكيل مضبوطة، فإنها تعرضها.
- سطر `trap 'proxy_check' DEBUG` يضمن تشغيل `proxy_check` قبل كل أمر.
- `checkproxy` تتيح لك عرض إعدادات الوكيل يدويًا، بما في ذلك تكوينات الوكيل الخاصة بـ Git.
- بعد إضافة هذا إلى `.bashrc`، أعد تشغيل Git-Bash أو شغّل `source ~/.bashrc` لتطبيق التغييرات.

#### **طريقة الاستخدام:**

- عند تشغيل أمر شبكة (مثل `git clone`، `curl`)، إذا كانت إعدادات الوكيل مضبوطة، فسيتم عرضها قبل تنفيذ الأمر.
- شغّل `checkproxy` لعرض إعدادات الوكيل يدويًا.

---

### **لـ PowerShell**

لا يحتوي PowerShell على مكافئ مباشر لفخ `DEBUG` في Bash، ولكن يمكننا استخدام `CommandValidationHandler` الخاص بوحدة `PSReadLine` لتحقيق وظيفة مماثلة. يعمل هذا المعالج قبل كل أمر، مما يسمح لنا بالتحقق من أوامر الشبكة وإعدادات الوكيل.

#### **الخطوات:**

1. **تحديد قائمة الأوامر المتعلقة بالشبكة.**
2. **إنشاء دالة لعرض إعدادات الوكيل.**
3. **إعداد `CommandValidationHandler` للتحقق من الأوامر وإعدادات الوكيل.**
4. **تحديد دالة يدوية `checkproxy` لعرض إعدادات الوكيل عند الطلب.**
5. **إضافة جميع التكوينات إلى ملف تعريف PowerShell الخاص بك.**

#### **التنفيذ:**

أولاً، حدد موقع ملف تعريف PowerShell الخاص بك عن طريق تشغيل `$PROFILE` في PowerShell. إذا لم يكن موجودًا، قم بإنشائه:

```powershell
New-Item -Type File -Force $PROFILE
```

أضف الكود التالي إلى ملف تعريف PowerShell الخاص بك (مثل `Microsoft.PowerShell_profile.ps1`):

```powershell
# قائمة الأوامر المتعلقة بالشبكة
$networkCommands = @(
    "gpa",
    "git",
    "ssh",
    "scp",
    "sftp",
    "rsync",
    "curl",
    "wget",
    "apt",
    "yum",
    "dnf",
    "npm",
    "yarn",
    "pip",
    "pip3",
    "gem",
    "cargo",
    "docker",
    "kubectl",
    "ping",
    "traceroute",
    "netstat",
    "ss",
    "ip",
    "ifconfig",
    "dig",
    "nslookup",
    "nmap",
    "telnet",
    "ftp",
    "nc",
    "tcpdump",
    "adb",
    "bundle",
    "brew",
    "cpanm",
    "bundle exec jekyll",
    "make",
    "python",
    "glcoud"
)

# دالة لعرض إعدادات الوكيل
function Display-Proxy {
    Write-Host "🚀 **تم اكتشاف إعدادات الوكيل:**"
    if ($env:HTTP_PROXY) { Write-Host "   - HTTP_PROXY: $env:HTTP_PROXY" }
    if ($env:http_proxy) { Write-Host "   - http_proxy: $env:http_proxy" }
    if ($env:HTTPS_PROXY) { Write-Host "   - HTTPS_PROXY: $env:HTTPS_PROXY" }
    if ($env:https_proxy) { Write-Host "   - https_proxy: $env:https_proxy" }
    if ($env:ALL_PROXY) { Write-Host "   - ALL_PROXY: $env:ALL_PROXY" }
    if ($env:all_proxy) { Write-Host "   - all_proxy: $env:all_proxy" }
    Write-Host ""
}

# إعداد CommandValidationHandler للتحقق من الأوامر قبل التنفيذ
Set-PSReadLineOption -CommandValidationHandler {
    param($command)
    # استخراج الكلمة الأولى من الأمر
    $cmd = ($command -split ' ')[0]
    
    if ($networkCommands -contains $cmd) {
        # التحقق مما إذا كانت أي متغيرات بيئة للوكيل مضبوطة
        if ($env:HTTP_PROXY -or $env:http_proxy -or $env:HTTPS_PROXY -or $env:https_proxy -or $env:ALL_PROXY -or $env:all_proxy) {
            Display-Proxy
        }
    }
    # إرجاع true دائمًا للسماح بتنفيذ الأمر
    return $true
}

# دالة للفحص اليدوي لإعدادات الوكيل
function checkproxy {
    Write-Host "HTTP_PROXY: $env:HTTP_PROXY"
    Write-Host "HTTPS_PROXY: $env:HTTPS_PROXY"
    Write-Host "Git HTTP Proxy:"
    git config --get http.proxy
    Write-Host "Git HTTPS Proxy:"
    git config --get https.proxy
}
```

#### **كيفية العمل:**

- `$networkCommands` عبارة عن مصفوفة من الأوامر المتعلقة بالشبكة.
- `Display-Proxy` تعرض جميع متغيرات بيئة الوكيل ذات الصلة إذا كانت مضبوطة.
- `Set-PSReadLineOption -CommandValidationHandler` يحدد كتلة نصية تعمل قبل كل أمر:
  - تستخرج الكلمة الأولى من الأمر.
  - تتحقق مما إذا كانت موجودة في `$networkCommands`.
  - إذا كانت متغيرات الوكيل مضبوطة، فإنها تستدعي `Display-Proxy`.
  - ترجع `$true` لضمان تنفيذ الأمر.
- `checkproxy` تتيح العرض اليدوي لإعدادات الوكيل، بما في ذلك وكلاء Git المحددين.
- بعد الإضافة إلى ملف التعريف الخاص بك، أعد تشغيل PowerShell أو شغّل `. $PROFILE` لتطبيق التغييرات.

#### **المتطلبات:**

- وحدة `PSReadLine` مطلوبة، وهي مدرجة بشكل افتراضي في PowerShell 5.1 والإصدارات الأحدث.
- إذا كنت تستخدم إصدارًا أقدم، فقد تحتاج إلى ترقية PowerShell أو إيجاد طريقة بديلة (لا يتم تغطيتها هنا، لأن معظم الأنظمة تستخدم إصدارات أحدث).

#### **طريقة الاستخدام:**

- عند تشغيل أمر شبكة (مثل `git pull`، `curl`)، إذا كانت إعدادات الوكيل مضبوطة، فسيتم عرضها قبل تنفيذ الأمر.
- شغّل `checkproxy` لعرض إعدادات الوكيل يدويًا.

---

### **ملاحظات حول "المحطة الطرفية"**

- إذا كانت "المحطة الطرفية" تشير إلى Windows Terminal، فهي ببساطة مضيف للأصداف مثل Git-Bash أو PowerShell أو Command Prompt (cmd.exe).
- التنفيذان أعلاه يعملان داخل جلسات Git-Bash أو PowerShell في Windows Terminal.
- تنفيذ وظيفة مماثلة في Command Prompt (cmd.exe) غير عملي بسبب قدرات البرمجة النصية المحدودة. يوصى باستخدام Git-Bash أو PowerShell بدلاً من ذلك.

---

### **اعتبارات إضافية**

- **تحليل الأمر:**
  - كلا التنفيذين يتحققان فقط من الكلمة الأولى من الأمر مقابل قائمة أوامر الشبكة. على سبيل المثال، `git clone` يتم تشغيلها لأن `git` موجود في القائمة.
  - سيتم تشغيل الأوامر متعددة الكلمات مثل `bundle exec jekyll` إذا كان `bundle` موجودًا في القائمة، وهو كافٍ لمعظم الحالات.
  - إذا لزم الأمر، يمكنك تعديل الكود للتحقق من جميع الكلمات في الأمر، ولكن هذا قد يؤدي إلى إيجابيات خاطئة وغير ضروري بشكل عام.

- **متغيرات الوكيل:**
  - كلا التنفيذين يتحققان من `HTTP_PROXY`، `http_proxy`، `HTTPS_PROXY`، `https_proxy`، `ALL_PROXY`، و `all_proxy` لتغطية الاختلافات الشائعة.
  - في Windows، متغيرات البيئة لا تحساس لحالة الأحرف، ولكننا نتحقق من كلتا الحالتين للاتساق مع سلوك شبيه بـ Unix (خاصة في Git-Bash).

- **إعدادات وكيل Git:**
  - دالة `checkproxy` في كلا الصدفتين تعرض إعدادات الوكيل الخاصة بـ Git باستخدام `git config --get`.

---

### **الاختبار**

- **ضبط متغيرات الوكيل:**
  - في Git-Bash: `export HTTP_PROXY=http://proxy.example.com:8080`
  - في PowerShell: `$env:HTTP_PROXY = "http://proxy.example.com:8080"`
- **تشغيل أوامر الشبكة:**
  - جرب أوامر مثل `git --version`، `curl -V`، إلخ.
  - يجب عرض إعدادات الوكيل قبل إخراج الأمر.
- **استخدم `checkproxy`:**
  - شغّل `checkproxy` في أي من الصدفتين لعرض إعدادات الوكيل يدويًا.

---

يوفر هذا التنفيذ فحصًا قويًا للوكيل في كل من Git-Bash و PowerShell، مناسب للاستخدام داخل Windows Terminal أو بشكل مستقل.