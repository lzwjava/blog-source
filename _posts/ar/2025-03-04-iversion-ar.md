---
audio: false
generated: true
lang: ar
layout: post
title: استخدام iVersion في iOS
translated: true
---

لاستخدام الحزمة `iVersion` في مشروع iOS الخاص بك، يجب عليك اتباع الخطوات التالية:

1. **فتح مشروعك في Xcode**: تأكد من أن مشروع Xcode الخاص بك مفتوح حيث تريد إضافة الحزمة `iVersion`.

2. **تهيئة CocoaPods**: إذا لم تهيئ CocoaPods في مشروعك من قبل، افتح Terminal، انتقل إلى مجلد المشروع، واسخدم:
   ```bash
   pod init
   ```
   هذا سيخلق ملف `Podfile` في مجلد المشروع.

3. **تحرير ملف Podfile**: افتح ملف `Podfile` في محرر النصوص أو Xcode. أضف الحزمة `iVersion` إلى ملف `Podfile` الخاص بك من خلال إضافة السطر التالي في القسم الخاص بالهدف:
   ```ruby
   pod 'iVersion', '1.11.4'
   ```

4. **تثبيت الحزمة**: احفظ ملف `Podfile` وارجع إلى Terminal. استخدم الأمر التالي لتثبيت الحزمة `iVersion`:
   ```bash
   pod install
   ```

5. **فتح ملف `.xcworkspace`**: بعد الانتهاء من التثبيت، اغلق مشروع Xcode إذا كان مفتوحًا، ثم افتح الملف `.xcworkspace` الجديد. هذا الملف عادة ما يكون في نفس المجلد الذي يحتوي على ملف `.xcodeproj` الأصلي.

6. **استيراد iVersion**: في ملفات Swift أو Objective-C الخاصة بك، استورد وحدة `iVersion` لتبدأ في استخدامها:
   ```swift
   import iVersion
   ```

7. **استخدام iVersion في كودك**: الآن يمكنك استخدام مكتبة `iVersion` في مشروعك. على سبيل المثال، يمكنك التحقق من إصدار التطبيق أو رقم البناء باستخدام الأساليب التي تقدمها `iVersion`.

بالتتابع مع هذه الخطوات، يجب أن تكون قادرًا على دمج واستخدام الحزمة `iVersion` في مشروع iOS الخاص بك بنجاح.