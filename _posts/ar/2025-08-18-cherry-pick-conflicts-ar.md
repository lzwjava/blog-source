---
audio: false
generated: true
image: false
lang: ar
layout: post
title: نزاعات انتقاء الكرز والحلول
translated: true
type: note
---

**cherry-pick** في Git هو أمر يسمح لك بأخذ commit من فرع واحد وتطبيقه على فرع آخر، دون دمج تاريخ الفرع بالكامل.

---

### آلية العمل

* عند تشغيل `git cherry-pick <commit-hash>`، يقوم Git بما يلي:

  1. يحدد موقع التغييرات التي أدخلها ذلك commit (مقارنة فرق diff مع الـ parent الخاص به).
  2. يعيد تطبيق تلك التغييرات على الفرع الحالي الخاص بك، مما ينشئ commit جديدًا بهاش hash جديد.
  3. هذا يعني أنه يتم إعادة استخدام محتوى الـ commit، لكنه لا يحافظ على النسب ancestry الأصلي للـ commit.

الأمر يشبه قول:
*"خذ رقعة patch هذا الـ commit الوحيد والصقها حيث أنا الآن."*

---

### لماذا تحدث التعارضات Conflicts

تنشأ التعارضات لأن cherry-pick يطبق التغييرات **خارج سياقها الأصلي**. فيما يلي بعض الأسباب الشائعة:

1. **الانحراف في الكود Code drift**
   إذا كان الفرع الهدف قد تغير في نفس مناطق الكود منذ الـ commit الأصلي، لا يستطيع Git تطبيق الرقعة patch بشكل نظيف. على سبيل المثال:

   * الـ commit الأصلي أضاف دالة في `fileA`.
   * في الفرع الهدف، قام شخص ما بإعادة تسمية أو إزالة تلك الدالة.
     → يحدث تعارض Conflict عند عمل cherry-pick.

2. **وجود commits معتمدة مفقودة**
   في بعض الأحيان يعتمد commit ما على commits سابقة (مثل إعادة الهيكلة refactorings، أو إعادة تسمية المتغيرات). إذا قمت بعمل cherry-pick له دون تلك الـ commits، يكون السياق مختلفًا وتظهر التعارضات.

3. **تغييرات متداخلة**
   إذا قام كلا الفرعين بلمس نفس الأسطر من الكود (حتى لو لأسباب مختلفة)، لا يستطيع Git اتخاذ القرار تلقائيًا ويتوقف للانتظار لحل المشكلة يدويًا.

---

### مثال توضيحي

* لديك commits بالترتيب A → B → C في `feature-branch`.
* تريد الـ commit B في الفرع `main`.
* تقوم بتشغيل `git checkout main && git cherry-pick B`.
* إذا كان الفرع `main` قد قام بالفعل بلمس نفس الملف/الأسطر التي قام بها B، يرفض Git الأمر ويعلن عن وجود تعارض Conflict. يجب عليك فتح الملف، وحل الاختلافات، ثم `git add`، ثم `git cherry-pick --continue`.

---

👉 باختصار: **يعيد cherry-pick تشغيل رقعة patch في سياق جديد. تحدث التعارضات عندما يتباعد هذا السياق بشكل كبير عن السياق الأصلي.**

هل تريد مني أيضًا أن أشرح لك **أفضل الممارسات لتقليل تعارضات cherry-pick** (مثل ترتيب الـ commits بالتسلسل، أو استخدام `-x`، أو إجراء rebase أولاً)؟