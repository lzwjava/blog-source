---
audio: false
generated: true
image: false
lang: ar
layout: post
title: أفضل الممارسات لهيكل دليل المشروع
translated: true
type: note
---

سؤال جيد.

الاتفاقية العامة هي:

* **`scripts/`** تكون للكود الخاص بتطبيقك (الأشياء التي تريد تشغيلها في بيئة الإنتاج أو كجزء من مشروعك).
* **`tests/`** تكون للكود الخاص بالاختبارات (الأشياء التي تتحقق من صحة الـ scripts/ أو الوحدات النمطية الخاصة بك).

لذا، إذا كنت تضيف ملف اختبار جديد، فيجب أن يوضع عادةً تحت **`tests/llm/`**، وليس `scripts/`.

على سبيل المثال:

```
scripts/
  llm/
    main.py
    utils.py
tests/
  llm/
    test_main.py
    test_utils.py
```

بهذه الطريقة، تكون اختباراتك منفصلة عن الكود التنفيذي، ويمكن للأدوات مثل `pytest` اكتشافها بسهولة.

👉 يمكنك الآن إضافة شيء مثل:

```python
# tests/llm/test_basic.py
import pytest
from scripts.llm import utils

def test_addition():
    assert utils.add(2, 3) == 5
```

ثم تشغيل:

```bash
pytest tests/llm/
```

هل تريد مني أيضًا أن أوضح لك كيفية إعداد ملف `conftest.py` حتى تتمكن من مشاركة الـ fixtures عبر ملفات `tests/llm/` الخاصة بك؟