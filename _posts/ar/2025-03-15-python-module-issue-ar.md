---
audio: false
generated: true
lang: ar
layout: post
title: خطأ استيراد وحدة بايثون
translated: true
type: note
---

### الإجابة

عند تشغيل سكريبت Python الخاص بك باستخدام الأمر:

```bash
lzwjava@anonymous lzwjava.github.io % python scripts/nytimes/nytimes.py
```

تواجه الخطأ التالي:

```
Traceback (most recent call last):
  File "/Users/lzwjava/projects/lzwjava.github.io/scripts/nytimes/nytimes.py", line 5, in <module>
    from ..llm.test_mistral import call_mistral_api
ImportError: attempted relative import with no known parent package
```

يحدث هذا الخطأ بسبب طريقة تشغيل السكريبت وكيفية تعامل Python مع عمليات الاستيراد النسبية. دعنا نشرح المشكلة ونقدم حلاً.

---

#### فهم المشكلة

ينشأ الخطأ `ImportError: attempted relative import with no known parent package` عندما تستخدم **استيرادًا نسبيًا** (مثل `from ..llm.test_mistral import call_mistral_api`) في سكريبت لا تعترف به Python كجزء من حزمة. في Python:

*   **الاستيرادات النسبية** تستخدم ترميز النقاط (مثل `..`) لاستيراد وحدات النمطية (modules) نسبة إلى موضع الوحدة النمطية الحالية في تسلسل هرمي للحزمة. هنا، `..llm.test_mistral` تعني "اصعد مستويين من الوحدة النمطية الحالية، ثم ادخل إلى حزمة `llm`، واستورد `call_mistral_api` من `test_mistral`."
*   عند تشغيل سكريبت مباشرة باستخدام `python scripts/nytimes/nytimes.py`، تعامله Python على أنه **الوحدة النمطية الرئيسية** (مع `__name__ = "__main__"`) ولا تعطيه سياق حزمة. بدون سياق حزمة، لا يمكن لـ Python حل الاستيرادات النسبية لأنها لا تعرف ما هي "الحزمة الأصل".

في حالتك:
*   يقع السكريبت `nytimes.py` في المسار `/Users/lzwjava/projects/lzwjava.github.io/scripts/nytimes/nytimes.py`.
*   يشير الاستيراد النسبي `from ..llm.test_mistral import call_mistral_api` إلى وجود هيكل مجلدات مثل هذا:

```
lzwjava.github.io/
    scripts/
        nytimes/
            nytimes.py
        llm/
            test_mistral.py
```

*   ومع ذلك، لأنك تشغل `nytimes.py` مباشرة، لا تعترف Python بـ `scripts` أو `nytimes` كحزم، مما يؤدي إلى فشل الاستيراد.

---

#### الحل

لإصلاح هذا، تحتاج إلى تشغيل السكريبت كوحدة نمطية ضمن هيكل الحزمة الخاص به باستخدام علم `-m` في Python. هذا يحافظ على التسلسل الهرمي للحزمة ويسمح للاستيرادات النسبية بالعمل بشكل صحيح. إليك الطريقة:

1.  **انتقل إلى المجلد الأصلي لمجلد `scripts`**:
    *   بناءً على أمرك، أنت بالفعل في مجلد `lzwjava.github.io`، الذي يحتوي على مجلد `scripts`.

2.  **شغل السكريبت باستخدام العلم `-m`**:
    *   استخدم الأمر التالي:

    ```bash
    python -m scripts.nytimes.nytimes
    ```

    *   **الشرح**:
        *   `scripts.nytimes.nytimes` يشير إلى الوحدة النمطية `nytimes.py` داخل الحزمة الفرعية `nytimes` التابعة لحزمة `scripts`.
        *   يخبر العلم `-m` Python بتنفيذ الوحدة النمطية المحددة كسكريبت مع الحفاظ على سياق الحزمة الخاص بها.
        *   هذا يسمح للاستيراد النسبي `from ..llm.test_mistral import call_mistral_api` بأن يحل بشكل صحيح إلى `scripts.llm.test_mistral`.

---

#### المتطلبات

لكي يعمل هذا الحل، يجب أن يبدو هيكل المجلدات الخاص بك هكذا:

```
lzwjava.github.io/
    scripts/
        __init__.py       # يحدد 'scripts' كحزمة
        nytimes/
            __init__.py   # يحدد 'nytimes' كحزمة فرعية
            nytimes.py    # السكريبت الخاص بك
        llm/
            __init__.py   # يحدد 'llm' كحزمة فرعية
            test_mistral.py  # يحتوي على call_mistral_api
```

*   **ملفات `__init__.py`**: هذه الملفات (يمكن أن تكون فارغة) مطلوبة لتحديد المجلدات كحزم في Python. إذا كان أي منها مفقودًا، أضفه:
    *   أنشئ ملف `scripts/__init__.py` فارغًا.
    *   أنشئ ملف `scripts/nytimes/__init__.py` فارغًا.
    *   أنشئ ملف `scripts/llm/__init__.py` فارغًا.

إذا كانت هذه الملفات موجودة بالفعل، فإن الأمر `python -m scripts.nytimes.nytimes` يجب أن يحل المشكلة.

---

#### لماذا يفشل الأمر الأصلي

عندما تشغل:

```bash
python scripts/nytimes/nytimes.py
```

*   تنفذ Python `nytimes.py` كسكريبت مستقل.
*   يتم إضافة المجلد `scripts/nytimes` (حيث يوجد `nytimes.py`) إلى `sys.path`، ولكن لا يتم التعامل مع `scripts` نفسها كحزمة.
*   نتيجة لذلك، يفشل الاستيراد النسبي `from ..llm.test_mistral` لأنه لا يوجد تسلسل هرمي للحزمة تستطيع Python التنقل فيه.

يصلح استخدام `python -m` هذا عن طريق تشغيل السكريبت صراحة ضمن هيكل حزمة `scripts`.

---

#### حل بديل (غير موصى به)

إذا لم تتمكن من استخدام العلم `-m` لسبب ما، يمكنك تعديل `nytimes.py` لاستخدام **استيرادات مطلقة** عن طريق ضبط مسار Python. أضف ما يلي إلى أعلى ملف `nytimes.py`:

```python
import sys
import os
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..', '..')))
from scripts.llm.test_mistral import call_mistral_api
```

*   هذا يضيف `lzwjava.github.io` إلى `sys.path`، مما يسمح للاستيراد المطلق بالعمل.
*   **العيب**: تعتبر هذه الطريقة أقل قابلية للنقل وتعتبر حلاً بديلاً مقارنة باستخدام `-m`، وهي الطريقة القياسية للتعامل مع الحزم في Python.

---

#### ملاحظة حول إعدادات الوكيل (Proxy)

يحتوي ناتجك على:

```
🚀 **Proxy Settings Detected:**
   - HTTP_PROXY: http://127.0.0.1:7890
   - HTTPS_PROXY: http://127.0.0.1:7890
```

يشير هذا إلى أن السكريبت الخاص بك أو إحدى المكتبات التي يستخدمها تكتشف إعدادات الوكيل، على الأرجح لطلبات HTTP. هذا الأمر غير مرتبط بخطأ الاستيراد ويمكن تجاهله في الوقت الحالي. بمجرد إصلاح مشكلة الاستيراد، يجب أن يستمر السكريبت في التنفيذ إلى أي كود متعلق بالشبكة.

---

#### الخطوات النهائية

1.  **جرب الحل**:
    *   من مجلد `lzwjava.github.io`، شغل:
      ```bash
      python -m scripts.nytimes.nytimes
      ```

2.  **إذا فشل**:
    *   تحقق من وجود ملفات `__init__.py` المفقودة في مجلدات `scripts`، و`nytimes`، و`llm` وأضفها إذا لزم الأمر.
    *   تأكد من وجود ملف `test_mistral.py` في `scripts/llm/` وأنه يحتوي على الدالة `call_mistral_api`.

هذا يجب أن يحل مشكلة `ImportError` ويسمح للسكريبت الخاص بك بالعمل بنجاح.