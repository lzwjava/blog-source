---
audio: false
generated: true
image: false
lang: ar
layout: post
title: دليل التحكم في الوصول القائم على الأدوار لجينكينز مع إضافة الأدوار
translated: true
type: note
---

في Jenkins، يتعلق **التحكم في الوصول القائم على الأدوار (RBAC)** بتحديد من يمكنه فعل ماذا، وأين. بشكل افتراضي، يمتلك Jenkins فقط نموذجًا أساسيًا للمستخدم/المسؤول، ولكن مع **إضافة استراتيجية التحكم القائمة على الأدوار** (أو استراتيجيات التفويض المماثلة) يمكنك تحديد وصول دقيق.

---

### المفاهيم الأساسية للـ RBAC في Jenkins

1. **الأدوار العالمية**
   هذه تنطبق على مثيل Jenkins بالكامل. أمثلة:

   * **المسؤول**: تحكم كامل.
   * **للقراءة فقط**: يمكن عرض المهام ولكن لا يمكن تشغيلها.
   * **المطور**: يمكن إنشاء وتكوين المهام.

2. **أدوار المشروع/المهمة**
   يمكنك منح الأذونات على أساس كل مهمة أو كل مجلد. على سبيل المثال:

   * يمكن للمستخدم A بناء *المشروع X* ولكن لا يمكنه رؤية *المشروع Y*.
   * يمكن للمقاولين تشغيل عمليات البناء ولكن لا يمكنهم تغيير التكوينات.

3. **أذونات البناء**
   يمكن تقسيم الأذونات:

   * `Job/Read` (رؤية تعريف المهمة وسجل البناء)
   * `Job/Build` (تشغيل عمليات البناء)
   * `Job/Configure` (تغيير pipeline أو الإعدادات)
   * `Job/Delete` (إزالة المهام أو عمليات البناء)
   * `Job/Cancel` (إلغاء عمليات البناء الجارية)

---

### كيفية إدارة صلاحيات الوصول إلى المهمة أو البناء

1. **تثبيت إضافة استراتيجية التحكم القائمة على الأدوار**

   * انتقل إلى **Manage Jenkins → Plugins → Available Plugins → Role-based Authorization Strategy**.
   * بعد التثبيت، حدده في **Manage Jenkins → Configure Global Security → Authorization**.

2. **تحديد الأدوار**

   * انتقل إلى **Manage Jenkins → Manage and Assign Roles**.
   * أنشئ أدوارًا عالمية (مثل *admin*، *viewer*، *developer*) وأدوار مشاريع (مثل *projectX-builder*).
   * عيّن لكل دور الأذونات اللازمة (`Job/Build`، `Job/Configure`، إلخ).

3. **تعيين الأدوار للمستخدمين/المجموعات**

   * لا يزال تحت **Manage and Assign Roles**، قم بربط مستخدمي Jenkins الخاصين بك (أو مجموعات LDAP إذا كانت متكاملة) بالأدوار.
   * على سبيل المثال:

     * ربط *alice* → `developer`
     * ربط *bob* → `viewer`
     * ربط *qa-team* → `projectX-builder`

4. **استخدم المجلدات لدقة أفضل**

   * نظم المهام في **Folders** (عبر *Folders Plugin*).
   * طبق قيود الأدوار على مستوى المجلد حتى يتمكن فقط الفريق الصحيح من رؤية/بناء/تعديل المهام الموجودة بداخله.

---

### أمثلة سيناريوهات

* **المطورون**: يمكنهم بناء وتكوين مهام فريقهم، ولكن لا يمكنهم لمس إعدادات المسؤول.
* **فريق ضمان الجودة**: يمكنهم فقط تشغيل عمليات البناء (وليس إعادة تكوين pipelines).
* **المستخدمون الخارجيون**: يمكنهم رؤية حالة المهمة ولكن لا يمكنهم تعديل أو تشغيل عمليات البناء.

---

👉 عمليًا: تحافظ على **قلة عدد المسؤولين**، وتعطي **المطورين صلاحية التكوين + الوصول للبناء** لمهامهم الخاصة، وتمنح **حقوق القراءة فقط أو البناء فقط** للآخرين. يضمن هذا الاتساق، ويقلل من الأخطاء، ويبقي المهام الحساسة محمية.

---

هل تريد مني أن أريك **مثالًا عمليًا خطوة بخطة للتكوين** (بما في ذلك الأدوار، الأذونات، وربط المهام) يتطابق مع الإعداد النموذجي في المؤسسات؟