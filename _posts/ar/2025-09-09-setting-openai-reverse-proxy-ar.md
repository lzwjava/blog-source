---
audio: false
date: 2025-09-09
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: ضبط OPENAI_REVERSE_PROXY في LibreChat
translated: true
type: note
---

فهمت 👍 أنت تقوم بتحرير ملف `.env` الخاص بـ LibreChat.
هذا الملف هو بالضبط المكان الذي تحدّد فيه `OPENAI_REVERSE_PROXY`.

---

### 1. وظيفة المتغير

يُستخدم `OPENAI_REVERSE_PROXY` إذا أردت أن يقوم LibreChat بتوجيه طلبات واجهة برمجة تطبيقات OpenAI عبر خادم وكيل (على سبيل المثال، إذا كنت تشغل وكيلاً عكسياً مستضافًا ذاتيًا لإضافة التسجيل، أو التخزين المؤقت، أو التوجيه الإقليمي، أو لتجاوز القيود).

---

### 2. كيفية ضبطه

في ملف `.env` الخاص بك، قم فقط بتعيينه عنوان URL الأساسي للوكيل العكسي الخاص بك. على سبيل المثال:

```ini
OPENAI_REVERSE_PROXY=https://your-proxy.example.com/v1
```

ملاحظات:

* يجب أن يشير العنوان URL إلى نقطة النهاية للوكيل التي تحاكي واجهة برمجة تطبيقات `/v1` الخاصة بـ OpenAI.
* لا تضع شرطة مائلة نهائية `/` ما لم يكن وكيلك يتطلب ذلك.
* اتركه فارغًا (`OPENAI_REVERSE_PROXY=`) إذا كنت لا تستخدم وكيلاً.

---

### 3. تطبيق التغييرات

بعد تحرير ملف `.env`، أعد تشغيل حاويات LibreChat الخاصة بك حتى تلتقط القيمة الجديدة:

```bash
docker compose down
docker compose up -d --force-recreate
```

---

### 4. التحقق

يمكنك التحقق داخل الحاوية قيد التشغيل:

```bash
docker compose exec librechat env | grep OPENAI_REVERSE_PROXY
```

يجب أن تظهر القيمة التي قمت بتعيينها.

---

هل لديك بالفعل عنوان URL لوكيل عكسي (مثل `https://openrouter.ai/api/v1` أو خادم Nginx خاص بك)، أم تريد مني أن أوضح لك كيفية **إعداد واحد** لـ LibreChat؟