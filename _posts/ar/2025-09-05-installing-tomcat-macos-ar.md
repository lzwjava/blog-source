---
audio: false
generated: true
image: false
lang: ar
layout: post
title: تثبيت أباتشي تومكات على نظام ماك أو إس
translated: true
type: note
---

إليك دليلًا خطوة بخطوة لتثبيت وتشغيل **Apache Tomcat على نظام macOS**:

---

### 1. التحقق من Java

تتطلب Tomcat تثبيت Java. تحقق مما إذا كانت مثبتة لديك:

```bash
java -version
```

إذا لم تظهر لك إصدار Java (مثل `1.8` أو `11` أو أعلى)، قم بتثبيت أحدها. أسهل طريقة على macOS هي عبر [Homebrew](https://brew.sh):

```bash
brew install openjdk@11
```

ثم اربطها:

```bash
brew link --force --overwrite openjdk@11
```

---

### 2. تثبيت Tomcat (الخيار أ: Homebrew – الأسهل)

يمكنك تثبيت Tomcat بسرعة باستخدام Homebrew:

```bash
brew install tomcat
```

عادةً ما يثبت هذا أحدث إصدار (مثل Tomcat 10).

ستجده في المسار:

```
/usr/local/Cellar/tomcat/<version>/libexec
```

أو إذا كنت تستخدم Apple Silicon:

```
/opt/homebrew/Cellar/tomcat/<version>/libexec
```

شغله باستخدام:

```bash
catalina run
```

---

### 3. تثبيت Tomcat (الخيار ب: التحميل اليدوي – الطريقة الرسمية)

1.  انتقل إلى [موقع Apache Tomcat](https://tomcat.apache.org/download-10.cgi) وقم بتنزيل أحدث إصدار مستقر (اختر الملف `.tar.gz`).

2.  فك ضغطه إلى دليل، على سبيل المثال:

    ```bash
    tar xvf apache-tomcat-10.*.tar.gz
    mv apache-tomcat-10.* ~/tomcat
    ```

3.  شغّل Tomcat:

    ```bash
    cd ~/tomcat/bin
    ./startup.sh
    ```

4.  لإيقافه:

    ```bash
    ./shutdown.sh
    ```

---

### 4. التحقق من التثبيت

افتح متصفحك وانتقل إلى:

```
http://localhost:8080
```

يجب أن ترى صفحة الترحيب الخاصة بـ Tomcat.

---

### 5. (اختياري) الإضافة إلى PATH

لتتمكن من تشغيل `startup.sh` من أي مكان، أضف هذا إلى ملف `~/.zshrc` أو `~/.bashrc` الخاص بك:

```bash
export CATALINA_HOME=~/tomcat
export PATH=$CATALINA_HOME/bin:$PATH
```

قم بإعادة التحميل:

```bash
source ~/.zshrc
```

---

👉 هذا كل شيء. لديك الآن Tomcat يعمل محليًا على macOS.

هل تريد مني أيضًا أن أوضح لك **كيفية نشر ملف WAR على Tomcat على macOS** بعد الإعداد؟