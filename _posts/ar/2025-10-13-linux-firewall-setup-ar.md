---
audio: false
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: دليل إعداد جدار الحماية لينكس
translated: true
type: note
---

سؤال جيد — إعداد جدار حماية في Linux (مثل **Red Hat** أو **Ubuntu**) يتضمن بشكل أساسي استخدام إما **`firewalld`** أو **`ufw`**، اعتمادًا على نظامك. فيما يلي دليل واضح لكل من العائلتين، حتى تتمكن من اختيار الأداة المناسبة.

---

### 🔹 على أنظمة **Red Hat / CentOS / Fedora** (firewalld)

#### 1. التحقق مما إذا كان `firewalld` مثبتًا وقيد التشغيل

```bash
sudo systemctl status firewalld
```

إذا لم يكن قيد التشغيل:

```bash
sudo systemctl enable firewalld
sudo systemctl start firewalld
```

#### 2. الاستخدام الأساسي

التحقق من المناطق النشطة (المجموعات التي تجمع الواجهات والقواعد):

```bash
sudo firewall-cmd --get-active-zones
```

سرد جميع القواعد للمنطقة الحالية:

```bash
sudo firewall-cmd --list-all
```

#### 3. السماح بحظر المنافذ/الخدمات أو حظرها

السماح بمنفذ (مثال: SSH على المنفذ 22):

```bash
sudo firewall-cmd --permanent --add-port=22/tcp
```

السماح بخدمة معروفة (مثال: HTTP):

```bash
sudo firewall-cmd --permanent --add-service=http
```

حذف/إزالة خدمة:

```bash
sudo firewall-cmd --permanent --remove-service=http
```

إعادة التحميل للتطبيق:

```bash
sudo firewall-cmd --reload
```

#### 4. التحقق من القواعد

```bash
sudo firewall-cmd --list-all
```

---

### 🔹 على أنظمة **Ubuntu / Debian** (ufw)

يأتي Ubuntu مع **UFW** (Uncomplicated Firewall)، وهو أبسط في الإدارة.

#### 1. تمكين UFW

```bash
sudo ufw enable
```

#### 2. السماح بحظر المنافذ أو الخدمات أو حظرها

السماح بـ SSH:

```bash
sudo ufw allow ssh
```

السماح بـ HTTP (المنفذ 80):

```bash
sudo ufw allow 80/tcp
```

السماح بنطاق منافذ محدد:

```bash
sudo ufw allow 1000:2000/tcp
```

رفض خدمة:

```bash
sudo ufw deny 23/tcp
```

#### 3. التحقق من الحالة

```bash
sudo ufw status verbose
```

#### 4. إعادة تعيين القواعد إذا لزم الأمر

```bash
sudo ufw reset
```

---

### 🔹 لكلا النظامين

لـ **حظر كل حركة المرور الواردة باستثناء القليل منها**، يمكنك:

1. رفض كل الحركة الواردة افتراضيًا

   ```bash
   sudo ufw default deny incoming
   sudo ufw default allow outgoing
   ```

   *(لـ `firewalld`، استخدم المناطق أو مناطق الإسقاط لسلوك مشابه)*

2. ثم السماح بشكل انتقائي بمنافذ معينة (مثال: 22، 80، 443).

---

### 🔹 نصيحة أمنية

دائمًا **اسمح بـ SSH أولاً** قبل تمكين أو تغيير قواعد جدار الحماية عن بُعد — وإلا فستغلق الاتصال على نفسك.

---

هل تريد مني عرض **مثال لإعداد حد أدنى لجدار الحماية** (مثال: لخادم ويب عادي يسمح فقط بـ SSH + HTTP + HTTPS)؟