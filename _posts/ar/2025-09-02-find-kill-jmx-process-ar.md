---
audio: false
generated: true
image: false
lang: ar
layout: post
title: البحث عن عملية منفذ JMX وإنهاؤها
translated: true
type: note
---

فهمت — تريد التحقق من برنامج Java على نظام Windows الذي فتح **وكيل JMX عن بُعد** على المنفذ `9091`، ثم إنهائه إذا لزم الأمر. إليك كيفية القيام بذلك خطوة بخطوة:

---

### 1. ابحث عن العملية التي تستخدم المنفذ 9091

افتح **موجه الأوامر** (كمسؤول إذا أمكن)، وقم بتشغيل:

```cmd
netstat -ano | findstr :9091
```

سيظهر هذا شيئًا مثل:

```
  TCP    0.0.0.0:9091    0.0.0.0:0    LISTENING    12345
```

* العمود الأخير (`12345` هنا) هو **معرف العملية (PID)**.

---

### 2. حدد البرنامج الذي ينتمي إليه هذا المعرف PID

الآن قم بربط المعرف PID بالبرنامج الفعلي:

```cmd
tasklist /FI "PID eq 12345"
```

سيظهر هذا شيئًا مثل:

```
Image Name                     PID   Session Name        Mem Usage
========================= ======== ================ ============
java.exe                    12345 Console           200,000 K
```

إذا كانت عملية Java، يمكنك الحصول على مزيد من التفاصيل:

```cmd
wmic process where processid=12345 get CommandLine
```

هذا يطبع **سطر الأوامر** المستخدم لبدء تشغيل JVM — سترى أعلام JMX (`-Dcom.sun.management.jmxremote.port=9091`)، مما يؤكد أنها وكيل JMX عن بُعد.

---

### 3. أوقف العملية

إذا تأكدت من أنه من الآمن إيقافها، يمكنك إنهاؤها:

```cmd
taskkill /PID 12345 /F
```

* `/PID` يحدد معرف العملية.
* `/F` يُجبر على الإنهاء.

---

### 4. بديل باستخدام PowerShell

إذا كنت تفضل PowerShell، يمكنك تشغيل:

```powershell
Get-Process -Id (Get-NetTCPConnection -LocalPort 9091).OwningProcess
```

لإنهاء العملية مباشرة:

```powershell
Stop-Process -Id (Get-NetTCPConnection -LocalPort 9091).OwningProcess -Force
```

---

🔎 ملخص:

1. `netstat -ano | findstr :9091` → احصل على PID
2. `tasklist /FI "PID eq <PID>"` → لمعرفة البرنامج
3. `wmic process where processid=<PID> get CommandLine` → لرؤية أعلام JVM
4. `taskkill /PID <PID> /F` → أوقف

---

هل تريد مني أيضًا أن أوضح لك كيفية **التحقق من عدة JVMs في وقت واحد** للبحث عن منافذ وكيل JMX (حتى تتمكن من رؤية ما إذا كانت هناك عمليات أخرى تعمل على منافذ مثل 9090، 9999، إلخ)؟