---
audio: false
generated: true
image: false
lang: ar
layout: post
title: دليل مقارنة التسلسل باستخدام مكتبة ديفليب في بايثون
translated: true
type: note
---

في بايثون، توفر وحدة **`difflib`** أدوات لمقارنة التسلسلات — السلاسل النصية، القوائم، أو أي كائن قابل للتكرار. غالبًا ما تُستخدم لمهام مثل اكتشاف الاختلافات بين النصوص (مثل أمر `diff` في يونكس) أو بناء أدوات الإكمال التلقائي وأدوات التشابه.

---

### كيف تعمل `difflib`

في صميم عملها، تحاول `difflib` العثور على أطول تسلسلات فرعية متطابقة ومتتالية بين مدخلين. ثم تستخدم هذه التطابقات لتسليط الضوء على أوجه التشابه والاختلاف. يمكن لهذه المكتبة أن:

* تولد مقارنات اختلاف قابلة للقراءة البشرية (`ndiff`, `unified_diff`, `context_diff`).
* تحسب نسب التشابه بين التسلسلات.
* تقترح تطابقات قريبة من قائمة (`get_close_matches`).

---

### `SequenceMatcher`

أهم فئة هي **`difflib.SequenceMatcher`**.

**كيف تعمل**:

* تقارن تسلسلين عنصرًا بعنصر.
* تبحث عن *أطول تسلسل فرعي مشترك* (ولكن ليس بالضرورة أن يكون متتاليًا).
* تنتج قائمة بالعمليات (`replace`, `delete`, `insert`, `equal`) تصف كيفية تحويل أحد التسلسلين إلى الآخر.

**الطرق الرئيسية**:

1. **`ratio()`**
   تُرجع عدد عشري في المدى `[0, 1]` يقيس درجة التشابه.
   الصيغة:

   $$
   \text{ratio} = \frac{2 \times M}{T}
   $$

   حيث `M` = عدد العناصر المتطابقة، و `T` = إجمالي العناصر في كلا التسلسلين.

2. **`quick_ratio()`** و **`real_quick_ratio()`**
   تقديرات أسرع للتشابه، تضحي قليلًا بالدقة من أجل السرعة.

3. **`get_opcodes()`**
   تُرجع قائمة بالعمليات لتحويل التسلسل `a` إلى التسلسل `b`. مثال: `[('replace', 0, 2, 0, 1), ('equal', 2, 4, 1, 3)]`.

4. **`get_matching_blocks()`**
   تُرجع جميع التسلسلات الفرعية المتطابقة مع مواقعها.

---

### مثال

```python
import difflib

a = "hello world"
b = "helo wrld"

s = difflib.SequenceMatcher(None, a, b)

print("Similarity ratio:", s.ratio())
print("Operations:", s.get_opcodes())
print("Matching blocks:", s.get_matching_blocks())
```

**النتيجة**:

```
Similarity ratio: 0.9090909090909091
Operations: [('equal', 0, 1, 0, 1), ('delete', 1, 2, 1, 1),
             ('equal', 2, 5, 1, 4), ('delete', 5, 6, 4, 4),
             ('equal', 6, 11, 4, 9)]
Matching blocks: [Match(a=0, b=0, size=1),
                  Match(a=2, b=1, size=3),
                  Match(a=6, b=4, size=5),
                  Match(a=11, b=9, size=0)]
```

---

👉 باختصار:

* `difflib` هي مجموعة أدوات لمقارنة التسلسلات.
* `SequenceMatcher` هو المحرك الأساسي — يجد التسلسلات الفرعية المتطابقة ويحسب التشابه.
* إنها مفيدة لمقارنة الاختلافات، المدقق الإملائي، كشف الانتحال، الاقتراح التلقائي، إلخ.

هل تريد مني أيضًا أن أوضح كيفية استخدام `get_close_matches()` (التي تستخدم داخليًا `SequenceMatcher`) للمطابقة التقريبية للسلاسل النصية، كما في المدقق الإملائي أو اقتراحات البحث؟