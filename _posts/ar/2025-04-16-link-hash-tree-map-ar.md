---
audio: false
generated: true
lang: ar
layout: post
title: LinkedHashMap مقابل TreeMap
translated: true
type: note
---

سؤال رائع! كل من `LinkedHashMap` و `TreeMap` هما تطبيقات لواجهة `Map` في جافا، لكن لديهما **استراتيجيات ترتيب مختلفة** وتستخدم **هياكل بيانات أساسية مختلفة**.

### 🌳 `TreeMap`
- **الترتيب**: يتم الفرز حسب **الترتيب الطبيعي** للمفاتيح (أو بواسطة مُقارِن `Comparator` مخصص إذا تم توفيره).
- **هيكل البيانات**: مدعوم بشجرة **Red-Black Tree** (نوع من أشجار البحث الثنائية ذاتية التوازن).
- **الأداء**:
  - `O(log n)` للعمليات get/put/remove
  - يتم الحفاظ على الترتيب المصنف في جميع الأوقات
- **حالة الاستخدام**: عندما تحتاج إلى **مفاتيح مرتبة**.

### 🔗 `LinkedHashMap`
- **الترتيب**: يحافظ على **ترتيب الإدراج** (أو ترتيب الوصول اختياريًا إذا تم تكوينه بهذه الطريقة).
- **هيكل البيانات**: مدعوم بـ **جدول تجزئة + قائمة مرتبة**.
- **الأداء**:
  - `O(1)` للعمليات get/put/remove (مثل `HashMap`)
  - حمولة ذاكرة إضافية طفيفة بسبب القائمة المرتبطة
- **حالة الاستخدام**: عندما تهتم **بالترتيب الذي تمت به إضافة المدخلات**.

---

### ✅ ترتيب المكرر (Iterator)
نعم، **سيكون ترتيب المكرر مختلفًا**:

| نوع الـ Map   | ترتيب المكرر                      |
|---------------|-----------------------------------|
| `TreeMap`     | مرتب حسب المفاتيح                |
| `LinkedHashMap`| ترتيب الإدراج (أو ترتيب الوصول)   |

---

### 🔍 مثال

```java
Map<String, Integer> treeMap = new TreeMap<>();
Map<String, Integer> linkedMap = new LinkedHashMap<>();

treeMap.put("banana", 2);
treeMap.put("apple", 3);
treeMap.put("cherry", 1);

linkedMap.put("banana", 2);
linkedMap.put("apple", 3);
linkedMap.put("cherry", 1);

System.out.println("TreeMap:");
for (String key : treeMap.keySet()) {
    System.out.println(key);
}

System.out.println("LinkedHashMap:");
for (String key : linkedMap.keySet()) {
    System.out.println(key);
}
```

**النتيجة:**
```
TreeMap:
apple
banana
cherry

LinkedHashMap:
banana
apple
cherry
```

أعلمني إذا كنت تريد تفصيلاً عن الأداء أو استخدام الذاكرة بمزيد من التفصيل.