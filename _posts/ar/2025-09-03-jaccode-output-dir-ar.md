---
audio: false
generated: true
image: false
lang: ar
layout: post
title: تكوين دليل إخراج وكيل جاكوكو
translated: true
type: note
---

نعم — تمامًا 👍.

افتراضيًا، إذا كتبت

```bash
-javaagent:/path/to/jacocoagent.jar=destfile=jacoco.exec,output=file
```

فإن `jacoco.exec` سيتم إنشاؤه في **دليل العمل الحالي** لعملية JVM (غالبًا مجلد الخادم root أو من أي مكان قمت بتشغيل Liberty منه).

إذا كنت تريد التحكم في مكان إنشائه، يمكنك تحديد **مسار مطلق أو نسبي**، على سبيل المثال:

```bash
-javaagent:/path/to/jacocoagent.jar=destfile=/opt/coverage-data/jacoco.exec,output=file
```

أو

```bash
-javaagent:/path/to/jacocoagent.jar=destfile=target/jacoco.exec,output=file
```

(حيث `target/` مسار نسبي بالنسبة لدليل التشغيل startup directory).

لذا نعم، إذا كنت تريد ضمان مكان إنشاء ملف `.exec`، استخدم المسار الكامل.

👉 نصيحة: من الشائع في مشاريع Maven/Gradle تحديد المسار إلى مجلد `target/` (في Maven) أو `build/` (في Gradle)، حتى تتمكن إضافات plugins المختصة بتقارير التغطية من استخدام الملف تلقائيًا لاحقًا.

هل تريد مني أن أعرض لك مثالاً لملف `jvm.options` الخاص بـ Liberty مع تكوين المسار الكامل؟