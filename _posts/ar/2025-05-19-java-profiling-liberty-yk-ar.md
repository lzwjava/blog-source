---
audio: false
generated: true
image: false
lang: ar
layout: post
title: تحديد أداء ليبرتي باستخدام برنامج YourKit لتحليل أداء جافا
translated: true
type: note
---

يمكن دمج WebSphere Liberty مع YourKit Java Profiler بسلاسة، نظرًا لأن Liberty هو مجرد وقت تشغيل آخر قائم على JVM. إليك كيف يعمل ذلك عادةً:

---

### 1. كيفية عمل الدمج

لا يحتاج YourKit Java Profiler إلى إضافات خاصة بـ Liberty؛ فهو يعمل من خلال الالتحاق بعملية JVM التي يعمل فيها Liberty. بمجرد الالتحاق، يعترض الملف استدعاءات الدوال، وتبايع الذاكرة، ونشاط الخيوط، وأحداث وقت التشغيل الأخرى.
يعمل Liberty على JVM OpenJ9 (أو HotSpot)، لذا يمكنك تمكين التنميط بإضافة وكيل YourKit إلى بدء تشغيل Liberty.

---

### 2. إرفاق وكيل YourKit

هناك طريقتان رئيسيتان:

* **الالتحاق عند بدء التشغيل (مفضل للخوادم)**
  أضف خيارات وكيل YourKit إلى ملف `jvm.options` الخاص بـ Liberty (في `<wlp_home>/usr/servers/<server_name>/`).
  مثال:

  ```
  -agentpath:/path/to/yourkit/bin/linux-x86-64/libyjpagent.so=port=10001,listen=all
  ```

  يؤدي هذا إلى تحميل وكيل المنقح بمجرد بدء تشغيل الخادم.

* **الالتحاق عند الطلب (ديناميكي)**
  إذا كان Liberty يعمل بالفعل، يمكنك إرفاق YourKit عبر واجهة المستخدم الرسومية. يفحص البرنامج عمليات JVM قيد التشغيل ويتيح لك الاتصال دون إعادة التشغيل. مفيد لتصحيح الأخطاء في بيئة الإنتاج، ولكنه يتطلب أذونات (نفس المستخدم / JDK tools.jar).

---

### 3. التنميط في سياق Liberty

عند التشغيل على Liberty، قد ترغب في:

* **التصفية حسب حزم التطبيقات** حتى لا تغرق في الكود الداخلي لـ Liberty.
* **استخدام عرض شجرة الاستدعاء / النقاط الساخنة** لتحليل الخدمات البطيئة، أو نقاط نهاية JAX-RS، أو حبوب CDI.
* **مراقبة JDBC ومجموعات الخيوط** نظرًا لأن تطبيقات Liberty تعتمد غالبًا على الموارد المدارة بواسطة الحاوية. يمكن لـ YourKit تتبع استدعاءات JDBC وقياس أوقات الانتظار.
* **معالجة تطبيقات متعددة** – إذا استضاف Liberty عدة تطبيقات، فاستخدم عوامل تصفية الخيوط والفئات في YourKit لتضييق نطاق التنميط إلى التطبيق الذي تهتم به.

---

### 4. الاعتبارات المتعلقة بالأداء

* يضيف التنميط دائمًا عبئًا. لأغراض التطوير، لا بأس من استخدام التنميط الكامل لوحدة المعالجة المركزية / الذاكرة؛ في الإنتاج، استخدم وضع *أخذ العينات* بدلاً من الأدوات.
* يمكن أن يتسبب التحميل الطبقي المعياري لـ Liberty أحيانًا في تكرار أسماء الفئات؛ يتعامل YourKit مع هذا بشكل عام ولكن كن حذرًا عند التصفية.
* إذا كنت تستخدم IBM JDK مع Liberty، فتأكد من تنزيل وكيل YourKit المخصص لـ IBM J9/OpenJ9، وليس فقط HotSpot.

---

### 5. سير العمل النموذجي

1. قم بتثبيت YourKit على جهاز التطوير الخاص بك.
2. قم بتعديل `jvm.options` الخاص بـ Liberty مع مسار الوكيل.
3. ابدأ تشغيل Liberty.
4. اتصل من واجهة المستخدم الرسومية لـ YourKit إلى JVM الخاص بـ Liberty (عبر المقبس أو الالتحاق).
5. قم بتنميط نقاط النهاية الخاصة بك، أو استخدام الذاكرة، أو أقفال الخيوط.
6. احفظ لقطات الحالة للتحليل دون اتصال بالإنترنت.

---

👉 هل تريد مني أن أعطيك **مثالاً خطوة بخطوة للإعداد** لـ Liberty على **OpenJ9** (JVM الافتراضي لـ Liberty) مع YourKit، بما في ذلك إعدادات `jvm.options` نموذجية للتنميط بوحدة المعالجة المركزية مقابل الذاكرة؟