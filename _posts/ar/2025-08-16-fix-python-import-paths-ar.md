---
audio: false
generated: true
image: false
lang: ar
layout: post
title: إصلاح مسارات استيراد وحدة بايثون
translated: true
type: note
---

الإجابة المختصرة: أنت بحاجة إلى `sys` هنا حتى تتمكن من تعديل مسار البحث عن وحدات بايثون (`sys.path`) قبل تنفيذ `from scripts.create.delete import delete_md`.

---

### لماذا `sys` مطلوبة

* تبحث بايثون عن الوحدات/الحزم لاستيرادها في قائمة الدلائل المخزنة في `sys.path`.
* بشكل افتراضي، تحتوي على:

  * دليل السكريبت الحالي.
  * دلائل المكتبة القياسية.
  * حزم site-packages المثبتة.
* إذا كان ملف `scripts/create/delete.py` الخاص بك ليس في أحد هذه الأماكن الافتراضية، فسيفشل الاستيراد مع `ModuleNotFoundError`.

لذلك يقوم الكود بالتالي:

```python
sys.path.append(
    os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
)
```

هذا يعني:

* خذ المسار المطلق للملف الحالي (`__file__`).
* انتقل ثلاثة مستويات لأعلى في هيكل الدلائل.
* أضف ذلك الدليل إلى `sys.path`.

بعد ذلك، يمكن لبايثون حل مسار `scripts.create.delete` كما لو كان مثبتًا أو موجودًا في جذر المشروع.

---

### بدون `sys`

إذا كان هيكل مشروعك كالتالي:

```
project/
├── scripts/
│   └── create/
│       └── delete.py
└── tools/
    └── myscript.py   <-- أنت تشغل هذا الملف
```

وقمت بتشغيل `myscript.py` مباشرة، لن تعرف بايثون تلقائيًا أن `project/` هو الجذر. سيفشل الاستيراد ما لم تقم إما بـ:

* تشغيله باستخدام `PYTHONPATH=...` (مثال: `PYTHONPATH=.. python myscript.py`)، أو
* تجميع المشروع وتثبيته (`pip install -e .`)، أو
* إضافة المسار ديناميكيًا في الكود باستخدام `sys.path.append(...)` (كما تم هنا).

---

👉 إذن `sys` مطلوبة حصريًا من أجل **معالجة مسار الاستيراد أثناء وقت التشغيل**، مما يسمح لك باستيراد وحدات مرتبطة بجذر مشروعك.

هل تريد مني أيضًا أن أعرض لك بعض **البدائل الأكثر نظافة** لتجنب التعامل مع `sys.path` يدويًا؟