---
audio: false
date: 2025-09-11
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: فهم دورات حياة مافن وجاكوكو
translated: true
type: note
---

سؤال جيد — هذا يعود إلى *مراحل دورة حياة Maven* وكيفية ارتباط الإضافات بها.

### أساسيات دورة الحياة

لدى Maven ثلاث دورات حياة قياسية: `clean`، و`default` (تُسمى أحيانًا *build*)، و`site`.  
داخل دورة الحياة *default*، تمر المراحل المهمة بالترتيب كالتالي:

* `validate`
* `compile`
* `test`
* `package`
* `verify`
* `install`
* `deploy`

كل مرحلة تشمل جميع المراحل السابقة (إذا قمت بتشغيل `mvn verify`، فإن Maven ينفذ كل شيء من `validate` حتى `verify`). تشغيل `mvn package` سيتوقف بعد إنشاء الـ artifact (JAR/WAR)، ولن يقوم بتشغيل `verify`.

### لماذا يتم تضمين JaCoCo

إضافة Maven الخاصة بـ JaCoCo تربط عادةً هدفها **`prepare-agent`** بمرحلة `initialize` أو `verify` اعتمادًا على التكوين.

* إذا كانت مرتبطة بمرحلة `verify` (الإعداد الشائع لتقارير التغطية)، فإن `mvn verify` سيُطلقها.
* `mvn package` لا يصل إلى تلك المرحلة في دورة الحياة، لذلك يتم تخطي مرحلة `verify` (وبالتالي خطوة JaCoCo).

لهذا السبب ترى JaCoCo يتم تنفيذه عند تشغيل `mvn verify` ولكن ليس عند تشغيل `mvn package`.

### الاستفادة العملية

* استخدم **`mvn package`** عندما تريد فقط artifact البناء (سريع، بدون فحوصات إضافية).
* استخدم **`mvn verify`** عندما تريد تشغيل *اختبارات التكامل، والفحوصات، وتقارير التغطية، أو إضافات ضمان الجودة* المرتبطة بالمراحل المتأخرة.

👉 إذا كنت تريد أن يعمل JaCoCo دائمًا حتى مع `mvn package`، يمكنك إعادة ربط هدف `prepare-agent` بمرحلة مبكرة مثل `test` أو `package` في ملف `pom.xml` الخاص بك.

هل تريد مني أن أعرض لك مقتطفًا محددًا من `pom.xml` يجعل `prepare-agent` الخاص بـ JaCoCo يعمل بالفعل عند مرحلة `test` بدلاً من `verify`؟