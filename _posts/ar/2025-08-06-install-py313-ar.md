---
audio: false
generated: true
image: false
lang: ar
layout: post
title: تثبيت رؤوس تطوير بايثون ٣.١٣
translated: true
type: note
---

يكشف هذا الخطأ الجديد عن العقبة النهائية في بناء `sentencepiece`: **رؤوس تطوير بايثون المفقودة**.

---

### ❌ خطأ:

```
fatal error: Python.h: No such file or directory
```

هذا يعني أن المترجم لا يستطيع العثور على `Python.h`، وهو ضروري لتجميع امتدادات Python C.

---

### ✅ الحل

تحتاج إلى تثبيت رؤوس تطوير Python 3.13:

#### لنظامي Ubuntu/Debian (Python 3.13):

نظرًا لأن Python 3.13 جديد جدًا، إذا كنت تستخدم بناءً مخصصًا أو `pyenv`، فقد لا تكون الرؤوس مثبتة افتراضيًا. جرب أحد الخيارات التالية:

1. **إذا كنت تستخدم Python 3.13 على مستوى النظام** (مثبت من deadsnakes أو المصدر):

```bash
sudo apt install python3.13-dev
```

2. **إذا كنت تستخدم `pyenv`:**

```bash
pyenv install 3.13.0 --with-openssl --enable-shared
```

وتأكد من أن بيئتك الافتراضية تستخدم هذا الإصدار.

---

### 🔁 أعد المحاولة بعد ذلك:

بمجرد تثبيت الرؤوس، حاول مرة أخرى:

```bash
pip install sentencepiece --use-pep517
```

---

أخبرني إذا كنت تستخدم تثبيت Python مخصصًا (مثل: تم تجميعه يدويًا، أو عبر `pyenv`، أو `conda`، إلخ)، وسأتمكن من تقديم إرشادات أكثر تحديدًا.