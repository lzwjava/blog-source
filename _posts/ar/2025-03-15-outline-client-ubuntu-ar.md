---
audio: false
generated: true
lang: ar
layout: post
title: تثبيت عميل Outline على أوبونتو
translated: true
type: note
---

نعم، يمكنك تثبيت عميل Outline على أوبونتو، لأنه توزيعة لينكس مبنية على دبيان، ويتم توفير عميل Outline كحزمة دبيان، والتي تتوافق مع أوبونتو. هناك طريقتان لتثبيته: الطريقة الموصى بها باستخدام المستودع وطريقة بديلة عن طريق تنزيل حزمة دبيان مباشرة. أدناه، سأشرح كيفية القيام بذلك باستخدام طريقة المستودع الموصى بها، وهي مفضلة لأنها تدمج مع مدير الحزم في أوبونتو، مما يجعل إدارة التحديثات أسهل.

### خطوات تثبيت عميل Outline على أوبونتو (الطريقة الموصى بها)

اتبع هذه الخطوات لتثبيت عميل Outline على نظام أوبونتو الخاص بك:

1. **افتح الطرفية**  
   شغّل تطبيق الطرفية على نظام أوبونتو الخاص بك. يمكنك القيام بذلك عن طريق البحث عن "Terminal" في قائمة التطبيقات أو الضغط على `Ctrl + Alt + T`.

2. **ثبّت مفتاح مستودع Outline**  
   شغّل الأمر التالي لتنزيل وإضافة مفتاح توقيع المستودع إلى المفاتيح الموثوقة في نظامك. يضمن هذا التحقق من صحة الحزم القادمة من المستودع:
   ```bash
   wget -qO- https://us-apt.pkg.dev/doc/repo-signing-key.gpg | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/gcloud-artifact-registry-us.gpg
   ```

3. **أضف مستودع عميل Outline**  
   أضف مستودع عميل Outline إلى قائمة مصادر نظامك بتشغيل هذا الأمر. يخبر هذا أوبونتو بمكان العثور على حزمة عميل Outline:
   ```bash
   echo "deb [arch=amd64] https://us-apt.pkg.dev/projects/jigsaw-outline-apps outline-client main" | sudo tee /etc/apt/sources.list.d/outline-client.list
   ```
   - ملاحظة: الجزء `[arch=amd64]` يحدد أن هذا لأنظمة 64-بت. معظم تثبيتات أوبونتو الحديثة هي أنظمة 64-بت، ولكن يمكنك التأكد من بنية نظامك بتشغيل `uname -m`. إذا كانت النتيجة `x86_64`، فأنت تستخدم نظام 64-بت، وهذا الأمر سيعمل كما هو.

4. **حدّث قائمة الحزم**  
   حدّث قائمة الحزم في نظامك لتشمل مستودع Outline المضاف حديثًا:
   ```bash
   sudo apt update
   ```

5. **ثبّت عميل Outline**  
   ثبّت أحدث إصدار من عميل Outline باستخدام هذا الأمر:
   ```bash
   sudo apt install outline-client
   ```

### بعد التثبيت

- **تشغيل عميل Outline**: بعد التثبيت، يمكنك العثور على عميل Outline في قائمة التطبيقات أو تشغيله من الطرفية بكتابة `outline-client`.
- **الحفاظ على تحديثه**: للتحقق من التحديثات وتثبيتها، استخدم أوامر التحديث القياسية لأوبونتو:
  ```bash
  sudo apt update
  sudo apt upgrade
  ```
  ستقوم هذه الأوامر بتحديث جميع الحزم المثبتة، بما في ذلك عميل Outline، حيث يتم إدارته عبر المستودع. لاحظ أن التحديث التلقائي داخل التطبيق معطل لعميل Outline على لينكس بدءًا من الإصدار 1.15، لذا فإن الاعتماد على مدير الحزم هو أفضل طريقة للبقاء محدثًا.
- **إلغاء التثبيت**: إذا كنت بحاجة إلى إزالة عميل Outline، شغّل:
  ```bash
  sudo apt purge outline-client
  ```

### لماذا نستخدم الطريقة الموصى بها؟

طريقة المستودع موصى بها لأن:
- تدمج مع مدير الحزم `apt` في أوبونتو، مما يسمح لك بتحديث عميل Outline جنبًا إلى جنب مع حزم النظام الأخرى.
- لا تحتاج إلى تنزيل وتثبيت الإصدارات الجديدة يدويًا، على عكس الطريقة البديلة حيث سيتعين عليك تكرار عملية التنزيل والتثبيت لكل تحديث.

### الافتراضات والمتطلبات

- **إصدار أوبونتو**: الإرشادات لا تحدد حدًا أدنى لإصدار أوبونتو، ولكن بما أن أوبونتو مبنية على دبيان والحزمة تستهدف أنظمة 64-بت، فمن المفترض أن يعمل أي إصدار حديث من أوبونتو 64-بت. لتكون في أمان، تأكد من أن نظامك محدث بتشغيل `sudo apt update && sudo apt upgrade` قبل البدء.
- **البنية**: تفترض هذه الإرشادات نظام 64-بت (amd64). إذا كنت تستخدم بنية مختلفة (مثل 32-بت أو ARM)، فإن هذه الطريقة لن تعمل كما هي، وستحتاج إلى التحقق من وجود حزم بديلة أو الاتصال بدعم Outline.

### طريقة بديلة (اختيارية)

إذا كنت تفضل عدم إضافة مستودع، يمكنك تنزيل وتثبيت حزمة دبيان مباشرة:
1. حمّل الحزمة:
   ```bash
   wget -O ./outline-client.deb https://s3.amazonaws.com/outline-releases/client/linux/stable/outline-client_amd64.deb
   ```
2. ثبّتها:
   ```bash
   sudo apt install ./outline-client.deb
   ```
ومع ذلك، ستحتاج إلى تكرار هذه العملية يدويًا للتحديثات، وهو أقل ملاءمة.

### خاتمة

باستخدام طريقة المستودع الموصى بها، يمكنك بسهولة تثبيت وصيانة عميل Outline على أوبونتو. ما عليك سوى اتباع الخطوات المذكورة أعلاه، وستتمكن من تشغيله مع القدرة على تحديثه بسهولة من خلال مدير الحزم في نظامك.