---
audio: false
generated: true
image: false
lang: ar
layout: post
model: x-ai/grok-4-fast
title: المصفوفات كخرائط هاشية موضحة
translated: true
type: note
---

نعم، أنت محق تمامًا! دعني أوضح الأمر بشكل مفصل لأؤكد وأوسع تفكيرك.

### 1. **المصفوفات كصورة مبسطة لـ HashMap**
   - في المصفوفة، يعمل **الفهرس** كـ **مفتاح ضمني** (عادةً ما يبدأ من 0 ويكون أعدادًا صحيحة متتالية).
   - **القيمة** في كل فهرس هي العنصر (والتي يمكن أن تكون بدائية مثل رقم/سلسلة نصية، أو معقدة مثل struct/كائن).
   - لذا، نظريًا، المصفوفة *هي* نوع من hashmap بمفاتيح رقمية. على سبيل المثال:
     - المصفوفة: `["apple", "banana", "cherry"]`
     - النظير في hashmap: `{0: "apple", 1: "banana", 2: "cherry"}`
   - هذا هو السبب في أن المصفوفات سريعة في الوصول العشوائي (O(1) عبر الفهرس)، تمامًا مثل الـ hashmaps، لكن الـ hashmaps تسمح بمفاتيح اعتباطية (مثل السلاسل النصية، الكائنات) لمرونة أكبر.

### 2. **تحويل مصفوفات الـ Structs/الكائنات إلى HashMaps**
   - إذا كانت عناصر مصفوفتك هي structs أو كائنات (على سبيل المثال، تحتوي على خصائص)، يمكنك بسهولة تحويل المصفوفة إلى hashmap كامل عن طريق:
     - استخدام إحدى خصائص الكائن كـ **مفتاح**.
     - استخدام الكائن بأكمله (أو خاصية أخرى) كـ **قيمة**.
   - هذا نمط شائع للبحث، التجميع، أو إزالة التكرار (على سبيل المثال، إنشاء خريطة مفتاحها ID للوصول السريع O(1)).

   **مثال في JavaScript** (باستخدام `reduce` لبناء الخريطة):
   ```javascript
   const arrayOfObjects = [
     { id: 1, name: "Alice", age: 30 },
     { id: 2, name: "Bob", age: 25 },
     { id: 3, name: "Charlie", age: 35 }
   ];

   // التحويل إلى hashmap: المفتاح = 'id'، القيمة = الكائن بأكمله
   const hashmap = arrayOfObjects.reduce((map, obj) => {
     map[obj.id] = obj;  // id كمفتاح
     return map;
   }, {});

   console.log(hashmap);
   // الناتج: {1: {id:1, name:'Alice', age:30}, 2: {id:2, name:'Bob', age:25}, 3: {id:3, name:'Charlie', age:35}}

   // الآن البحث سريع: hashmap[1].name === "Alice"
   ```

   **مثال في Python** (باستخدام dict comprehension):
   ```python
   array_of_objects = [
       {'id': 1, 'name': 'Alice', 'age': 30},
       {'id': 2, 'name': 'Bob', 'age': 25},
       {'id': 3, 'name': 'Charlie', 'age': 35}
   ]

   # التحويل إلى dict: المفتاح = 'id'، القيمة = الـ dict بأكمله
   hashmap = {obj['id']: obj for obj in array_of_objects}

   print(hashmap)
   # الناتج: {1: {'id':1, 'name':'Alice', 'age':30}, 2: {'id':2, 'name':'Bob', 'age':25}, 3: {'id':3, 'name':'Charlie', 'age':35}}

   # البحث: hashmap[1]['name'] == 'Alice'
   ```

   **مثال في Java** (باستخدام `HashMap` و streams):
   ```java
   import java.util.*;
   import java.util.stream.Collectors;

   class Person {
       int id;
       String name;
       int age;
       // Constructor, getters, etc.
   }

   List<Person> arrayOfObjects = Arrays.asList(
       new Person(1, "Alice", 30),
       new Person(2, "Bob", 25),
       new Person(3, "Charlie", 35)
   );

   // التحويل إلى HashMap: المفتاح = id، القيمة = كائن Person
   Map<Integer, Person> hashmap = arrayOfObjects.stream()
       .collect(Collectors.toMap(Person::getId, person -> person));

   System.out.println(hashmap.get(1).getName());  // "Alice"
   ```

### متى/لماذا نقوم بهذا التحويل؟
- **الأداء**: المصفوفات رائعة للوصول المرتب، لكن الـ hashmaps تتألق في البحث المعتمد على المفتاح (مثل إيجاد مستخدم بالبريد الإلكتروني دون مسح المصفوفة بأكملها).
- **حالات الاستخدام**: APIs (مثل تجميع البيانات حسب الفئة)، التخزين المؤقت، أو عندما تحتاج لتجنب التكرار (الـ hashmaps تستبدل في حالة تكرار المفتاح).
- **محاذير**:
  - يجب أن تكون المفاتيح فريدة (أو معالجة التكرار).
  - لا تعامل جميع اللغات المصفوفات تمامًا مثل الـ hashmaps في الأساس (على سبيل المثال، المصفوفات تستخدم ذاكرة متجاورة، بينما الـ hashmaps تستخدم التجزئة).
  - إذا كانت مصفوفتك ضخمة، فكر في استخدام الذاكرة — الـ hashmaps تستهلك مساحة أكبر بسبب النفقات العامة.

إذا كنت تعمل بلغة محددة أو لديك مثال كود تحاول تحويله، شارك المزيد من التفاصيل للحصول على مساعدة مخصصة! 😊