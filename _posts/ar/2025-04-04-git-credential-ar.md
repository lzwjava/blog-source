---
audio: false
generated: true
lang: ar
layout: post
title: مدير بيانات اعتماد Git
translated: true
type: note
---

أنت تستخدم **Git Credential Manager (GCM) الإصدار 2.6.1** على نظام **macOS**، وواجهت مشكلة حيث أن الأمر `git config --global credential.helper manager-core` يظهر خطأ:

```
warning: credential.helper has multiple values
error: cannot overwrite multiple values with a single value
```

يحدث هذا لأنه تم تعيين عدة مساعدين للأمان (credential helpers) في إعدادات Git الخاصة بك. تحتاج إلى **مسح القيم الحالية** قبل تعيين قيمة جديدة.

---

### **🛠 إصلاح المشكلة: إعادة تعيين مساعد الأمان**
#### **الخطوة 1: التحقق من مساعدي الأمان الحاليين**
نفّذ:
```sh
git config --global --get-all credential.helper
```
سيؤدي هذا إلى سرد جميع مساعدي الأمان المُهيأين.

#### **الخطوة 2: إلغاء تعيين مساعدي الأمان السابقين**
أعِد تعيين جميع مساعدي الأمان باستخدام:
```sh
git config --global --unset-all credential.helper
```

#### **الخطوة 3: تعيين GCM كمساعد الأمان الوحيد**
الآن، عيّن **Git Credential Manager (GCM)** كمساعد الأمان مرة أخرى:
```sh
git config --global credential.helper manager-core
```

#### **الخطوة 4: التحقق من التغيير**
تحقق مما إذا تم التعيين بشكل صحيح:
```sh
git config --global --get credential.helper
```
الناتج المتوقع:
```
manager-core
```

---

### **🛠 بديل: استخدام `--replace-all`**
إذا كنت تريد الكتابة فوق القيم الحالية بدلاً من إلغاء تعيينها أولاً، استخدم:
```sh
git config --global --replace-all credential.helper manager-core
```

---

### **🧩 تصحيح أخطاء إضافي**
إذا كنت لا تزال تواجه مشاكل، يمكنك التحقق من إعدادات Git العامة الكاملة:
```sh
git config --global --list
```
إذا استمرت المشكلة، أعلمني! 🚀

---

Git Credential Manager (GCM) هو مساعد أمان آمن لـ Git يقوم بتخزين وإدارة بيانات اعتماد المصادقة لمستودعات Git التي تستخدم HTTPS. وهو يعمل مع خدمات مثل GitHub و GitLab و Bitbucket و Azure DevOps.

---

## **1. تثبيت Git Credential Manager**
يتم تضمين GCM مع Git لنظام Windows والتوزيعات الرسمية لـ Git لأنظمة macOS و Linux. إذا لم يكن مثبتًا، قم بتنزيله من:

🔗 **[Git Credential Manager على GitHub](https://github.com/GitCredentialManager/git-credential-manager)**

### **التحقق مما إذا كان GCM مثبتًا**
نفّذ:
```sh
git credential-manager version
```
إذا كان مثبتًا، فسترى رقم إصدار. إذا لم يكن مثبتًا، قم بتثبيته يدويًا.

---

## **2. تكوين Git لاستخدام GCM**
نفّذ الأمر التالي:
```sh
git config --global credential.helper manager
```
لأنظمة macOS/Linux، استخدم:
```sh
git config --global credential.helper manager-core
```

للتحقق من مساعد الأمان الحالي:
```sh
git config --global credential.helper
```

---

## **3. المصادقة مع خادم Git**
بمجرد التهيئة، في المرة الأولى التي تتفاعل فيها مع مستودع بعيد عبر HTTPS (مثل `git clone`، `git pull`)، سيطالبك GCM بإدخال بيانات الاعتماد.

- إذا كنت تستخدم GitHub أو GitLab أو Bitbucket، سيفتح GCM المتصفح للمصادقة باستخدام OAuth.
- إذا كنت تستخدم رمز وصول شخصي (PAT)، فأدخله بدلاً من كلمة المرور.

---

## **4. تخزين وإدارة بيانات الاعتماد**
يخزن GCM بيانات الاعتماد بشكل آمن في مخزن بيانات اعتماد نظام التشغيل:
- **Windows**: Windows Credential Manager
- **macOS**: macOS Keychain
- **Linux**: ملف مشفر بـ GPG (`~/.git-credentials` أو `~/.gitconfig`)

لإضافة بيانات اعتماد يدويًا:
```sh
git credential approve
```

لإزالة بيانات اعتماد مخزنة:
```sh
git credential reject https://github.com
```

لمسح جميع بيانات الاعتماد:
```sh
git credential reject
```

---

## **5. استخدام Git Credential Manager مع موفري Git المختلفين**
### **GitHub**
استخدم **رمز وصول شخصي (PAT)** بدلاً من كلمة المرور:
```sh
git credential reject https://github.com
git pull
```
سيُطلب منك تسجيل الدخول وإدخال الرمز المميز الخاص بك.

### **GitLab**
بالنسبة لـ GitLab، استخدم OAuth أو **PAT**:
```sh
git credential reject https://gitlab.com
git pull
```

### **Azure DevOps**
استخدم **PAT** أو مصادقة Azure.

---

## **6. تصحيح المشكلات**
إذا لم يتم حفظ بيانات الاعتماد أو استرجاعها بشكل صحيح:
1. تحقق من مساعد الأمان:
   ```sh
   git config --global credential.helper
   ```
2. أعِد تعيين Git Credential Manager:
   ```sh
   git credential reject https://your-git-service.com
   ```
3. أعد تثبيت GCM إذا لزم الأمر.

هل تريد المساعدة في تكوينه لخدمة معينة؟ 🚀