---
audio: false
generated: true
lang: ar
layout: post
title: تكوين مترجم جافا في فيجوال ستوديو كود
translated: true
type: note
---

مرحبًا! لضبط مترجم جافا في Visual Studio Code (VSCode) الإصدار 1.96.4 على نظام Windows، خاصةً وأنك قمت بالفعل بتثبيت Java Extension Pack، ستحتاج إلى إعداد مسار Java Development Kit (JDK). مترجم جافا هو جزء من JDK، ويحتاج VSCode إلى معرفة مكان العثور عليه لتجميع كود جافا الخاص بك. إليك دليل خطوة بخطوة لإعدادك:

---

### الخطوة 1: تأكد من تثبيت JDK
قبل تكوين أي شيء في VSCode، تأكد من تثبيت JDK (وليس فقط JRE) على جهاز Windows الخاص بك. يحتوي JDK على مترجم جافا (`javac`). إذا لم تثبته بعد، يمكنك تنزيله من موفر مثل Oracle، أو OpenJDK، أو Adoptium (مثل JDK 17 أو إصدار آخر متوافق مع مشروعك). بعد التثبيت، لاحظ مسار التثبيت (مثال: `C:\Program Files\Java\jdk-17.0.1`).

---

### الخطوة 2: افتح إعدادات VSCode
لإخبار VSCode بمكان وجود JDK الخاص بك، ستحتاج إلى تعديل إعداداته:

- **من خلال واجهة مستخدم الإعدادات:**
  - اضغط على `Ctrl + ,` لفتح لوحة الإعدادات.
  - بدلاً من ذلك، انتقل إلى `File > Preferences > Settings`.
- **من خلال settings.json (اختياري):**
  - اضغط على `Ctrl + Shift + P` لفتح Command Palette.
  - اكتب **"Open Settings (JSON)"** وحدده لتحرير ملف `settings.json` مباشرة.

---

### الخطوة 3: عيّن مسار JDK باستخدام `java.home`
تعتمد Java Extension Pack على الإعداد `java.home` لتحديد موقع JDK الخاص بك للتجميع وميزات اللغة (مثل IntelliSense). إليك كيفية تكوينه:

- **في واجهة مستخدم الإعدادات:**
  - في لوحة الإعدادات، ابحث عن **"java.home"**.
  - في حقل "Java: Home"، أدخل المسار الكامل لتثبيت JDK الخاص بك. على سبيل المثال:
    ```
    C:\Program Files\Java\jdk-17.0.1
    ```
  - استخدم الشرطات المائلة للخلف (`\`) لأنك على نظام Windows، وتأكد من أن المسار يشير إلى الدليل الجذري لـ JDK (يجب أن يحتوي على مجلد `bin` بداخله `javac.exe`).

- **في settings.json:**
  - إذا كنت تقوم بتحرير `settings.json`، أضف هذا السطر (استبدل المسار بموقع JDK الفعلي الخاص بك):
    ```json
    "java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
    ```
  - مثال لملف `settings.json` كامل:
    ```json
    {
        "java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
    }
    ```
  - احفظ الملف بعد التحرير.

---

### الخطوة 4: تحقق من المسار
راجع مرة أخرى للتأكد من:
- أن المسار يشير إلى JDK (وليس JRE). يجب أن يحتوي مجلد `bin` التابع لـ JDK على `javac.exe`.
- عدم وجود أخطاء إملائية في المسار، وأنه يتطابق مع موقع تثبيت JDK الخاص بك (مثال: `C:\Program Files\Java\jdk-17.0.1`).

إذا لم تكن متأكدًا من مكان تثبيت JDK الخاص بك، يمكنك العثور عليه في `C:\Program Files\Java` أو في أي مكان اخترته أثناء التثبيت.

---

### الخطوة 5: اختياري - تكوين عدة إصدارات من JDK
إذا كان لديك عدة إصدارات من JDK مثبتة وتريد التبديل بينها (مثل JDK 8 لمشروع واحد، و JDK 17 لمشروع آخر)، يمكنك استخدام الإعداد `java.configuration.runtimes`:

- أضف هذا إلى `settings.json` الخاص بك:
  ```json
  "java.configuration.runtimes": [
      {
          "name": "JavaSE-1.8",
          "path": "C:\\Program Files\\Java\\jdk1.8.0_291"
      },
      {
          "name": "JavaSE-17",
          "path": "C:\\Program Files\\Java\\jdk-17.0.1",
          "default": true
      }
  ]
  ```
- يحدد الخيار `default: true` وقت التشغيل الافتراضي لتشغيل الكود الخاص بك. ومع ذلك، للتجميع، يستخدم امتداد جافا بشكل أساسي JDK المحدد في `java.home`.

---

### الخطوة 6: أعد تحميل أو أعد تشغيل VSCode
بعد تعيين `java.home`، قد تحتاج إلى:
- إعادة تحميل نافذة VSCode (اضغط `Ctrl + Shift + P`، اكتب **"Reload Window"**، وحدده).
- أو إعادة تشغيل VSCode بالكامل لبدء سريان التغييرات.

---

### ملاحظات حول التجميع
- **ملفات جافا المفردة:** إذا كنت تعمل مع ملفات `.java` فردية (بدون أدوات بناء)، يستخدم "Language Support for Java(TM) by Red Hat" الموجود في Java Extension Pack JDK من `java.home` لتجميع وتشغيل الكود الخاص بك. يمكنك استخدام زر "Run" أو أمر **"Java: Run"**.
- **المشاريع باستخدام أدوات البناء:** إذا كنت تستخدم Maven، أو Gradle، أو أداة بناء أخرى، يتم إدارة المترجم من خلال تكوين أداة البناء (مثل متغير البيئة `JAVA_HOME`، أو `pom.xml` لـ Maven، أو `build.gradle` لـ Gradle). ومع ذلك، فإن تعيين `java.home` يضمن仍然 عمل ميزات اللغة في VSCode بشكل صحيح.

---

### استكشاف الأخطاء وإصلاحها
- **مشاكل المسار:** إذا لم يتمكن VSCode من العثور على JDK، راجع المسار في `java.home` مرة أخرى. تجنب المسافات الإضافية أو علامات الاقتباس ما لم تكن ضرورية.
- **JDK غير مثبت:** إذا ظهرت لك أخطاء حول `javac` مفقود، تأكد من تثبيت JDK.
- **توافق الإصدار:** تأكد من أن إصدار JDK الخاص بك يتطابق مع متطلبات مشروعك (مثل JDK 8، 11، 17).

---

هذا كل شيء! يجب أن يكون لديك الآن مترجم جافا مضبوطًا في VSCode. إذا واجهت أي مشاكل أو كان لديك المزيد من الأسئلة— مثل العمل مع أداة بناء محددة— لا تتردد في إخباري، وسأكون سعيدًا لمساعدتك further! 😊

---

عيّن `maven.terminal.userJavaHome` إلى true. يمكنك التحقق من إصدار جافا في الطرفية.