---
audio: false
lang: ar
layout: post
title: تنظيم مفسر جاڤا في فيجوال ستوديو كود
translated: true
---

مرحبًا! لتكوين مدمج Java في Visual Studio Code (VSCode) الإصدار 1.96.4 على Windows، خاصة بعد أن قمت بتثبيت حزمة امتداد Java، عليك إعداد مسار Development Kit Java (JDK). فالمدمج Java جزء من JDK، ويحتاج VSCode إلى معرفة مكان وجوده لتجميع كودك Java. إليك دليل خطوة بخطوة للحصول على الإعداد:

---

### الخطوة 1: التأكد من وجود JDK مثبت
قبل إعداد أي شيء في VSCode، تأكد من أن لديك JDK (ليس مجرد JRE) مثبتًا على جهازك Windows. يحتوي JDK على مدمج Java (`javac`). إذا لم تقم بتثبيته بعد، يمكنك تحميله من مزود مثل Oracle، OpenJDK، أو Adoptium (على سبيل المثال، JDK 17 أو إصدار آخر متوافق مع مشروعك). بعد التثبيت، احفظ مسار التثبيت (على سبيل المثال، `C:\Program Files\Java\jdk-17.0.1`).

---

### الخطوة 2: فتح إعدادات VSCode
لتخبر VSCode مكان JDK، عليك تعديل إعداداته:

- **من خلال واجهة إعدادات:**
  - اضغط على `Ctrl + ,` لفتح لوحة إعدادات.
  - أو اذهب إلى `File > Preferences > Settings`.
- **من خلال settings.json (اختياري):**
  - اضغط على `Ctrl + Shift + P` لفتح لوحة الأوامر.
  - اكتب **"Open Settings (JSON)"** واخترها لتحرير ملف `settings.json` مباشرة.

---

### الخطوة 3: تعيين مسار JDK مع `java.home`
تتم الاعتماد على إعداد `java.home` من قبل حزمة امتداد Java لتحديد JDK لتجميع وخصائص اللغة (مثل IntelliSense). إليك كيفية تكوينه:

- **في واجهة إعدادات:**
  - في لوحة إعدادات، ابحث عن **"java.home"**.
  - في حقل "Java: Home"، أدخل المسار الكامل لتثبيت JDK. على سبيل المثال:
    ```
    C:\Program Files\Java\jdk-17.0.1
    ```
  - استخدم شريطين مائلين (`\`) لأنك على Windows، واكد أن المسار يشير إلى مجلد JDK الجذر (يجب أن يحتوي على مجلد `bin` يحتوي على `javac.exe`).

- **في settings.json:**
  - إذا كنت تحرر `settings.json`، أضف هذه السطر (استبدل المسار بموقع JDK الخاص بك):
    ```json
    "java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
    ```
  - مثال كامل `settings.json`:
    ```json
    {
        "java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
    }
    ```
  - احفظ الملف بعد التعديل.

---

### الخطوة 4: التحقق من المسار
تأكد من أن:
- المسار يشير إلى JDK (ليس JRE). يجب أن يحتوي مجلد `bin` في JDK على `javac.exe`.
- لا يوجد أخطاء في الكتابة، ويطابق المسار موقع تثبيت JDK الخاص بك (على سبيل المثال، `C:\Program Files\Java\jdk-17.0.1`).

إذا كنت غير متأكد من مكان تثبيت JDK، يمكنك العثور عليه في `C:\Program Files\Java` أو أي مكان اخترته أثناء التثبيت.

---

### الخطوة 5: اختياري - إعداد JDKات متعددة
إذا كنت تمتلك عدة إصدارات من JDK وتريد التبديل بينها (على سبيل المثال، JDK 8 لمشروع، JDK 17 لمشروع آخر)، يمكنك استخدام إعداد `java.configuration.runtimes`:

- أضف هذا إلى `settings.json`:
  ```json
  "java.configuration.runtimes": [
      {
          "name": "JavaSE-1.8",
          "path": "C:\\Program Files\\Java\\jdk1.8.0_291"
      },
      {
          "name": "JavaSE-17",
          "path": "C:\\Program Files\\Java\\jdk-17.0.1",
          "default": true
      }
  ]
  ```
- يحدد خيار `default: true` وقت التشغيل الافتراضي لتشغيل كودك. ومع ذلك، يستخدم امتداد Java بشكل أساسي JDK الذي يتم تحديده في `java.home` لتجميع.

---

### الخطوة 6: إعادة تحميل أو إعادة تشغيل VSCode
بعد تعيين `java.home`، قد تحتاج إلى:
- إعادة تحميل نافذة VSCode (اضغط على `Ctrl + Shift + P`، اكتب **"Reload Window"**، واخترها).
- أو إعادة تشغيل VSCode بالكامل لتتحقق التغييرات.

---

### ملاحظات حول التجميع
- **ملفات Java الفردية:** إذا كنت تعمل مع ملفات `.java` الفردية (بدون أدوات بناء)، يستخدم حزمة امتداد Java "Language Support for Java(TM) by Red Hat" JDK من `java.home` لتجميع وتشغيل كودك. يمكنك استخدام زر "Run" أو الأمر **"Java: Run"**.
- **المشاريع مع أدوات بناء:** إذا كنت تستخدم Maven، Gradle، أو أداة بناء أخرى، يتم إدارة المدمج بواسطة إعدادات أداة البناء (على سبيل المثال، متغير البيئة `JAVA_HOME`، `pom.xml` لمافن، أو `build.gradle` لمجرادل). ومع ذلك، يضمن تعيين `java.home` أن تعمل خصائص اللغة في VSCode بشكل صحيح.

---

### حل المشكلات
- **مشاكل المسار:** إذا لم يتمكن VSCode من العثور على JDK، تأكد من المسار في `java.home`. تجنب الفواصل الإضافية أو علامات الاقتباس إلا إذا كان ذلك ضروريًا.
- **JDK غير مثبت:** إذا حصلت على أخطاء حول `javac` المفقود، تأكد من تثبيت JDK.
- **التوافق بين الإصدارات:** تأكد من أن إصدار JDK يتوافق مع متطلبات مشروعك (على سبيل المثال، JDK 8، 11، 17).

---

ذلك كل شيء! الآن يجب أن يكون لديك مدمج Java مهيأ في VSCode. إذا واجهت أي مشاكل أو لديك أسئلة أخرى - مثل العمل مع أداة بناء محددة - فلا تتردد في إخبارني، وسأكون سعيدًا بمساعدتك! 😊

---

قم بتحديد `maven.terminal.userJavaHome` على `true`. يمكنك التحقق من إصدار Java في المصفح.