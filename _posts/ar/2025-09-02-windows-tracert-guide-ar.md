---
audio: false
generated: true
image: false
lang: ar
layout: post
title: دليل تشخيص مسار الشبكة باستخدام Tracert في Windows
translated: true
type: note
---

في نظام Windows، **`tracert`** (اختصارًا لـ *تتبع المسار*) هو أداة تشخيصية سطر أوامر تُستخدم لتتبع المسار الذي تسلكه الحزم من جهاز الكمبيوتر الخاص بك إلى المضيف الوجهة (مثل موقع ويب أو عنوان IP). يُستخدم بشكل أساسي لتحديد مكان حدوث التأخيرات أو مشاكل الاتصال في مسار الشبكة.

إليك كيف يعمل خطوة بخطوة:

### 1. طلبات صدى ICMP مع زيادة TTL تدريجيًا

* يرسل `tracert` **حزم طلبات صدى ICMP** (مشابهة لأمر `ping`).
* يقوم بالتلاعب بحقل **Time-To-Live (TTL)** في رأس حزمة IP.
* يبدأ TTL من القيمة 1، ثم يزداد (2، 3، 4، ...) مع كل مسبار.

### 2. انتهاء صلاحية TTL واستجابات "انتهت المهلة"

* عندما يستقبل جهاز التوجيه (router) حزمة، فإنه يُنقص قيمة TTL بمقدار 1.
* إذا وصل TTL إلى **0**، يتخلص جهاز التوجيه من الحزمة ويرسل إلى المرسل رسالة **ICMP Time Exceeded**.
* هذا يُعلم `tracert` بهوية (عنوان IP / اسم المضيف) ذلك الموجه.

### 3. بناء المسار

* بالنسبة لـ TTL = 1، ترى القفزة الأولى (البوابة الافتراضية لشبكتك).
* بالنسبة لـ TTL = 2، ترى الموجه التالي، وهكذا.
* يستمر هذا حتى:
  * يستجيب المضيف الوجهة برسالة **ICMP Echo Reply**، أو
  * يتم الوصول إلى الحد الأقصى للقفزات (30 افتراضيًا).

### 4. قياس زمن الوصول

* يرسل `tracert` **ثلاث مسابر** لكل قفزة (افتراضيًا).
* يعرض **زمن الذهاب والإياب (RTT)** بالمللي ثانية لكل مسبار.
* إذا لم يستجب أحد الموجهات، فسترى `*` بدلاً من الوقت.

### 5. مثال على الناتج

```text
تتبع المسار إلى example.com [93.184.216.34]
بحد أقصى 30 قفزة:

  1     1 ms    <1 ms     1 ms  192.168.1.1
  2    10 ms     9 ms    11 ms  isp-gateway.example.net [203.0.113.1]
  3    25 ms    23 ms    24 ms  core-router.example.net [198.51.100.1]
  ...
  8    45 ms    46 ms    47 ms  example.com [93.184.216.34]
```

### 6. ملاحظات حول السلوك

* قد تمنع بعض الموجهات أو تُخفض أولوية استجابات ICMP، مما يتسبب في ظهور `* * *`.
* يستخدم `tracert` في **Windows** طلبات صدى ICMP، بينما يستخدم `traceroute` في أنظمة Linux/Unix غالبًا **مسابر UDP** (على الرغم من إمكانية تغيير ذلك).
* يمكن أحيانًا لجدران الحماية أو تصفية موفر خدمة الإنترنت إخفاء أجزاء من المسار.

---

👉 هل تريد مني أيضًا شرح **كيفية تفسير حالات مثل `* * *` أو التأخيرات الكبيرة** في نتيجة `tracert`، حتى تتمكن من استخدامه في عملية استكشاف الأخطاء وإصلاحها الفعلية؟