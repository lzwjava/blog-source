---
audio: false
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: استخدام CMake وشرحه على نظام ويندوز
translated: true
type: note
---

سؤال رائع! دعنا نتناول ما هو **CMake**، وكيفية استخدامه – خاصة على **Windows**، وما الذي تعنيه أوامر مثل `cmake --build` فعليًا.

---

### 🔹 ما هو CMake؟
- **CMake** هو نظام توليد أنظمة بناء متعددة المنصات.
- هو **لا** يقوم ببناء المشاريع بنفسه – بل ينشئ ملفات البناء المناسبة (مثل حلول Visual Studio، وملفات Make، وملفات Ninja، إلخ) لمنصتك ومترجمك.
- ثم تستخدم تلك الملفات المُنشأة (أو أداة البناء الخاصة بـ CMake) لتجميع مشروعك فعليًا.

---

### 🔹 تثبيت CMake على Windows
1.  قم بالتحميل من: [https://cmake.org/download/](https://cmake.org/download/)
2.  قم بالتثبيت، وتأكد من تحديد خيار **"Add CMake to system PATH"** (أو أضفه يدويًا).
3.  تحقق من التثبيت في الطرفية (PowerShell أو CMD):
    ```sh
    cmake --version
    ```

---

### 🔹 سير العمل النموذجي لـ CMake على Windows

لنفترض أن مشروعك يحتوي على:
```
project_root/
 ├─ CMakeLists.txt
 └─ src/
     └─ main.cpp
```

#### 1. افتح طرفية
(يمكنك استخدام **x64 Native Tools Command Prompt for VS** إذا كنت تستخدم MSVC، أو PowerShell العادي إذا كنت تستخدم `MinGW`/`Ninja`.)

#### 2. أنشئ دليل بناء
من الأفضل فصل المصدر عن البناء:
```sh
cd project_root
mkdir build
cd build
```

#### 3. توليد ملفات نظام البناء
مثال مع Visual Studio:
```sh
cmake .. -G "Visual Studio 17 2022" -A x64
```

مثال مع Ninja:
```sh
cmake .. -G Ninja
```

هذه الخطوة تقرأ ملف `../CMakeLists.txt` وتولد تعريفات البناء اللازمة.

#### 4. بناء المشروع
الآن يمكنك استخدام:
```sh
cmake --build .
```

- على Windows مع Visual Studio، هذا يستدعي `MSBuild` في الخلفية.
- مع Ninja، يستدعي أداة `ninja`.

يمكنك أيضًا أن تخبر CMake بالبناء في وضع **Release**:
```sh
cmake --build . --config Release
```
⚠ ملاحظة: `--config` تُستخدم فقط مع مولدات التكوينات المتعددة (Visual Studio, Xcode). إذا كنت تستخدم Ninja أو Makefiles، فحدد ذلك أثناء التهيئة:
```sh
cmake .. -DCMAKE_BUILD_TYPE=Release
```

#### 5. تشغيل الملف التنفيذي الخاص بك
بعد البناء، ابحث في الدليل المناسب:
- لـ **Visual Studio**: `build/Debug/` أو `build/Release/`
- لـ **Ninja**: داخل `build/` مباشرة

---

### 🔹 ورقة غش للأوامر الشائعة
```sh
# تهيئة المشروع (توليد ملفات البناء)
cmake -S . -B build

# التهيئة مع نوع بناء Release (لمولد التكوين المفرد)
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

# بناء المشروع
cmake --build build

# البناء بتكوين محدد (لـ Visual Studio/Xcode)
cmake --build build --config Release

# تثبيت المخرجات المترجمة (إذا تم تعريفها في CMakeLists)
cmake --install build
```

---

✅ **الخلاصة**:  
- **`cmake ..`** → تهيئة وتوليد ملفات مشروع/بناء خاصة بالمنصة.  
- **`cmake --build .`** → يقوم بالتجميع الفعلي باستخدام أداة البناء المختارة.  
- على Windows، ستستخدم غالبًا **مولدات Visual Studio** أو **Ninja** للحصول على أداء أفضل.  

---

هل تريد مني أن أعطيك **مثالًا بسيطًا لملف CMakeLists.txt** وأشرح خطوة بخطوة بناء مشروع C++ بسيط "Hello World" على Windows؟