---
audio: false
generated: true
image: false
lang: ar
layout: post
title: رموز لغة لانجيد ومستوى الثقة
translated: true
type: note
---

في `langid`، ترجع دالة `classify(text)` مجموعة `(lang, confidence)` حيث:

* **`lang`** هو رمز لغة مكون من حرفين وفقًا لمعيار ISO 639-1 (يُمدد أحيانًا برموز المناطق مثل `zh-cn` أو `zh-tw`).
* **`confidence`** هو رقم عشري بين 0 و 1 يشير إلى مدى ثقة النموذج.

تأتي مجموعة القيم الممكنة لـ `lang` من اللغات المدعومة بواسطة `langid.py`. بشكل افتراضي، تتضمن **97 لغة**. بعض الأمثلة الشائعة:

* `en` → الإنجليزية
* `zh` / `zh-cn` / `zh-tw` → الصينية (عام / المبسطة / التقليدية)
* `ja` → اليابانية
* `ko` → الكورية
* `fr` → الفرنسية
* `de` → الألمانية
* `es` → الإسبانية
* `hi` → الهندية
* `ar` → العربية
* `ru` → الروسية

والعديد غيرها — تغطي القائمة الكاملة معظم لغات العالم الرئيسية (الأفريقانية، البنغالية، اليونانية، العبرية، الإيطالية، البرتغالية، السواحيلية، التايلاندية، التركية، الفيتنامية، إلخ).

👉 إذا كنت ترغب، يمكنني إعطاؤك **القائمة الكاملة لجميع الرموز الـ 97** المدعومة بواسطة `langid`. هل تريد مني إخراج ذلك؟