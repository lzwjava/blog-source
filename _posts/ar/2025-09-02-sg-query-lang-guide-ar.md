---
audio: false
generated: true
image: false
lang: ar
layout: post
title: دليل لغة الاستعلامات في سورسغراف
translated: true
type: note
---

يتمتع بحث Sourcegraph بلغة استعلام خاصة به مرنة وقوية. إليك تفصيل للتركيبات الشائعة التي يمكنك استخدامها:

---

### 1. **البحث الأساسي بالكلمات المفتاحية**

* ما عليك سوى كتابة المصطلحات:

  ```
  http client
  ```

  يبحث عن الملفات أو الكود الذي يحتوي على كلتا الكلمتين.

---

### 2. **المشغلات المنطقية**

* `AND`، `OR`، `NOT` (يجب أن تكون بأحرف كبيرة):

  ```
  React AND useEffect
  React OR Vue
  logger NOT debug
  ```

---

### 3. **الفلاتر**

* `repo:` → التحديد حسب المستودع

  ```
  repo:github.com/myorg/myrepo
  ```
* `file:` → مطابقة مسار الملف (يدعم التعبيرات النمطية)

  ```
  file:\.java$
  ```
* `lang:` → التحديد بلغة برمجة معينة

  ```
  lang:python requests
  ```
* `content:` → إجبار بحث نصي (يتجاهل المطابقات الهيكلية)

  ```
  content:"SELECT * FROM"
  ```
* `case:yes` → جعل البحث حساسًا لحالة الأحرف

---

### 4. **المطابقة بالتعبيرات النمطية**

* يدعم Sourcegraph التعبيرات النمطية بشكل افتراضي.

  ```
  foo(bar|baz)
  ^class\s+\w+
  ```

---

### 5. **البحث الهيكلي**

استخدم فجوات النمط مثل `:[var]` لمطابقة الهياكل البرمجية:

```
if (:[cond]) { :[body] }
```

---

### 6. **البحث في المراجعة / الفرع**

* ألحق `@branch`، أو `@commit`، أو `@tag`:

  ```
  repo:myrepo@main lang:go fmt.Println
  ```

---

### 7. **الرموز والتعريفات**

* `type:symbol` → البحث عن أسماء الرموز
* `type:commit` → البحث في رسائل الحفظ
* `type:diff` → البحث في فروق الكود

---

### 8. **أمثلة**

* ابحث عن جميع تعليقات `TODO` في ملفات Go:

  ```
  TODO lang:go
  ```
* ابحث فقط في مجلد `src/` عن دوال Java:

  ```
  file:^src/ lang:java "public static"
  ```
* ابحث عبر مستودعات متعددة:

  ```
  repo:github.com/org/.* file:\.ts$ "useState"
  ```

---

👉 هل تريد مني إعداد **ملخص سريع لأكثر استعلامات Sourcegraph استخدامًا** (مثل الاستعلامات المكونة من سطر واحد والتي يمكنك إعادة استخدامها يوميًا)، أم **شرحًا متعمقًا مع أنماط متقدمة**؟