---
audio: false
date: 2025-09-09
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: إدارة منافذ وعمليات ويندوز
translated: true
type: note
---

على نظام Windows، يمكنك التحقق من المنافذ التي يتم الاستماع إليها — وأي عملية (PID) تقوم بذلك — باستخدام الأمر `netstat`، ثم إدارة أو إنهاء تلك العمليات باستخدام `taskkill`. غالبًا ما تكون هذه العملية ضرورية عند تصحيح أخطاء مشاريع backend مثل تطبيقات Java Spring Boot أو Redis أو MySQL. إليك الطريقة:

---

### 1. التحقق من المنافذ المستمع إليها

شغّل **موجه الأوامر (cmd)** أو **PowerShell** كمسؤول:

```bash
netstat -ano | findstr LISTENING
```

* `-a` → إظهار جميع الاتصالات والمنافذ المستمع إليها
* `-n` → إظهار العناوين وأرقام المنافذ في شكل رقمي
* `-o` → تضمين معرف العملية (PID)

مثال على الناتج:

```
TCP    0.0.0.0:8080     0.0.0.0:0     LISTENING     12345
TCP    0.0.0.0:3306     0.0.0.0:0     LISTENING     6789
```

هنا:

* المنفذ `8080` مستخدم بواسطة العملية ذات المعرف `12345` (على الأرجح تطبيق Spring Boot الخاص بك).
* المنفذ `3306` مستخدم بواسطة العملية ذات المعرف `6789` (MySQL).

---

### 2. معرفة العملية التي ينتمي إليها هذا المعرف (PID)

يمكنك مطابقة المعرف مع اسم العملية:

```bash
tasklist | findstr 12345
```

قد يظهر الناتج:

```
java.exe                  12345 Console                    1     512,000 K
```

هذا يعني أن تطبيق Spring Boot الخاص بك يعمل على المنفذ 8080.

---

### 3. إنهاء العملية إذا لزم الأمر

إذا أردت إيقاف تلك العملية:

```bash
taskkill /PID 12345 /F
```

* `/PID` → يحدد معرف العملية
* `/F` → إجبار الإنهاء

---

### 4. المنافذ النموذجية لخدمات backend الشائعة

* **Spring Boot (تطبيق Java):** 8080, 8081، إلخ.
* **MySQL:** 3306
* **Redis:** 6379
* **PostgreSQL:** 5432

لذا، إذا رأيت هذه المنافذ في ناتج `netstat`، فستعرف أي خدمة تعمل.

---

👉 نصيحة احترافية: يمكنك دمج هذه الأوامر في أمر واحد للعثور مباشرة على العملية التي تستخدم منفذًا معينًا (مثل 8080):

```bash
netstat -ano | findstr :8080
```

ثم خذ المعرف وتحقق منه باستخدام `tasklist`.

---

هل تريد مني أيضًا أن أريك **أمرًا واحدًا في PowerShell** يقوم بتعيين المنافذ لأسماء العمليات مباشرة دون الحاجة للتحقق بشكل منفصل باستخدام `tasklist`؟