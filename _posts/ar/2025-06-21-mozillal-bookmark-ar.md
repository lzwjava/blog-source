---
audio: false
generated: true
lang: ar
layout: post
title: تحليل ملف إشارات موزيلا
translated: true
type: note
---

الملف الذي تنظر إليه هو **نسخة احتياطية لإشارات Firefox** بصيغة `.jsonlz4`. هذا يعني أنه **ملف JSON مضغوط باستخدام خوارزمية ضغط LZ4**، ويحتوي على إشارات Firefox الخاصة بك.

### 📁 اسم الملف:
```
bookmarks-2025-06-21_10_MU9VVxxx.jsonlz4
```

هذا نمط تسمية قياسي للنسخ الاحتياطية لإشارات Firefox.

---

## ✅ الهدف: كيفية تحليله

لا يمكنك قراءة هذا الملف مباشرة لأنه **مضغوط بـ LZ4**. أنت بحاجة إلى:

1. **فك ضغطه** (LZ4 → JSON)
2. **تنسيقه بشكل جميل** أو تحليل الـ JSON الناتج

---

## 🔧 التعليمات خطوة بخطوة

### الخيار 1: استخدام Python (موصى به)

قم بتثبيت الاعتمادات:

```bash
pip install lz4
```

ثم شغّل سكريبت Python هذا:

```python
import json
import lz4.block
import sys

def decompress_jsonlz4(file_path):
    with open(file_path, 'rb') as f:
        # اقرأ أول 8 بايت - رأس السحر (header magic)
        magic = f.read(8)
        if magic != b'mozLz40\0':
            raise ValueError("Invalid file header - not a JSON-LZ4 file")

        # اقرأ بقية الملف وقم بفك الضغط
        compressed_data = f.read()
        json_bytes = lz4.block.decompress(compressed_data)
        json_str = json_bytes.decode('utf-8')
        return json.loads(json_str)

# مثال للاستخدام
if __name__ == "__main__":
    import sys
    file_path = sys.argv[1]
    data = decompress_jsonlz4(file_path)
    print(json.dumps(data, indent=4))
```

احفظه باسم `parse_bookmarks.py`، ثم شغّل:

```bash
python parse_bookmarks.py 'bookmarks-2025-06-21_10_MU9VV5NmyUCq9RTxuMze5iBNh7sKyRPF_ZlV5EpMqZo=.jsonlz4'
```

> 🔍 سيكون الناتج عبارة عن شجرة JSON منسقة بشكل جميل لإشاراتك.

---

### الخيار 2: استخدام أداة عبر الإنترنت (سريع لكن محفوف بالمخاطر مع البيانات الحساسة)

هناك أدوات مثل:

- [https://arewecompressedyet.com/jsonlz4/](https://arewecompressedyet.com/jsonlz4/)
- أو ابحث عن "jsonlz4 decoder"

⚠️ **تحذير:** لا تقم برفع ملفات حساسة إلى الأدوات عبر الإنترنت إلا إذا كنت متأكدًا من أنها آمنة وموثوقة.

---

### الخيار 3: استخدام Node.js

إذا كنت تفضل JavaScript/Node:

```bash
npm install firefox-jsonlz4
```

أو راجع هذا المستودع: https://github.com/overdodactyl/firefox-jsonlz4

---

## 📊 ماذا يوجد بالداخل؟

بعد التحليل، ستحصل على هيكل JSON مثل هذا:

```json
{
  "title": "Bookmarks Menu",
  "children": [
    {
      "title": "Mozilla Firefox",
      "children": [
        {
          "title": "Get Help",
          "uri": "https://support.mozilla.org/products/firefox/"
        },
        ...
      ]
    }
  ]
}
```

يمكنك استخدام هذه البيانات لـ:
- البحث في الإشارات القديمة
- ترحيل الإشارات
- تحليل عادات التصفح/وضع الإشارات الخاصة بك

---

## ✅ ملخص

| الخطوة | الإجراء |
|------|--------|
| 1 | تعرّف على أن `.jsonlz4` هو **ملف JSON مضغوط** |
| 2 | استخدم Python أو أدوات أخرى **لفك الضغط وتحليله** |
| 3 | اعرض/صدّر الـ JSON المنظم لإشارات Firefox الخاصة بك |

أعلمني إذا كنت تريد تحويل هذا إلى CSV أو HTML، أو استخراج روابط محددة!

---

سؤال رائع! دعنا نشرح **لماذا تستخدم Mozilla صيغة `.jsonlz4`** للإشارات بدلاً من JSON عادي، ونعالج مخاوفك بشأن **الأمان**.

---

## 🔍 لماذا تستخدم Mozilla `.jsonlz4` بدلاً من JSON عادي

### ✅ 1. **كفاءة الضغط**
- `.jsonlz4` هو **بيانات JSON مضغوطة باستخدام خوارزمية LZ4**.
- LZ4 **سريعة**، **خفيفة الوزن**، وتوفر نسب ضغط جيدة — مثالية لحالة استخدام Firefox.
- هذا يقلل حجم الملف بشكل كبير (غالبًا بنسبة **50–70%**)، مما:
  - يوفر مساحة على القرص
  - يسرع النسخ الاحتياطي والاستعادة
  - يقلل من عمليات الإدخال/الإخراج عند قراءة/كتابة ملفات الإشارات

> 📦 مثال: ملف إشارات JSON عادي بحجم 2 ميجابايت قد ينضغط إلى ~600–800 كيلوبايت باستخدام LZ4.

---

### ✅ 2. **الأداء**
- LZ4 مُحسّنة من أجل **السرعة على حساب أقصى ضغط**.
- يمكن لـ Firefox ضغط وفك ضغط هذه الملفات بسرعة دون تأخير ملحوظ.
- الخوارزميات الأخرى مثل gzip أو zstd أكثر قوة ولكنها أبطأ أو أكثر تعقيدًا.

---

### ✅ 3. **الصيغة الداخلية**
- لا يعرض Firefox هذا كصيغة للمستخدم النهائي؛ إنها تُستخدم داخليًا لـ:
  - النسخ الاحتياطية للإشارات (`bookmarkbackups/`)
  - تخزين الجلسة (`sessionstore-backups/`)
- ليست مخصصة للتحرير البشري أو التشغيل البيني (مثل التصدير للاستيراد في Chrome).

> للتصدير/الاستيراد، يوفر Firefox **JSON عادي** عبر قائمة "استيراد ونسخ احتياطي".

---

## 🔒 هل `.jsonlz4` أقل أمانًا من JSON؟

لا، **ليس بطبيعته**.

لنوضح:

| الجانب | JSON | JSONLZ4 |
|-------|------|---------|
| **تنسيق البيانات** | نص مقروء | مضغوط ثنائيًا |
| **الأمان** | لا يوجد مدمج | لا يوجد مدمج |
| **تشفير؟** | لا | لا |
| **حماية من العبث؟** | لا | لا |

كلا الصيغتين **غير مشفرتين** و **غير محميتين** افتراضيًا.

لذا:
- إذا كان لدى شخص ما حق الوصول إلى ملف تعريف Firefox الخاص بك، فيمكنه قراءة أي من الملفين.
- لا تحمي أي من الصيغتين البيانات الحساسة مثل العناوين أو الروابط.

---

## 💡 إذن لماذا لا نستخدم صيغ مشفرة أو آمنة؟

Mozilla **لا تشفر بيانات ملف التعريف المحلي افتراضيًا**، بما في ذلك:
- الإشارات
- السجل
- الكوكيز
- كلمات المرور

### وإليك السبب:
- **تجربة المستخدم**: التشفير يضيف تعقيدًا.
- **الاستعادة**: يمكن للمستخدمين فقدان البيانات إذا نسوا كلمات المرور.
- **افتراض الثقة**: تفترض Firefox أن نظامك آمن.
- **توجد أدوات بديلة**: يمكنك تشفير دليل ملف تعريف Firefox يدويًا باستخدام أدوات على مستوى نظام التشغيل (مثل VeraCrypt، BitLocker، إلخ).

إذا كنت تريد ملفات تعريف مشفرة:
- استخدم **Firefox Lockwise** لتشفير كلمات المرور.
- استخدم **أدوات طرف ثالث** لتشفير مجلد ملف التعريف بالكامل.
- أو انتقل إلى Firefox محمول على محرك أقراص USB مشفر.

---

## 🧠 نصيحة إضافية: كيفية التحويل من `.jsonlz4` → `.json`

أنت تعرف بالفعل كيفية القيام بذلك من الإجابة السابقة، ولكن إليك ملخصًا سريعًا باستخدام Python:

```bash
python parse_bookmarks.py bookmarks-*.jsonlz4 > bookmarks.json
```

أو استخدم هذا الأمر في سطر الأوامر إذا كنت مرتاحًا مع Shell + Python:

```bash
cat bookmarks-*.jsonlz4 | python3 -c '
import lz4.block, sys, json
data = sys.stdin.buffer.read()[8:]
print(json.dumps(json.loads(lz4.block.decompress(data)), indent=2))' > bookmarks.json
```

---

## ✅ ملخص

| السؤال | الإجابة |
|--------|--------|
| **لماذا `.jsonlz4` بدلاً من JSON؟** | الضغط، الأداء، الاستخدام الداخلي |
| **هل `.jsonlz4` أقل أمانًا من JSON؟** | لا — كلاهما غير مشفر ويمكن قراءته |
| **هل يمكن لشخص ما سرقة إشاراتي؟** | نعم، إذا كان لديه حق الوصول إلى جهازك |
| **كيفة جعلها أكثر أمانًا؟** | قم بتشفير مجلد ملف تعريف Firefox يدويًا |

---

إذا كنت قلقًا بشأن الخصوصية أو الأمان، فإن الحل الحقيقي هو **حماية الوصول إلى جهاز الكمبيوتر الخاص بك** أو **تشفير مجلد ملف تعريف Firefox**، وليس الاعتماد فقط على تنسيقات الملفات.

أعلمني إذا كنت تريد المساعدة في كتابة سكريبت لأتمتة فك التشفير أو تشفير النسخ الاحتياطية!