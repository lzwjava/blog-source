---
audio: false
generated: true
lang: ar
layout: post
title: التحقق من سرعة وصلة الإيثرنت في أوبونتو
translated: true
type: note
---

في أوبونتو، يمكنك التحقق من سرعة وصلة الإيثرنت باستخدام عدة أدوات سطر الأوامر. إليك الطرق الأكثر شيوعًا:

### **1. استخدام `ethtool` (موصى به)**
`ethtool` هي الطريقة الأكثر موثوقية للتحقق من سرعة الوصلة الحالية لواجهة الإيثرنت الخاصة بك.

#### تثبيت `ethtool` (إذا لم يكن مثبتًا مسبقًا):
```bash
sudo apt install ethtool
```

#### التحقق من سرعة الوصلة:
```bash
sudo ethtool <اسم_الواجهة>
```
استبدل `<اسم_الواجهة>` باسم واجهة الإيثرنت الخاصة بك (مثل `eth0`، `enp3s0`، `ens33`).

#### مثال:
```bash
sudo ethtool enp3s0
```
ابحث عن حقلي `Speed` و `Duplex` في الناتج:
```
Settings for enp3s0:
    Supported ports: [ TP ]
    Supported link modes:   10baseT/Half 10baseT/Full 
                            100baseT/Half 100baseT/Full 
                            1000baseT/Full 
    Supported pause frame use: No
    Supports auto-negotiation: Yes
    Supported FEC modes: Not reported
    Advertised link modes:  10baseT/Half 10baseT/Full 
                            100baseT/Half 100baseT/Full 
                            1000baseT/Full 
    Advertised pause frame use: No
    Advertised auto-negotiation: Yes
    Advertised FEC modes: Not reported
    Speed: 1000Mb/s   # <--- هذه هي سرعة الوصلة الحالية لديك
    Duplex: Full      # <--- اتصال ثنائي كامل أو نصفي
    Port: Twisted Pair
    PHYAD: 1
    Transceiver: internal
    Auto-negotiation: on
    MDI-X: on (auto)
    Supports Wake-on: pumbg
    Wake-on: d
    Current message level: 0x00000007 (7)
                           drv probe link
    Link detected: yes      # <--- يشير إلى ما إذا كان الكابل متصلاً
```

---

### **2. استخدام `ip` أو `ifconfig` (معلومات أساسية)**
هذه الأوامر تُظهر ما إذا كانت الواجهة نشطة ولكنها لا تعرض سرعة الوصلة مباشرة.

#### استخدام `ip`:
```bash
ip link show <اسم_الواجهة>
```
مثال:
```bash
ip link show enp3s0
```
(لا تُظهر السرعة، فقط حالة الوصلة.)

#### استخدام `ifconfig` (مهمَل ولكن لا يزال مستخدمًا):
```bash
ifconfig <اسم_الواجهة>
```
مثال:
```bash
ifconfig enp3s0
```
(يُظهر ما إذا كانت الواجهة نشطة ولكن ليس السرعة.)

---

### **3. استخدام `dmesg` (التحقق من سجلات التعريف)**
في بعض الأحيان، يقوم تعريف الإيثرنت بتسجيل سرعة التفاوض أثناء التهيئة.
```bash
dmesg | grep <اسم_الواجهة>
```
مثال:
```bash
dmesg | grep enp3s0
```
ابحث عن أسطر مثل:
```
[    5.123456] e1000e: enp3s0 NIC Link is Up 1000 Mbps Full Duplex
```

---

### **4. استخدام `mii-tool` (الأنظمة القديمة)**
للأنظمة القديمة جدًا، قد يعمل `mii-tool`:
```bash
sudo apt install net-tools
sudo mii-tool -v <اسم_الواجهة>
```
مثال:
```bash
sudo mii-tool -v eth0
```
(غير موصى به للإيثرنت الحديثة من نوع Gigabit.)

---

### **الخلاصة**
- **أفضل طريقة:** `sudo ethtool <الواجهة>` (تُظهر السرعة، الازدواجية، التفاوض التلقائي).
- **بديل:** تحقق من سجلات `dmesg` إذا كان `ethtool` غير متاح.
- **الحالة الأساسية:** `ip link show` أو `ifconfig` (لا تُظهر السرعة).

دعني أعرف إذا كنت بحاجة إلى مزيد من التوضيح! 🚀