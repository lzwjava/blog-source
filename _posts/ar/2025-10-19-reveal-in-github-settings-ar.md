---
audio: false
generated: true
image: false
lang: ar
layout: post
model: x-ai/grok-4-fast
title: نافذة إعدادات ريفيل-إن-غيت هاب
translated: true
type: note
---

### نظرة عامة
هذا الملف XML هو مستند Interface Builder XIB (واجهة منشئ XML) لتطبيق macOS. تُستخدم ملفات XIB في Xcode لتحديد واجهات المستخدم بطريقة تعريفية، خاصة للنوافذ والعناصر والتحكمات في تطبيقات Cocoa. يحدد هذا الملف تحديدًا واجهة المستخدم لنافذة إعدادات في تطبيق يسمى **Reveal-In-GitHub** (على الأرجح أداة مساعدة لفتح ملفات الكود المصدري أو المستودعات في GitHub بسرعة من بيئة التطوير المتكاملة IDE أو نظام الملفات الخاص بك).

يستهدف الملف نظام macOS (وقت تشغيل Cocoa) ويستخدم Auto Layout (`useAutolayout="YES"`). وهو مبني حول كائن نافذة رئيسي، مع اتصالات بفئة تحكم مخصصة (`RIGSettingWindowController`)، ومنافذ للعناصر الرئيسية، وعناصر واجهة مستخدم فرعية متنوعة مثل الأزرار والتسميات وحاوية مخصصة.

البيانات الوصفية الرئيسية:
- **إصدار الأدوات**: Xcode 9.0.1 (toolsVersion="9059")، مبني على macOS 10.12.6 (systemVersion="14F27").
- **سلوك النافذة**: تحتوي على عنوان، قابلة للإغلاق، قابلة للتصغير، وقابلة لتغيير الحجم. لا تعيد حساب حلقة العرض الرئيسي تلقائيًا وتستخدم الرسوم المتحركة الافتراضية.
- **الموضع/الحجم الابتدائي**: تفتح عند الموضع (527, 176) على الشاشة بأبعاد 651x497 بكسل (على شاشة مقاس 1440x877).

جذر الملف هو `<document>` يحتوي على `<dependencies>` (لإضافة Cocoa) و `<objects>` (تسلسل واجهة المستخدم الفعلي).

### المكونات الرئيسية

#### 1. **مالك الملف (وحدة التحكم المخصصة)**
   - **الفئة**: `RIGSettingWindowController`
   - تعمل كوحدة تحكم للنافذة، تدير منطقًا مثل تحميل/حفظ الإعدادات.
   - **المنافذ** (اتصالات بعناصر واجهة المستخدم):
     - `configsView` → عنصر عرض مخصص لعرض خيارات التكوين (المعرف: `IKd-Ev-B9V`).
     - `mainView` → عرض محتوى النافذة (المعرف: `se5-gp-TjO`).
     - `window` → نافذة الإعدادات نفسها (المعرف: `F0z-JX-Cv5`).
   - `delegate` النافذة مربوط أيضًا بوحدة التحكم هذه.

#### 2. **الكائنات القياسية**
   - **المستجيب الأول** (`-1`): عنصر نائب لمعالجة أحداث لوحة المفاتيح.
   - **التطبيق** (`-3`): يمثل نسخة NSApplication (غير مستخدمة مباشرة هنا).

#### 3. **نافذة الإعدادات**
   - **المعرف**: `F0z-JX-Cv5`
   - **العنوان**: "إعدادات Reveal-In-GitHub"
   - **عرض المحتوى** (المعرف: `se5-gp-TjO`): عرض بالحجم الكامل (651x497) يغير حجمه تلقائيًا مع النافذة. يحتوي على جميع العناصر الفرعية، موضوعة بإطارات ثابتة (على الرغم من تفعيل Auto Layout، مما يشير إلى أنه قد تتم إضافة القيود برمجيًا أو في ملف .storyboard مصاحب).

   **تخطيط العناصر الفرعية** (جميعها تستخدم إطارات ثابتة للتحديد؛ إحداثيات y تزداد للأسفل من الأعلى):

   | العنصر | النوع | الموضع (x, y) | الحجم (w x h) | الوصف |
   |---------|------|-----------------|--------------|-------------|
   | **زر الحفظ** | `NSButton` (المعرف: `EuN-9g-Vcg`) | (14, 13) | 137x32 | زر "حفظ" في أسفل اليسار (حد منحني). يشغل الإجراء `saveButtonClcked:` على وحدة التحكم. يستخدم خط نظام صغير (13pt). |
   | **زر إعادة تعيين القوائم الافتراضية** | `NSButton` (المعرف: `KvN-fn-w7m`) | (151, 12) | 169x32 | زر "إعادة تعيين القوائم الافتراضية" المجاور. يشغل الإجراء `resetMenusButtonClicked:`. خط نظام صغير (13pt). |
   | **عرض التكوين** | `NSView` (مخصص، المعرف: `IKd-Ev-B9V`) | (20, 54) | 611x330 | عرض مخصص مركزي كبير مسمى "عرض التكوين". على الأرجح حاوية لمحتوى ديناميكي مثل الجداول أو القوائم أو مفاتيح التبديل لتكوينات مستودع GitHub (مثل مسارات المستودع، رموز المصادقة). هذا مربوط بمنفذ `configsView`. |
   | **تسمية عناصر القائمة المخصصة** | `NSTextField` (المعرف: `G1C-Td-n9Y`) | (18, 425) | 187x17 | تسمية ثابتة "عناصر القائمة المخصصة" بالقرب من الأسفل. Helvetica Neue (17pt)، لون التسمية. |
   | **زر مسح المستودعات الافتراضية** | `NSButton` (المعرف: `KvN-fn-w7m`) | (14, 449) | 164x32 | زر "مسح المستودعات الافتراضية" في أسفل اليسار. يشغل الإجراء `clearButtonClicked:`. خط نظام صغير (13pt). |
   | **تسمية عنوان القائمة** | `NSTextField` (المعرف: `UUf-Cr-5zs`) | (20, 392) | 77x18 | تسمية ثابتة "عنوان القائمة". Helvetica Neue (14pt)، لون التسمية. |
   | **تسمية اختصار لوحة المفاتيح** | `NSTextField` (المعرف: `rMv-by-SKS`) | (112, 391) | 63x19 | تسمية ثابتة "⌃⇧⌘ +" (Control+Shift+Command +). Lucida Grande UI (15pt)، لون التسمية. تشير إلى اختصار عام قابل للتخصيص لقائمة التطبيق. |
   | **تسمية نمط URL** | `NSTextField` (المعرف: `zW4-cw-Rhb`) | (410, 392) | 94x18 | تسمية ثابتة "نمط URL ". خط النظام (15pt)، لون التسمية. على الأرجح لتكوين قوالب URL لـ GitHub (مثل الروابط العميقة لعروض الملفات/المسؤولية). |

   - **ملاحظات التخطيط**:
     - معظم العناصر محاذاة لليسار (x=14-20) لتصميم مضغوط يشبه النموذج.
     - الأعلى: أزرار الإجراءات (حفظ/إعادة تعيين).
     - الوسط: عرض التكوين الكبير (معظم المساحة لإدارة المستودع/الإعدادات).
     - الأسفل: تسميات لتخصيص القائمة وزر مسح.
     - جميع حقول النص غير قابلة للتحرير (تسميات ثابتة)، مما يشير إلى أن إدخال المستخدم يحدث داخل عرض التكوين أو عبر حقول منفصلة غير محددة هنا.
     - الألوان: إعدادات النظام الافتراضية (ألوان التسمية/التحكم لإمكانية الوصول).
     - الخطوط: مزيج من خطوط النظام وHelvetica Neue وLucida Grande لمظهر macOS أصلي.

### الغرض والوظائف
تسمح نافذة الإعدادات هذه للمستخدمين بـ:
- **إدارة المستودعات/التكوينات**: عبر "عرض التكوين" المركزي (مثل إضافة/إزالة مستودعات GitHub الافتراضية).
- **تخصيص القوائم**: تعيين عنوان قائمة، واخصار لوحة مفاتيح عام (⌃⇧⌘+)، وأنماط URL لكشف الملفات في GitHub.
- **الحفاظ على التغييرات**: حفظ الإعدادات أو إعادة تعيين/مسح الإعدادات الافتراضية بأزرار مخصصة.

تتعامل وحدة التحكم (`RIGSettingWindowController`) مع إجراءات الأزرار:
- `saveButtonClcked:` – يحفظ تغييرات المستخدم (مثلًا، في UserDefaults أو ملف plist).
- `resetMenusButtonClicked:` – يستعيد إعدادات القائمة الأصلية.
- `clearButtonClicked:` – يزيل إدخالات المستودع الافتراضية.

من المرجح أن يتم تحميل هذا الملف XIB برمجيًا في كود التطبيق (مثل `NSNib(nibNamed: "Settings", bundle: nil)`)، مع تعبئة العرض المخصص ديناميكيًا (مثلًا، باستخدام NSTableView لقوائم المستودعات).

إذا كنت تبني أو تعدل هذا التطبيق، يمكنك فتح الملف XIB في Interface Builder الخاص بـ Xcode لتصميم/تحرير التخطيط بصريًا. للتكامل مع الكود، ابحث عن طرق IBAction في فئة وحدة التحكم التي تطابق محددات الإجراء.