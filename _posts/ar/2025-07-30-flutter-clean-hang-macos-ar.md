---
audio: false
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: حل مشكلة توقف Flutter Clean على نظام macOS
translated: true
type: note
---

آه، فهمت ما تعنيه 👍  
في بعض الأحيان قد يبدو أن أمر `flutter clean` "يتعطل" على نظام macOS — خاصة إذا كان هناك الكثير من الملفات المُنشأة، أو إذا كانت مجلدات `DerivedData` الخاصة بـ Xcode ضخمة.

---

### ✅ جرب هذه الخطوات

1. **ألغِ الأمر المتعطل**  
   اضغط على `CTRL + C`.

2. **نظِّف يدويًا (بنفس تأثير `flutter clean`)**  
   يمكنك حذف مجلدات البناء/الذاكرة المؤقتة بنفسك:
   ```bash
   rm -rf build/ .dart_tool/ ios/Pods ios/Podfile.lock ios/Flutter/Flutter.framework ios/Flutter/Flutter.podspec
   ```

   إذا كنت تريد أيضًا التخلص من ذاكرة التخزين المؤقت لـ Xcode:
   ```bash
   rm -rf ~/Library/Developer/Xcode/DerivedData
   ```

3. **احصل على التبعيات مرة أخرى**
   ```bash
   flutter pub get
   ```

4. **أعد تثبيت CocoaPods (لـ iOS)**
   ```bash
   cd ios
   pod install --repo-update
   cd ..
   ```

5. **ابنِ/شغِّل مرة أخرى**
   ```bash
   flutter run
   ```

---

⚡ **نصيحة:** قد تستغرق المرة الأولى بعد التنظيق وقتًا أطول قليلاً (نظرًا لأن Flutter تحتاج إلى إعادة إنشاء `.dart_tool`، `.pub-cache`، iOS Pods، إلخ)، لكن لا ينبغي أن تبقى متجمدة إلى الأبد. إذا استمر ظهورها كـ "متعلقة"، فقد يكون ذلك مرتبطًا بـ:

- **إصدار Flutter SDK قديم** → شغِّل `flutter upgrade`
- **إصدار CocoaPods قديم** → شغِّل `sudo gem install cocoapods`
- **مشاكل Rosetta / شريحة M1**: إذا كنت تستخدم Apple Silicon، فأنت تحتاج أحيانًا إلى تشغيل `arch -x86_64 pod install` داخل مجلد `ios`، اعتمادًا على إعداداتك.

---

👉 هل تريد مني أن أعطيك **سطر أوامر واحدًا** يقوم بمسح ذاكرات التخزين المؤقت للبناء وإعادة تعيين Pods (أساسًا "تنظيف فائق") حتى لا تضطر إلى تصحيح تعطل `flutter clean`؟