---
audio: false
generated: true
image: false
lang: ar
layout: post
title: دليل الإعداد المحلي لتايب سكريبت
translated: true
type: note
---

لتشغيل TypeScript محليًا، عادةً ما تستخدم **npm** ومُصرِّف TypeScript (`tsc`). إليك دليل خطوة بخطوة:

---

### 1. تهيئة مشروعك

إذا لم يكن لديك ملف `package.json` بالفعل:

```bash
npm init -y
```

هذا ينشئ ملف `package.json` حتى تتمكن من إدارة التبعيات.

---

### 2. تثبيت TypeScript

قم بتثبيته كتبعية للتطوير:

```bash
npm install typescript --save-dev
```

إذا أردت أيضًا أن يكون مُصرِّف TypeScript (`tsc`) متاحًا بشكل عام:

```bash
npm install -g typescript
```

---

### 3. إنشاء ملف tsconfig.json

قم بإنشاء ملف الإعدادات الافتراضي:

```bash
npx tsc --init
```

هذا ينشئ ملف `tsconfig.json` الذي يتحكم في كيفية تحويل TypeScript لملفاتك (الهدف، نظام الوحدات، الوضع الصارم، إلخ).

---

### 4. اكتب ملف TypeScript

مثال `index.ts`:

```ts
function greet(name: string): string {
  return `Hello, ${name}!`;
}

console.log(greet("World"));
```

---

### 5. تحويل TypeScript

شغّل:

```bash
npx tsc
```

هذا يحوّل جميع ملفات `.ts` وفقًا لـ `tsconfig.json`.

أو، لتحويل ملف واحد فقط:

```bash
npx tsc index.ts
```

هذا يُنشئ ملف `.js` (مثل `index.js`).

---

### 6. تشغيل JavaScript المُحوّل

استخدم Node.js:

```bash
node index.js
```

---

### 7. اختياري: التحويل التلقائي عند الحفظ

شغّل باستخدام `--watch` حتى يعيد التحويل عند التعديل:

```bash
npx tsc --watch
```

---

👉 هذا هو سير العمل القياسي:
`npm install` → `tsc` (تحويل) → `node` (تشغيل الكود المُحوّل).

---

هل تريد مني أيضًا أن أوضح لك كيفية **تخطي التحويل اليدوي** بتشغيل TypeScript مباشرة باستخدام `ts-node`؟