---
audio: false
generated: true
image: false
lang: ar
layout: post
title: شرح ترميز ASCII و Hex و UTF-8 ببساطة
translated: true
type: note
---

دعونا نشرحها بوضوح:

---

### ASCII

* **التعريف**: ASCII (الشفرة القياسية الأمريكية لتبادل المعلومات) هي واحدة من أولى ترميزات الأحرف.
* **النطاق**: تستخدم **7 بت** (قيم من 0 إلى 127).
* **المعنى**: كل رقم يقابله حرف معين.
  على سبيل المثال:

  * `65` → `"A"`
  * `97` → `"a"`
  * `48` → `"0"`
* **القيد**: لا تغطي سوى الأحرف الإنجليزية، والأرقام، وعلامات الترقيم، ورموز التحكم (مثل سطر جديد).

---

### النظام الست عشري (Hexadecimal Representation)

* **التعريف**: النظام الست عشري ليس مجموعة أحرف بحد ذاته، بل هو **تمثيل عددي** (أساس-16) للبيانات.
* **الاستخدام**: يُستخدم عادة لعرض البايتات بطريقة مدمجة يسهل قراءتها.
* **مثال**:

  * الحرف `"A"` في ASCII = العدد العشري `65` = العدد الست عشري `41`.
  * الحرف `"B"` في ASCII = العدد العشري `66` = العدد الست عشري `42`.
  * لذا فإن النص `"AB"` يبدو بالشكل `41 42` في النظام الست عشري.

---

### UTF-8

* **التعريف**: UTF-8 هو معيار **ترميز Unicode**.
* **الميزات**:

  * طول متغير: يشفر كل حرف باستخدام **من 1 إلى 4 بايت**.
  * متوافق مع الإصدارات السابقة من ASCII: الأحرف من `0–127` هي نفسها في ASCII (1 بايت).
  * يمكنه تمثيل جميع أحرف Unicode (الحروف، الرموز، الإيموجي، الأحرف الصينية، إلخ).
* **أمثلة**:

  * `"A"` → `41` (نفس قيمة ASCII).
  * `"中"` → `E4 B8 AD` (ثلاثة بايتات بالنظام الست عشري).
  * `"😊"` → `F0 9F 98 8A` (أربعة بايتات بالنظام الست عشري).

---

✅ **باختصار**:

* ASCII = شفرة قديمة 7 بت للأحرف الإنجليزية.
* النظام الست عشري = طريقة لكتابة الأرقام/البايتات (أساس-16).
* UTF-8 = ترميز حديث يتضمن ASCII ويدعم أيضًا جميع اللغات والرموز.

---

هل تريد مني أيضًا أن أعرض لك **مثالاً مقارنًا لنفس النص** (`"Hello 世界😊"`) باستخدام ASCII (حيثما أمكن)، والنظام الست عشري، وUTF-8 حتى تتمكن من رؤية الفرق مباشرة؟