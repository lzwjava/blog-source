---
audio: false
generated: true
lang: ar
layout: post
title: مشكلة بناء NVIDIA DKMS
translated: true
type: note
---

يبدو أنك تواجه خطأً حيث فشل بناء وحدة NVIDIA DKMS (دعم وحدة النواة الديناميكية) للنواة الجديدة الخاصة بك `6.8.0-60-generic`. هذه مشكلة شائعة عند تحديث النواة، حيث تحتاج وحدة التشغيل إلى إعادة تجميعها لإصدار النواة الجديد، وأحياناً تظهر مشاكل عدم توافق أو أخطاء في البناء.

رسائل الخطأ الرئيسية هي:
* `Building module(s)......(bad exit status: 2)`
* `Error! Bad return status for module build on kernel: 6.8.0-60-generic (x86_64)`
* `Consult /var/lib/dkms/nvidia/535.183.06/build/make.log for more information.`
* `dpkg: error processing package linux-image-6.8.0-60-generic (--configure):`
* `E: Sub-process /usr/bin/dpkg returned an error code (1)`

هذا يعني أن إصدار مشغل NVIDIA `535.183.06` لم يتم تجميعه بنجاح مقابل النواة `6.8.0-60-generic`، مما أدى بدوره إلى إيقاف تكوين صورة النواة والحزم المرتبطة بها بشكل صحيح.

إليك كيف يمكنك محاولة إصلاح هذا:

---

### 1. فحص سجل البناء 🪵

الخطوة الأكثر أهمية هي التحقق من سجل البناء التفصيلي لرسائل الخطأ المحددة. سيخبرك هذا السجل *بسبب* فشل التجميع.
افتح طرفية وقم بتشغيل:
```bash
cat /var/lib/dkms/nvidia/535.183.06/build/make.log
```
ابحث عن الأسطر التي تبدأ بـ `error:` أو `fatal error:`. غالبًا ما تشير هذه إلى ملفات مفقودة، أو مشاكل في المترجم، أو عدم توافق مع شيفرة النواة. يشير الخطأ `Applying patch disable_fstack-clash-protection_fcf-protection.patch... done.` يليه فشل في البناء إلى أن التصحيح قد لا يكون متوافقًا بالكامل، أو أن هناك مشاكل أخرى لم يعالجها التصحيح.

---

### 2. تنظيف ومحاولة إصلاح الحزم التالفة 🛠️

أولاً، يمكنك محاولة السماح لـ `apt` بمحاولة إصلاح التبعيات التالفة وإنهاء تكوين الحزم.
```bash
sudo rm /var/crash/nvidia-dkms-535.0.crash # إزالة تقرير التعطل القديم للسماح بإنشاء تقارير جديدة
sudo apt --fix-broken install
sudo dpkg --configure -a
```
قد يؤدي هذا إلى إعادة تشغيل بناء DKMS. إذا فشل مرة أخرى، فتابع الخطوات التالية.

---

### 3. التأكد من تثبيت رؤوس النواة بشكل صحيح 🧠

تتطلب عملية بناء DKMS رؤوس النواة الصحيحة للنواة التي تحاول البناء ضدها.
```bash
sudo apt update
sudo apt install --reinstall linux-headers-6.8.0-60-generic linux-headers-generic-hwe-22.04
```
إذا لم تكن تقوم بتشغيل نواة `6.8.0-60-generic` بعد (على سبيل المثال، إذا تمت مقاطعة التحديث)، يمكنك أيضًا تحديد رؤوس النواة قيد التشغيل حاليًا، ولكن الهدف الأساسي هنا هو `6.8.0-60-generic`.

---

### 4. إزالة وحدة DKMS الفاشلة وإعادة التكوين ♻️

يمكنك إزالة الإصدار المسبب للمشكلة من وحدة NVIDIA يدويًا من DKMS ثم محاولة السماح للنظام بإعادة بنائها.
```bash
sudo dkms remove nvidia/535.183.06 --all
```
بعد ذلك، حاول إعادة تكوين الحزم مرة أخرى:
```bash
sudo dpkg --configure -a
sudo apt update && sudo apt upgrade
```
يجب أن تدفع هذه العملية DKMS لمحاولة إعادة بناء الوحدة لنواة `6.8.0-60-generic`.

---

### 5. إعادة تثبيت مشغل NVIDIA 💻

إذا فشلت الوحدة في البناء مرة أخرى، فقد يكون إصدار المشغل `535.183.06` به مشاكل توافق مع النواة `6.8.0-60-generic`.

* **إزالة المشغل الحالي المسبب للمشكلة:**
    ```bash
    sudo apt-get purge nvidia-dkms-535 nvidia-driver-535
    sudo apt autoremove
    sudo apt clean
    ```

* **تثبيت مشغل مناسب:**
    يمكنك السماح لأوبونتو بمحاولة تثبيت المشغل الموصى به:
    ```bash
    sudo ubuntu-drivers autoinstall
    ```
    بدلاً من ذلك، يمكنك محاولة تثبيت نفس الإصدار (إذا اعتبرته المستودعات متوافقًا الآن) أو إصدار أحدث وأكثر توافقًا. على سبيل المثال، لمحاولة تثبيت مشغل السلسلة 535 مرة أخرى:
    ```bash
    sudo apt update
    sudo apt install nvidia-driver-535
    ```
    (يجب أن يختار مدير الحزم إصدار DKMS المناسب.)

    بعد هذا، ستحاول النظام بناء وحدة DKMS مرة أخرى.

---

### 6. النظر في إصدار أحدث من مشغل NVIDIA 🚀

النواة 6.8 جديدة نسبيًا. من الممكن أن يكون لمشغل NVIDIA من السلسلة 535 قيود مع إصدار النواة هذا. قد تحتاج إلى سلسلة مشغل أحدث (مثل 545 أو 550 أو أحدث، إذا كانت متاحة في مستودعاتك أو عبر القنوات الرسمية/مستودعات PPA الخاصة بـ NVIDIA).

* تحقق من المشغلات المتاحة:
    ```bash
    sudo apt search nvidia-driver
    ```
* إذا كان مشغل احتكاري أحدث متاحًا (مثل `nvidia-driver-550`)، فيمكنك محاولة تثبيت ذلك بدلاً من `nvidia-driver-535`.
    ```bash
    sudo apt install nvidia-driver-VERSION_NUMBER # استبدل VERSION_NUMBER
    ```

---

### 7. إذا كنت عالقًا في حلقة إقلاع أو ليس لديك واجهة رسومية 📉

إذا فشل النظام في الإقلاع إلى بيئة رسومية:
* قم بالإقلاع في وضع الاسترداد أو نواة أقدم (يمكن الوصول إليها عادةً من قائمة GRUB أثناء بدء التشغيل).
* بمجرد وجودك في بيئة سطر الأوامر، يمكنك تجربة الخطوات المذكورة أعلاه.
* إذا كنت بحاجة إلى استخدام مشغل مفتوح المصدر مؤقتًا لاستعادة الواجهة الرسومية أثناء إصلاح مشغل NVIDIA:
    ```bash
    sudo apt-get purge nvidia*
    sudo apt autoremove
    sudo apt install xserver-xorg-video-nouveau
    sudo reboot
    ```
    ثم، حاول تثبيت مشغل NVIDIA مرة أخرى.

---

**الخلاصة الرئيسية:** محتويات `/var/lib/dkms/nvidia/535.183.06/build/make.log` مهمة جدًا. إذا لم تنجح الخطوات المذكورة أعلاه، فستوفر الأخطاء في ذلك الملف أدلة أكثر تحديدًا، وقد تحتاج إلى البحث عبر الإنترنت عن تلك الأخطاء المحددة بالتزامن مع إصدار النواة وإصدار مشغل NVIDIA الخاص بك.

---

يشير هذا الناتج إلى سلسلة من مشاكل التبعيات والتكوين في نظام أوبونتو الخاص بك، على الأرجح تتعلق بتحديثات النواة ومشغل NVIDIA. دعونا نحلل المشكلات ونقترح حلولاً محتملة.

**المشاكل الرئيسية:**

1.  **مشاكل التبعيات مع `linux-generic-hwe-22.04`:**
    * يعتمد على `linux-headers-generic-hwe-22.04` بإصدار محدد (`= 6.8.0-60.63~22.04.1`).
    * لم يتم تكوين `linux-headers-generic-hwe-22.04` بعد، مما تسبب في فشل تكوين `linux-generic-hwe-22.04`.

2.  **فشل في تكوين `linux-image-6.8.0-60-generic`:**
    * فشل نص ما بعد التثبيت الخاص بصورة النواة هذه بخروج الحالة 1.
    * يشير سجل الخطأ إلى أن هذا مرتبط بمشغل NVIDIA (`nvidia/535.183.06`) الذي فشل في البناء لإصدار النواة المحدد هذا (`6.8.0-60-generic`).
    * فشلت عملية بناء DKMS (دعم وحدة النواة الديناميكية) لمشغل NVIDIA. سيحتوي ملف السجل `/var/lib/dkms/nvidia/535.183.06/build/make.log` على مزيد من التفاصيل حول خطأ البناء.
    * هناك أيضًا خطأ متعلق بإنشاء تقرير تعطل لفشل NVIDIA DKMS، مما يشير إلى مشكلة محتملة في آلية الإبلاغ عن التعطل للنظام أو أذونات نظام الملفات.

3.  **فشل في تكوين `linux-headers-6.8.0-60-generic` و `linux-headers-generic-hwe-22.04`:**
    * فشل هذه على الأرجح لأن تكوين حزمة `linux-image-6.8.0-60-generic` فشل، والتي قد تعتمد عليها.

**الأسباب المحتملة:**

* **تحديث نواة غير مكتمل أو مقطوع:** ربما تمت مقاطعة النظام أثناء ترقية النواة، تاركًا بعض الحزم في حالة غير متناسقة.
* **عدم توافق مشغل NVIDIA:** قد يكون إصدار مشغل NVIDIA المثبت (`535.183.06`) يواجه مشاكل في البناء مقابل إصدار النواة الجديد (`6.8.0-60-generic`).
* **مشاكل في DKMS:** قد تكون هناك مشاكل في إطار عمل DKMS نفسه، مما يمنع مشغل NVIDIA من البناء.
* **مشاكل في نظام الملفات:** قد يشير الخطأ حول عدم القدرة على إنشاء تقرير تعطل إلى مشكلة في مساحة القرص أو أذونات الملفات في دليل `/var/crash/`.

**خطوات استكشاف الأخطاء وإصلاحها:**

1.  **محاولة إعادة تكوين الحزم:**
    افتح طرفيتك وقم بتشغيل الأمر التالي:
    ```bash
    sudo dpkg --configure -a
    ```
    يحاول هذا الأمر تكوين جميع الحزم الموجودة في حالة نصف مهيأة.

2.  **فحص سجل بناء NVIDIA DKMS:**
    افحص ملف السجل للحصول على رسائل خطأ مفصلة أثناء بناء مشغل NVIDIA:
    ```bash
    less /var/lib/dkms/nvidia/535.183.06/build/make.log
    ```
    قد يوفر هذا السجل أدلة حول التبعيات المفقودة أو أخطاء التجميع.

3.  **محاولة إعادة تثبيت مشغل NVIDIA:**
    يمكنك محاولة إزالة ثم إعادة تثبيت مشغل NVIDIA. أولاً، حاول إزالته تمامًا:
    ```bash
    sudo apt remove --purge nvidia-*
    sudo apt autoremove
    ```
    ثم، حاول إعادة تثبيته. قد ترغب في تجربة إصدار مختلف إذا كان الإصدار الحالي به مشاكل:
    ```bash
    sudo apt install nvidia-driver-535  # أو إصدار موصى به مختلف
    ```
    بعد التثبيت، أعد تشغيل نظامك.

4.  **تنظيف ذاكرة التخزين المؤقت للحزمة والمحاولة مرة أخرى:**
    ```bash
    sudo apt clean
    sudo apt update
    sudo apt upgrade
    sudo dpkg --configure -a
    ```

5.  **إعادة تثبيت النواة ورؤوسها المسببة للمشكلة:**
    يمكنك محاولة إعادة تثبيت حزم النواة والرؤوس المحددة التي تسبب المشاكل:
    ```bash
    sudo apt install --reinstall linux-image-6.8.0-60-generic linux-headers-6.8.0-60-generic linux-headers-generic-hwe-22.04 linux-generic-hwe-22.04
    ```
    بعد هذا، حاول التكوين مرة أخرى:
    ```bash
    sudo dpkg --configure -a
    ```

6.  **فحص مساحة القرص:**
    تأكد من وجود مساحة حرة كافية على قسم الجذر (`/`) حيث يمكن أن يسبب هذا أحيانًا مشاكل أثناء تثبيت الحزم وتكوينها.
    ```bash
    df -h /
    ```

7.  **معالجة خطأ تقرير التعطل (إذا استمر):**
    إذا استمر الخطأ بشأن `/var/crash/nvidia-dkms-535.0.crash`، يمكنك محاولة إزالة الملف (إذا كان موجودًا ولا تحتاجه لتصحيح الأخطاء) أو التحقق من أذونات دليل `/var/crash/`.
    ```bash
    sudo rm -f /var/crash/nvidia-dkms-535.0.crash
    sudo chmod 777 /var/crash  # كن حذرًا مع الأذونات الواسعة، استرجعها إذا لم تكن ضرورية
    ```

8.  **الإقلاع إلى نواة سابقة:**
    إذا كان لديك إصدار نواة سابق مثبت، يمكنك محاولة الإقلاع إليه من قائمة GRUB. يمكن أن يسمح لك هذا بتنفيذ خطوات استكشاف الأخطاء وإصلاحها من بيئة مستقرة. إذا عمل النظام بنواة أقدم، فمن المرجح أن المشكلة خاصة بنواة `6.8.0-60-generic` أو تفاعلها مع مشغل NVIDIA.

**اعتبارات مهمة:**

* **النسخ الاحتياطي:** قبل إجراء تغييرات كبيرة، خاصة تلك التي تتضمن إزالة الحزم، من الجيد دائمًا الاحتفاظ بنسخة احتياطية من بياناتك المهمة.
* **كن صبورًا:** قد يستغرق تكوين الحزم وتثبيتها بعض الوقت.
* **استشارة السجلات:** رسائل الخطأ وملفات السجل (خاصة سجل بناء NVIDIA DKMS) ضرورية لتشخيص السبب الدقيق للمشكلة.

ابدأ بالخطوات القليلة الأولى وانظر إذا ما كانت تحل المشكلة. إذا لم يكن الأمر كذلك، فتابع الحلول الأكثر تعقيدًا وافحص بعناية أي رسائل خطأ تواجهها. حظًا سعيدًا!

---

يشير هذا الناتج من سجل بناء NVIDIA DKMS إلى أن التجميع يفشل بسبب **خيار سطر أوامر غير معترف به: `-ftrivial-auto-var-init=zero`**.

خيار المترجم هذا هو إضافة حديثة نسبيًا لـ GCC (على الأرجح الإصدار 12 أو أحدث) و Clang. يبدو أن المترجم المستخدم لبناء مشغل NVIDIA لنواتك (6.8.0-60-generic) لا يتعرف على هذا الخيار.

**الأسباب والحلول المحتملة:**

1.  **مترجم قديم:** قد يكون لديك إصدار أقدم من GCC أو Clang مثبتًا كمترجم افتراضي على نظامك. ربما تم تجميع رؤوس النواة بمترجم أحدث يستخدم هذا الخيار، لكن نظام بناء مشغل NVIDIA يلتقط مترجمًا أقدم.

    **الحل:**
    * **تثبيت مترجم أحدث:** يمكنك محاولة تثبيت إصدار أحدث من GCC.
        ```bash
        sudo apt update
        sudo apt install gcc-12  # أو إصدار أحدث مثل gcc-13
        ```
    * **تحديث بيئة البناء:** تأكد من أن أدوات البناء لديك محدثة.
        ```bash
        sudo apt update
        sudo apt install build-essential
        ```
    * **تحديد المترجم (إذا أمكن):** تسمح بعض أنظمة البناء بتحديد المترجم المراد استخدامه. تحقق من تعليمات بناء مشغل NVIDIA أو ملفات التكوين للحصول على خيارات متعلقة بالمترجم (على سبيل المثال، متغير البيئة `CC`).

2.  **عدم التوافق مع تكوين بناء النواة:** قد تكون النواة التي تستخدمها قد تم بناؤها باستخدام مترجم مكّن هذا الخيار، ونظام بناء مشغل NVIDIA يرثه أو يواجهه بطريقة تسبب فشلاً مع مترجمه الخاص.

    **الحل:**
    * **تجربة إصدار مختلف من مشغل NVIDIA:** قد يكون لمشغل NVIDIA الأحدث توافق أفضل مع النواة الأحدث وميزات المترجم. يمكنك محاولة تثبيت إصدار مستقر أحدث.
        ```bash
        sudo apt update
        sudo apt install nvidia-driver-<latest-version>
        ```
        استبدل `<latest-version>` بأحدث اسم حزمة مشغل موصى به لنظامك. يمكنك عادةً العثور على هذا عن طريق البحث `apt search nvidia-driver`.
    * **التراجع عن إصدار النواة (كحل مؤقت):** إذا كان لديك إصدار نواة سابق مثبتًا ويعمل مع مشغل NVIDIA الخاص بك، فيمكنك الإقلاع إلى تلك النواة من قائمة GRUB. هذا ليس حلاً دائمًا ولكن يمكن أن يمنحك نظامًا يعمل أثناء استكشاف مشكلة المشغل مع النواة الأحدث.

3.  **مشكلة في حزمة مشغل NVIDIA:** قد تكون هناك مشكلة في حزمة مشغل NVIDIA المحددة التي تحاول تثبيتها فيما يتعلق بإعدادات النواة والمترجم الخاصة بك.

    **الحل:**
    * **تجربة طريقة تثبيت مختلفة:** إذا قمت بتثبيت المشغل عبر `apt`، فيمكنك محاولة تنزيل المشغل مباشرة من موقع NVIDIA على الويب وتثبيته باستخدام ملف `.run`. اتبع التعليمات التي تقدمها NVIDIA بعناية.
    * **التحقق من المشاكل المعروفة:** ابحث في المنتديات عبر الإنترنت أو موقع دعم NVIDIA عن مشاكل معروفة مع إصدار المشغل وإصدار النواة المحددين اللذين تستخدمهما.

**خطوات للمحاولة:**

1.  **تحقق من إصدار GCC لديك:**
    ```bash
    gcc --version
    ```
    قارن هذا بإصدار GCC الذي ربما تم استخدامه لبناء النواة (قد تجد معلومات حول بيئة بناء النواة عبر الإنترنت لإصدار أوبونتو الخاص بك).

2.  **محاولة تثبيت مشغل NVIDIA أحدث:**
    ```bash
    sudo apt update
    sudo apt install nvidia-driver-550  # مثال: جرب إصدارًا أحدث
    sudo reboot
    ```

3.  **إذا لم ينجح ما سبق، فحاول إزالة المشغل المثبت تمامًا وإعادة تثبيت المشغل الموصى به:**
    ```bash
    sudo apt remove --purge nvidia-*
    sudo apt autoremove
    sudo apt update
    sudo ubuntu-drivers autoinstall  # يثبت المشغلات الموصى بها
    sudo reboot
    ```

4.  **إذا كنت لا تزال تواجه مشاكل، ففكر في تثبيت إصدار أحدث من GCC (كن حذرًا لأن هذا يمكن أن يكون له أحيانًا آثار جانبية غير مقصودة):**
    ```bash
    sudo apt install gcc-12 g++-12
    sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-12 12
    sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-12 12
    # قد ترغب في الاحتفاظ بالإصدار الأقدم أيضًا والتبديل مرة أخرى إذا لزم الأمر
    # للتبديل مرة أخرى: sudo update-alternatives --config gcc و sudo update-alternatives --config g++
    sudo apt install build-essential # تأكد من تحديث أدوات البناء بعد تغيير المترجم
    sudo dpkg --configure -a # إعادة تكوين الحزم
    sudo reboot
    ```

تذكر إعادة تشغيل نظامك بعد تجربة أي تثبيت لمشغل أو تغييرات كبيرة في النظام. راقب الناتج بعناية لأي رسائل خطأ جديدة. إذا استمرت مشكلاتك، فقد يساعد تقديم المزيد من التفاصيل حول إصدار أوبونتو الخاص بك في العثور على حل أكثر تحديدًا.

---

يبدو أنك تواجه مشاكل في التبعيات و DKMS (دعم وحدة النواة الديناميكية) أثناء محاولة تكوين حزم نواة لينكس، خاصة مع فشل بناء وحدة مشغل NVIDIA للنواة الجديدة (6.8.0-60-generic).

### إليك كيفية حل هذا:

---

### **1. إصلاح التبعيات وتكوين الحزم**
أولاً، حاول إصلاح مشاكل التبعيات وتكوين الحزم:

```bash
sudo apt --fix-broken install
sudo dpkg --configure -a
```

إذا لم ينجح ذلك، قم بإجبار تكوين الحزم المسببة للمشكلة يدويًا:

```bash
sudo apt install -f linux-headers-generic-hwe-22.04 linux-generic-hwe-22.04
```

---

### **2. إعادة تثبيت مشغلات NVIDIA**
يظهر الخطأ أن وحدة NVIDIA DKMS (`nvidia/535.183.06`) فشلت في البناء للنواة `6.8.0-60-generic`. لإصلاح هذا:

#### **الخيار أ: إعادة تثبيت مشغل NVIDIA**
```bash
sudo apt install --reinstall nvidia-dkms-535 nvidia-driver-535
```

#### **الخيار ب: إزالة وإعادة تثبيت مشغلات NVIDIA**
إذا لم تنجح إعادة التثبيت، جرب إعادة تثبيت نظيفة:
```bash
sudo apt purge nvidia-*
sudo apt autoremove
sudo apt install nvidia-driver-535
```

#### **الخيار ج: الإقلاع مؤقتًا إلى نواة أقدم**
إذا كان النظام لا يمكن إقلاعه، أعد التشغيل إلى نواة أقدم (اخترها في GRUB) ثم:
```bash
sudo apt remove linux-image-6.8.0-60-generic linux-headers-6.8.0-60-generic
sudo update-grub
```

---

### **3. فحص سجل بناء DKMS**
يذكر الخطأ `/var/lib/dkms/nvidia/535.183.06/build/make.log`. افحصه للحصول على التفاصيل:
```bash
cat /var/lib/dkms/nvidia/535.183.06/build/make.log
```

قد يكشف هذا عن سبب فشل بناء الوحدة (على سبيل المثال، تبعيات مفقودة، مشاكل في المترجم).

---

### **4. تثبيت التبعيات المفقودة يدويًا**
إذا أظهر السجل تبعيات مفقودة، قم بتثبيتها:
```bash
sudo apt install build-essential linux-headers-generic
```

---

### **5. إعادة بناء وحدات DKMS**
بعد إصلاح التبعيات، أعد بناء الوحدات يدويًا:
```bash
sudo dkms remove nvidia/535.183.06 --all
sudo dkms install nvidia/535.183.06
```

---

### **6. تحديث Initramfs**
بعد إصلاح مشغل NVIDIA، قم بتحديث initramfs:
```bash
sudo update-initramfs -u -k all
```

---

### **7. محاولة الإصلاح النهائية**
إذا فشل كل شيء آخر، قم بإزالة النواة المسببة للمشكلة والتزم بنواة أقدم:
```bash
sudo apt purge linux-image-6.8.0-60-generic linux-headers-6.8.0-60-generic
sudo apt autoremove
sudo update-grub
```

ثم، انتظر حتى يتم إصدار تحديث لمشغل NVIDIA أو النواة قبل المحاولة مرة أخرى.

---

### **8. منع المشاكل المستقبلية**
لتجنب مشاكل مماثلة، فكر في:
- **تعطيل تحديثات النواة التلقائية** (إذا كنت تعتمد على مشغلات NVIDIA):
  ```bash
  sudo apt-mark hold linux-image-generic linux-headers-generic
  ```
- **استخدام مشغل NVIDIA الرسمي** (بدلاً من حزمة أوبونتو) إذا كانت مشاكل التوافق متكررة.

---

### **ملاحظات نهائية**
- إذا أصبح النظام غير قابل للإقلاع، استخدم **GRUB** لتحديد نواة أقدم.
- إذا كانت مشغلات NVIDIA ضرورية لسير عملك، ففكر في استخدام **نواة LTS** (غير HWE) لتحقيق استقرار أفضل.

دعني أعرف إذا كنت بحاجة إلى مزيد من المساعدة!

---

إذا كنت تريد فقط المتابعة مع `apt upgrade` و **تخطي الحزم المسببة للمشكلة**، يمكنك استبعادها مؤقتًا من عملية الترقية. إليك الطريقة:

### **الخيار 1: تخطي الحزم التالفة ومتابعة الترقية**
شغّل:
```bash
sudo apt --fix-broken install
sudo apt upgrade -y --ignore-missing --allow-downgrades
```
- `--ignore-missing` يتخطى الحزم غير المتاحة.
- `--allow-downgrades` يسمح لـ APT بترجيع الحزم إذا لزم الأمر.

### **الخيار 2: حظر (منع) الحزم المسببة للمشكلة**
إذا استمرت المشكلة، **احظر** (امنع) الحزم المسببة للمشكلة لمنع ترقيتها:
```bash
sudo apt-mark hold linux-generic-hwe-22.04 linux-headers-generic-hwe-22.04 linux-image-6.8.0-60-generic
```
ثم أعد المحاولة:
```bash
sudo apt upgrade -y
```

### **الخيار 3: فرض الترقية (غير موصى به)**
إذا كنت لا تمانع في احتمال كسر الأشياء أكثر، ففرض الترقية:
```bash
sudo apt -o Dpkg::Options::="--force-overwrite" --fix-broken install
sudo apt upgrade -y
```
⚠️ **تحذير:** قد يسبب هذا عدم استقرار إذا كانت التبعيات غير متطابقة.

### **بعد التخطي:**
- سيعمل النظام仍然، ولكن **قد لا يعمل مشغل NVIDIA** على النواة `6.8.0-60`.
- يمكنك **الإقلاع إلى نواة أقدم** (اخترها في GRUB) إذا لزم الأمر.
- بمجرد إصدار NVIDIA تحديثًا متوافقًا، يمكنك **إلغاء حظر** الحزم:
  ```bash
  sudo apt-mark unhold linux-generic-hwe-22.04 linux-headers-generic-hwe-22.04
  ```

هل تريد المساعدة في التحقق من النواة النشطة حاليًا بعد الترقية؟