---
audio: false
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: دليل التصحيح في وضع التفصيل في فلاتر
translated: true
type: note
---

آه فهمت 👍 — نعم، Flutter لديها **وضع التفصيل** الذي يطبع *كل شيء* تقوم به خلف الكواليس حتى تتمكن من رؤية أين تتوقف.

---

### ✅ تشغيل أوامر `flutter` مع تسجيل مفصل

ما عليك سوى إضافة علامة `-v` (أو `--verbose`):

- لـ `flutter clean`:
  ```bash
  flutter clean -v
  ```

- لجلب التبعيات:
  ```bash
  flutter pub get -v
  ```

- لتشغيل تطبيقك:
  ```bash
  flutter run -v
  ```

سيؤدي هذا إلى إلقاء سجلات مفصلة (مكالمات الشبكة، حذف الملفات، أوامر pod، إلخ) إلى الطرفية.

---

### 📌 نصائح لقراءة سجلات الوضع المفصل
- انظر إلى **الأسطر القليلة الأخيرة** المعروضة قبل أن تتوقف عن الاستجابة → هذا يخبرك بأي خطوة تتوقف.
- غالبًا ما تحدث التوقفات أثناء:
  - حل **CocoaPods** (شبكة بطيئة / مستودع قديم).
  - **مزامنة Gradle** (في بناء Android).
  - الأذونات (إذا لم يكن لدى Flutter حق الوصول إلى بعض المجلدات).

---

👉 إذا ظل `flutter clean -v` متوقفًا، الصق آخر ~20 سطرًا تطبعه هنا ⬇️، وسأتمكن من المساعدة في تحليل مكان التوقف بالضبط.

هل تريد مني كتابة **برنامج تنظيف مختصر مع الوضع المفصل** لك حتى تتمكن من تشغيل `./super_clean.sh` بدلاً من كتابة كل أمر؟