---
audio: false
generated: true
lang: ar
layout: post
title: معالجة الصور باستخدام الشبكات العصبية الالتفافية ومحولات الرؤية
translated: true
type: note
---

فيما يلي أمثلة على التعليمات البرمجية التي توضح كيفية معالجة الصور باستخدام **الشبكات العصبية التلافيفية (CNNs)** و **Vision Transformers (ViT)** في Python باستخدام **PyTorch**.

---

## **1. معالجة الصور باستخدام CNN (شبكة عصبية تلافيفية)**
تُستخدم الـ CNNs على نطاق واسع في تصنيف الصور، واكتشاف الأشياء، واستخراج الميزات.

### **مثال: استخدام CNN مُدرَّب مسبقًا (ResNet)**
```python
import torch
import torchvision.models as models
import torchvision.transforms as transforms
from PIL import Image

# تحميل نموذج ResNet مُدرَّب مسبقًا
model = models.resnet18(pretrained=True)
model.eval()  # الانتقال إلى وضع التقييم

# تعريف المعالجة المسبقة للصورة
preprocess = transforms.Compose([
    transforms.Resize(256),
    transforms.CenterCrop(224),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]),
])

# تحميل الصورة ومعالجتها مسبقًا
image = Image.open("example.jpg")  # استبدل بمسار صورتك
input_tensor = preprocess(image)
input_batch = input_tensor.unsqueeze(0)  # إضافة بُعد الدُفعة

# النقل إلى GPU إذا كان متاحًا
if torch.cuda.is_available():
    input_batch = input_batch.to('cuda')
    model.to('cuda')

# استخراج الميزات (قبل طبقة التصنيف النهائية)
with torch.no_grad():
    features = model(input_batch)

print("شكل متجه الميزات:", features.shape)  # على سبيل المثال: torch.Size([1, 1000])
```
**الشرح**:
1. **ResNet18** هو بنية CNN مُدرَّب مسبقًا على مجموعة بيانات ImageNet.
2. تتم المعالجة المسبقة للصورة (إعادة التحجيم، التطبيع).
3. يحول النموذج الصورة إلى **متجه ميزات** (مثل 1000 بُعد لـ ResNet18).

---

## **2. معالجة الصور باستخدام Vision Transformer (ViT)**
تعامل ViTs الصور كسلاسل من الرقع وتستخدم آليات الانتباه الذاتي (كما في معالجة اللغات الطبيعية).

### **مثال: استخدام ViT مُدرَّب مسبقًا (Hugging Face)**
```python
from transformers import ViTFeatureExtractor, ViTModel
from PIL import Image
import torch

# تحميل Vision Transformer (ViT) مُدرَّب مسبقًا
model_name = "google/vit-base-patch16-224-in21k"
feature_extractor = ViTFeatureExtractor.from_pretrained(model_name)
model = ViTModel.from_pretrained(model_name)

# تحميل صورة
image = Image.open("example.jpg")  # استبدل بمسار صورتك

# المعالجة المسبقة للصورة (تحويلها إلى رقع)
inputs = feature_extractor(images=image, return_tensors="pt")

# استخراج الميزات (رمز CLS أو تضمينات الرقع)
with torch.no_grad():
    outputs = model(**inputs)

# الحصول على متجه الميزات (رمز CLS)
features = outputs.last_hidden_state[:, 0, :]  # الشكل: [1, 768]

print("شكل متجه الميزات:", features.shape)  # على سبيل المثال: torch.Size([1, 768])
```
**الشرح**:
1. **ViT** يقسم الصورة إلى **رقع بحجم 16x16** ويعالجها مثل الرموز المميزة في معالجة اللغات الطبيعية.
2. يمثل **رمز CLS** (الرمز الأول) متجه الميزات للصورة بأكملها.
3. الناتج هو **متجه بـ 768 بُعد** (لـ `vit-base`).

---

## **3. مقارنة استخراج الميزات بين CNN و ViT**

| النموذج | النهج | حجم متجه الميزات | المكتبات |
|-------|----------|---------------------|-----------|
| **CNN (ResNet18)** | طبقات تلافيفية + تجميع | 1000 (فئات ImageNet) | `torchvision` |
| **ViT (Google ViT-Base)** | تضمينات الرقع + المحول | 768 (البُعد المخفي) | `transformers` |

---

## **4. تطبيقات متجهات ميزات الصور**
- **بحث الصور**: مقارنة متجهات الميزات (مثل تشابه جيب التمام).
- **التعلم بالانتقال**: استخدام الميزات المُدرَّبة مسبقًا للمهام المخصصة.
- **كشف الأشياء**: استخراج مناطق الاهتمام (ROI) من الميزات.

```python
# مثال: حساب التشابه بين صورتين
from sklearn.metrics.pairwise import cosine_similarity

# افترض أن features1 و features2 هما متجها الميزات المستخرجة
similarity = cosine_similarity(features1, features2)
print("نتيجة التشابه:", similarity[0][0])
```

---

## **النقاط الرئيسية**
1. تستخدم **CNNs** (مثل ResNet) **الطبقات التلافيفية** لاستخراج الميزات الهرمية.
2. تعامل **ViTs** (مثل ViT من Google) الصور كـ **سلاسل من الرقع** وتستخدم **الانتباه الذاتي**.
3. يحول كلا النهجين الصور إلى **متجهات ميزات** للمهام اللاحقة.

هل تريد مثالًا عن **تدريب CNN/ViT مخصص** من الصفر؟ دعني أعلم! 🚀