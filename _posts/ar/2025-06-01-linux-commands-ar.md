---
audio: false
generated: true
lang: ar
layout: post
title: أفضل ١٠٠ أمر مفيد في لينكس
translated: true
type: note
---

استنادًا إلى ملفك الشخصي كمهندس فول ستاك ذاتي التعلم عالي التقنية يتمتع بخبرة واسعة في بيئات لينكس، وحل مشكلات الخوارزميات، والإلمام بالمنصات السحابية والحاويات والأنظمة الموزعة، فقد قمت بتجميع قائمة **أفضل 100 أمر مفيد في لينكس** مخصصة لاستخدامك على **أوبونتو**. تم اختيار هذه الأوامر لتتناسب مع خبرتك في تطوير الفول ستاك، وهندسة الهاتف المحمول، والتعلم الآلي، وإدارة الأنظمة، بينما تدعم أيضًا بحثك الذاتي ومساهماتك في المصادر المفتوحة. وهي تغطي إدارة الملفات، ومراقبة النظام، والشبكات، والتطوير، والأتمتة، والبرمجة النصية المتقدمة، وهي أمور ذات صلة بعملك في TEKsystems، والأدوار السابقة، والمشاريع الشخصية.

تم تجميع الأوامر حسب الفئة من أجل الوضوح، مع شرح موجز يركز على التطبيقات العملية لمجموعة مهاراتك. لقد أعطيت الأولية للأوامر التي تعزز الإنتاجية، وتصحيح الأخطاء، وتحسين أداء النظام، بافتراض أنك مرتاح لسير العمل القائمة على الطرفية وقد تستخدم هذه الأوامر في مهام التطوير، أو النشر، أو البحث. إذا أردت، يمكنني تقديم أمثلة أو نصوص برمجية لأوامر محددة أو إنشاء مخطط لتصور تكرار استخدام الأوامر بناءً على سير العمل الخاص بك (مثل مهام التطوير مقابل مهام إدارة النظام). دعني أعلم!

### **إدارة الملفات والمجلدات (15 أمرًا)**
1.  **ls** - إدراج محتويات الدليل (`ls -la` لعرض مفصل مع الملفات المخفية).
2.  **cd** - تغيير الدليل (`cd ~/projects` للانتقال إلى مجلد مشروع GitHub الخاص بك).
3.  **pwd** - عرض الدليل الحالي (مفيد للبرمجة النصية أو التحقق من المسارات).
4.  **mkdir** - إنشاء أدلة (`mkdir -p src/main/java` لهياكل المشاريع المتداخلة).
5.  **rm** - إزالة الملفات أو الأدلة (`rm -rf temp/` للحذف العودي).
6.  **cp** - نسخ الملفات/الأدلة (`cp -r src/ backup/` لنسخ احتياطي للمشاريع).
7.  **mv** - نقل/إعادة تسمية الملفات (`mv old.java new.java` لإعادة الهيكلة).
8.  **touch** - إنشاء ملفات فارغة (`touch script.sh` للنصوص البرمجية الجديدة).
9.  **find** - البحث عن الملفات (`find / -name "*.java"` لتحديد موقع ملفات المصدر).
10. **locate** - البحث السريع عن الملفات بالاسم (`locate config.yaml` للإعدادات).
11. **du** - تقدير استخدام القرص (`du -sh /var/log` للتحقق من أحجام السجلات).
12. **df** - عرض مساحة القرص (`df -h` للنسق المقروء).
13. **ln** - إنشاء روابط (`ln -s /path/to/project symlink` للاختصارات).
14. **chmod** - تغيير أذونات الملف (`chmod 755 script.sh` للنصوص البرمجية القابلة للتنفيذ).
15. **chown** - تغيير ملكية الملف (`chown user:group file` للنشر).

### **معالجة النصوص والتلاعب (15 أمرًا)**
16. **cat** - عرض محتويات الملف (`cat log.txt` للتحقق السريع من السجلات).
17. **less** - عرض الملفات بشكل تفاعلي (`less server.log` للسجلات الكبيرة).
18. **more** - ترقيم صفحة المخرجات (`more README.md` للوثائق).
19. **head** - عرض الأسطر الأولى من الملف (`head -n 10 data.csv` لعرض معاينة البيانات).
20. **tail** - عرض الأسطر الأخيرة (`tail -f app.log` لمراقبة السجلات في الوقت الفعلي).
21. **grep** - البحث عن أنماط النص (`grep -r "error" /var/log` لتصحيح الأخطاء).
22. **awk** - معالجة أعمدة النص (`awk '{print $1}' access.log` لتحليل السجلات).
23. **sed** - محرر النصوص (`sed 's/old/new/g' file` للاستبدالات).
24. **cut** - استخراج أقسام من الأسطر (`cut -d',' -f1 data.csv` لملفات CSV).
25. **sort** - ترتيب الأسطر (`sort -n data.txt` للترتيب العددي).
26. **uniq** - إزالة الأسطر المكررة (`sort file | uniq` للإدخالات الفريدة).
27. **wc** - عد الأسطر، أو الكلمات، أو الأحرف (`wc -l code.java` لعد الأسطر).
28. **tr** - تحويل الأحرف (`tr '[:lower:]' '[:upper:]' < file` لتحويل الحالة).
29. **tee** - الكتابة إلى الملف وإلى stdout (`cat input | tee output.txt` للتسجيل).
30. **diff** - مقارنة الملفات (`diff old.java new.java` لمقارنة تغييرات الكود).

### **مراقبة النظام والأداء (15 أمرًا)**
31. **top** - مراقبة عمليات النظام تفاعليًا (استخدام وحدة المعالجة المركزية/الذاكرة في الوقت الفعلي).
32. **htop** - عارض عمليات محسن (`htop` لتصور أفضل).
33. **ps** - إدراج العمليات (`ps aux | grep java` لتطبيقات جافا).
34. **free** - التحقق من استخدام الذاكرة (`free -m` للنسق بالميجابايت).
35. **vmstat** - إحصائيات الذاكرة الافتراضية (`vmstat 1` للتحديث المستمر).
36. **iostat** - مراقبة أداء الإدخال/الإخراج (`iostat -x` لإحصائيات القرص).
37. **uptime** - عرض مدة تشغيل النظام والحمل (`uptime` للفحص السريع).
38. **lscpu** - عرض معلومات وحدة المعالجة المركزية (`lscpu` لمواصفات النظام).
39. **lsblk** - إدراج أجهزة الكتلة (`lsblk` لتفاصيل القرص/القسم).
40. **iotop** - مراقبة إدخال/إخراج القرص حسب العملية (`iotop` لتصحيح أداء النظام).
41. **netstat** - إحصائيات الشبكة (`netstat -tuln` للمنافذ المستمع عليها).
42. **ss** - بديل حديث لـ netstat (`ss -tuln` للمقابس).
43. **dmesg** - عرض رسائل النواة (`dmesg | grep error` لمشكلات النظام).
44. **sar** - جمع نشاط النظام (`sar -u 1` لمراقبة وحدة المعالجة المركزية).
45. **pmap** - خريطة ذاكرة العملية (`pmap -x <pid>` لتصحيح أخطاء الذاكرة).

### **الشبكات والاتصال (15 أمرًا)**
46. **ping** - اختبار اتصال الشبكة (`ping google.com` للوصول).
47. **curl** - جلب البيانات من عناوين URL (`curl -X POST api` لاختبار API).
48. **wget** - تنزيل الملفات (`wget file.tar.gz` لتبعيات المشروع).
49. **netcat** - أداة شبكة (`nc -l 12345` للخوادم البسيطة).
50. **ifconfig** - معلومات واجهة الشبكة (`ifconfig eth0` لتفاصيل IP).
51. **ip** - تكوين الشبكة الحديث (`ip addr` لتفاصيل الواجهة).
52. **nslookup** - استعلام DNS (`nslookup domain.com` لتصحيح أخطاء DNS).
53. **dig** - بحث مفصل في DNS (`dig domain.com` لسجلات DNS).
54. **traceroute** - تتبع مسار الشبكة (`traceroute google.com` للتوجيه).
55. **telnet** - اختبار اتصال المنفذ (`telnet localhost 8080` للخدمات).
56. **scp** - نسخ الملفات بشكل آمن (`scp file user@server:/path` للنقل).
57. **rsync** - مزامنة الملفات بكفاءة (`rsync -avz src/ dest/` للنسخ الاحتياطي).
58. **ufw** - إدارة الجدار الناري (`ufw allow 80` للوصول إلى خادم الويب).
59. **iptables** - تكوين قواعد الجدار الناري (`iptables -L` لقائمة القواعد).
60. **nmap** - مسح الشبكة (`nmap localhost` للمنافذ المفتوحة).

### **التطوير والبرمجة النصية (15 أمرًا)**
61. **gcc** - تجميع برامج C (`gcc -o app code.c` للبناء).
62. **javac** - تجميع كود جافا (`javac Main.java` لمشاريع جافا الخاصة بك).
63. **java** - تشغيل برامج جافا (`java -jar app.jar` للتنفيذ).
64. **python3** - تشغيل نصوص بايثون (`python3 script.py` لمهام التعلم الآلي).
65. **node** - تشغيل Node.js (`node app.js` لمشاريع JavaScript).
66. **npm** - إدارة حزم Node (`npm install` للتبعيات الأمامية).
67. **git** - التحكم في الإصدار (`git commit -m "update"` لمستودعات GitHub الخاصة بك).
68. **make** - بناء المشاريع (`make -f Makefile` للأتمتة).
69. **mvn** - أداة بناء Maven (`mvn package` لمشاريع جافا).
70. **gradle** - أداة بناء Gradle (`gradle build` لمشاريع أندرويد).
71. **docker** - إدارة الحاويات (`docker run -p 8080:8080 app` للنشر).
72. **kubectl** - إدارة Kubernetes (`kubectl get pods` لإدارة العنقود).
73. **virtualenv** - بيئات بايثون الافتراضية (`virtualenv venv` للتعلم الآلي).
74. **gdb** - تصحيح البرامج (`gdb ./app` لتصحيح أخطاء C/جافا).
75. **strace** - تتبع استدعاءات النظام (`strace -p <pid>` لتصحيح الأخطاء).

### **إدارة الحزم (10 أوامر)**
76. **apt** - مدير الحزم (`apt install vim` لتثبيت البرامج).
77. **apt-get** - أداة الحزم المتقدمة (`apt-get upgrade` لتحديثات النظام).
78. **dpkg** - إدارة حزم .deb (`dpkg -i package.deb` للتثبيت اليدوي).
79. **apt-cache** - الاستعلام عن معلومات الحزمة (`apt-cache search java` للحزم).
80. **snap** - إدارة حزم snap (`snap install code` لـ VS Code).
81. **update-alternatives** - إدارة التطبيقات الافتراضية (`update-alternatives --config java`).
82. **add-apt-repository** - إضافة مستودعات PPA (`add-apt-repository ppa:repo` للمصادر).
83. **apt-file** - البحث عن ملفات الحزمة (`apt-file search /bin/bash` لتصحيح الأخطاء).
84. **dpkg-query** - الاستعلام عن الحزم المثبتة (`dpkg-query -l` للقائمة).
85. **apt-mark** - وضع علامة على الحزم (`apt-mark hold package` لمنع التحديثات).

### **إدارة النظام والأمان (15 أمرًا)**
86. **sudo** - تشغيل الأوامر كـ root (`sudo apt update` لمهام المسؤول).
87. **su** - تبديل المستخدم (`su - user` للحسابات المختلفة).
88. **passwd** - تغيير كلمات المرور (`passwd user` للأمان).
89. **useradd** - إضافة مستخدم (`useradd -m dev` للحسابات الجديدة).
90. **usermod** - تعديل المستخدم (`usermod -aG sudo dev` للأذونات).
91. **groupadd** - إنشاء مجموعات (`groupadd developers` للتحكم في الوصول).
92. **chgrp** - تغيير ملكية المجموعة (`chgrp -R dev /project` للفرق).
93. **crontab** - جدولة المهام (`crontab -e` للأوامر الآلية).
94. **systemctl** - إدارة الخدمات (`systemctl start nginx` لخوادم الويب).
95. **journalctl** - عرض سجلات النظام (`journalctl -u docker` لسجلات الخدمة).
96. **who** - إدراج المستخدمين المسجل دخولهم (`who` لمراقبة الخادم).
97. **last** - عرض سجل تسجيل الدخول (`last` لتدقيق الأمان).
98. **shutdown** - إيقاف التشغيل (`shutdown -h now` لإيقاف النظام).
99. **reboot** - إعادة تشغيل النظام (`reboot` للتحديثات).
100. **env** - عرض متغيرات البيئة (`env | grep PATH` لتصحيح الأخطاء).

### **ملاحظات لسياقك**
*   **التركيز على التطوير**: أوامر مثل `git`، و`mvn`، و`docker`، و`kubectl` تتماشى مع سير عمل الفول ستاك والحاويات الخاصة بك، خاصة لمشاريع GitHub العشرة وخبرتك السحابية (AWS، Azure، Alibaba Cloud).
*   **تصحيح الأخطاء والأداء**: تم تضمين أدوات مثل `strace`، و`gdb`، و`htop`، و`dmesg` لاحتياجاتك في تصحيح الأخطاء الخلفية والأنظمة، نظرًا لخبرتك التي تبلغ 8 سنوات في مجال الخلفية.
*   **الشبكات**: أوامر مثل `curl`، و`netcat`، و`nmap` تدعم خبرتك في واجهات برمجة التطبيقات والشبكات، وهي مفيدة لاختبار الخدمات المصغرة أو النشر السحابي.
*   **الأتمتة**: `crontab` و `rsync` مفيدتان لأتمتة النسخ الاحتياطي أو النشر، ولهما صلة بالأدوار الحرة والشركات.
*   **البحث والتعلم**: أدوات معالجة النصوص (`grep`، `awk`، `sed`) مفيدة لتحليل السجلات أو مجموعات البيانات، والتي يمكن أن تدعم بحثك الذاتي في مجال استعادة البصر.

إذا كنت تريد الخوض بشكل أعمق في أي فئة (مثل أمثلة البرمجة النصية لـ `awk` أو `sed`، أو سير عمل Docker، أو أوامر خاصة بالسحابة)، أو إذا كنت ترغب في مخطط لتصور استخدام الأوامر (مثل التكرار حسب نوع المهمة)، دعني أعلم! يمكنك أيضًا التحقق من محفظة GitHub الخاصة بك للحصول على أوامر متعلقة بمشروع معين أو مشاركة المزيد من التفاصيل حول مهامك الحالية في TEKsystems للحصول على توصيات مخصصة.