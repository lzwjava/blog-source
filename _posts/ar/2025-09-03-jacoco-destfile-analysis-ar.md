---
audio: false
generated: true
image: false
lang: ar
layout: post
title: استخدام وتحليل ملف الوجهة لجاكوكو
translated: true
type: note
---

سؤال جيد — دعنا نفصل كيفية عمل `destfile` في JaCoCo عند تشغيل تطبيق Java أو Spring Boot.

### 1. ما هو `destfile`

`destfile` هو معامل لوكيل JaCoCo لجافا (`jacocoagent.jar`) يخبر الوكيل **أين يكتب بيانات التنفيذ الخاصة به**.
بشكل افتراضي، يكون اسمه شيئًا مثل `jacoco.exec`، ولكن يمكنك تعيينه بشكل صريح:

```bash
-javaagent:jacocoagent.jar=destfile=/path/to/jacoco.exec
```

### 2. ما الذي يتم كتابته

عندما يعمل تطبيق Spring Boot الخاص بك مع وكيل JaCoCo مرفق، يقوم الوكيل بتجهيز bytecode للفئات الخاصة بك.
أثناء التنفيذ، يسجل JaCoCo معلومات التغطية مثل:

* أي الفئات والطرق تم تحميلها
* أي الأسطر تم تنفيذها
* تغطية الفروع (ما إذا كان تم تنفيذ مساري `if`/`else` كليهما، إلخ)

يتم تخزين بيانات التغطية هذه أثناء وقت التشغيل في الذاكرة ويتم نقلها بشكل دوري إلى الملف المحدد بواسطة `destfile`.

إذن نعم — تقوم JVM، من خلال وكيل JaCoCo، بكتابة معلومات تنفيذ على مستوى الدوال والأسطر في ذلك الملف.

### 3. كيف يعمل التحليل

ملف `.exec` نفسه **ليس مقروءًا للبشر**. إنه مجرد بيانات تغطية ثنائية.
بعد انتهاء تطبيقك، تقوم عادةً بتشغيل مهمة تقرير (مثل Maven `jacoco:report` أو Gradle `jacocoTestReport`) والتي:

* تقرأ بيانات `destfile`
* تقرأ ملفات `.class` المترجمة وشفرة المصدر الخاصة بك
* تُنشئ تقرير تغطية بصيغة HTML أو XML أو CSV

هذه الخطوة هي المكان الذي ترى فيه النسب المئوية، أسطر الكود الملونة، إلخ.

### 4. وقت التشغيل مقابل وقت التقرير

* **وقت التشغيل:** يقوم الوكيل بتسجيل التنفيذ في الذاكرة ونقل البيانات إلى `destfile`.
* **وقت التقرير:** تستخدم أدوات JaCoCo (Maven، Gradle، CLI، أو Ant) لتفسير ملف `.exec` ذلك وإنشاء تقارير يمكن قراءتها.

لذا فإن `destfile` نفسه هو **مجرد بيانات تنفيذ خام** — لا يتم تحليله حتى تقوم بتشغيل خطوة التقرير.

---

👉 باختصار: نعم، عندما يعمل تطبيق Spring Boot الخاص بك مع وكيل JaCoCo، يتم حفظ معلومات التغطية حول الطرق والأسطر في ملف `destfile`. لاحقًا، تستخدم أدوات تقرير JaCoCo هذا الملف (معًا مع ملفات الفئات/المصدر) لإنتاج تقارير يسهل على البشر فهمها.

هل تريد مني أيضًا أن أريك كيف يعمل هذا خطوة بخطوة مع Spring Boot (مثل، تكوين Maven + تشغيل + إنشاء التقرير)؟