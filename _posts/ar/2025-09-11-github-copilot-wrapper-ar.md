---
audio: false
date: 2025-09-11
generated: true
image: false
lang: ar
layout: post
model: mistralai/mistral-medium-3.1
title: غلاف بايثون غير رسمي لـ GitHub Copilot
translated: true
type: note
---

مستودع **GitHub [ericc-ch/copilot-api](https://github.com/ericc-ch/copilot-api)** هو **غلاف Python غير رسمي** للتفاعل مع **واجهة برمجة تطبيقات GitHub Copilot** (أداة إكمال التعليمات البرمجية المدعومة بالذكاء الاصطناعي). يسمح للمطورين بإنشاء اقتراحات التعليمات البرمجية، والإكمالات، والشروحات برمجياً باستخدام البنية التحتية لـ Copilot دون الاعتماد على امتداد VS Code الرسمي أو تكاملات بيئة التطوير المتكاملة الأخرى.

---

## **ما هي استخداماته؟**
يمكن استخدام غلاف واجهة برمجة التطبيقات هذا لـ:
1. **إنشاء إكمالات التعليمات البرمجية** (كما في VS Code ولكن برمجياً).
2. **الحصول على شروحات** لمقتطفات التعليمات البرمجية.
3. **دمج Copilot في التطبيقات المخصصة** (مثل أدوات سطر الأوامر، التطبيقات الويب، أو سير العمل الآلية).
4. **تجربة استجابات Copilot** دون الحاجة إلى بيئة تطوير متكاملة.
5. **تجاوز حدود المعدل** (إذا تم استخدامه بحذر، على الرغم من أن هذا قد ينتهك شروط الخدمة من GitHub).

⚠️ **تحذير:**
- هذه واجهة برمجة تطبيقات **غير رسمية**، مما يعني أن GitHub قد يغير أو يمنع الوصول في أي وقت.
- قد يؤدي استخدام هذا إلى **انتهاك شروط خدمة GitHub Copilot** إذا تم استخدامه للأتمتة أو الأغراض التجارية دون إذن.
- **تطبق حدود المعدل** (قد يحظر GitHub الحسابات للطلبات المفرطة).

---

## **كيفية استخدامه؟**
### **1. التثبيت**
انسخ المستودع وقم بتثبيت التبعيات:
```bash
git clone https://github.com/ericc-ch/copilot-api.git
cd copilot-api
pip install -r requirements.txt
```

### **2. المصادقة**
تحتاج إلى **رمز GitHub Copilot** (ليس هو نفسه رمز الوصول الشخصي لـ GitHub).
#### **كيفية الحصول على رمز Copilot؟**
1. **باستخدام أدوات المطور في المتصفح (موصى به)**
   - افتح **VS Code** مع تمكين Copilot.
   - افتح **أدوات المطور** (`F12` أو `Ctrl+Shift+I`).
   - انتقل إلى علامة التبويب **الشبكة**.
   - قم بتصفية طلبات `copilot`.
   - ابحث عن طلب إلى `https://api.github.com/copilot_internal/v2/token`.
   - انسخ **رمز التفويض** من الاستجابة.

2. **باستخدام السكريبت (إذا كان متاحًا)**
   تحتوي بعض النسخ المشتقة من هذا المستودع على سكريبت لاستخراج الرمز.

#### **تعيين الرمز في Python**
```python
from copilot import Copilot

copilot = Copilot(
    auth_token="YOUR_COPILOT_TOKEN",  # من أدوات المطور
    proxy="http://your-proxy:port"    # اختياري (إذا كنت خلف وكيل)
)
```

---

### **3. أمثلة الاستخدام الأساسية**
#### **الحصول على إكمالات التعليمات البرمجية**
```python
response = copilot.get_completion(
    prompt="def calculate_factorial(n):",
    language="python",
    n=3  # عدد الاقتراحات
)
print(response)
```
**مثال على المخرجات:**
```python
[
    "def calculate_factorial(n):\n    if n == 0:\n        return 1\n    else:\n        return n * calculate_factorial(n-1)",
    "def calculate_factorial(n):\n    result = 1\n    for i in range(1, n+1):\n        result *= i\n    return result",
    "def calculate_factorial(n):\n    return 1 if n <= 1 else n * calculate_factorial(n - 1)"
]
```

#### **الحصول على شرح للتعليمة البرمجية**
```python
explanation = copilot.explain_code(
    code="def factorial(n): return 1 if n <= 1 else n * factorial(n - 1)",
    language="python"
)
print(explanation)
```
**مثال على المخرجات:**
```
هذه دالة متكررة لحساب مضروب الرقم `n`.
- إذا كان `n` يساوي 0 أو 1، فإنها ترجع 1 (الحالة الأساسية).
- وإلا، فإنها ترجع `n * factorial(n-1)`، مما يقسم المشكلة إلى مشكلات فرعية أصغر.
```

#### **الدردشة مع Copilot (إذا كانت مدعومة)**
تسمح بعض الإصدارات بالتفاعلات المحادثة:
```python
response = copilot.chat(
    message="How do I sort a list in Python?",
    context="python"
)
print(response)
```

---

### **4. استخدامات متقدمة**
#### **الاستخدام في أداة CLI مخصصة**
```python
import argparse
from copilot import Copilot

parser = argparse.ArgumentParser()
parser.add_argument("--prompt", type=str, required=True)
parser.add_argument("--language", type=str, default="python")
args = parser.parse_args()

copilot = Copilot(auth_token="YOUR_TOKEN")
completions = copilot.get_completion(args.prompt, args.language, n=3)

for i, code in enumerate(completions, 1):
    print(f"--- الاقتراح {i} ---")
    print(code)
    print()
```

#### **التكامل مع FastAPI (تطبيق ويب)**
```python
from fastapi import FastAPI
from copilot import Copilot

app = FastAPI()
copilot = Copilot(auth_token="YOUR_TOKEN")

@app.get("/complete")
def complete_code(prompt: str, language: str = "python"):
    return copilot.get_completion(prompt, language, n=1)
```

---

## **القيود والمخاطر**
✅ **الإيجابيات:**
- يعمل بدون VS Code.
- يمكن دمجه في سير العمل المخصصة.
- مفيد للأتمتة (إذا كان مسموحًا به).

❌ **السلبيات:**
- **واجهة برمجة تطبيقات غير رسمية** (قد تتوقف عن العمل في أي وقت).
- **حدود المعدل** (قد يحظر GitHub حسابك).
- **مخاوف أخلاقية** (تحقق من شروط خدمة GitHub قبل الاستخدام).
- **لا يوجد دعم رسمي**.

---

## **البدائل**
إذا كنت تريد طريقة **قانونية** لاستخدام Copilot برمجياً:
1. **GitHub Copilot CLI الرسمي** (إذا تم إصداره في المستقبل).
2. **GitHub Copilot Labs** (الامتدادات التجريبية).
3. **مساعدات الذكاء الاصطناعي الأخرى للتعليمات البرمجية** (مثل **Amazon CodeWhisperer**، **TabNine**، **Sourcegraph Cody**).

---

## **أفكار أخيرة**
هذا المستودع مفيد **للتجربة** و**لفهم كيفية عمل Copilot من الداخل**، ولكن **استخدمه بمسؤولية**. إذا كنت تبني أداة للإنتاج، ففكر في الانتظار للحصول على واجهة برمجة تطبيقات رسمية أو استخدام بدائل معتمدة.

هل تحتاج إلى مساعدة في حالة استخدام محددة؟ 🚀