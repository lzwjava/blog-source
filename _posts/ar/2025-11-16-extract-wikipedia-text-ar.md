---
audio: false
generated: true
image: false
lang: ar
layout: post
model: x-ai/grok-4-fast
title: استخراج النص من مستودعات ويكيبيديا
translated: true
type: note
---

إليك أكثر الطرق شيوعًا وفعالية لاستخراج النص العادي من ملفات إخراج Wikipedia `.bz2` مثل ملفك (`enwiki-latest-pages-articles1.xml-p1p41242.bz2`).

### الأدوات والطرق الموصى بها (من الأسهل إلى الأكثر كفاءة)

#### 1. استخدام wikiextractor (الأفضل لمعظم المستخدمين – سريع ونظيف)
هذه هي الأداة القياسية التي يستخدمها الجميع تقريبًا في معالجة اللغة الطبيعية والتعلم الآلي لإخراج Wikipedia.

```bash
# التثبيت (مرة واحدة)
pip install wikiextractor

# استخراج النص (يزيل القوالب، الجداول، إلخ، ويعطي نصًا نظيفًا لكل مقالة)
wikiextractor enwiki-latest-pages-articles1.xml-p1p41242.bz2 --output wiki_text
```

- المخرجات: ينشئ مجلد `wiki_text/` به مجلدات فرعية AA, AB, … تحتوي على ملفات `.bz2` من JSON/نص نظيف.
- للحصول على نص عادي (مقالة واحدة لكل سطر):
```bash
wikiextractor enwiki-latest-pages-articles1.xml-p1p41242.bz2 --processes 8 -o wiki_text --json
# ثم تحويل JSON إلى نص عادي إذا لزم الأمر
find wiki_text -name '*' -exec cat {} \; | grep '"text":' | sed 's/.*"text": "//;s/"$//' > all_text.txt
```

#### 2. استخدام WikiCorpus من مكتبة gensim (رائع إذا كنت تعمل على معالجة اللغة الطبيعية بـ Python)

```python
from gensim.corpora import WikiCorpus

# هذه الطريقة تتعامل تلقائيًا مع .bz2 وتستخرج النص + تقوم بالتجزئة (Tokenization)
wiki = WikiCorpus("enwiki-latest-pages-articles1.xml-p1p41242.bz2", dictionary=False)

# التكرار عبر المقالات
with open("output.txt", "w", encoding="utf-8") as f:
    for i, text in enumerate(wiki.get_texts()):
        f.write(" ".join(text) + "\n")
        if i % 10000 == 0:
            print(f"Processed {i} articles")
```

#### 3. استخدام bzip2 + grep/awk (سريع وغير نظيف، بدون تنظيف)

```bash
# مجرد فك الضغط وعرض XML الخام
bzip2 -dc enwiki-latest-pages-articles1.xml-p1p41242.bz2 | less

# استخراج محتوى <text> فقط (تقريبي جدًا، يحتفظ بترميز الويكي)
bzip2 -dc enwiki-latest-pages-articles1.xml-p1p41242.bz2 | \
  sed -n '/<text /,/<\/text>/p' | \
  sed 's/<text[^>]*>//g;s/<\/text>//g' > raw_text_with_markup.txt
```

#### 4. إذا كان لديك الإخراج الكامل وتريد كل شيء بسرعة (متوازي)
ملفك هو فقط الجزء الأول (p1p41242)، ولكن إذا كان لديك كل الـ 30+ جزء لاحقًا:

```bash
# استخراج كل شيء بشكل متوازي
ls *.bz2 | xargs -P 8 -I {} wikiextractor {} --output wiki_extracted
```

### الملخص – ما أوصي به لك الآن:

```bash
pip install wikiextractor
wikiextractor enwiki-latest-pages-articles1.xml-p1p41242.bz2 -o extracted_wiki --bytes 1M --json
```

هذا سيمنحك مقالات نظيفة مقروءة في بضع دقائق (ملفك مضغوط بحجم ~2-3 جيجابايت فقط، لذا يمكن التعامل معه بسهولة).

أعلمني إذا كنت تريد:
- namespace الرئيسي فقط (المقالات الحقيقية، وليس صفحات النقاش)
- نص عادي بدلاً من JSON
- مخرجات مجزأة (Tokenized)
- Wikipedia الصينية بدلاً من ذلك (zhwiki) – العملية مطابقة

استخراج سعيد! 🚀