---
audio: false
generated: true
image: false
lang: ar
layout: post
title: مقارنة بين وكيل ليبرتي ووكيل جاكو
translated: true
type: note
---

في WebSphere Liberty (WLP)، فإن `ws-javaagent.jar` هو وكيل أدوات Java الخاص بـ IBM. يُستخدم بشكل أساسي لمراقبة وقت التشغيل، والتشخيص، والتكامل مع أدوات مثل Application Performance Management (APM)، وHealth Center، أو قدرات المراقبة الخاصة بـ Liberty. عند تمكينه، يمكن لـ Liberty جمع مقاييس التنميط، وجمع البيانات المهملة، والخيوط، ومقاييس مستوى JVM الأخرى تلقائيًا.

من ناحية أخرى، فإن `jacocoagent.jar` الخاص بـ JaCoCo هو بشكل خاص **وكيل تغطية التعليمات البرمجية**. هدفه هو أداء تحويل البايت كود أثناء وقت التشغيل بحيث تُنتج عمليات تشغيل الاختبار أو تنفيذ التطبيق بيانات التغطية (مثل، أي فئات، ووظائف، وفروع تم تنفيذها).

لذا فإن التمييز هو:

* **`ws-javaagent.jar`** → مراقبة عامة، وتنميط، وتشخيص لأدوات Liberty و IBM.
* **`jacocoagent.jar`** → إعداد تقارير تغطية الاختبار/الكود، مستقل عن Liberty.

لا يجب عليك استبدال وكيل JaCoCo بوكيل Liberty. إذا كان هدفك هو **إعداد تقارير تغطية الاختبار** (لسلسلة التكامل المستمر (CI)، وSonarQube، وتقارير Maven/Gradle، إلخ)، فيجب عليك الاستمرار في استخدام **وكيل JaCoCo**. لن يقوم وكيل Liberty بإنشاء ملفات تغطية JaCoCo من نوع `.exec`.

ولكنك *يمكنك* تشغيل الاثنين في نفس الوقت إذا لزم الأمر — حيث يدعم Java وسيطات `-javaagent` متعددة. على سبيل المثال:

```bash
-javaagent:/path/to/ws-javaagent.jar
-javaagent:/path/to/jacocoagent.jar=destfile=jacoco.exec,output=file
```

👉 باختصار:
استخدم **`jacocoagent.jar`** للتغطية.
استخدم **`ws-javaagent.jar`** إذا كنت بحاجة إلى تشخيص/تنميط Liberty.
إنهما يخدمان أغراضًا مختلفة ولا يمكن استبدال أحدهما بالآخر.

هل تريد مني أيضًا أن أوضح لك كيفية تكليفهما معًا في ملف خيارات JVM لخادم Liberty؟