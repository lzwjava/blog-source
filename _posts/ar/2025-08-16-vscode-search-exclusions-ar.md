---
audio: false
generated: true
image: false
lang: ar
layout: post
title: تخصيص استثناءات البحث في VS Code
translated: true
type: note
---

في VS Code، يلتزم البحث العام في الملفات بـ **استثناءات البحث** التي تقوم بتكوينها. يمكنك طلب تجاهل ملفات أو مجلدات معينة حتى لا تظهر في نتائج البحث.

إليك الطرق الرئيسية:

---

### 1. استخدام `search.exclude` في **settings.json**

افتح الإعدادات (`⌘,` / `Ctrl+,` → أعلى اليمين "فتح الإعدادات (JSON)") وأضف:

```json
"search.exclude": {
  "**/node_modules": true,
  "**/dist": true,
  "**/*.log": true
}
```

* المفاتيح هي [أنماط جلوب](https://code.visualstudio.com/docs/editor/codebasics#_advanced-search-options).
* `true` تعني "استبعاد من البحث".
* أمثلة:

  * `**/node_modules` → جميع مجلدات node\_modules
  * `**/*.log` → جميع ملفات `.log` في أي مكان
  * `**/build/**` → كل شيء داخل مجلدات build

---

### 2. استخدام `.gitignore`

إذا كان مشروعك يحتوي على ملف `.gitignore`، يمكنك إخبار VS Code بمراعاته أثناء البحث:

```json
"search.useIgnoreFiles": true
```

بهذه الطريقة، أي شيء يتم تجاهله بواسطة Git لن يظهر في نتائج البحث.

---

### 3. استخدام `.ignore`

يمكنك أيضًا إنشاء ملف `.ignore` في المجلد الجذري لمشروعك (بنفس تنسيق `.gitignore`) خصيصًا لأدوات المحرر. سيقوم VS Code بمراعاته إذا كان `search.useIgnoreFiles` مفعلًا.

---

### 4. الاستبعاد السريع في واجهة مستخدم البحث

عندما تبحث، مرر مؤشر الفأرة فوق ملف/مجلد في النتائج وانقر على **"Exclude"**. هذا يضيف ذلك المسار مؤقتًا إلى `search.exclude` في الإعدادات.

---

👉 إذن الإعداد الأكثر شيوعًا هو:

* إضافة عمليات تجاهل عامة على مستوى المشروع في `.gitignore`.
* إضافة عمليات تجاهل خاصة بالمحرر (مثل `dist`, `.vscode`) في `search.exclude`.

---

هل تريد مني أن أوضح لك كيفية **الاستبعاد أثناء البحث فقط** مع الإبقاء على تلك الملفات في Explorer، أو إخفائها أيضًا من اللوحة الجانبية لـ Explorer؟