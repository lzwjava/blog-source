---
audio: false
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: نماذج اللغة الكبيرة والاستدلال البرمجي واستخدام الأدوات
translated: true
type: note
---

سؤال رائع! أنت تسأل بشكل أساسي عن كيفية تعامل نموذج مثل **Claude من Anthropic (ونماذج LLM المشابهة)** داخليًا مع:

1.  **تنفيذ الكود أو التفكير المتعلق بالكود**
2.  **التحقق من مخطط JSON / التحقق من صحة المخرجات المنظمة**
3.  **استخدام الأدوات والتوجيه بينها**

دعني أوضح ذلك بالتفصيل.

---

### 1. التفكير في الكود وتنفيذه
نماذج مثل Claude (وغيرها من نماذج LLM المتطورة) لا *تنفذ* الكود مباشرة داخل النموذج — بل **تحاكي التفكير في الكود** من خلال توقع الرموز المميزة (tokens) للكود أو خطوات المنطق.

- في **وضع النص العادي**، يمكن لـ Claude "كتابة" الكود من خلال استكمال نمط معين، لكنه لا يقوم بتشغيله داخليًا.
- عند استخدامه مع **نظام خلفي (مثل Workbench من Anthropic، أو Code Interpreter من OpenAI)**، يمكن للنظام التقاط مقاطع الكود هذه ثم *تشغيلها* فعليًا في بيئة معزولة.
- يمكن بعد ذلك إعادة نتائج التنفيذ إلى النموذج كسياق، حتى يتمكن من ربط التفكير على النحو التالي:
  → إنشاء الكود → التنفيذ خارجيًا → استلام الناتج → تحديد الخطوة التالية.

إذن الذكاء يكمن في الحلقة: النموذج يولد → المنفذ الخارجي ينفذ → النموذج يفسر النتائج.

---

### 2. التحقق من مخطط JSON
تدعم العديد من واجهات برمجة التطبيقات (مثل Claude، OpenAI، إلخ) الآن **وضع المخرجات المنظمة**، حيث يمكنك إرفاق مخطط JSON. إليك كيفية عمل ذلك داخليًا:

- تقوم بتمرير **تعريف مخطط JSON** مع طلبك.
- يتم توجيه النموذج لإخراج **JSON صالح فقط** يتوافق مع المخطط.
- إذا حاول إخراج JSON غير صالح، يكتشف النظام ذلك (باستخدام أداة تحقق مثل [jsonschema](https://pypi.org/project/jsonschema/)) ويطلب من النموذج "إصلاح" المخرجات.
- داخليًا، غالبًا ما تكون هذه آلية **فك ترميز مقيدة** أو **فك ترميز قائم على القواعد النحوية** — أي تقييد توليد الرموز المميزة بحيث يُسمح فقط **بـ "الرموز المميزة التالية الصالحة في المخطط"**.

يضمن ذلك أن المخرجات هي JSON صحيح من الناحية النحوية والدلالية، وهو أمر بالغ الأهمية لأتمتة استخدام الأدوات.

---

### 3. استخدام الأدوات والتوجيه
يتم التعامل مع استخدام الأدوات من خلال شيء يشبه **طبقة موجه** بين النموذج وبيئة تنفيذ الأداة:

- **منطق التوجيه**: يتم تحفيز النموذج لـ *اتخاذ قرار بشأي أداة يجب استدعاؤها* (أو الرد مباشرة).
- **فرض المخطط**: لكل أداة مخطط إدخال. يُطلب من النموذج إنتاج JSON صالح لتلك الأداة.
- **محولات التنفيذ**: تأخذ طبقة التنظيم (خارج النموذج) الـ JSON من النموذج، وتحقق من صحته مقابل المخطط، وتنفذ الأداة، ثم تعيد تغذية النتائج كسياق.
- **التفكير المتسلسل (نمط ReAct)**: النموذج "يفكر بصوت عالٍ" (قد ينتج خطوات تفكير مخفية عن المستخدم)، ثم يصدر استدعاءات للأدوات عند الضرورة، ليصل تدريجيًا إلى إجابة نهائية.

على سبيل المثال:
```json
{
  "tool": "web_search",
  "query": "latest research on quantum error correction"
}
```
→ يرى الموجه `"tool": "web_search"`، يتحقق من المخطط، يقوم بالبحث على الويب، يضع النتائج مرة أخرى في سياق النموذج، ويستمر النموذج في التفكير.

تدعم Claude من Anthropic، وGPTs من OpenAI، وLangChain، وغيرها، هذا النمط.

---

### 🔑 الملخص
- **كود Claude**: لا يشغل الكود داخليًا؛ يولد الكود → البيئة المعزولة تنفذ → تُعاد النتائج → تفكير تكرارى.
- **التحقق من مخطط JSON**: يستخدم فك ترميز مقيد / التحقق من صحة المخطط لضمان مخرجات منظمة صالحة.
- **توجيه استخدام الأدوات**: يختار النموذج استدعاءات الأدوات في JSON، محرك التنظيم يتحقق من صحتها وينفذها، ويغذي النتائج مرة أخرى في الحلقة.

---

👉 هل تريد مني **رسم مخطط** يوضح تدفق العمل (النموذج ↔ موجه الأدوات ↔ مدقق المخطط ↔ محرك التنفيذ) ليصورة أكثر وضوحًا بصريًا؟