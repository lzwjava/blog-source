---
audio: false
generated: true
image: false
lang: ar
layout: post
model: x-ai/grok-4-fast
title: إصلاح أخطاء ربط التعريفات المتعددة
translated: true
type: note
---

هذا خطأ **ربط متعدد التعريفات** — خطأ كلاسيكي في لغة C عندما **يتم تعريف المتغيرات العامة في ملف رأس (`variables.h`)** بدلاً من مجرد *الإعلان* عنها فيها.

دعنا نفصل ما يحدث وكيفية إصلاحه.

---

### 🔍 المشكلة

يقول خطأك:

```
multiple definition of `curl' ...
multiple definition of `nec' ...
multiple definition of `contador' ...
multiple definition of `descargadas' ...
```

هذا يعني أن ملف الرأس `variables.h` يبدو على الأرجح هكذا:

```c
// variables.h (مشكلة)
#include <curl/curl.h>

CURL *curl;
int nec;
int contador;
int descargadas;
```

كل ملف `.c` يقوم بتضمين `variables.h` يحصل على **نسخته الخاصة** من تلك المتغيرات.
في وقت الربط، يجد الرابط *تعريفات متعددة* لنفس الرموز العامة، واحدة من كل ملف كائن.

---

### ✅ النمط الصحيح

يجب **الإعلان** عن المتغيرات العامة في ملف الرأس، و**تعريفها** مرة واحدة فقط في ملف `.c` واحد.

#### الخطوة 1: إصلاح `variables.h`

غيّره إلى:

```c
// variables.h
#ifndef VARIABLES_H
#define VARIABLES_H

#include <curl/curl.h>

extern CURL *curl;
extern int nec;
extern int contador;
extern int descargadas;

#endif
```

#### الخطوة 2: تعريفها مرة واحدة (مثلاً، في `variables.c`)

أنشئ أو حدّث `variables.c`:

```c
#include "variables.h"

CURL *curl = NULL;
int nec = 0;
int contador = 0;
int descargadas = 0;
```

#### الخطوة 3: إعادة البناء

نظّف وأعد البناء:

```bash
make clean
make
```

---

### 🧠 لماذا هذا الحل يعمل

* `extern` تخبر المترجم أن المتغير *موجود في مكان آخر*.
* ملف `.c` واحد فقط هو من يقوم بتعريفه فعلياً (يخصص ذاكرة له).
* عندها يصبح لدى الرابط تعريف واحد ومراجع متعددة → لا يوجد تعارض.

---

إذا كنت ترغب، يمكنني أن أعرض لك مثالاً مصغراً للكود قبل وبعد لتوضيح هذا أكثر. هل تريد ذلك؟