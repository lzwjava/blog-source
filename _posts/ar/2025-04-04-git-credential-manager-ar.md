---
audio: false
generated: true
lang: ar
layout: post
title: دفع Git باستخدام مدير بيانات اعتماد Git
translated: true
type: note
---

**العنوان: تنفيذ دفع Git بسلاسة باستخدام مدير بيانات اعتماد Git وتكوين الوكيل**

**المقدمة:**

يجب أن تكون عملية دفع تغييرات Git المحلية إلى مستودع بعيد، مثل GitHub، عملية سلسة. ومع ذلك، يمكن لإعدادات الشبكة، خاصة عند استخدام الوكلاء، أن تعقد الأمور في بعض الأحيان. في هذه المشاركة، سأشارك تجربتي في دفع التغييرات بنجاح إلى مستودعي `lzwjava.github.io`، مع التركيز على دور مدير بيانات اعتماد Git (GCM) وإعدادات الوكيل الصحيحة.

**السيناريو:**

كنت بحاجة إلى دفع تحديثات إلى مستودعي `lzwjava.github.io` على GitHub. كان نظامي مهيأ لاستخدام خادم وكيل، مما تسبب في البداية في مشاكل في المصادقة.

**الخطوات المتخذة:**

1.  **التحقق من إعدادات الوكيل:**

    *   قمت أولاً بتأكيد إعدادات الوكيل الخاصة بي باستخدام أمر `git credential-manager`. ساعدني هذا الأمر في عرض إعدادات الوكيل الحالية لـ HTTP و HTTPS:

    ```bash
    git credential-manager
    ```

    *   أظهر الناتج:

    ```
    🚀 **تم اكتشاف إعدادات الوكيل:**
      - HTTP_PROXY: http://127.0.0.1:7890
      - HTTPS_PROXY: http://127.0.0.1:7890
    ```

    *   هذا أكد أنه تم اكتشاف إعدادات الوكيل الخاصة بي بشكل صحيح.

2.  **تسجيل الدخول إلى GitHub باستخدام GCM:**

    *   للتأكد من أن Git لديه بيانات الاعتماد الصحيحة، استخدمت GCM لتسجيل الدخول إلى حسابي على GitHub:

    ```bash
    git credential-manager github login
    ```

    *   فتح هذا الأمر نافذة متصفح، مطالبًا إياي بالمصادقة مع GitHub. بعد المصادقة الناجحة، قام GCM بتخزين بيانات اعتمادي بشكل آمن.

3.  **التحقق من حساب GitHub:**

    *   للتأكد من تسجيل دخول حسابي على github بشكل صحيح، قمت بتنفيذ الأمر التالي.

    ```bash
    git credential-manager github list
    ```

    *   أظهر هذا الأمر اسم حسابي على github.

4.  **تعيين عنوان URL البعيد:**

    *   ثم قمت بالتحقق من عنوان URL البعيد لمستودعي وتعيينه:

    ```bash
    git remote set-url origin https://github.com/lzwjava/lzwjava.github.io.git
    ```

5.  **دفع التغييرات:**

    *   أخيرًا، دفعت تغييراتي المحلية إلى المستودع البعيد:

    ```bash
    git push
    ```

    *   نجح أمر `git push` في تحميل تغييراتي.

6.  **تنبيه أمان GitHub:**

    *   بعد الدفع، عرض GitHub تنبيهًا أمنيًا بشأن الثغرات الأمنية في تبعيات مستودعي. هذا تذكير مهم للتحقق بانتظام من المشكلات الأمنية ومعالجتها:

    ```
    remote: وجد GitHub 3 ثغرات أمنية على الفرع الافتراضي لـ lzwjava/lzwjava.github.io (2 عالية، 1 منخفضة). لمعرفة المزيد، تفضل بزيارة:
    remote:    https://github.com/lzwjava/lzwjava.github.io/security/dependabot
    ```

**الاستنتاجات الرئيسية:**

*   **مدير بيانات اعتماد Git (GCM):** يبسط GCM مصادقة Git من خلال تخزين بيانات الاعتماد بشكل آمن ومعالجة سير عمل المصادقة.
*   **تكوين الوكيل:** عند العمل خلف وكيل، تأكد من تكوين بيئة Git الخاصة بك بشكل صحيح لاستخدام الوكيل. قدرة GCM على اكتشاف واستخدام إعدادات الوكيل لا تقدر بثمن.
*   **تنبيهات الأمان:** انتبه جيدًا لتنبيهات الأمان من GitHub للحفاظ على سلامة مستودعاتك.
*   **أدوات سطر الأوامر:** يعد سطر الأوامر أداة قوية لإدارة مستودعات git.

**الخاتمة:**

باستخدام مدير بيانات اعتماد Git وتكوين إعدادات الوكيل الخاصة بي بشكل صحيح، تمكنت من دفع تغييراتي إلى GitHub بنجاح. تسلط هذه التجربة الضوء على أهمية فهم وإدارة مصادقة Git وتكوينات الشبكة. آمل أن يساعدك هذا الدليل في تبسيط سير عمل Git الخاص بك.