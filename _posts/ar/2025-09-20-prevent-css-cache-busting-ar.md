---
audio: false
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: منع إبطال ذاكرة التخزين المؤقت لـ Jekyll CSS
translated: true
type: note
---

ما تراه هو Jekyll (من خلال السمة أو خط المعالجة `jekyll-assets`/`jekyll-feed`/`minima`) يضيف تلقائيًا **بصمة (تجزئة)** إلى عنوان URL لورقة الأنماط الخاصة بك من أجل كسر ذاكرة التخزين المؤقت. هذا هو السبب في أن ملف CSS نفسه ينتهي به المطاف بسلاسل استعلام مختلفة `?...v=` عند إعادة البناء.

إذا كنت لا تريد أن يغير Jekyll سلسلة الاستعلام تلك في كل عملية بناء، يمكنك تعطيل أو تجاوز سلوك "كسر ذاكرة التخزين المؤقت"/"التجزئة":

### الخيار 1: استخدام رابط ثابت

بدلاً من وسم المساعدة في Jekyll (مثل `{{ "/assets/css/style.css" | relative_url }}` أو `{{ "/assets/css/style.css" | asset_path }}`)، يمكنك كتابته يدويًا:

```html
<link rel="stylesheet" href="/assets/css/style.css">
```

بهذه الطريقة، لا يتم حقن أي بصمة.

### الخيار 2: تعطيل تجزئة الأصول

إذا كنت تستخدم **jekyll-assets** أو سمة تقوم بتجزئة الملفات، يمكنك تعطيل التجزئة في ملف `_config.yml`:

```yml
assets:
  digest: false
```

### الخيار 3: استخدام `relative_url` بدون تجزئة

إذا كانت سمتك تستخدم شيئًا مثل:

```liquid
<link rel="stylesheet" href="{{ '/assets/css/style.css' | relative_url }}">
```

تأكد من أنك لا تمرره عبر `asset_path` أو `absolute_url` مع تمكين التجزئة. استخدم فقط `relative_url`.

---

👉 باختصار:

* إذا كنت تريد أن **لا يتغير أبدًا**، فاكتب المسار يدويًا.
* إذا كنت تريد التحكم فيه عبر الإعدادات، اضبط `assets.digest: false`.
* إذا كنت لا تزال تريد إدارة الإصدار ولكن مع الحفاظ على ثباته، فستحتاج إلى إدارة `?...v=` الخاص بك يدويًا.

هل تريد أن يكون **ثابتًا تمامًا** (لا يتغير أبدًا)، أم أنك تريد فقط منع Jekyll من **إنشاء تجزئات جديدة في كل عملية بناء**؟