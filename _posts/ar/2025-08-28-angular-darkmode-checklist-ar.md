---
audio: false
generated: true
image: false
lang: ar
layout: post
title: قائمة مراجعة عنصر الوضع المظلم في أنغولار
translated: true
type: note
---

عند تنفيذ الوضع الداكن في واجهة Angular الأمامية، عادةً ما تبدأ بـ `body` و `select` و `option`، ولكن هناك عدد لا بأس به من عناصر HTML الشائعة الأخرى التي سترغب في إعادة تنسيقها ليشعر الثيم بالاتساق. إليك قائمة مراجعة عملية:

### مستوى الصفحة والحاويات

* `html`, `body` → لون الخلفية والنص.
* `div`, `section`, `article`, `aside`, `header`, `footer`, `main`, `nav` → تعيين ألوان الخلفية والنص (خاصة إذا كانت تحتوي على خلفيات بيضاء افتراضية).

### النص والعناوين

* `h1, h2, h3, h4, h5, h6` → تأكد من أن النص مرئي مقابل الخلفية الداكنة.
* `p`, `span`, `a` → اضبط لون النص، وألوان التمرير على الروابط، وحالات الروابط التي تمت زيارتها.

### النماذج والحقول

* `input[type="text"], input[type="password"], input[type="email"], textarea` → الخلفية، ولون النص، والإطار.
* `input[type="checkbox"], input[type="radio"]` → تحتاج أحيانًا إلى معالجة خاصة للرؤية.
* `select, option` → كما ذكرت، كل من لون الخلفية والنص.
* `button` → الخلفية، والإطار، ولون النص، بالإضافة إلى حالات التمرير/النشط.
* `label` → تأكد من تباين النص مع الخلفية الداكنة.
* `fieldset, legend` → الخلفية والإطار.

### القوائم والجداول

* `ul, ol, li` → لون النص.
* `table` → لون الخلفية، ولون الإطار، ولون النص.
* `thead, tbody, tr, th, td` → تعديلات فردية للخلفيات والإطارات.

### العناصر التفاعلية

* `details, summary` → الخلفية والنص.
* `dialog` → الخلفية والنص.
* `mark` → يجب أن تعمل ألوان التمييز على الخلفية الداكنة (قد يكون اللون الأصفر الافتراضي ساطعًا جدًا).

### الوسائط والمتنوع

* `hr` → لون الإطار (غالبًا ما يكون الافتراضي فاتحًا جدًا على الداكن).
* `code, pre` → لون الخلفية والنص لسهولة القراءة.
* `blockquote` → لون الخلفية والإطار.
* `progress, meter` → ألوان المسار والقيمة.

---

💡 **أفضل ممارسة**: بدلاً من استخدام `!important` في كل مكان، عرّف متغيرات CSS للألوان (`--bg-color`, `--text-color`، إلخ) وقم بتبديلها عندما يكون الوضع الداكن نشطًا (على سبيل المثال، أضف فئة `.dark-mode` إلى `body`). بهذه الطريقة، يمكن للمكونات والعناصر المخصصة في Angular أيضًا أن تتفاعل مع الثيم بطريقة نظيفة.

هل تريد مني أن أعد **مقتطف CSS أساسي** للوضع الداكن (مع متغيرات) يمكنك إضافته إلى مشروع Angular الخاص بك كنقطة بداية؟