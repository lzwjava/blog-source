---
audio: false
date: 2025-09-12
generated: true
image: false
lang: ar
layout: post
model: mistralai/mistral-medium-3.1
title: دليل البحث في كود سورسغراف
translated: true
type: note
---

سورسجراف هو أداة قوية للبحث في الكود والتنقل فيه تتيح للمطورين البحث عبر مستودعات متعددة، وفهم تبعيات الكود، والتنقل في قواعد الكود الكبيرة بكفاءة. يغطي هذا الدليل **بناء جملة البحث، وأفضل الممارسات، والبحث الخاص باللغات (جافا وبايثون)**.

---

## **1. بناء جملة البحث الأساسي**
يدعم سورسجراف **البsearch الحرفي، والعادي (regex)، والهيكلي** مع عوامل التصفية.

### **1.1. البحث الحرفي**
ابحث عن النص المحدد:
```
"def calculate_sum"
```

### **1.2. البحث العادي (Regex)**
استخدم `/.../` للبحث العادي:
```
/def \w+_sum\(/
```

### **1.3. البحث الهيكلي (نسخة تجريبية)**
ابحث عن أنماط الكود (مثل تعريفات الدوال):
```
type:func def calculate_sum
```

### **1.4. عوامل التصفية**
صقل نتائج البحث باستخدام عوامل التصفية:
- `repo:` – البحث في مستودع محدد
  ```
  repo:github.com/elastic/elasticsearch "def search"
  ```
- `file:` – البحث في ملفات محددة
  ```
  file:src/main/java "public class"
  ```
- `lang:` – البحث بلغة محددة
  ```
  lang:python "def test_"
  ```
- `type:` – البحث عن الرموز (الدوال، الفئات، إلخ)
  ```
  type:func lang:go "func main"
  ```

---

## **2. تقنيات البحث المتقدمة**
### **2.1. العوامل المنطقية**
- `AND` (الإعداد الافتراضي): `def calculate AND sum`
- `OR`: `def calculate OR def sum`
- `NOT`: `def calculate NOT def subtract`

### **2.2. الأحرف البديلة (Wildcards)**
- `*` – يطابق أي تسلسل من الأحرف
  ```
  "def calculate_*"
  ```
- `?` – يطابق حرفًا واحدًا
  ```
  "def calculate_?"
  ```

### **2.3. الحساسية لحالة الأحرف**
- البحث غير حساس لحالة الأحرف افتراضيًا
- فرض الحساسية لحالة الأحرف باستخدام `case:yes`
  ```
  case:yes "Def Calculate"
  ```

### **2.4. البحث في التعليقات**
استخدم `patternType:literal` للبحث في التعليقات:
```
patternType:literal "// TODO:"
```

---

## **3. البحث في كود الجافا**
### **3.1. البحث عن الفئات**
```
type:symbol lang:java "public class"
```
### **3.2. البحث عن الدوال (الطرق)**
```
type:func lang:java "public void"
```
### **3.3. البحث عن التعليقات التوضيحية (Annotations)**
```
lang:java "@Override"
```
### **3.4. البحث عن عبارات الاستيراد**
```
lang:java "import org.springframework"
```
### **3.5. البحث عن معالجة الاستثناءات**
```
lang:java "try {" AND "catch (Exception"
```

---

## **4. البحث في كود البايثون**
### **4.1. البحث عن الدوال**
```
type:func lang:python "def calculate"
```
### **4.2. البحث عن الفئات**
```
type:symbol lang:python "class Calculator"
```
### **4.3. البحث عن عبارات الاستيراد**
```
lang:python "import pandas"
```
### **4.4. البحث عن الديكورات (Decorators)**
```
lang:python "@app.route"
```
### **4.5. البحث عن توثيق الدوال (Docstrings)**
```
lang:python '"""'
```

---

## **5. أفضل الممارسات**
1. **استخدم `lang:`** لتضييق نطاق النتائج.
2. **اجمع بين عوامل التصفية** (`repo:`, `file:`, `type:`) للحصول على دقة أعلى.
3. **استخدم البحث العادي (regex)** لمطابقة الأنماط المرنة.
4. **احفظ عمليات البحث** للاستعلامات المتكررة.
5. **استخدم `case:yes`** عند الحاجة.

---

## **6. أمثلة على استعلامات البحث**

| **الهدف** | **الاستعلام** |
|----------|-----------|
| البحث عن دوال `main` في الجافا | `lang:java "public static void main"` |
| البحث عن دوال الاختبار في البايثون | `lang:python "def test_"` |
| البحث عن تعليقات TODO في لغة Go | `lang:go "// TODO:"` |
| البحث عن وحدات تحكم Spring Boot | `lang:java "@RestController"` |
| البحث عن جميع عبارات `if` في JavaScript | `lang:javascript "if ("` |

---

## **7. الخلاصة**
بحث سورسجراف **قوي ومرن**، حيث يدعم **البsearch الحرفي، والعادي، والهيكلي** عبر لغات متعددة. من خلال إتقان استخدام عوامل التصفية (`lang:`, `repo:`, `type:`)، يمكنك التنقل بكفاءة في قواعد الكود الكبيرة.

### **الخطوات التالية**
- جرب **عمليات البحث المحفوظة** للاستعلامات المتكررة.
- استكشف **الذكاء البرمجي للكود** (تلميحات التمرير، التنقل عبر المستودعات).
- استخدم **امتدادات سورسجراف** (VS Code، JetBrains).

هل ترغب في الحصول على شرح أعمق لأي ميزة محددة؟ 🚀