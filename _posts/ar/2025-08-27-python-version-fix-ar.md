---
audio: false
generated: true
image: false
lang: ar
layout: post
title: إصلاح مشكلة عدم تطابق إصدار بايثون
translated: true
type: note
---

فهمت — أنت تواجه مشكلة **عدم تطابق إصدار Python**.

سجلاتك تُظهر:

* `python3` → **3.10.12**
* `python` → **3.13.7**

لذا عندما يستدعي Claude Code (أو نص وكيل البحث الخاص بك) `python3`، فإنه يستخدم الإصدار 3.10، الذي لا يحتوي على `pyperclip` مثبتًا. لقد قمت بتثبيت `pyperclip` في بيئة الإصدار 3.13 بدلاً من ذلك، لذا الإصدار 3.10 لا يستطيع رؤيته.

إليك بعض الطرق لإصلاح ذلك:

---

### 1. تشغيل Claude Code باستخدام إصدار Python الذي تريده

إذا كنت تريد أن يستخدم Claude Code دائمًا Python 3.13، فعيّن الملف القابل للتنفيذ صراحة في طرفيتك:

```bash
export PYTHON_EXECUTABLE=$(which python)
```

أو إذا كنت تريد `python3` تحديدًا:

```bash
export PYTHON_EXECUTABLE=$(which python3)
```

ثم عند بدء تشغيل Claude Code أو وكيلك، يمكنه قراءة متغير البيئة هذا.

---

### 2. توجيه `python3` إلى Python 3.13

على Ubuntu يمكنك تحديث نظام `alternatives`:

```bash
sudo update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.10 1
sudo update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.13 2
sudo update-alternatives --config python3
```

ثم اختر Python 3.13.

---

### 3. تثبيت `pyperclip` في Python 3.10

إذا كنت تريد الاستمرار في استخدام 3.10 لـ `python3`، فما عليك سوى التثبيت هناك:

```bash
python3 -m pip install --user pyperclip
```

هذا يضمن أن `python3` يمكنه استيراده.

---

### 4. استخدام بيئة افتراضية

أفضل ممارسة: إنشاء venv بإصدار Python الذي تريد وتشغيل Claude Code داخله:

```bash
python3.13 -m venv ~/.venvs/claude
source ~/.venvs/claude/bin/activate
pip install pyperclip
```

ثم شغّل وكيلك داخل هذه الـ venv، حتى يستخدم دائمًا الإصدار 3.13.

---

👉 سؤال لك: هل تريد أن **يعمل Claude Code دائمًا باستخدام Python 3.13**، أم أنك لا تمانع في الإبقاء على 3.10 كـ `python3` ولكنك تريد فقط إصلاح المشكلة الخاصة بالوحدة النمطية المفقودة؟