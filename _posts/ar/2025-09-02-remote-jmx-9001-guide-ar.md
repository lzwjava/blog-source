---
audio: false
generated: true
image: false
lang: ar
layout: post
title: دليل المنفذ البعيد لجافا للإدارة والمراقبة 9001
translated: true
type: note
---

**وكيل JMX البعيد** هو جزء من امتدادات إدارة جافا (JMX). يسمح لك بمراقبة وإدارة تطبيق جافا قيد التشغيل عن بُعد. عند تمكين الإدارة البعيدة لـ JMX، تقوم آلة جافا الافتراضية (JVM) بتشغيل وكيل يعرض حبوبًا مُدارة (MBeans) عبر منفذ TCP. يمكن لأدوات مثل **jconsole** أو **VisualVM** أو برامج المراقبة المخصصة الاتصال بهذا المنفذ لفحص استخدام الذاكرة، وحالات الخيوط (threads)، ونشاط جامع القمامة (GC)، وحتى استدعاء العمليات.

حول **المنفذ 9001**:

* رقم المنفذ الفعلي يعتمد على كيفية تكوين التطبيق. عادةً ما تشاهد أرقامًا مثل 9001 أو 9090 أو 9091، إلخ.
* إذا رأيت شيئًا يستمع على المنفذ `9001` وكان **وكيل JMX بعيدًا**، فهذا يعني أن عملية جافا قد بدأت بخصائص نظام مثل:

  ```bash
  -Dcom.sun.management.jmxremote
  -Dcom.sun.management.jmxremote.port=9001
  -Dcom.sun.management.jmxremote.authenticate=false
  -Dcom.sun.management.jmxremote.ssl=false
  ```

يفتح هذا JMX على المنفذ `9001` بدون مصادقة/SSL (غالبًا ما يكون للاستخدام الداخلي أو أثناء التطوير فقط، لأنه غير آمن إذا تم تعريضه للعامة).

---

### كيفية التحقق من البرنامج الذي يستخدم منفذ JMX 9001 على Windows

1. افتح **موجه الأوامر (كمسؤول)**.
2. قم بتشغيل الأمر:

   ```cmd
   netstat -ano | findstr :9001
   ```

   سيعرض هذا معرف العملية (PID) المرتبطة بهذا المنفذ.
3. لتحديد البرنامج:

   ```cmd
   tasklist /FI "PID eq <PID>"
   ```

   استبدل `<PID>` بالرقم من الخطوة 2.

---

### كيفية إيقافه / إنهاؤه قسريًا

* إذا تأكدت أنها عملية جافا لا تريدها، يمكنك إنهاؤها باستخدام:

  ```cmd
  taskkill /PID <PID> /F
  ```
* ولكن إذا كانت جزءًا من خادم التطبيقات الخاص بك (مثل Tomcat أو WebSphere أو تطبيق Spring Boot، إلخ)، فمن الأفضل إيقافها من خلال خدمتها أو واجهة التحكم الخاصة بها بدلاً من إنهاء العملية قسريًا، حتى يتم إغلاقها بشكل نظيف.

---

👉 هل تريد مني أيضًا أن أوضح لك كيفية **تعطيل وكيل JMX البعيد** في تطبيق جافا حتى لا يفتح المنفذ 9001 من الأساس؟