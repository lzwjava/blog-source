---
audio: false
generated: true
image: false
lang: ar
layout: post
title: دليل مُنسِّق كود بايثون الأسود
translated: true
type: note
---

حزمة `black` على PyPI هي **منسق أكواد بايثون**. تقوم تلقائيًا بإعادة تنسيق كود البايثون الخاص بك لاتباع قواعد نمط متسقة (متوافقة مع PEP 8، ولكن بآراء صارمة خاصة بها). إليك كيفية استخدامها:

---

### 1. تثبيت Black

```bash
pip install black
```

إذا كنت تريد التأكد من حصولك دائمًا على أحدث إصدار مستقر:

```bash
pip install -U black
```

---

### 2. تنسيق ملف أو مجلد

شغّل Black على ملف:

```bash
black my_script.py
```

شغّل Black على مجلد كامل (بشكل متكرر):

```bash
black src/
```

بشكل افتراضي، سيقوم الكتابة فوق الملفات في مكانها بالكود المنسق.

---

### 3. التحقق من التنسيق دون إجراء تغييرات

استخدم `--check` لمعرفة ما إذا كانت ملفاتك تحتاج إلى إعادة تنسيق:

```bash
black --check my_script.py
```

هذا مفيد في خطوط أنابيب التكامل المستمر (CI)، لضمان اتساق النمط.

---

### 4. الخيارات الشائعة

* `--line-length 88` → تغيير طول السطر (الافتراضي: 88).
* `--diff` → عرض ما سيتغير، دون تعديل الملف.
* `--skip-string-normalization` → عدم تغيير علامات الاقتباس في النصوص.
* `--exclude` → تخطي ملفات أو مجلدات معينة.

مثال:

```bash
black --line-length 100 --diff src/
```

---

### 5. التكامل

* **دعم بيئات التطوير المتكاملة**: Black متاح كإضافة في PyCharm، VS Code، Vim، Emacs، إلخ.
* **خطاف Pre-commit**: أضف Black إلى ملف `.pre-commit-config.yaml` ليتم تنسيق الكود قبل عمليات الالتزام:

  ```yaml
  repos:
    - repo: https://github.com/psf/black
      rev: stable
      hooks:
        - id: black
  ```
* **التكامل المستمر/التوزيع المستمر**: أضف `black --check .` في خط أنابيب التكامل المستمر الخاص بك.

---

👉 هل تريد مني أيضًا أن أوضح كيفية إعداد **التنسيق التلقائي في VSCode أو PyCharm**، حتى لا تحتاج إلى تشغيل `black` يدويًا؟