---
audio: false
generated: true
image: false
lang: ar
layout: post
model: openai/gpt-5
title: دليل تثبيت المتصفح على أوبونتو 24.04
translated: true
type: note
---

بالإضافة إلى **Firefox** و **Chromium**، هناك عدد لا بأس به من المتصفحات الجيدة التي يمكنك تثبيتها على **Ubuntu 24.04** — اعتمادًا على أولوياتك (السرعة، الخصوصية، التوافق، استهلاك الموارد، إلخ). إليك قائمة مصنفة مع ملاحظات:

---

### 🧠 المتصفحات السائدة

**1. Google Chrome**

* مبني على Chromium ولكنه يتضمن مزامنة Google والبرامج المساعدة للمحتوى والميزات الاحتكارية.
* توافق رائع مع تطبيقات الويب (مثل Google Docs و Meet وغيرها).
* ثبّت باستخدام `.deb` من الموقع الرسمي لـ Google.

**2. Microsoft Edge (إصدار Linux)**

* مبني على Chromium، واجهة مستخدم مصقولة، يتكامل مع خدمات Microsoft.
* غالبًا ما يكون أسرع من Chrome في إدارة الذاكرة.
* حزمة `.deb` من موقع Microsoft.

---

### 🔒 متصفحات تركز على الخصوصية

**3. Brave**

* مبني على Chromium ولكنه مزود بمانع إعلانات مدمج، وحظر المتعقبات، ووضع نافذة Tor.
* يحافظ على توافق إضافات Chrome.
* التثبيت:

  ```bash
  sudo apt install apt-transport-https curl
  sudo curl -fsSLo /usr/share/keyrings/brave-browser-archive-keyring.gpg https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
  echo "deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main" | sudo tee /etc/apt/sources.list.d/brave-browser-release.list
  sudo apt update
  sudo apt install brave-browser
  ```

**4. Vivaldi**

* مبني أيضًا على Chromium، ولكنه قابل للتخصيص بدرجة كبيرة (مثل تجانب علامات التبويب، اختصارات لوحة المفاتيح، أدوات الشريط الجانبي).
* أكثر ثراءً بالميزات من Chrome أو Edge.
* حمل `.deb` من [vivaldi.com](https://vivaldi.com).

**5. Tor Browser**

* يركز على إخفاء الهوية وتجاوز الرقابة.
* مبني على Firefox ESR، يقوم بتوجيه الحركة عبر شبكة Tor.
* ثبّت عبر:

  ```bash
  sudo apt install torbrowser-launcher
  ```

---

### 🪶 متصفحات خفيفة الوزن

**6. Falkon (Qt WebEngine)**

* خفيف الوزن، واجهة مستخدم بسيطة، يتكامل بشكل جيد مع أنظمة KDE/Qt.
* التثبيت:

  ```bash
  sudo apt install falkon
  ```

**7. Midori**

* مشروع قديم تم إحياؤه من أجل البساطة واستخدام الموارد المنخفض.
* جيد للتصفح البسيط، وليس مثاليًا لتطبيقات الويب الثقيلة.
* التثبيت:

  ```bash
  sudo apt install midori
  ```

**8. Epiphany (GNOME Web)**

* متصفح GNOME الافتراضي، مبني على WebKit (وليس Chromium).
* بسيط، خفيف الوزن، يناسب بيئة GNOME بشكل مثالي.
* التثبيت:

  ```bash
  sudo apt install epiphany-browser
  ```

---

### 🧪 متصفحات المطورين / المتخصصة

**9. Ungoogled Chromium**

* إصدار من Chromium تمت إزالة جميع تعقبات Google وتبعيات واجهات برمجة التطبيقات (API) الخاصة بها منه.
* إعداد يدوي ولكنه جيد للخصوصية الصارمة.
* غالبًا ما يوجد عبر حزم `.deb` مجتمعية أو Flatpak.

**10. Floorp**

* فرع حديث من Firefox مع تحسينات في الخصوصية والتخصيص.
* صنعه مطورون يابانيون؛ أصبح شائعًا جدًا.
* Flatpak متوفر من Flathub.

---

هل تريد أن أوصي بمجموعة (مثل *واحد يركز على الخصوصية، وواحد خفيف الوزن، وواحد كامل الميزات*) تناسب سير عملك على إعداد Ubuntu هذا؟