---
audio: false
generated: true
image: false
lang: ar
layout: post
title: دليل شهادات SSL في بايثون
translated: true
type: note
---

حزمة **`certifi`** على PyPI هي مكتبة صغيرة توفر **شهادات الجذر الموثوقة من موزيلا** في حزمة. تُستخدم عادةً من قبل مكتبات بايثون مثل `requests`، `urllib3`، أو أي عميل HTTPS يحتاج إلى التحقق من صحة شهادات SSL/TLS.

إليك كيفية استخدامها:

---

### 1. تثبيت `certifi`

```bash
pip install certifi
```

---

### 2. الحصول على مسار حزمة CA

في بايثون:

```python
import certifi

print(certifi.where())
```

سيؤدي هذا إلى طباعة المسار الكامل لحزمة شهادات CA (ملف `.pem`). على سبيل المثال:

```
/usr/local/lib/python3.11/site-packages/certifi/cacert.pem
```

---

### 3. الاستخدام مع `requests` أو `urllib3`

```python
import requests
import certifi

response = requests.get("https://example.com", verify=certifi.where())
print(response.status_code)
```

افتراضيًا، تستخدم `requests` حزمة `certifi` داخليًا، لكن تمرير `verify=certifi.where()` بشكل صريح يضمن استخدامها للحزمة المحدثة.

---

### 4. الاستخدام في مكتبات HTTPS أخرى

إذا كنت تعمل مع `urllib3` مباشرة:

```python
import urllib3
import certifi

http = urllib3.PoolManager(
    cert_reqs="CERT_REQUIRED",
    ca_certs=certifi.where()
)

response = http.request("GET", "https://example.com")
print(response.status)
```

---

### 5. الاستخدام على مستوى النظام (اختياري)

في بعض الأحيان، قد ترغب في توجيه تطبيق لاستخدام حزمة CA الخاصة بـ `certifi`:

```bash
export SSL_CERT_FILE=$(python -m certifi)
```

---

👉 باختصار:

* قم بالتثبيت باستخدام `pip install certifi`
* استخدم `certifi.where()` للحصول على مسار حزمة الشهادات
* قم بتمريرها إلى المكتبات التي تحتاج إلى التحقق من صحة الشهادات

---

هل تريد مني أيضًا أن أوضح لك **كيفية جعل بيئة بايثون بأكملها تستخدم شهادات `certifi` افتراضيًا**، حتى لا تحتاج إلى تكوين كل مكتبة على حدة؟