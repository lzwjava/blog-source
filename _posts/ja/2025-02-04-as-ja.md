---
audio: false
lang: ja
layout: post
title: macOSでのアセンブリ：ハローワールド
translated: true
---

これはコードで「Hello, World!」プログラムを作成します。システムコールを使ってメッセージをコンソールに出力し、その後終了します。`.text` セクションには、`_start` から始まる実行可能命令が格納されています。まず `sys_write` コール（stdoutへの出力）を設定し、次に `sys_exit` コール（クリーンな終了）を設定します。`.data` セクションにはメッセージ自体が格納され、その長さが計算されます。

```bash
.global _start
.text

_start:
    // Write "Hello, World!" to stdout
    mov x8, #64             // syscall number for write (sys_write)
    mov x0, #1              // file descriptor 1 (stdout)
    ldr x1, =msg            // address of the message
    mov x2, #14             // length of the message
    svc #0                  // invoke syscall

    // Exit the program
    mov x8, #93             // syscall number for exit (sys_exit)
    mov x0, #0              // exit code 0
    svc #0                  // invoke syscall

.data
msg:
    .asciz "Hello, World!\n"
```

```bash
as -o hello.o hello.s

% clang -o hello hello.o -nostdlib -e _start -Wl,-platform_version,macos,15.0,15.0 -arch arm64

% ./hello
zsh: invalid system call  ./hello
```
