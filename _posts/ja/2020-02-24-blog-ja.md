---
audio: false
lang: ja
layout: post
title: lzwjava.github.io
translated: true
---

これは、GitHubプロジェクト [https://github.com/lzwjava/lzwjava.github.io](https://github.com/lzwjava/lzwjava.github.io) のREADME.mdです。

---

# lzwjava.github.io

私の個人ブログへようこそ！ここに、さまざまなトピックについての私の考え、経験、知識を共有します。

## 主要な改善点

このブログは、標準的なJekyllブログ（jekyll-theme-caymanを使用）に比べていくつかの向上点を含んでいます：

*   **AIによる翻訳：** 高度な言語モデルを活用して、正確で文脈に適した翻訳を行い、コンテンツのアクセス性を世界中の人々に広げます。
*   **XeLaTeX PDF 生成：** XeLaTeXを統合して、高品質で印刷準備が整ったPDFを生成し、オフラインで読むためと共有するために使用できます。
*   **Google Cloud Text-to-Speech:** Google CloudのText-to-Speechサービスを利用して、記事のオーディオバージョンを生成し、視覚障害者やオーディオコンテンツを好む人々のアクセス性を向上させます。
*   **CSSスタイルの向上:** 豊富でカスタマイズされたCSSデザインを特徴とし、視覚的に魅力的に、ユーザーフレンドリーな体験を提供します。
*   **MathJax対応:** MathJaxを実装し、複雑な数学式や方程式をレンダリングして、技術的なコンテンツのアクセス性を向上させます。
*   **ナイトモード:** 目の疲れを軽減し、低光条件での読みやすさを向上させるナイトモードオプションを含む。
*   **柔軟な投稿選択:** カテゴリまたはタグでフィルタリングするなど、さまざまな投稿選択オプションを提供して、ナビゲーションを向上させます。
*   **定期的な更新:** ブログのライブラリと依存関係を最新に保ち、最適なパフォーマンスとセキュリティを確保します。
*   **`awesome-cv`統合:** ブログから直接プロフェッショナルな履歴書を生成するために`awesome-cv`を使用します。
*   **RSSフィード対応:** `feed.xml`を通じてRSSフィードを提供し、ユーザーがブログを購読できるようにします。
*   **バイリンガルコンテンツ:** 中国語と英語の両方のコンテンツに対応して、多様なオーディエンスを対象にします。
*   **GitHubワークフロー自動化:** 自動ビルド、テスト、デプロイを行うためにGitHub Actionsを実装し、開発プロセスをスムーズにします。
*   **自動翻訳ワークフロー:** GitHub Actionsを使用して、新しい又は更新された投稿を複数言語に自動的に翻訳します。
*   **EPUB対応:** MarkdownをEPUBに変換して電子書籍リーダーで使用します。

## 手始めに

ローカルのJekyll環境を設定するには、以下の手順を実行してください：

```shell
gem install jekyll bundler

jekyll new myblog

cd myblog

bundle install

bundle exec jekyll serve

bundle exec jekyll serve --draft

bundle add webrick

jekyll serve
```

## Markdownスタイル

構文の強調をカスタマイズするには、`rougify`を使用してください：

```shell
rougify help style

rougify style github > _sass/syntax.css

rougify style gruvbox.dark > _sass/syntax.css

rougify style base16.monokai.dark > _sass/syntax.css
```

## オーディオとPDFパイプライン

オーディオファイルとPDFファイルを生成するには、次のコマンドを使用してください：

```bash
conda activate google-cloud-env

python scripts/audio_pipeline.py --task posts --n 10

python scripts/audio_pipeline.py --task pages

python scripts/audio_pipeline.py --task notes

python scripts/audio_pipeline.py --task notes --n 100

python scripts/pdf_pipeline.py --task pages

python scripts/pdf_pipeline.py --task posts --n 10

python scripts/pdf_pipeline.py --task notes

python scripts/generate_notes_link.py

python scripts/auto-ss-config/upload_configs.py

python scripts/epub_book.py notes

python scripts/epub_book.py _posts/en

python scripts/epub_book.py _posts/ja
```

## スクリプト

新しい下書き投稿を作成するには、以下を使用してください：

```
python scripts/draft.py <name>
```

## コンテンツガイド

*   `-en`: 英語の投稿を指定します。
*   `-zh`: 中国語の投稿を指定します。

 `_posts`と`pages`ディレクトリは、両方の`-en`と`-zh`サフィックスに対応しています。

`notes`ディレクトリは主に個人の学習目的のための英語コンテンツを含み、翻訳は不要です。

## 参考文献

[Jekyllでの構文ハイライト](https://mcpride.github.io/posts/development/2018/03/06/syntax-highlighting-with-jekyll/)