---
audio: false
generated: true
lang: ja
layout: post
title: LinkedHashMapとTreeMap
translated: true
type: note
---

素晴らしい質問です！`LinkedHashMap`と`TreeMap`はどちらもJavaの`Map`インターフェースの実装ですが、**異なる順序付け戦略**を持ち、**異なる基盤データ構造**を使用しています。

### 🌳 `TreeMap`
- **順序付け**: キーの**自然順序**（またはカスタム`Comparator`が提供された場合はそれ）でソートされます
- **データ構造**: **レッドブラックツリー**（自己平衡二分探索木の一種）でバックアップされています
- **パフォーマンス**:
  - get/put/remove操作に`O(log n)`
  - 常にソート順序が維持されます
- **使用例**: **ソートされたキー**が必要な場合

### 🔗 `LinkedHashMap`
- **順序付け**: **挿入順序**（または設定によってはアクセス順序）を維持します
- **データ構造**: **ハッシュテーブル + リンクリスト**でバックアップされています
- **パフォーマンス**:
  - get/put/remove操作に`O(1)`（`HashMap`と同様）
  - リンクリストによるメモリオーバーヘッドが若干あります
- **使用例**: **エントリが追加された順序**が重要な場合

---

### ✅ イテレータの順序
はい、それらの**イテレータの順序は異なります**:

| Map タイプ     | イテレータの順序                  |
|---------------|----------------------------------|
| `TreeMap`     | キーでソートされた順序            |
| `LinkedHashMap`| 挿入順序（またはアクセス順序）    |

---

### 🔍 例

```java
Map<String, Integer> treeMap = new TreeMap<>();
Map<String, Integer> linkedMap = new LinkedHashMap<>();

treeMap.put("banana", 2);
treeMap.put("apple", 3);
treeMap.put("cherry", 1);

linkedMap.put("banana", 2);
linkedMap.put("apple", 3);
linkedMap.put("cherry", 1);

System.out.println("TreeMap:");
for (String key : treeMap.keySet()) {
    System.out.println(key);
}

System.out.println("LinkedHashMap:");
for (String key : linkedMap.keySet()) {
    System.out.println(key);
}
```

**出力:**
```
TreeMap:
apple
banana
cherry

LinkedHashMap:
banana
apple
cherry
```

パフォーマンスやメモリ使用量についてより詳細な説明が必要でしたら、お知らせください。