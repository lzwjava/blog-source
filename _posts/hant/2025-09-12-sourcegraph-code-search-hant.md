---
audio: false
date: 2025-09-12
generated: true
image: false
lang: hant
layout: post
model: mistralai/mistral-medium-3.1
title: Sourcegraph 代碼搜尋指南
translated: true
type: note
---

Sourcegraph 是一款強大的程式碼搜尋與導航工具，讓開發者能夠跨多個儲存庫進行搜尋、理解程式碼依賴關係，並高效地瀏覽大型程式碼庫。本指南涵蓋**搜尋語法、最佳實踐及語言特定搜尋（Java 與 Python）**。

---

## **1. 基本搜尋語法**
Sourcegraph 支援**字面搜尋、正則表達式搜尋及結構化搜尋**，並提供篩選功能。

### **1.1. 字面搜尋**
搜尋完全相符的文字：
```
"def calculate_sum"
```

### **1.2. 正則表達式搜尋**
使用 `/.../` 進行正則表達式搜尋：
```
/def \w+_sum\(/
```

### **1.3. 結構化搜尋（測試版）**
搜尋程式碼模式（例如函數定義）：
```
type:func def calculate_sum
```

### **1.4. 篩選器**
使用篩選器精煉搜尋結果：
- `repo:` – 在特定儲存庫中搜尋
  ```
  repo:github.com/elastic/elasticsearch "def search"
  ```
- `file:` – 在特定檔案中搜尋
  ```
  file:src/main/java "public class"
  ```
- `lang:` – 以特定語言搜尋
  ```
  lang:python "def test_"
  ```
- `type:` – 搜尋符號（函數、類別等）
  ```
  type:func lang:go "func main"
  ```

---

## **2. 進階搜尋技巧**
### **2.1. 布林運算子**
- `AND`（預設）：`def calculate AND sum`
- `OR`：`def calculate OR def sum`
- `NOT`：`def calculate NOT def subtract`

### **2.2. 萬用字元**
- `*` – 匹配任意字元序列
  ```
  "def calculate_*"
  ```
- `?` – 匹配單一字元
  ```
  "def calculate_?"
  ```

### **2.3. 大小寫敏感度**
- 預設為不區分大小寫
- 使用 `case:yes` 強制區分大小寫
  ```
  case:yes "Def Calculate"
  ```

### **2.4. 在註解中搜尋**
使用 `patternType:literal` 搜尋註解內容：
```
patternType:literal "// TODO:"
```

---

## **3. 搜尋 Java 程式碼**
### **3.1. 尋找類別**
```
type:symbol lang:java "public class"
```
### **3.2. 尋找方法**
```
type:func lang:java "public void"
```
### **3.3. 尋找註解**
```
lang:java "@Override"
```
### **3.4. 尋找匯入語句**
```
lang:java "import org.springframework"
```
### **3.5. 尋找例外處理**
```
lang:java "try {" AND "catch (Exception"
```

---

## **4. 搜尋 Python 程式碼**
### **4.1. 尋找函數**
```
type:func lang:python "def calculate"
```
### **4.2. 尋找類別**
```
type:symbol lang:python "class Calculator"
```
### **4.3. 尋找匯入語句**
```
lang:python "import pandas"
```
### **4.4. 尋找裝飾器**
```
lang:python "@app.route"
```
### **4.5. 尋找文件字串**
```
lang:python '"""'
```

---

## **5. 最佳實踐**
1. **使用 `lang:`** 縮小搜尋範圍。
2. **結合篩選器**（`repo:`、`file:`、`type:`）以提升精確度。
3. **使用正則表達式**進行靈活的模式匹配。
4. **儲存常用搜尋**以便重複使用。
5. **在需要時使用 `case:yes`**。

---

## **6. 搜尋範例**

| **目標** | **搜尋語法** |
|----------|-----------|
| 尋找所有 Java `main` 方法 | `lang:java "public static void main"` |
| 尋找 Python 測試函數 | `lang:python "def test_"` |
| 尋找 Go 語言中的 TODO 註解 | `lang:go "// TODO:"` |
| 尋找 Spring Boot 控制器 | `lang:java "@RestController"` |
| 尋找 JavaScript 中的所有 `if` 語句 | `lang:javascript "if ("` |

---

## **7. 總結**
Sourcegraph 的搜尋功能**強大且靈活**，支援**字面、正則表達式及結構化搜尋**，並能跨越多種語言。透過熟練運用篩選器（`lang:`、`repo:`、`type:`），您將能高效地瀏覽大型程式碼庫。

### **後續步驟**
- 嘗試使用**儲存搜尋**功能處理重複查詢。
- 探索**程式碼智能**功能（懸浮提示、跨儲存庫導航）。
- 使用 **Sourcegraph 擴充功能**（VS Code、JetBrains）。

是否需要深入瞭解任何特定功能？🚀