---
audio: false
generated: true
image: false
lang: hant
layout: post
model: x-ai/grok-4-fast
title: 訓練時間估計：57分鐘
translated: true
type: note
---

### 目前進度
- 訓練已完成 20,000 次總迭代中的 1,300 次（`max_iters = 20000`）。
- 剩餘迭代次數：18,700 次。
- 每次非評估迭代的平均時間：約 170 毫秒（根據第 100–1300 次迭代的日誌記錄）。
- 每 500 次迭代進行一次評估（`eval_interval = 500`），每次評估使用 `eval_iters = 200` 個樣本。這些評估會帶來顯著的開銷，使得「評估迭代」時間約為 5.7–6 秒（日誌顯示第 1000 次迭代為 5.7 秒，第 500 次迭代為 6 秒，包括訓練批次和評估時間）。

### 剩餘評估次數
- 下一次評估在第 1500 次迭代，之後是第 2000 次，依此類推，直到第 20,000 次。
- 總剩餘評估次數：38 次。
- 每次評估的額外時間：約 5.8 秒（超出標準的 170 毫秒訓練步驟）。

### 時間預估
- 18,662 次正常迭代的時間：約 3,172 秒（53 分鐘）。
- 38 次評估迭代的時間：約 228 秒（3.8 分鐘，完整持續時間）。
- **剩餘總時間：約 3,400 秒（57 分鐘）**。
  - 此預估假設硬體性能穩定（根據 `nvidia-smi` 顯示，RTX 4070 使用率為 100%）、無中斷情況，且無額外的保存/檢查點操作（如果驗證結果改善，這些操作會帶來輕微開銷）。
  - 時間範圍：50–65 分鐘，考慮到迭代時間的輕微波動（日誌顯示為 168–186 毫秒）或 GPU 熱節流情況。

訓練預計在當地時間 03:25–03:35 左右完成（根據您的 `nvidia-smi` 時間戳記 02:30 推算）。請監控是否出現記憶體不足錯誤或速度下降，但根據您的配置（batch_size=16、gradient_accumulation_steps=4、block_size=512）及 12 GB VRAM 使用量（6.5 GB / 12 GB），目前狀態看來穩定。