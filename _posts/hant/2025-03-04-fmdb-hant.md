---
audio: false
generated: true
lang: hant
layout: post
title: 使用 FMDB 管理 iOS 數據庫
translated: true
type: note
---

FMDB 是一個流行的 Objective-C SQLite 封裝庫，為 iOS 應用程式中的資料庫操作提供了更簡單直觀的介面。以下是關於如何在 iOS 專案中使用 FMDB 的詳細指南。

---

## FMDB 簡介

FMDB（Flying Meat Database）是一個基於 SQLite 的 Objective-C 封裝庫，能讓資料庫操作變得更簡單高效。它提供了便捷的方式來操作 SQLite 資料庫，無需處理原始 SQLite API 的複雜性。FMDB 因其簡單性和效能而在 iOS 開發中被廣泛使用。

## 為何選擇 FMDB？

- **簡單易用**：FMDB 透過易用的方法簡化了資料庫操作。
- **高效能**：它針對效能進行了優化，適合行動應用程式。
- **執行緒安全**：FMDB 提供執行緒安全的資料庫操作。
- **活躍社群**：由於廣泛使用，FMDB 擁有活躍的社群和豐富的資源。

## 開始使用 FMDB

### 步驟 1：安裝 FMDB

你可以使用 CocoaPods、Carthage 或 Swift Package Manager 安裝 FMDB。這裡我們使用 CocoaPods 進行安裝。

1. **將 FMDB 加入 Podfile**：
   ```ruby
   platform :ios, '10.0'
   use_frameworks!

   target 'YourAppTarget' do
     pod 'FMDB', '~> 2.7'
   end
   ```

2. **安裝 Pods**：
   ```bash
   pod install
   ```

### 步驟 2：匯入 FMDB

在 Swift 或 Objective-C 檔案中匯入 FMDB：

```swift
import FMDB
```

### 步驟 3：基本資料庫操作

#### 開啟資料庫

```swift
let fileURL = try! FileManager.default
    .url(for: .documentDirectory, in: .userDomainMask, appropriateFor: nil, create: false)
    .appendingPathComponent("database.sqlite")

let database = FMDatabase(url: fileURL)

if database.open() {
    print("Database opened successfully")
} else {
    print("Unable to open database")
}
```

#### 建立資料表

```swift
let createTableQuery = """
CREATE TABLE IF NOT EXISTS users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    age INTEGER
)
"""

if database.executeStatements(createTableQuery) {
    print("Table created successfully")
} else {
    print("Unable to create table")
}
```

#### 插入資料

```swift
let insertQuery = "INSERT INTO users (name, age) VALUES (?, ?)"

if database.executeUpdate(insertQuery, withArgumentsIn: ["Alice", 30]) {
    print("Data inserted successfully")
} else {
    print("Unable to insert data")
}
```

#### 查詢資料

```swift
let selectQuery = "SELECT * FROM users"

if let results = database.executeQuery(selectQuery, withArgumentsIn: []) {
    while results.next() {
        let id = results.int(forColumn: "id")
        let name = results.string(forColumn: "name")
        let age = results.int(forColumn: "age")
        print("User ID: \\(id), Name: \\(name ?? ""), Age: \\(age)")
    }
} else {
    print("Unable to fetch data")
}
```

#### 更新資料

```swift
let updateQuery = "UPDATE users SET age = ? WHERE name = ?"

if database.executeUpdate(updateQuery, withArgumentsIn: [31, "Alice"]) {
    print("Data updated successfully")
} else {
    print("Unable to update data")
}
```

#### 刪除資料

```swift
let deleteQuery = "DELETE FROM users WHERE name = ?"

if database.executeUpdate(deleteQuery, withArgumentsIn: ["Alice"]) {
    print("Data deleted successfully")
} else {
    print("Unable to delete data")
}
```

### 步驟 4：關閉資料庫

完成操作後務必關閉資料庫以釋放資源。

```swift
database.close()
```

## 最佳實踐

- **執行緒安全**：使用 `FMDatabaseQueue` 進行執行緒安全操作。
- **錯誤處理**：始終檢查並適當處理錯誤。
- **資源管理**：不再需要時關閉資料庫。
- **效能優化**：對批次操作使用交易以提高效能。

## 結論

FMDB 是管理 iOS 應用程式中 SQLite 資料庫的強大工具。其簡單性和高效能使其成為開發者的熱門選擇。透過遵循本指南，你可以將 FMDB 整合到專案中，並高效管理資料庫操作。

---

這篇部落格文章提供了 FMDB 的全面概述，從安裝到基本操作，並包含了高效資料庫管理的最佳實踐。