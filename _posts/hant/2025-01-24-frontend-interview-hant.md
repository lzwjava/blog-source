---
audio: false
generated: false
lang: hant
layout: post
title: 前端工程師面試
translated: true
type: note
---

從 HTML 開始：

1. 語義標籤：理解並使用 `<article>`、`<section>`、`<header>`、`<footer>`、`<nav>`。

2. 表單：實作驗證，處理 `<input>`、`<textarea>`、`<select>`、`<button>`。

3. 表格：使用 `<table>`、`<thead>`、`<tbody>`、`<tfoot>` 建立無障礙表格。

4. 元數據：使用 `<meta>` 標籤設定字符集、視口和 SEO。

5. 連結和錨點：理解 `<a>` 標籤、`href`、`target` 和 `download` 屬性。

6. 媒體元素：正確使用 `<img>`、`<video>`、`<audio>` 及 `src`、`alt`、`controls` 等屬性。

7. 列表：建立有序 `<ol>` 和無序 `<ul>` 列表，包括嵌套列表。

8. 標題：使用正確的標題層級 `<h1>` 到 `<h6>`。

9. 嵌入內容：使用 `<iframe>`、`<embed>` 和 `<object>` 嵌入外部內容。

10. HTML5 API：熟悉 Geolocation、Web Storage 和 Fetch API。

現在是 CSS：

11. 盒模型：理解 margin、padding、border 及其對佈局的影響。

12. Flexbox：掌握 Flexbox 屬性的對齊、換行和排序。

13. 網格佈局：使用 CSS Grid 建立複雜佈局。

14. 響應式設計：使用媒體查詢、視口 meta 標籤和響應式圖片。

15. CSS 預處理器：了解 Sass、Less 或 Stylus 的語法和功能。

16. CSS-in-JS：理解 styled-components 或 emotion 等框架。

17. 動畫和過渡：實作流暢的過渡和關鍵幀動畫。

18. 表單樣式：自訂表單元素並改善外觀。

19. CSS Reset 和 Normalize：了解何時及為何使用它們。

20. CSS Grid 與 Flexbox：理解差異並選擇合適的工具。

JavaScript：

21. ES6+ 功能：使用箭頭函數、解構、展開/其餘運算符和模板字面值。

22. DOM 操作：選擇元素、修改 DOM 和處理事件。

23. 非同步 JavaScript：理解 Promise、async/await 和 fetch API。

24. 事件循環：解釋 JavaScript 中的事件循環工作原理。

25. 閉包：理解並有效使用閉包。

26. 原型繼承：解釋 JavaScript 中的原型繼承機制。

27. 模組：使用 `import` 和 `export` 的 ES6 模組。

28. 錯誤處理：使用 try/catch 區塊並理解未處理的 Promise 拒絕。

29. JavaScript 效能：優化程式碼以提升效能。

30. 瀏覽器控制台：使用瀏覽器開發者工具進行除錯。

框架：

31. React.js：理解元件、JSX、狀態、屬性和鉤子。

32. Vue.js：理解 Vue 實例、指令、元件和響應式系統。

33. Angular：理解元件、服務、依賴注入和路由。

34. 狀態管理：使用 Redux、Vuex 或 Context API 進行狀態管理。

35. 路由：使用 React Router、Vue Router 等實作客戶端路由。

36. 元件化架構：理解並實作可重用元件。

37. 生命週期方法：了解 React 生命週期方法或 Vue 鉤子。

38. UI 函式庫：使用 Bootstrap、Tailwind 或 Material-UI 等函式庫。

39. 測試框架：使用 Jest、Jasmine 或 Cypress 編寫測試。

40. 建置工具：使用 Webpack、Babel 或 Parcel 建置專案。

工具和版本控制：

41. Git：使用 git 進行版本控制，包括分支、合併和變基。

42. npm/yarn：管理專案依賴項和腳本。

43. Package.json：理解 scripts、dependencies 和 devDependencies。

44. 任務執行器：使用 Gulp 或 Grunt 自動化任務。

45. 代碼檢查：使用 ESLint 或 Prettier 確保代碼品質。

46. Browsersync：在開發過程中實現即時重新加載。

47. Figma/Adobe XD：理解設計交付並與設計師協作。

48. API 整合：從 RESTful 或 GraphQL API 獲取數據。

49. 環境變數：管理環境特定配置。

50. 持續整合：使用 GitHub Actions 或 Jenkins 設置 CI/CD 管道。

效能優化：

51. 代碼分割：使用 Webpack 或動態導入實現代碼分割。

52. 懶加載：懶加載圖片、元件和腳本。

53. 最小化：最小化 CSS、JavaScript 和 HTML 文件。

54. 快取策略：使用 HTTP 快取標頭和 Service Worker。

55. 圖片優化：壓縮和優化圖片以用於網頁。

56. 關鍵 CSS：內聯關鍵 CSS 以加快頁面加載速度。

57. 網頁效能指標：理解 Lighthouse、GTmetrix 和 PageSpeed Insights。

58. 字體加載：使用 WebFont Loader 或自託管優化字體加載。

59. 避免渲染阻塞資源：確保腳本和樣式不阻塞渲染。

60. 效能預算：設定並遵守效能預算。

無障礙性：

61. ARIA 角色：使用 ARIA 角色、狀態和屬性提升無障礙性。

62. 語義 HTML：選擇語義元素以改善無障礙性。

63. 圖片替代文字：為圖片提供有意義的替代文字。

64. 鍵盤導航：確保網站可僅用鍵盤導航。

65. 顏色對比：使用工具檢查和改進顏色對比。

66. 屏幕閱讀器測試：使用 NVDA 或 VoiceOver 等屏幕閱讀器測試。

67. 焦點管理：確保互動元素的正確焦點管理。

68. 無障礙指南：遵循 WCAG 2.1 指南。

69. 表單無障礙性：正確使用標籤、佔位符和驗證。

70. EPub 和 AODA 合規性：了解基本合規標準。

最佳實踐：

71. 代碼組織：保持整潔和模組化的代碼結構。

72. 文檔編寫：為元件和 API 編寫清晰文檔。

73. 跨瀏覽器測試：在多種瀏覽器和設備上測試。

74. 漸進增強：建立適用於所有用戶的網站，無論瀏覽器支援如何。

75. 安全性：預防 XSS 攻擊、使用內容安全策略並保護 API。

76. SEO 最佳實踐：使用 meta 標籤、標題和替代文字優化搜尋引擎。

77. 版本控制：對函式庫和依賴項使用語義版本控制。

78. 協作工具：使用 GitHub、GitLab 或 Bitbucket 進行團隊協作。

79. 代碼審查：參與代碼審查並提供建設性反饋。

80. 學習資源：透過 MDN、博客和在線課程保持更新。

進階主題：

81. WebSockets：使用 WebSockets 實作即時通訊。

82. PWA（漸進式網頁應用）：了解 Service Worker、離線支援和推送通知。

83. Canvas 和 SVG：使用 Canvas 和 SVG 元素建立圖形。

84. CSS Grid 和 Flexbox 佈局：使用 CSS Grid 和 Flexbox 實作複雜佈局。

85. 自訂元素：使用 Web Components 建立自訂 HTML 元素。

86. Shadow DOM：理解並使用 Shadow DOM 進行封裝。

87. CSS 變數：使用自訂屬性進行主題化和動態樣式。

88. JavaScript 設計模式：實作 Singleton、Observer 和 Factory 等設計模式。

89. 國際化（i18n）：實作語言支援和本地化。

90. 效能分析：使用 Chrome DevTools 等工具分析 JavaScript 和 DOM 效能。

跨學科技能：

91. 用戶體驗（UX）：理解 UX 原則並與 UX 設計師協作。

92. 用戶界面（UI）：建立視覺吸引且用戶友好的界面。

93. 專案管理：使用 Agile、Scrum 或 Kanban 方法進行專案管理。

94. 溝通技巧：與團隊成員和利益相關者有效溝通。

95. 解決問題：有條理地處理問題並找到最佳解決方案。

96. 適應性：快速學習並適應新技術和工具。

97. 團隊協作：在團隊中良好合作，分享知識並指導他人。

98. 時間管理：優先處理任務並有效管理時間。

99. 創造力：為設計和編碼挑戰帶來創意解決方案。

100. 學習熱情：保持好奇心並不斷提升技能。