---
audio: false
generated: true
image: false
lang: hant
layout: post
model: openai/gpt-5
title: 防止 Jekyll CSS 緩存破壞
translated: true
type: note
---

你所看到的是 Jekyll（透過主題或 `jekyll-assets`/`jekyll-feed`/`minima` 管道）自動在樣式表網址後附加**指紋（雜湊值）**以實現快取破壞。這就是為什麼同一個 CSS 檔案在重建後會帶有不同的 `?v=...` 查詢字串。

如果你不希望 Jekyll 每次建置時都更改該查詢字串，可以停用或覆寫「快取破壞」/「摘要」行為：

### 選項 1. 使用固定連結

不使用 Jekyll 的輔助標籤（例如 `{{ "/assets/css/style.css" | relative_url }}` 或 `{{ "/assets/css/style.css" | asset_path }}`），直接寫死：

```html
<link rel="stylesheet" href="/assets/css/style.css">
```

這樣就不會注入指紋。

### 選項 2. 停用資源摘要

如果你使用 **jekyll-assets** 或會對檔案進行指紋處理的主題，可以在 `_config.yml` 中停用摘要功能：

```yml
assets:
  digest: false
```

### 選項 3. 使用不帶摘要的 `relative_url`

如果你的主題使用類似這樣的程式碼：

```liquid
<link rel="stylesheet" href="{{ '/assets/css/style.css' | relative_url }}">
```

請確保沒有透過啟用摘要功能的 `asset_path` 或 `absolute_url` 進行管道傳遞。只需使用 `relative_url`。

---

👉 簡而言之：

* 如果你希望它**永不改變**，直接寫死路徑。
* 如果你想透過設定控制，設定 `assets.digest: false`。
* 如果你仍需要版本控制但保持穩定，則需手動管理自己的 `?v=...` 參數。

你希望它是**完全靜態**（永不改變），還是只想阻止 Jekyll**每次建置都產生新雜湊值**？