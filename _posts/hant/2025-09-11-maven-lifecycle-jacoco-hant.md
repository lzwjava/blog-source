---
audio: false
date: 2025-09-11
generated: true
image: false
lang: hant
layout: post
model: openai/gpt-5
title: 理解 Maven 生命週期與 JaCoCo
translated: true
type: note
---

好問題——這歸結於 Maven 的*生命週期階段*以及插件如何綁定到這些階段。

### 生命週期基礎

Maven 有三個標準生命週期：`clean`、`default`（有時稱為*build*）和 `site`。
在 *default* 生命週期內，重要的階段按以下順序進行：

* `validate`
* `compile`
* `test`
* `package`
* `verify`
* `install`
* `deploy`

每個階段都包含之前的所有階段（如果你執行 `mvn verify`，Maven 會執行從 `validate` 到 `verify` 的所有步驟）。執行 `mvn package` 會在建立構件（JAR/WAR）後停止，不會執行 `verify`。

### 為何 JaCoCo 會參與其中

JaCoCo Maven 插件通常根據配置，將其 **`prepare-agent`** 目標綁定到 `initialize` 或 `verify` 階段。

* 如果它綁定到 `verify`（用於覆蓋率報告的常見設定），則 `mvn verify` 會觸發它。
* `mvn package` 不會執行到生命週期的那麼後期，因此 `verify` 階段（以及 JaCoCo 的步驟）會被跳過。

這就是為什麼你在執行 `mvn verify` 時會看到 JaCoCo 被執行，但在執行 `mvn package` 時則不會。

### 實用結論

* 當你只需要建構產物時，使用 **`mvn package`**（快速，無額外檢查）。
* 當你想要執行*整合測試、檢查、覆蓋率報告或綁定到後期階段的 QA 插件*時，使用 **`mvn verify`**。

👉 如果你希望 JaCoCo 即使在執行 `mvn package` 時也能始終運行，可以在你的 `pom.xml` 中將 `prepare-agent` 目標重新綁定到更早的階段，例如 `test` 或 `package`。

需要我展示一個具體的 `pom.xml` 代碼片段，讓 JaCoCo 的 `prepare-agent` 在 `test` 階段而非 `verify` 階段就運行嗎？