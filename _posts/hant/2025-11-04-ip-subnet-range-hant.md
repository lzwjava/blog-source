---
audio: false
generated: true
image: false
lang: hant
layout: post
model: x-ai/grok-4-fast
title: IP 地址子網範圍解析
translated: true
type: note
---

是的，正確。IP 地址 `192.168.1.35/24` 屬於子網 `192.168.1.0/24`，該子網涵蓋完整的 256 個地址範圍：從 `192.168.1.0`（網絡地址）到 `192.168.1.255`（廣播地址）。

### 快速解析：
- **/24** 表示子網遮罩為 `255.255.255.0`，固定前三個位元組（`192.168.1`），最後一個位元組可從 0 到 255 變動。
- 可用主機地址：`192.168.1.1` 至 `192.168.1.254`（不包括網絡地址和廣播地址）。
- 你的特定主機（`192.168.1.35`）只是該範圍內的一個地址。

如果你需要計算不同前綴長度的子網，或想透過工具驗證，請告訴我！