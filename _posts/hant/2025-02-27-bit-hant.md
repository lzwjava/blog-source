---
audio: false
generated: true
lang: hant
layout: post
title: Java 位元運算
translated: true
type: note
---

位元操作使用位元運算子來高效解決問題。讓我們探討如何找出單一數字以及檢查一個數字是否為2的冪。

## 1. 單一數字：XOR的魔法
在一個所有其他數字都出現兩次的陣列中，使用XOR找出唯一不重複的數字。

### Java 實現
```java
public class SingleNumber {
    public static int singleNumber(int[] nums) {
        int result = 0;
        for (int num : nums) result ^= num;
        return result;
    }

    public static void main(String[] args) {
        int[] nums = {2, 2, 1, 1, 4};
        System.out.println("Single number: " + singleNumber(nums));
    }
}
```
**輸出：** `Single number: 4`

## 2. 2的冪：位元檢查
如果一個數字的二進位表示中只有一個1位元，那麼它就是2的冪。

### Java 實現
```java
public class PowerOfTwo {
    public static boolean isPowerOfTwo(int n) {
        return n > 0 && (n & (n - 1)) == 0;
    }

    public static void main(String[] args) {
        int n = 16;
        System.out.println(n + " is power of 2: " + isPowerOfTwo(n));
        n = 18;
        System.out.println(n + " is power of 2: " + isPowerOfTwo(n));
    }
}
```
**輸出：**
```
16 is power of 2: true
18 is power of 2: false
```

---

## 跨部落格結論
- **排序：** 快速排序、合併排序和氣泡排序涵蓋了速度、穩定性和簡潔性。
- **字串：** KMP、字典樹和Rabin-Karp處理模式匹配和前綴搜尋。
- **搜尋：** 二分搜尋和線性搜尋提供對數和線性解決方案。
- **圖論：** DFS、BFS、Dijkstra演算法和Kruskal演算法解決遍歷、最短路徑和最小生成樹問題。
- **位元操作：** XOR和位元檢查以最小運算量解決棘手問題。

每個類別都有其優勢——根據你的問題選擇合適的工具，並嘗試這些實現！如果你希望在任何類別中獲得更多深度或額外演算法，請告訴我。