---
audio: false
generated: true
image: false
lang: hant
layout: post
title: Sourcegraph 查詢語言指南
translated: true
type: note
---

Sourcegraph 搜尋擁有靈活且強大的專屬查詢語言。以下是常用的語法分類說明：

---

### 1. **基礎關鍵字搜尋**

* 直接輸入詞語：

  ```
  http client
  ```

  可找到包含這兩個詞語的檔案或程式碼。

---

### 2. **邏輯運算子**

* `AND`、`OR`、`NOT`（必須大寫）：

  ```
  React AND useEffect
  React OR Vue
  logger NOT debug
  ```

---

### 3. **篩選器**

* `repo:` → 限定儲存庫範圍

  ```
  repo:github.com/myorg/myrepo
  ```
* `file:` → 匹配檔案路徑（支援正則表達式）

  ```
  file:\.java$
  ```
* `lang:` → 限定程式語言

  ```
  lang:python requests
  ```
* `content:` → 強制進行文字搜尋（忽略結構匹配）

  ```
  content:"SELECT * FROM"
  ```
* `case:yes` → 啟用大小寫敏感搜尋

---

### 4. **正則表達式匹配**

* 預設支援正則表達式：

  ```
  foo(bar|baz)
  ^class\s+\w+
  ```

---

### 5. **結構化搜尋**

使用 `:[var]` 等模式佔位符匹配程式碼結構：

```
if (:[cond]) { :[body] }
```

---

### 6. **版本/分支搜尋**

* 在查詢末尾添加 `@branch`、`@commit` 或 `@tag`：

  ```
  repo:myrepo@main lang:go fmt.Println
  ```

---

### 7. **符號與定義搜尋**

* `type:symbol` → 搜尋符號名稱
* `type:commit` → 搜尋提交訊息
* `type:diff` → 搜尋程式碼差異

---

### 8. **範例應用**

* 尋找 Go 檔案中的所有待辦註解：

  ```
  TODO lang:go
  ```
* 僅在 `src/` 目錄中搜尋 Java 方法：

  ```
  file:^src/ lang:java "public static"
  ```
* 跨多儲存庫搜尋：

  ```
  repo:github.com/org/.* file:\.ts$ "useState"
  ```

---

👉 您希望我準備 **常用 Sourcegraph 查詢速查表**（包含可每日重複使用的單行指令），還是進行 **進階模式的深度解析**？