---
audio: false
generated: true
image: false
lang: hant
layout: post
title: 語言檢測誤判文本為尼泊爾文
translated: true
type: note
---

啊，原來如此——感謝你的說明。

從你貼上的日誌來看：

```
Debug: raw langdetect output: [('ne', 0.9999953270916105)]
Debug: normalized langdetect output: [('ne', 0.9999953270916105)]
Debug: Detected languages: [('ne', 0.9999953270916105)]
```

這裡的 **`ne` 並非法文單字**——它實際上是 `langdetect` 輸出的**語言代碼**。

`langdetect` 函式庫有時會將 `"ne"` 作為**尼泊爾語**的 ISO 639-1 代碼返回。

* `"en"` = 英文
* `"fr"` = 法文
* `"hi"` = 印地文
* `"ne"` = 尼泊爾文

因此在此案例中，`langdetect` 判定你的文字有 `~0.99999` 的機率是尼泊爾文。

這就是為什麼你的驗證會失敗並顯示：

```
Translated text does not contain the target language 'hi' (detected: [('ne', 0.9999953270916105)])
```

👉 總結來說：

* 此處的 `ne` = **尼泊爾文**的 ISO 語言代碼，而非法文否定詞 `"ne"`。
* 當 `langdetect` 判定你的輸入內容類似尼泊爾文時，就會出現此代碼。

需要我示範如何在你嘅 `_map_target_code` 中**正規化或重新映射** `ne`（尼泊爾文），以避免驗證器在遇到非預期語言時發生錯誤嗎？