---
audio: false
generated: true
image: false
lang: hant
layout: post
title: 工具描述按字母順序排列
translated: true
type: note
---

以下為列表中各工具/套件的簡要說明（基於常見用途，主要用於開發、系統管理及媒體處理等場景）。我已按字母順序排列以便查閱，並附上其主要功能的簡要描述。

- **ack**：專為程式設計師優化的類 grep 搜尋工具，專注於原始碼並忽略二進位檔案。
- **awscli**：用於管理 Amazon Web Services (AWS) 資源（如 S3、EC2 等）的命令列介面。
- **azure-cli**：用於管理 Microsoft Azure 雲端服務與資源的命令列工具。
- **c-ares**：用於非同步 DNS 請求的 C 語言函式庫，常見於網路應用程式。
- **ca-certificates**：包含可信根憑證的套件包，用於安全連線（如 HTTPS）。
- **certifi**：提供精選根憑證的 Python 套件，用於 SSL/TLS 驗證。
- **cloc**：用於統計多種語言原始碼檔案中的程式碼行數、註解及空行的工具。
- **cmake**：跨平台建置系統生成器，用於管理軟體專案的編譯流程。
- **cryptography**：提供加密與雜湊等安全加密原語的 Python 函式庫。
- **curl**：用於透過 URL 傳輸資料的命令列工具與函式庫（支援 HTTP、FTP 等協定）。
- **doctl**：用於管理 DigitalOcean 雲端基礎架構（如 droplets 與資料庫）的命令列介面。
- **docker**：用於在容器中開發、運送及執行應用程式的平台。
- **docker-completion**：為 bash/zsh 提供 Docker 命令的 shell 自動完成指令碼。
- **emacs**：高度可擴充的文字編輯器，內建支援程式設計、電子郵件等功能。
- **ffmpeg**：用於錄製、轉換及串流音訊/影片的多媒體框架。
- **flac**：無損音訊壓縮編解碼器及用於編碼/解碼 FLAC 檔案的相關工具。
- **gdbm**：GNU 資料庫管理函式庫，用於鍵值儲存。
- **gettext**：用於軟體訊息國際化的函式庫。
- **ghostscript**：用於渲染與轉換 PostScript 及 PDF 檔案的解譯器。
- **git-lfs**：Git 擴充功能，用於高效版本化管理大型檔案（如二進位檔、媒體檔）。
- **gnutls**：實作 TLS/SSL 協定的傳輸層安全函式庫。
- **go**：Go 程式語言的編譯器與工具集，用於建置可擴充應用程式。
- **gradle**：用於 Java、Android 及多語言專案的建置自動化工具。
- **harfbuzz**：用於渲染複雜文字布局的 OpenType 文字成形引擎函式庫。
- **htop**：Unix 系統的互動式程序檢視器，功能較 top 更豐富。
- **icu4c@76**：Unicode 國際化元件函式庫（版本 76），用於處理文字與在地化。
- **imagemagick**：用於建立、編輯及轉換點陣圖影像的工具套件。
- **jansson**：用於編碼、解碼及操作 JSON 資料的 C 語言函式庫。
- **libarchive**：支援多種格式的歸檔與壓縮函式庫（如 tar、zip）。
- **libass**：用於在媒體播放器中渲染 ASS/SSA 字幕的函式庫。
- **libde265**：用於解碼 HEVC/H.265 影片串流的函式庫。
- **libheif**：用於讀寫 HEIF 影像檔案（高效影像格式）的函式庫。
- **libidn**：用於處理國際化網域名稱（IDNA）的函式庫。
- **libimobiledevice-glue**：輔助 iOS 裝置通訊的 libimobiledevice 實用工具函式庫。
- **libmicrohttpd**：用於在應用程式中嵌入 HTTP 伺服器的小型 C 語言函式庫。
- **libnghttp3**：實作 HTTP/3 協定的函式庫，無需依賴 nghttp2。
- **libogg**：用於操作 Ogg 多媒體容器格式的函式庫。
- **libomp**：用於 C/C++/Fortran 平行程式設計的 OpenMP 執行期函式庫。
- **libplist**：用於處理 Apple Property List (plist) 檔案（二進位/XML 格式）的函式庫。
- **libpng**：用於讀寫 PNG 影像檔案的函式庫。
- **libsndfile**：用於讀寫多種音訊格式（如 WAV、AIFF 等）的 C 語言函式庫。
- **libssh**：實作 SSHv2 協定以實現安全遠端存取的函式庫。
- **libusbmuxd**：用於透過 USB 與 iOS 裝置進行多工連線的函式庫。
- **libuv**：用於非同步 I/O 的跨平台函式庫，被 Node.js 所使用。
- **libvpx**：用於 VP8/VP9 影片編解碼器（WebM 格式）的函式庫。
- **llama.cpp**：用於在 CPU/GPU 上高效執行大型語言模型（如 Llama）的 C++ 函式庫。
- **llvm**：模組化編譯器與工具鏈技術集合（例如 clang 編譯器）。
- **lua**：輕量級腳本語言，常被嵌入於應用程式中使用。
- **maven**：用於 Java 專案的建置自動化與依賴管理工具。
- **mbedtls**：專為嵌入式系統設計的輕量級加密函式庫（TLS/SSL）。
- **mpg123**：快速的命令列 MP3 播放器與解碼器函式庫。
- **nettle**：提供 AES 與 RSA 等演算法的底層加密函式庫。
- **node**：基於 Chrome V8 引擎建構的 JavaScript 執行環境，用於伺服器端腳本。
- **nspr**：Netscape 可移植執行期，為網路應用提供平台抽象層函式庫。
- **nss**：網路安全服務函式庫，用於加密操作（常見於瀏覽器）。
- **ollama**：用於在本機執行與管理大型語言模型的工具。
- **openjdk@17**：開放原始碼 Java 開發工具包（版本 17），用於建置/執行 Java 應用程式。
- **openjdk@21**：開放原始碼 Java 開發工具包（版本 21），用於建置/執行 Java 應用程式。
- **openssl@3**：實作 SSL/TLS 協定的加密函式庫（版本 3）。
- **pandoc**：通用文件轉換工具（例如將 Markdown 轉為 PDF/HTML）。
- **pango**：用於圖形應用程式中文字布局與渲染的函式庫。
- **perl**：適用於文字處理與腳本編寫的高階程式語言。
- **php**：用於網頁開發的伺服器端腳本語言。
- **pixman**：用於圖形渲染中像素操作的底層函式庫。
- **poppler**：PDF 渲染函式庫及相關工具（如 pdftohtml、pdftotext）。
- **postgresql@14**：關聯式資料庫管理系統（版本 14）。
- **python-packaging**：用於處理套件元資料與依賴關係的 Python 函式庫。
- **rav1e**：專注於速度與安全性的 AV1 影片編碼器（以 Rust 撰寫）。
- **readline**：提供命令列編輯與歷史記錄功能的函式庫。
- **redis**：用作資料庫、快取或訊息代理的記憶體內資料儲存系統。
- **ruby**：用於網頁開發與腳本編寫的動態程式語言。
- **rust**：專注於安全性與效能的系統程式語言。
- **sdl2**：用於跨平台多媒體開發（遊戲、影片）的 Simple DirectMedia Layer 函式庫。
- **snappy**：Google 推出的快速壓縮/解壓縮函式庫。
- **sqlite**：輕量級嵌入式 SQL 資料庫引擎。
- **svt-av1**：用於高效能影片的 Scalable Video Technology AV1 編解碼器函式庫。
- **telnet**：Telnet 協定的用戶端，用於遠端終端存取（不安全，多屬舊系統）。
- **tesseract**：用於從影像中提取文字的光學字元辨識 (OCR) 引擎。
- **tree-sitter**：用於語法突顯的解析器生成工具與增量解析函式庫。
- **unbound**：驗證性、遞迴式、快取式 DNS 解析器函式庫。
- **wireshark**：用於擷取與檢查封包的網路協定分析器。
- **yt-dlp**：用於從 YouTube 及其他網站下載影片的命令列工具（youtube-dl 的分支）。
- **zeromq**：高效能非同步訊息傳遞函式庫 (ZeroMQ)。