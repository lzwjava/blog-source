---
audio: false
generated: true
image: false
lang: hant
layout: post
title: 安裝 Python 3.13 開發標頭檔
translated: true
type: note
---

這個新的錯誤揭示了建置 `sentencepiece` 時的最終阻礙：**缺少 Python 開發標頭檔**。

---

### ❌ 錯誤：

```
fatal error: Python.h: No such file or directory
```

這表示編譯器找不到 `Python.h` 檔案，而此檔案是編譯 Python C 擴充功能所必需的。

---

### ✅ 解決方案

你需要安裝 Python 3.13 的開發標頭檔：

#### 適用於 Ubuntu/Debian（Python 3.13）：

由於 Python 3.13 非常新，如果你使用的是自訂建置版本或 `pyenv`，標頭檔可能預設未安裝。請嘗試以下方法之一：

1. **如果你使用系統全域的 Python 3.13**（從 deadsnakes 或原始碼安裝）：

```bash
sudo apt install python3.13-dev
```

2. **如果你使用 `pyenv`：**

```bash
pyenv install 3.13.0 --with-openssl --enable-shared
```

並確保你的虛擬環境正在使用此版本。

---

### 🔁 然後重試：

安裝標頭檔後，請再次嘗試：

```bash
pip install sentencepiece --use-pep517
```

---

如果你使用的是自訂 Python 安裝（例如手動編譯、透過 `pyenv`、`conda` 等），請告知我，我可以提供更具體的指引。