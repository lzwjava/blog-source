---
audio: false
generated: true
image: false
lang: hi
layout: post
title: एंगुलर डार्क मोड एलिमेंट चेकलिस्ट
translated: true
type: note
---

जब आप Angular फ्रंटएंड में डार्क मोड लागू करते हैं, तो आमतौर पर `body`, `select`, और `option` से शुरुआत करेंगे, लेकिन कई अन्य सामान्य HTML एलिमेंट्स भी हैं जिन्हें आप रीस्टाइल करना चाहेंगे ताकि थीम एक जैसी लगे। यहाँ एक व्यावहारिक चेकलिस्ट दी गई है:

### पेज-लेवल और कंटेनर्स

* `html`, `body` → बैकग्राउंड और टेक्स्ट कलर।
* `div`, `section`, `article`, `aside`, `header`, `footer`, `main`, `nav` → बैकग्राउंड और टेक्स्ट कलर सेट करें (खासकर अगर उनमें डिफॉल्ट सफेद बैकग्राउंड है)।

### टेक्स्ट और हेडिंग्स

* `h1, h2, h3, h4, h5, h6` → सुनिश्चित करें कि टेक्स्ट डार्क बैकग्राउंड के खिलाफ दिखाई दे।
* `p`, `span`, `a` → टेक्स्ट कलर, लिंक हॉवर कलर और विज़िटेड लिंक स्टेट्स एडजस्ट करें।

### फॉर्म्स और इनपुट्स

* `input[type="text"], input[type="password"], input[type="email"], textarea` → बैकग्राउंड, टेक्स्ट कलर, और बॉर्डर।
* `input[type="checkbox"], input[type="radio"]` → कभी-कभी दृश्यता के लिए विशेष हैंडलिंग की आवश्यकता होती है।
* `select, option` → जैसा आपने बताया, दोनों का बैकग्राउंड और टेक्स्ट कलर।
* `button` → बैकग्राउंड, बॉर्डर, और टेक्स्ट कलर, साथ ही हॉवर/एक्टिव स्टेट्स।
* `label` → सुनिश्चित करें कि टेक्स्ट डार्क बैकग्राउंड के साथ कंट्रास्ट करे।
* `fieldset, legend` → बैकग्राउंड और बॉर्डर।

### लिस्ट्स और टेबल्स

* `ul, ol, li` → टेक्स्ट कलर।
* `table` → बैकग्राउंड, बॉर्डर कलर, और टेक्स्ट कलर।
* `thead, tbody, tr, th, td` → बैकग्राउंड और बॉर्डर के लिए अलग-अलग एडजस्टमेंट।

### इंटरएक्टिव एलिमेंट्स

* `details, summary` → बैकग्राउंड और टेक्स्ट।
* `dialog` → बैकग्राउंड और टेक्स्ट।
* `mark` → हाइलाइट कलर डार्क मोड पर काम करने चाहिए (डिफॉल्ट पीला बहुत चमकीला हो सकता है)।

### मीडिया और अन्य

* `hr` → बॉर्डर कलर (डिफॉल्ट अक्सर डार्क पर बहुत हल्का होता है)।
* `code, pre` → पठनीयता के लिए बैकग्राउंड और टेक्स्ट कलर।
* `blockquote` → बैकग्राउंड और बॉर्डर कलर।
* `progress, meter` → ट्रैक और वैल्यू कलर।

---

💡 **बेस्ट प्रैक्टिस**: हर जगह `!important` का उपयोग करने के बजाय, रंगों के लिए CSS वेरिएबल्स (`--bg-color`, `--text-color`, आदि) परिभाषित करें और डार्क मोड एक्टिव होने पर उन्हें बदल दें (उदाहरण के लिए, `body` में एक `.dark-mode` क्लास जोड़ें)। इस तरह, Angular कंपोनेंट्स और कस्टम एलिमेंट्स भी थीम में साफ तरीके से जुड़ सकते हैं।

क्या आप चाहेंगे कि मैं डार्क मोड के लिए एक **बेस CSS स्निपेट** (वेरिएबल्स के साथ) तैयार करूं, जिसे आप शुरुआत के तौर पर अपने Angular प्रोजेक्ट में इस्तेमाल कर सकते हैं?