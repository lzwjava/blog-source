---
audio: false
generated: true
image: false
lang: hi
layout: post
title: JaCoCo एजेंट आउटपुट निर्देशिका कॉन्फ़िगर करना
translated: true
type: note
---

हाँ — बिल्कुल सही 👍.

डिफ़ॉल्ट रूप से, यदि आप लिखते हैं

```bash
-javaagent:/path/to/jacocoagent.jar=destfile=jacoco.exec,output=file
```

तो `jacoco.exec` JVM प्रक्रिया के **वर्तमान कार्यशील निर्देशिका** में बनाया जाता है (अक्सर सर्वर रूट या वह स्थान जहाँ से आपने Liberty शुरू की थी)।

यदि आप इसे कहाँ जाना है यह नियंत्रित करना चाहते हैं, तो आप इसे एक **पूर्ण या सापेक्ष पथ** देते हैं, उदाहरण के लिए:

```bash
-javaagent:/path/to/jacocoagent.jar=destfile=/opt/coverage-data/jacoco.exec,output=file
```

या

```bash
-javaagent:/path/to/jacocoagent.jar=destfile=target/jacoco.exec,output=file
```

(जहाँ `target/` स्टार्टअप निर्देशिका के सापेक्ष है)।

तो हाँ, यदि आप गारंटी देना चाहते हैं कि `.exec` फ़ाइल कहाँ समाप्त होती है, तो पूरा पथ उपयोग करें।

👉 सुझाव: Maven/Gradle प्रोजेक्ट्स में इसे `target/` (Maven) या `build/` (Gradle) में इंगित करना आम बात है, ताकि बाद में रिपोर्टिंग प्लगइन्स इसे स्वचालित रूप से उठा सकें।

क्या आप चाहते हैं कि मैं आपको पूर्ण पथ के साथ कॉन्फ़िगर किया गया एक Liberty `jvm.options` उदाहरण दिखाऊँ?