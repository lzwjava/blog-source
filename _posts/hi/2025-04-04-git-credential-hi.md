---
audio: false
generated: true
lang: hi
layout: post
title: गिट क्रेडेंशियल मैनेजर
translated: true
type: note
---

आप **macOS** पर **Git Credential Manager (GCM) version 2.6.1** का उपयोग कर रहे हैं, और आपको एक समस्या का सामना करना पड़ रहा है जहाँ `git config --global credential.helper manager-core` एक त्रुटि देता है:

```
warning: credential.helper has multiple values
error: cannot overwrite multiple values with a single value
```

ऐसा तब होता है जब आपकी Git कॉन्फ़िगरेशन में पहले से कई क्रेडेंशियल हेल्पर सेट हैं। एक नया सेट करने से पहले आपको **मौजूदा मानों को साफ़ करना** होगा।

---

### **🛠 समस्या का समाधान: क्रेडेंशियल हेल्पर रीसेट करें**
#### **चरण 1: वर्तमान क्रेडेंशियल हेल्पर जांचें**
चलाएँ:
```sh
git config --global --get-all credential.helper
```
यह सभी कॉन्फ़िगर किए गए क्रेडेंशियल हेल्पर की सूची देगा।

#### **चरण 2: पिछले क्रेडेंशियल हेल्पर को अनसेट करें**
सभी क्रेडेंशियल हेल्पर को रीसेट करने के लिए इसका उपयोग करें:
```sh
git config --global --unset-all credential.helper
```

#### **चरण 3: GCM को एकमात्र क्रेडेंशियल हेल्पर के रूप में सेट करें**
अब, **Git Credential Manager (GCM)** को फिर से हेल्पर के रूप में सेट करें:
```sh
git config --global credential.helper manager-core
```

#### **चरण 4: परिवर्तन सत्यापित करें**
जांचें कि क्या यह सही तरीके से सेट हो गया है:
```sh
git config --global --get credential.helper
```
अपेक्षित आउटपुट:
```
manager-core
```

---

### **🛠 वैकल्पिक तरीका: `--replace-all` का उपयोग करें**
यदि आप पहले अनसेट किए बिना मौजूदा मानों को ओवरराइट करना चाहते हैं, तो उपयोग करें:
```sh
git config --global --replace-all credential.helper manager-core
```

---

### **🧩 अतिरिक्त डीबगिंग**
यदि आपको अभी भी समस्या हो रही है, तो आप अपनी पूरी Git ग्लोबल कॉन्फ़िगरेशन जांच सकते हैं:
```sh
git config --global --list
```
यदि समस्या बनी रहती है, तो मुझे बताएं! 🚀

---

Git Credential Manager (GCM) एक सुरक्षित Git क्रेडेंशियल हेल्पर है जो HTTPS Git रिपॉजिटरीज़ के लिए प्रमाणीकरण क्रेडेंशियल्स को स्टोर और प्रबंधित करता है। यह GitHub, GitLab, Bitbucket, और Azure DevOps जैसी सेवाओं के साथ काम करता है।

---

## **1. Git Credential Manager इंस्टॉल करें**
GCM, Git for Windows और macOS तथा Linux के आधिकारिक Git डिस्ट्रीब्यूशन के साथ शामिल है। यदि यह इंस्टॉल नहीं है, तो इसे यहाँ से डाउनलोड करें:

🔗 **[Git Credential Manager on GitHub](https://github.com/GitCredentialManager/git-credential-manager)**

### **जांचें कि क्या GCM इंस्टॉल है**
चलाएँ:
```sh
git credential-manager version
```
यदि यह इंस्टॉल है, तो आपको एक वर्जन नंबर दिखाई देगा। यदि नहीं, तो इसे मैन्युअली इंस्टॉल करें।

---

## **2. Git को GCM का उपयोग करने के लिए कॉन्फ़िगर करें**
निम्नलिखित कमांड चलाएँ:
```sh
git config --global credential.helper manager
```
macOS/Linux के लिए, उपयोग करें:
```sh
git config --global credential.helper manager-core
```

वर्तमान क्रेडेंशियल हेल्पर जांचने के लिए:
```sh
git config --global credential.helper
```

---

## **3. Git सर्वर के साथ प्रमाणीकरण करें**
एक बार कॉन्फ़िगर हो जाने पर, जब आप पहली बार HTTPS के माध्यम से रिमोट रिपॉजिटरी के साथ इंटरैक्ट करेंगे (जैसे `git clone`, `git pull`), GCM आपसे क्रेडेंशियल्स के लिए प्रॉम्प्ट करेगा।

- यदि GitHub, GitLab, या Bitbucket का उपयोग कर रहे हैं, तो GCM OAuth प्रमाणीकरण के लिए एक ब्राउज़र खोलेगा।
- यदि पर्सनल एक्सेस टोकन (PAT) का उपयोग कर रहे हैं, तो पासवर्ड के बजाय इसे दर्ज करें।

---

## **4. क्रेडेंशियल्स को स्टोर और प्रबंधित करें**
GCM क्रेडेंशियल्स को OS के क्रेडेंशियल स्टोर में सुरक्षित रूप से संग्रहीत करता है:
- **Windows**: Windows Credential Manager
- **macOS**: macOS Keychain
- **Linux**: GPG-एन्क्रिप्टेड फ़ाइल (`~/.git-credentials` or `~/.gitconfig`)

मैन्युअली एक क्रेडेंशियल जोड़ने के लिए:
```sh
git credential approve
```

एक संग्रहीत क्रेडेंशियल को हटाने के लिए:
```sh
git credential reject https://github.com
```

सभी क्रेडेंशियल्स साफ़ करने के लिए:
```sh
git credential reject
```

---

## **5. विभिन्न Git प्रदाताओं के साथ Git Credential Manager का उपयोग करना**
### **GitHub**
पासवर्ड के बजाय एक **पर्सनल एक्सेस टोकन (PAT)** का उपयोग करें:
```sh
git credential reject https://github.com
git pull
```
आपसे लॉग इन करने और अपना टोकन दर्ज करने के लिए कहा जाएगा।

### **GitLab**
GitLab के लिए, OAuth या एक **PAT** का उपयोग करें:
```sh
git credential reject https://gitlab.com
git pull
```

### **Azure DevOps**
एक **PAT** या Azure प्रमाणीकरण का उपयोग करें।

---

## **6. समस्याओं का डीबगिंग करना**
यदि क्रेडेंशियल्स सही ढंग से सहेजे या पुनर्प्राप्त नहीं हो रहे हैं:
1. क्रेडेंशियल हेल्पर जांचें:
   ```sh
   git config --global credential.helper
   ```
2. Git Credential Manager रीसेट करें:
   ```sh
   git credential reject https://your-git-service.com
   ```
3. यदि आवश्यक हो तो GCM को पुनः इंस्टॉल करें।

क्या आप इसे किसी विशिष्ट सेवा के लिए कॉन्फ़िगर करने में सहायता चाहेंगे? 🚀