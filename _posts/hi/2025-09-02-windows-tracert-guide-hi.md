---
audio: false
generated: true
image: false
lang: hi
layout: post
title: विंडोज़ ट्रेसरट नेटवर्क पथ निदान गाइड
translated: true
type: note
---

Windows पर, **`tracert`** (*trace route* का संक्षिप्त रूप) एक डायग्नोस्टिक कमांड-लाइन टूल है जिसका उपयोग उस पथ को ट्रेस करने के लिए किया जाता है जो पैकेट्स आपके कंप्यूटर से एक डेस्टिनेशन होस्ट (जैसे किसी वेबसाइट या IP एड्रेस) तक लेते हैं। इसका मुख्य रूप से उपयोग नेटवर्क रूट में होने वाली देरी या कनेक्शन समस्याओं के स्थान की पहचान करने के लिए किया जाता है।

यहां बताया गया है कि यह चरण दर चरण कैसे काम करता है:

### 1. इंक्रीमेंटल TTL के साथ ICMP इको रिक्वेस्ट्स

* `tracert` **ICMP इको रिक्वेस्ट पैकेट्स** (`ping` के समान) भेजता है।
* यह IP हेडर में **Time-To-Live (TTL)** फ़ील्ड को मैनिपुलेट करता है।
* TTL 1 से शुरू होता है, फिर प्रत्येक प्रोब के साथ बढ़ता (2, 3, 4, …) है।

### 2. TTL एक्सपायरी और "Time Exceeded" रिस्पांस

* जब कोई राउटर एक पैकेट प्राप्त करता है, तो वह TTL को 1 से कम कर देता है।
* यदि TTL **0** तक पहुँच जाता है, तो राउटर पैकेट को डिसकार्ड कर देता है और एक **ICMP Time Exceeded** मैसेज भेजने वाले को वापस भेजता है।
* इससे `tracert` को उस राउटर की पहचान (IP/होस्टनेम) का पता चलता है।

### 3. रूट का निर्माण

* TTL = 1 के लिए, आप पहली हॉप (आपका डिफ़ॉल्ट गेटवे) देखते हैं।
* TTL = 2 के लिए, आप अगला राउटर देखते हैं, और इसी तरह आगे।
* यह तब तक जारी रहता है जब तक:

  * डेस्टिनेशन होस्ट एक **ICMP इको रिप्लाई** के साथ रिस्पॉन्ड नहीं कर देता, या
  * अधिकतम हॉप लिमिट (डिफ़ॉल्ट 30) तक नहीं पहुँच जाती।

### 4. लेटेंसी का मापन

* `tracert` प्रति हॉप **तीन प्रोब** (डिफ़ॉल्ट रूप से) भेजता है।
* यह प्रत्येक प्रोब के लिए **राउंड-ट्रिप टाइम (RTT)** को मिलीसेकंड में दिखाता है।
* यदि कोई राउटर रिस्पॉन्ड नहीं करता है, तो आपको समय के बजाय `*` दिखाई देगा।

### 5. उदाहरण आउटपुट

```text
Tracing route to example.com [93.184.216.34]
over a maximum of 30 hops:

  1     1 ms    <1 ms     1 ms  192.168.1.1
  2    10 ms     9 ms    11 ms  isp-gateway.example.net [203.0.113.1]
  3    25 ms    23 ms    24 ms  core-router.example.net [198.51.100.1]
  ...
  8    45 ms    46 ms    47 ms  example.com [93.184.216.34]
```

### 6. व्यवहार पर नोट्स

* कुछ राउटर ICMP रिस्पांस को ब्लॉक या डिप्रायरिटाइज़ कर सकते हैं, जिससे `* * *` दिखाई देता है।
* **Windows** में `tracert` ICMP इको रिक्वेस्ट का उपयोग करता है, जबकि Linux/Unix पर `traceroute` अक्सर **UDP प्रोब** का उपयोग करता है (हालाँकि इसे बदला जा सकता है)।
* फ़ायरवॉल या ISP फ़िल्टरिंग कभी-कभी रूट के कुछ हिस्सों को छिपा सकती है।

---

👉 क्या आप चाहते हैं कि मैं `tracert` रिजल्ट में **`* * *` या बड़ी देरी जैसे मामलों को इंटरप्रेट करने का तरीका** भी समझाऊं, ताकि आप इसका उपयोग वास्तविक समस्या निवारण के लिए कर सकें?