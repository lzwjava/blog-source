---
audio: false
date: 2025-09-12
generated: true
image: false
lang: hi
layout: post
model: mistralai/mistral-medium-3.1
title: सोर्सग्राफ कोड सर्च गाइड
translated: true
type: note
---

सोर्सग्राफ एक शक्तिशाली कोड सर्च और नेविगेशन टूल है जो डेवलपर्स को कई रिपॉजिटरीज़ में सर्च करने, कोड डिपेंडेंसीज़ को समझने और बड़े कोडबेस को कुशलतापूर्वक नेविगेट करने की सुविधा देता है। यह गाइड **सर्च सिंटैक्स, बेस्ट प्रैक्टिसेज़ और लैंग्वेज-स्पेसिफिक सर्च (Java और Python)** को कवर करती है।

---

## **1. बेसिक सर्च सिंटैक्स**
सोर्सग्राफ फ़िल्टर्स के साथ **लिटरल, रेजेक्स और स्ट्रक्चरल सर्च** को सपोर्ट करता है।

### **1.1. लिटरल सर्च**
एग्ज़ैक्ट टेक्स्ट के लिए सर्च करें:
```
"def calculate_sum"
```

### **1.2. रेजेक्स सर्च**
रेजेक्स के लिए `/.../` का उपयोग करें:
```
/def \w+_sum\(/
```

### **1.3. स्ट्रक्चरल सर्च (बीटा)**
कोड पैटर्न (जैसे, फ़ंक्शन डेफिनिशन) के लिए सर्च करें:
```
type:func def calculate_sum
```

### **1.4. फ़िल्टर्स**
फ़िल्टर्स के साथ सर्च को रिफाइन करें:
- `repo:` – किसी विशिष्ट रिपॉजिटरी में सर्च करें
  ```
  repo:github.com/elastic/elasticsearch "def search"
  ```
- `file:` – विशिष्ट फ़ाइलों में सर्च करें
  ```
  file:src/main/java "public class"
  ```
- `lang:` – किसी विशिष्ट भाषा में सर्च करें
  ```
  lang:python "def test_"
  ```
- `type:` – सिंबल्स (फ़ंक्शन, क्लासेज़, आदि) के लिए सर्च करें
  ```
  type:func lang:go "func main"
  ```

---

## **2. एडवांस्ड सर्च टेक्नीक्स**
### **2.1. बूलियन ऑपरेटर्स**
- `AND` (डिफ़ॉल्ट): `def calculate AND sum`
- `OR`: `def calculate OR def sum`
- `NOT`: `def calculate NOT def subtract`

### **2.2. वाइल्डकार्ड्स**
- `*` – कैरेक्टर्स के किसी भी सीक्वेंस से मेल खाता है
  ```
  "def calculate_*"
  ```
- `?` – एक सिंगल कैरेक्टर से मेल खाता है
  ```
  "def calculate_?"
  ```

### **2.3. केस सेंसिटिविटी**
- डिफ़ॉल्ट रूप से केस-इनसेंसिटिव
- `case:yes` के साथ केस-सेंसिटिव फोर्स करें
  ```
  case:yes "Def Calculate"
  ```

### **2.4. कमेंट्स में सर्च करना**
कमेंट्स में सर्च करने के लिए `patternType:literal` का उपयोग करें:
```
patternType:literal "// TODO:"
```

---

## **3. Java कोड में सर्च करना**
### **3.1. क्लासेज़ ढूँढें**
```
type:symbol lang:java "public class"
```
### **3.2. मेथड्स ढूँढें**
```
type:func lang:java "public void"
```
### **3.3. एनोटेशन्स ढूँढें**
```
lang:java "@Override"
```
### **3.4. इम्पोर्ट्स ढूँढें**
```
lang:java "import org.springframework"
```
### **3.5. एक्सेप्शन हैंडलिंग ढूँढें**
```
lang:java "try {" AND "catch (Exception"
```

---

## **4. Python कोड में सर्च करना**
### **4.1. फ़ंक्शन्स ढूँढें**
```
type:func lang:python "def calculate"
```
### **4.2. क्लासेज़ ढूँढें**
```
type:symbol lang:python "class Calculator"
```
### **4.3. इम्पोर्ट्स ढूँढें**
```
lang:python "import pandas"
```
### **4.4. डेकोरेटर्स ढूँढें**
```
lang:python "@app.route"
```
### **4.5. डॉकस्ट्रिंग्स ढूँढें**
```
lang:python '"""'
```

---

## **5. बेस्ट प्रैक्टिसेज़**
1. परिणामों को सीमित करने के लिए **`lang:` का उपयोग करें**।
2. सटीकता के लिए **फ़िल्टर्स को कंबाइन करें** (`repo:`, `file:`, `type:`)।
3. लचीले पैटर्न मिलान के लिए **रेजेक्स का उपयोग करें**।
4. बार-बार होने वाले क्वेरीज़ के लिए **सर्च सेव करें**।
5. ज़रूरत पड़ने पर **`case:yes` का उपयोग करें**।

---

## **6. उदाहरण क्वेरीज़**

| **लक्ष्य** | **क्वेरी** |
|----------|-----------|
| सभी Java `main` मेथड्स ढूँढें | `lang:java "public static void main"` |
| Python टेस्ट फ़ंक्शन्स ढूँढें | `lang:python "def test_"` |
| Go में TODO कमेंट्स ढूँढें | `lang:go "// TODO:"` |
| Spring Boot कंट्रोलर्स ढूँढें | `lang:java "@RestController"` |
| JS में सभी `if` स्टेटमेंट्स ढूँढें | `lang:javascript "if ("` |

---

## **7. निष्कर्ष**
सोर्सग्राफ की सर्च **शक्तिशाली और लचीली** है, जो कई भाषाओं में **लिटरल, रेजेक्स और स्ट्रक्चरल सर्च** को सपोर्ट करती है। फ़िल्टर्स (`lang:`, `repo:`, `type:`) में महारत हासिल करके, आप बड़े कोडबेस को कुशलतापूर्वक नेविगेट कर सकते हैं।

### **अगले कदम**
- आवर्ती क्वेरीज़ के लिए **सेव्ड सर्चेज़** आज़माएँ।
- **कोड इंटेलिजेंस** (होवर टूलटिप्स, क्रॉस-रिपो नेविगेशन) एक्सप्लोर करें।
- **सोर्सग्राफ एक्सटेंशन्स** (VS Code, JetBrains) का उपयोग करें।

क्या आप किसी विशिष्ट फ़ीचर में गहराई से जाना चाहेंगे? 🚀