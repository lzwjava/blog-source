---
audio: false
generated: true
image: false
lang: hi
layout: post
title: पायथन 3.13 डेव हेडर इंस्टॉल करें
translated: true
type: note
---

यह नई त्रुटि `sentencepiece` बनाने में अंतिम बाधा को उजागर करती है: **Python development headers का गायब होना**।

---

### ❌ त्रुटि:

```
fatal error: Python.h: No such file or directory
```

इसका मतलब है कि compiler को `Python.h` नहीं मिल रहा है, जो Python C extensions को compile करने के लिए आवश्यक है।

---

### ✅ समाधान

आपको Python 3.13 development headers install करने की आवश्यकता है:

#### Ubuntu/Debian के लिए (Python 3.13):

चूंकि Python 3.13 बहुत नया है, यदि आप custom build या `pyenv` का उपयोग कर रहे हैं, तो headers डिफ़ॉल्ट रूप से installed नहीं हो सकते हैं। निम्न में से एक को आज़माएं:

1. **यदि आप system-wide Python 3.13 का उपयोग कर रहे हैं** (deadsnakes या source से installed):

```bash
sudo apt install python3.13-dev
```

2. **यदि आप `pyenv` का उपयोग कर रहे हैं:**

```bash
pyenv install 3.13.0 --with-openssl --enable-shared
```

और सुनिश्चित करें कि आपका virtual environment इस version का उपयोग कर रहा है।

---

### 🔁 फिर से प्रयास करें:

एक बार headers installed हो जाने के बाद, फिर से कोशिश करें:

```bash
pip install sentencepiece --use-pep517
```

---

मुझे बताएं कि क्या आप custom Python install (जैसे, मैन्युअल रूप से compiled, `pyenv`, `conda`, आदि के माध्यम से) का उपयोग कर रहे हैं, और मैं अधिक विशिष्ट मार्गदर्शन दे सकता हूं।