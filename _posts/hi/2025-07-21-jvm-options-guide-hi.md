---
audio: false
generated: true
image: false
lang: hi
layout: post
title: जेवीएम विकल्प वर्गीकृत त्वरित मार्गदर्शिका
translated: true
type: note
---

JVM दुनिया में, **कई विकल्प** हैं जिन्हें आप कॉन्फ़िगर कर सकते हैं, और सटीक सेट JVM इम्प्लीमेंटेशन (हॉटस्पॉट, ओपनजे9, ग्रालवीएम, आदि) और वर्जन पर निर्भर करता है। मोटे तौर पर, JVM विकल्प इन श्रेणियों में आते हैं:

---

## 1. स्टैंडर्ड विकल्प

ये स्थिर, क्रॉस-वर्जन विकल्प हैं जो आधिकारिक तौर पर दस्तावेजित हैं और काम करने की गारंटी है। उदाहरण:

* `-classpath` / `-cp` → क्लासपाथ निर्दिष्ट करें।
* `-Dproperty=value` → सिस्टम प्रॉपर्टी सेट करें।
* `-version` → JVM वर्जन प्रिंट करें।
* `-Xms` / `-Xmx` → प्रारंभिक और अधिकतम हीप साइज़।
* `-ea` → असेर्शन सक्षम करें।

---

## 2. नॉन-स्टैंडर्ड विकल्प (`-X`)

ये JVM-विशिष्ट हैं, स्थिर रहने की गारंटी नहीं है। उदाहरण (हॉटस्पॉट):

* `-Xint` → केवल इंटरप्रेट करें (कोई JIT नहीं)।
* `-Xcomp` → पहले उपयोग पर सभी मेथड को कंपाइल करें।
* `-Xbatch` → बैकग्राउंड कंपाइलेशन अक्षम करें।
* `-Xss512k` → थ्रेड स्टैक साइज़ सेट करें।

---

## 3. एडवांस्ड विकल्प (`-XX`)

ये GC, JIT, और रनटाइम व्यवहार के लिए बारीक ट्यूनिंग प्रदान करते हैं। उदाहरण:

* `-XX:+UseG1GC` → G1 गार्बेज कलेक्टर सक्षम करें।
* `-XX:+PrintGCDetails` → GC एक्टिविटी लॉग करें।
* `-XX:MaxMetaspaceSize=256m` → मेटास्पेस को सीमित करें।
* `-XX:+HeapDumpOnOutOfMemoryError` → OOM होने पर हीप डंप करें।
* `-XX:+UnlockExperimentalVMOptions` → एक्सपेरिमेंटल फ्लैग्स की अनुमति दें।
* `-XX:+UseStringDeduplication` → स्ट्रिंग्स को डीडुप्लिकेट कर मेमोरी उपयोग कम करें।

---

## 4. डायग्नोस्टिक और एक्सपेरिमेंटल

कुछ फ्लैग छिपे होते हैं और अनलॉक करने की आवश्यकता होती है:

* `-XX:+UnlockDiagnosticVMOptions` → डायग्नोस्टिक फ्लैग सक्षम करें।
* `-XX:+UnlockExperimentalVMOptions` → एक्सपेरिमेंटल ट्यूनिंग की अनुमति दें।

फिर आप इस तरह के विकल्पों का उपयोग कर सकते हैं:

* `-XX:+PrintFlagsFinal` → सभी JVM विकल्प और उनके मान सूचीबद्ध करें।
* `-XX:+LogCompilation` → JIT कंपाइलर एक्टिविटी लॉग करें।

---

## 5. JVM विकल्पों के साथ काम करने के टूल्स

* **`java -X`** → आपके JVM बिल्ड के लिए नॉन-स्टैंडर्ड विकल्पों की सूची देता है।
* **`java -XX:+PrintFlagsFinal`** → सभी मान्यता प्राप्त विकल्पों को उनके डिफॉल्ट और वर्तमान मानों के साथ प्रिंट करता है।
* **`jcmd <pid> VM.flags`** → एक चल रही प्रक्रिया के लिए JVM फ्लैग दिखाता है।
* **`jcmd <pid> VM.uptime`**, `jcmd <pid> VM.system_properties` → रनटाइम निरीक्षण।
* **`jps` + `jinfo`** → Java प्रक्रियाओं को सूचीबद्ध करें और JVM विकल्प प्रिंट करें।
* **विज़ुअलवीएम / जेकंसोल** → हीप, GC, और रनटाइम पैरामीटर्स की निगरानी करें।

---

👉 अपने एनवायरनमेंट को एक्सप्लोर करने का एक प्रैक्टिकल तरीका है चलाना:

```bash
java -XX:+PrintFlagsFinal | grep GC
```

सभी GC-संबंधित विकल्प देखने के लिए, या बस:

```bash
java -X
```

उपलब्ध नॉन-स्टैंडर्ड फ्लैग जांचने के लिए।

---

क्या आप चाहेंगे कि मैं आपको **सबसे उपयोगी JVM विकल्पों (GC, मेमोरी, डीबगिंग, परफॉर्मेंस ट्यूनिंग) की एक श्रेणीबद्ध चीट-शीट** दूं ताकि आपको बड़ी `PrintFlagsFinal` सूची को पार्स न करना पड़े?