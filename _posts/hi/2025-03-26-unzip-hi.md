---
audio: false
generated: true
lang: hi
layout: post
title: पायथन में फ़ाइलों को अनज़िप करना
translated: true
type: note
---

पायथन में ज़िप फाइलों को अनज़िप करना:

```python
import zipfile
import os
import argparse

# आर्गुमेंट पार्सिंग सेट करें
parser = argparse.ArgumentParser(description="Unzip a file to the same directory with the same name.")
parser.add_argument('zip_file', help="Path to the zip file")

# कमांड-लाइन आर्गुमेंट्स को पार्स करें
args = parser.parse_args()

# ज़िप फाइल का पथ और उसके डायरेक्टरी का पथ प्राप्त करें
zip_file_path = args.zip_file
directory = os.path.dirname(zip_file_path)

# .zip एक्सटेंशन हटाकर नई डायरेक्टरी/फोल्डर का नाम प्राप्त करें
folder_name = os.path.splitext(os.path.basename(zip_file_path))[0]
destination_folder = os.path.join(directory, folder_name)

# सुनिश्चित करें कि डेस्टिनेशन फोल्डर मौजूद है
os.makedirs(destination_folder, exist_ok=True)

# फाइल को अनज़िप करें
with zipfile.ZipFile(zip_file_path, 'r') as zip_ref:
    zip_ref.extractall(destination_folder)

print(f'Contents extracted to {destination_folder}')

```