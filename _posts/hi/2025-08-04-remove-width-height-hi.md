---
audio: false
generated: true
image: false
lang: hi
layout: post
title: चौड़ाई और ऊँचाई हटाएँ
translated: true
type: note
---

आपके दिख रही त्रुटि:

```
Extra inputs are not permitted [type=extra_forbidden, input_value=1200, input_type=int]
```

इसका कारण `GenerateImagesConfig` में `width` और `height` पास करना है, जो उन **फ़ील्ड्स को सपोर्ट नहीं** करता है। त्रुटि संदेश और Gemini के API डिज़ाइन के अनुसार, `image_size` **पूर्वनिर्धारित enum मानों** में से एक होना चाहिए, और सार्वजनिक Gemini API के लिए (अभी के लिए) `CUSTOM` **सपोर्टेड नहीं** है।

---

### ✅ समाधान

आपको `width` और `height` को **हटा देना चाहिए**, और एक वैध `image_size` enum जैसे `"1024x1024"` या `"512x512"` का उपयोग करना चाहिए।

यहाँ सही किया गया संस्करण है:

```python
from google import genai
from google.genai.types import GenerateImagesConfig
import os

# Vertex AI क्लाइंट सेट अप करें
client = genai.Client(
    vertexai=True,
    project=os.getenv('GOOGLE_CLOUD_PROJECT'),
    location=os.getenv('GOOGLE_CLOUD_LOCATION')
)

image = client.models.generate_images(
    model="imagen-4.0-generate-preview-06-06",
    prompt="A dog reading a newspaper",
    config=GenerateImagesConfig(
        image_size="1024x1024",  # CUSTOM के बजाय अनुमत मान का उपयोग करें
        number_of_images=1,
        safety_filter_level="BLOCK_LOW_AND_ABOVE",
        person_generation="ALLOW_ADULT",
    ),
)

print(f"Created OG image using {len(image.generated_images[0].image.image_bytes)} bytes")
```

---

### 🔍 **कस्टम आकार की छवि** जैसे `1200x630` जनरेट करने के लिए:

वर्तमान Gemini/Imagen सार्वजनिक API **मनमाना छवि आकार सपोर्ट नहीं** करते हैं। यदि आपको 1200×630 चाहिए (जैसे, OG छवियों के लिए), तो आपके विकल्प हैं:

1. छवि जनरेट होने के बाद उसे अपने वांछित आयामों में आकार बदलने/क्रॉप करने के लिए **पोस्ट-प्रोसेस** करें।
2. **एक अलग मॉडल/API** का उपयोग करें जो कस्टम आयाम सपोर्ट करता हो (जैसे, Stability AI का SDXL, Midjourney, या निजी एंडपॉइंट्स)।
3. यदि आप आंतरिक एक्सेस का उपयोग कर रहे हैं (जैसे, Google के अंदर), आंतरिक दस्तावेज़ या अपने Vertex AI अकाउंट मैनेजर से पुष्टि करें।

यदि आप छवि जनरेट होने के बाद उसका आकार बदलने में सहायता चाहते हैं तो मुझे बताएं।