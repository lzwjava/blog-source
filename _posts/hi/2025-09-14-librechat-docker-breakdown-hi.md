---
audio: false
date: 2025-09-14
generated: true
image: false
lang: hi
layout: post
model: openai/gpt-5
title: लिब्रेचैट डॉकर कंपोज़ सेवा विवरण
translated: true
type: note
---

आपने जो पेस्ट किया है वह **LibreChat** और उसकी निर्भरताओं को चलाने के लिए एक `docker-compose.yaml` सेटअप है। मैं इसे स्पष्ट रूप से समझाता हूँ:

---

## उच्च-स्तरीय दृश्य

यह सेटअप **6 सेवाओं (कंटेनरों)** को परिभाषित करता है जो एक साथ काम करती हैं:

1. **LibreChat API/UI (`api`)**
2. **MongoDB (`mongodb`)**
3. **Meilisearch (`meilisearch`)**
4. **Postgres with pgvector (`vectordb`)**
5. **RAG API (`rag_api`)**
6. **Volumes for persistent storage**

ये सभी Docker Compose का उपयोग करके एक साथ जुड़े हुए हैं ताकि आप एक ही `docker-compose up` कमांड से सब कुछ शुरू कर सकें।

---

## सेवा स्पष्टीकरण

### 1. LibreChat API (`api`)

* **कंटेनर नाम:** `LibreChat`
* **इमेज:** `ghcr.io/danny-avila/librechat-dev:latest` (LibreChat का dev build)
* **पोर्ट्स:** `.env` से `${PORT}` को आपकी होस्ट मशीन पर एक्सपोज़ करता है।
* **निर्भरताएँ:** शुरू होने से पहले `mongodb` और `rag_api` का इंतज़ार करता है।
* **एनवायरनमेंट वेरिएबल्स:**

  * `MONGO_URI`: MongoDB के लिए कनेक्शन स्ट्रिंग।
  * `MEILI_HOST`: Meilisearch सेवा की ओर इशारा करता है।
  * `RAG_API_URL`: लोकल RAG API कंटेनर से कनेक्ट होता है।
* **वॉल्यूम:**

  * `.env` फ़ाइल कंटेनर के अंदर माउंट की गई है।
  * `images`, `uploads`, और `logs` को लोकल रूप से स्थायी रूप से संग्रहीत किया जाता है ताकि कंटेनर रीस्टार्ट होने पर वे डेटा खो न जाएँ।

👉 यह वह मुख्य ऐप है जिससे आप इंटरैक्ट करते हैं (LibreChat वेब/API सेवा)।

---

### 2. MongoDB (`mongodb`)

* **कंटेनर नाम:** `chat-mongodb`
* **इमेज:** `mongo` (ऑफिशियल MongoDB इमेज)।
* **उद्देश्य:** चैट डेटा, यूज़र सेशन, कॉन्फ़िग, आदि स्टोर करता है।
* **कमांड:** `mongod --noauth` चलाता है (बिना ऑथेंटिकेशन के)।
* **वॉल्यूम:** `./data-node:/data/db` ताकि आपका डेटाबेस कंटेनर के बाहर स्थायी रहे।

---

### 3. Meilisearch (`meilisearch`)

* **कंटेनर नाम:** `chat-meilisearch`
* **इमेज:** `getmeili/meilisearch:v1.12.3`
* **उद्देश्य:** LibreChat के लिए **फास्ट फुल-टेक्स्ट सर्च** क्षमताएँ प्रदान करता है।
* **एनवायरनमेंट वेरिएबल्स:**

  * `MEILI_NO_ANALYTICS=true` (प्राइवेसी)।
  * `MEILI_MASTER_KEY` को `.env` से लिया जाता है।
* **वॉल्यूम:** `./meili_data_v1.12:/meili_data` स्थायित्व के लिए।

---

### 4. वेक्टर डेटाबेस (`vectordb`)

* **कंटेनर नाम:** `vectordb`
* **इमेज:** `pgvector/pgvector:0.8.0-pg15-trixie` (pgvector एक्सटेंशन के साथ Postgres)।
* **उद्देश्य:** RAG (Retrieval Augmented Generation) के लिए एम्बेडिंग्स स्टोर करता है।
* **एनवायरनमेंट:**

  * `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD`।
* **वॉल्यूम:** `pgdata2:/var/lib/postgresql/data` स्थायित्व के लिए।

---

### 5. RAG API (`rag_api`)

* **कंटेनर नाम:** `rag_api`
* **इमेज:** `ghcr.io/danny-avila/librechat-rag-api-dev-lite:latest`
* **उद्देश्य:** RAG वर्कफ़्लो को हैंडल करता है — डॉक्युमेंट्स रिट्रीव करना और मॉडल रिस्पॉन्सेज़ को एक्सटर्नल नॉलेज के साथ बढ़ाना।
* **एनवायरनमेंट:**

  * `DB_HOST=vectordb` (Postgres वेक्टर डीबी से कनेक्ट होता है)।
  * `RAG_PORT` डिफ़ॉल्ट रूप से `8000` पर सेट है।
* **निर्भर करता है:** `vectordb` पर।
* **Env फ़ाइल:** आपके `.env` का पुन: उपयोग करती है।

---

### 6. वॉल्यूम

* **`pgdata2`** → Postgres/pgvector के लिए स्थायी डेटा।
* अन्य वॉल्यूम जैसे `./meili_data_v1.12`, `./data-node`, और माउंटेड डायरेक्टरीज़ (`images`, `uploads`, `logs`) यह सुनिश्चित करते हैं कि रीस्टार्ट के बाद भी आपका डेटा सुरक्षित रहे।

---

## ये एक साथ कैसे काम करते हैं

1. **LibreChat (UI/API)** वह केंद्रीय सेवा है जिससे आप इंटरैक्ट करते हैं।
2. यह **MongoDB** में **चैट/सेशन डेटा** स्टोर करता है।
3. यह टेक्स्ट सर्च सुविधाओं के लिए **Meilisearch** को क्वेरी करता है।
4. RAG (डॉक्युमेंट रिट्रीवल) के लिए, LibreChat **rag\_api** को कॉल करता है।
5. **RAG API** **vectordb (Postgres + pgvector)** से वेक्टर एम्बेडिंग्स फ़ेच करता है।
6. सब कुछ लोकल वॉल्यूम पर स्थायी रूप से संग्रहीत रहता है, इसलिए कंटेनरों के रीस्टार्ट होने पर डेटा खोता नहीं है।

---

✅ संक्षेप में:

* **MongoDB** → चैट/यूज़र डेटा
* **Meilisearch** → फास्ट कीवर्ड सर्च
* **Postgres+pgvector** → RAG के लिए वेक्टर एम्बेडिंग्स
* **RAG API** → रिट्रीवल ऑग्मेंटेशन लेयर
* **LibreChat** → वह ऐप जो इन सभी को एक साथ जोड़ता है

---

क्या आप चाहते हैं कि मैं आपको यह भी दिखाऊँ कि अनुरोध इन कंटेनरों के माध्यम से कैसे आगे बढ़ते हैं (आप → LibreChat → RAG API → Vector DB) का एक **डायग्राम-स्टाइल फ्लो (टेक्स्टुअल)**? यह इसे और भी स्पष्ट कर सकता है।