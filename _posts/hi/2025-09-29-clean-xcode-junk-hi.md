---
audio: false
generated: true
image: false
lang: hi
layout: post
model: openai/gpt-5
title: 'डिस्क स्थान खाली करें: एक्सकोड जंक की सफाई'
translated: true
type: note
---

अच्छा सवाल है 👍 Xcode (और iOS Simulator) समय के साथ बहुत सारा *कचरा* जमा कर सकता है—बिल्ड कैश, डेराइव्ड डेटा, सिम्युलेटर डिवाइस, डिवाइस सपोर्ट फाइलें, लॉग्स, आदि। इन्हें साफ करने से गीगाबाइट्स जगह मुक्त होती है। यहां एक संरचित तरीका दिया गया है:

---

### 1. डेराइव्ड डेटा साफ करें (बिल्ड कैश)

यह पुराने बिल्ड प्रोडक्ट्स, इंटरमीडिएट्स और इंडेक्सेस को हटाता है।

* **Xcode UI** से:
  `Xcode → Settings → Locations → Derived Data → ऐरो पर क्लिक करें → फोल्डर्स डिलीट करें।`
* **Terminal** से:

  ```bash
  rm -rf ~/Library/Developer/Xcode/DerivedData/*
  ```

---

### 2. सिम्युलेटर डेटा साफ करें

* सिम्युलेटर कंटेंट रीसेट या डिलीट करें:

  ```bash
  xcrun simctl erase all
  ```

  (ऐप्स और डेटा मिटा देता है, लेकिन डिवाइस डेफिनिशन्स रहती हैं)

* सभी अनयूज्ड सिम्युलेटर डिवाइस हटाने के लिए (अगर आपने पुराने iOS वर्जन जमा कर लिए हैं):

  ```bash
  xcrun simctl delete unavailable
  ```

* सभी सिम्युलेटर डिवाइस डिलीट करने के लिए (सावधानी बरतें, सब कुछ हट जाएगा):

  ```bash
  xcrun simctl delete all
  ```

* हैवी डायरेक्टरीज मैन्युअली डिलीट करें:

  ```bash
  rm -rf ~/Library/Developer/CoreSimulator/Devices/*
  ```

---

### 3. पुरानी डिवाइस सपोर्ट फाइलें हटाएं

जब आप रियल डिवाइस कनेक्ट करते हैं, तो Xcode हर iOS वर्जन के लिए डीबगिंग सिंबल डाउनलोड करता है। ये जमा होती रहती हैं।

```bash
rm -rf ~/Library/Developer/Xcode/iOS\ DeviceSupport/*
```

---

### 4. आर्काइव्स साफ करें

"Product → Archive" से बने ऐप आर्काइव्स बहुत जगह लेते हैं।

* Xcode में Organizer से → Archives → पुराने आर्काइव्स डिलीट करें।
* या Terminal के जरिए:

  ```bash
  rm -rf ~/Library/Developer/Xcode/Archives/*
  ```

---

### 5. लॉग्स साफ करें

```bash
rm -rf ~/Library/Developer/Xcode/DocumentationCache/*
rm -rf ~/Library/Developer/Xcode/ios device logs/*
rm -rf ~/Library/Logs/CoreSimulator/*
```

---

### 6. डेराइव्ड इंडेक्सेस और मॉड्यूल कैश साफ करें

```bash
rm -rf ~/Library/Developer/Xcode/DerivedDataModuleCache/*
```

---

### 7. वैकल्पिक: फुल रीसेट

अगर आप Xcode-संबंधित सभी कैशेज पूरी तरह रीसेट करना चाहते हैं:

```bash
rm -rf ~/Library/Developer/Xcode/*
rm -rf ~/Library/Developer/CoreSimulator/*
```

(यह सब कुछ हटा देता है: सिम्युलेटर्स, आर्काइव्स, डेराइव्ड डेटा, आदि। अगली बार Xcode लॉन्च करने पर "फ्रेश इंस्टॉल" जैसा लगेगा।)

---

⚠️ **टिप्स**

* इन कमांड्स को चलाने से पहले हमेशा Xcode और Simulator बंद कर दें।
* यह देखने के लिए कि कौन से फोल्डर्स सबसे बड़े हैं, `du -sh ~/Library/Developer/*` का उपयोग करें।
* कई बार Xcode में `Clean Build Folder` (`⇧⌘K`) काफी होता है, लेकिन डीप क्लीनिंग से ज्यादा जगह बचती है।

---

क्या आप चाहते हैं कि मैं आपको एक **सिंगल स्क्रिप्ट** दूं जो सभी कॉमन कचरे को एक ही बार में साफ कर दे (ऑप्शनल फ्लैग्स के साथ)?