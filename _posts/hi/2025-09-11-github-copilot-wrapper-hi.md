---
audio: false
date: 2025-09-11
generated: true
image: false
lang: hi
layout: post
model: mistralai/mistral-medium-3.1
title: गिटहब कोपायलट के लिए अनाधिकृत पायथन रैपर
translated: true
type: note
---

**GitHub रिपॉजिटरी [ericc-ch/copilot-api](https://github.com/ericc-ch/copilot-api)** **GitHub Copilot के API** (AI-पावर्ड कोड कम्प्लीशन टूल) के साथ इंटरैक्ट करने के लिए एक **अनऑफिशियल Python रैपर** है। यह डेवलपर्स को Copilot के बैकएंड का उपयोग करके प्रोग्रामेटिक रूप से कोड सजेशन, कम्प्लीशन और एक्सप्लेनेशन जनरेट करने की अनुमति देता है, बिना ऑफिशियल VS Code एक्सटेंशन या अन्य IDE इंटीग्रेशन पर निर्भर रहे।

---

## **इसका उपयोग किस लिए है?**
इस API रैपर का उपयोग निम्न के लिए किया जा सकता है:
1.  **कोड कम्प्लीशन जनरेट करना** (VS Code की तरह, लेकिन प्रोग्रामेटिक रूप से)।
2.  कोड स्निपेट्स के लिए **स्पष्टीकरण प्राप्त करना**।
3.  **Copilot को कस्टम एप्लिकेशन में इंटीग्रेट करना** (जैसे, CLI टूल, वेब ऐप, या ऑटोमेटेड वर्कफ़्लो)।
4.  **बिना IDE के Copilot की प्रतिक्रियाओं के साथ प्रयोग करना**।
5.  **रेट लिमिट को बायपास करना** (सावधानी से उपयोग करने पर, हालांकि यह GitHub की सेवा की शर्तों का उल्लंघन कर सकता है)।

⚠️ **चेतावनी:**
- यह एक **अनऑफिशियल** API है, मतलब GitHub कभी भी एक्सेस बदल या ब्लॉक कर सकता है।
- इसका उपयोग **GitHub Copilot की सेवा की शर्तों का उल्लंघन** कर सकता है, अगर बिना अनुमति के ऑटोमेशन या कमर्शियल उद्देश्यों के लिए उपयोग किया जाए।
- **रेट लिमिट लागू** होती हैं (अत्यधिक रिक्वेस्ट के लिए GitHub अकाउंट बैन कर सकता है)।

---

## **इसका उपयोग कैसे करें?**
### **1. इंस्टालेशन**
रिपॉजिटरी को क्लोन करें और डिपेंडेंसीज़ इंस्टॉल करें:
```bash
git clone https://github.com/ericc-ch/copilot-api.git
cd copilot-api
pip install -r requirements.txt
```

### **2. ऑथेंटिकेशन**
आपको एक **GitHub Copilot टोकन** की आवश्यकता है (GitHub पर्सनल एक्सेस टोकन के समान नहीं)।
#### **Copilot टोकन कैसे प्राप्त करें?**
1.  **ब्राउज़र DevTools का उपयोग करके (रिकमेंडेड)**
    - Copilot एनेबल्ड **VS Code** खोलें।
    - **डेवलपर टूल्स** (`F12` या `Ctrl+Shift+I`) खोलें।
    - **नेटवर्क** टैब पर जाएं।
    - `copilot` रिक्वेस्ट्स के लिए फ़िल्टर करें।
    - `https://api.github.com/copilot_internal/v2/token` को रिक्वेस्ट देखें।
    - रिस्पॉन्स से **ऑथराइज़ेशन टोकन** कॉपी करें।

2.  **स्क्रिप्ट का उपयोग करके (अगर उपलब्ध हो)**
    इस रेपो के कुछ फोर्क्स में एक टोकन एक्सट्रैक्टर स्क्रिप्ट शामिल होती है।

#### **Python में टोकन सेट करें**
```python
from copilot import Copilot

copilot = Copilot(
    auth_token="YOUR_COPILOT_TOKEN",  # DevTools से
    proxy="http://your-proxy:port"    # ऑप्शनल (अगर प्रॉक्सी के पीछे हों)
)
```

---

### **3. बेसिक उपयोग के उदाहरण**
#### **कोड कम्प्लीशन प्राप्त करें**
```python
response = copilot.get_completion(
    prompt="def calculate_factorial(n):",
    language="python",
    n=3  # सजेशन की संख्या
)
print(response)
```
**आउटपुट उदाहरण:**
```python
[
    "def calculate_factorial(n):\n    if n == 0:\n        return 1\n    else:\n        return n * calculate_factorial(n-1)",
    "def calculate_factorial(n):\n    result = 1\n    for i in range(1, n+1):\n        result *= i\n    return result",
    "def calculate_factorial(n):\n    return 1 if n <= 1 else n * calculate_factorial(n - 1)"
]
```

#### **कोड एक्सप्लेनेशन प्राप्त करें**
```python
explanation = copilot.explain_code(
    code="def factorial(n): return 1 if n <= 1 else n * factorial(n - 1)",
    language="python"
)
print(explanation)
```
**आउटपुट उदाहरण:**
```
यह एक नंबर `n` के फैक्टोरियल की गणना करने के लिए एक रिकर्सिव फंक्शन है।
- अगर `n` 0 या 1 है, तो यह 1 रिटर्न करता है (बेस केस)।
- अन्यथा, यह `n * factorial(n-1)` रिटर्न करता है, समस्या को छोटी उप-समस्याओं में तोड़ता है।
```

#### **Copilot के साथ चैट करें (अगर सपोर्टेड हो)**
कुछ वर्जन कन्वर्सेशनल इंटरैक्शन की अनुमति देते हैं:
```python
response = copilot.chat(
    message="How do I sort a list in Python?",
    context="python"
)
print(response)
```

---

### **4. एडवांस्ड उपयोग**
#### **कस्टम CLI टूल में उपयोग करना**
```python
import argparse
from copilot import Copilot

parser = argparse.ArgumentParser()
parser.add_argument("--prompt", type=str, required=True)
parser.add_argument("--language", type=str, default="python")
args = parser.parse_args()

copilot = Copilot(auth_token="YOUR_TOKEN")
completions = copilot.get_completion(args.prompt, args.language, n=3)

for i, code in enumerate(completions, 1):
    print(f"--- सजेशन {i} ---")
    print(code)
    print()
```

#### **FastAPI के साथ इंटीग्रेशन (वेब ऐप)**
```python
from fastapi import FastAPI
from copilot import Copilot

app = FastAPI()
copilot = Copilot(auth_token="YOUR_TOKEN")

@app.get("/complete")
def complete_code(prompt: str, language: str = "python"):
    return copilot.get_completion(prompt, language, n=1)
```

---

## **सीमाएँ और जोखिम**
✅ **फायदे:**
- VS Code के बिना काम करता है।
- कस्टम वर्कफ़्लो में इंटीग्रेट किया जा सकता है।
- ऑटोमेशन के लिए उपयोगी (अगर अनुमति हो)।

❌ **नुकसान:**
- **अनऑफिशियल API** (कभी भी ब्रेक हो सकता है)।
- **रेट लिमिट** (GitHub आपका अकाउंट ब्लॉक कर सकता है)।
- **नीतिगत चिंताएं** (उपयोग से पहले GitHub की सेवा की शर्तों की जांच करें)।
- **कोई ऑफिशियल सपोर्ट नहीं**।

---

## **विकल्प**
अगर आप Copilot को प्रोग्रामेटिक रूप से उपयोग करने का **कानूनी** तरीका चाहते हैं:
1.  **ऑफिशियल GitHub Copilot CLI** (अगर भविष्य में रिलीज़ हो)।
2.  **GitHub Copilot Labs** (एक्सपेरिमेंटल एक्सटेंशन)।
3.  **अन्य AI कोड असिस्टेंट** (जैसे, **Amazon CodeWhisperer**, **TabNine**, **Sourcegraph Cody**)।

---

## **अंतिम विचार**
यह रिपॉजिटरी **प्रयोग** और **यह सीखने के लिए कि Copilot आंतरिक रूप से कैसे काम करता है** के लिए उपयोगी है, लेकिन **इसका उपयोग जिम्मेदारी से करें**। अगर आप कोई प्रोडक्शन टूल बना रहे हैं, तो ऑफिशियल API का इंतजार करने या स्वीकृत विकल्पों का उपयोग करने पर विचार करें।

क्या आपको किसी विशिष्ट उपयोग के मामले में मदद चाहिए? 🚀