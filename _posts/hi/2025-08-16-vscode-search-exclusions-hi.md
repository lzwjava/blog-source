---
audio: false
generated: true
image: false
lang: hi
layout: post
title: वीएस कोड में खोज बहिष्करण अनुकूलित करें
translated: true
type: note
---

VS Code में, ग्लोबल फ़ाइल खोज आपके द्वारा कॉन्फ़िगर किए गए **खोज अपवर्जन (search exclusions)** का सम्मान करती है। आप इसे कुछ विशिष्ट फ़ाइलों या फ़ोल्डर्स को अनदेखा करने के लिए कह सकते हैं ताकि वे खोज परिणामों में दिखाई न दें।

यहां मुख्य तरीके दिए गए हैं:

---

### 1. **settings.json** में `search.exclude` का उपयोग करें

अपनी सेटिंग्स खोलें (`⌘,` / `Ctrl+,` → शीर्ष दाएं "Open Settings (JSON)") और जोड़ें:

```json
"search.exclude": {
  "**/node_modules": true,
  "**/dist": true,
  "**/*.log": true
}
```

* कुंजियाँ [ग्लोब पैटर्न](https://code.visualstudio.com/docs/editor/codebasics#_advanced-search-options) हैं।
* `true` का मतलब है "खोज से बाहर करें"।
* उदाहरण:

  * `**/node_modules` → सभी node\_modules फ़ोल्डर्स
  * `**/*.log` → सभी `.log` फ़ाइलें कहीं भी
  * `**/build/**` → बिल्ड फ़ोल्डर्स के अंदर की सब कुछ

---

### 2. `.gitignore` का उपयोग करें

यदि आपके प्रोजेक्ट में एक `.gitignore` फ़ाइल है, तो आप VS Code को खोज के दौरान इसका सम्मान करने के लिए कह सकते हैं:

```json
"search.useIgnoreFiles": true
```

इस तरह, Git द्वारा अनदेखा की गई कोई भी चीज़ खोज परिणामों में नहीं दिखेगी।

---

### 3. `.ignore` का उपयोग करें

आप अपने प्रोजेक्ट रूट में विशेष रूप से एडिटर टूलिंग के लिए एक `.ignore` फ़ाइल (`.gitignore` के समान प्रारूप) भी बना सकते हैं। यदि `search.useIgnoreFiles` सक्षम है तो VS Code इसका सम्मान करेगा।

---

### 4. खोज UI में त्वरित अपवर्जन

जब आप खोज करते हैं, तो परिणामों में किसी फ़ाइल/फ़ोल्डर पर होवर करें और **"Exclude"** पर क्लिक करें। यह अस्थायी रूप से उस पथ को सेटिंग्स में `search.exclude` में जोड़ देता है।

---

👉 तो सबसे आम सेटअप है:

* `.gitignore` में सामान्य प्रोजेक्ट-वाइड अपवर्जन जोड़ें।
* `search.exclude` में एडिटर-विशिष्ट अपवर्जन (जैसे `dist`, `.vscode`) जोड़ें।

---

क्या आप चाहते हैं कि मैं आपको दिखाऊं कि कैसे **केवल खोज के दौरान बाहर करें** लेकिन फिर भी उन फ़ाइलों को Explorer में रखें, या उन्हें Explorer साइड पैनल से भी छुपाएं?