---
audio: false
generated: true
image: false
lang: hi
layout: post
title: पायथन संस्करण विसंगति समस्या का समाधान
translated: true
type: note
---

समझ गया — आपको **Python वर्जन मिसमैच** की समस्या आ रही है।

आपके लॉग दिखा रहे हैं:

* `python3` → **3.10.12**
* `python` → **3.13.7**

तो जब Claude Code (या आपका रिसर्च एजेंट स्क्रिप्ट) `python3` को कॉल करता है, तो वह 3.10 का उपयोग करता है, जिसमें `pyperclip` इंस्टॉल नहीं है। आपने `pyperclip` को 3.13 के एनवायरनमेंट में इंस्टॉल किया है, इसलिए 3.10 इसे नहीं देख पा रहा है।

इसे ठीक करने के कुछ तरीके यहाँ हैं:

---

### 1. Claude Code को वांछित Python के साथ चलाएँ

यदि आप चाहते हैं कि Claude Code हमेशा Python 3.13 का उपयोग करे, तो अपनी शेल में एक्सिक्यूटेबल को स्पष्ट रूप से सेट करें:

```bash
export PYTHON_EXECUTABLE=$(which python)
```

या यदि आप विशेष रूप से `python3` चाहते हैं:

```bash
export PYTHON_EXECUTABLE=$(which python3)
```

फिर जब Claude Code या आपका एजेंट शुरू हो, तो वह उस env var को पढ़ सकता है।

---

### 2. `python3` को Python 3.13 की ओर इंगित करें

Ubuntu पर आप `alternatives` सिस्टम को अपडेट कर सकते हैं:

```bash
sudo update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.10 1
sudo update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.13 2
sudo update-alternatives --config python3
```

फिर Python 3.13 चुनें।

---

### 3. `pyperclip` को Python 3.10 में इंस्टॉल करें

यदि आप `python3` के लिए 3.10 का उपयोग जारी रखना चाहते हैं, तो बस वहाँ इंस्टॉल कर दें:

```bash
python3 -m pip install --user pyperclip
```

यह सुनिश्चित करता है कि `python3` इसे इम्पोर्ट कर सके।

---

### 4. एक वर्चुअल एनवायरनमेंट का उपयोग करें

बेस्ट प्रैक्टिस: अपनी इच्छानुसार Python के साथ एक venv बनाएँ और Claude Code को उसके अंदर चलाएँ:

```bash
python3.13 -m venv ~/.venvs/claude
source ~/.venvs/claude/bin/activate
pip install pyperclip
```

फिर इसी venv के अंदर अपने एजेंट को लॉन्च करें, ताकि यह हमेशा 3.13 का उपयोग करे।

---

👉 आपके लिए सवाल: क्या आप चाहते हैं कि Claude Code **हमेशा Python 3.13 के साथ चले**, या क्या आप 3.10 को `python3` के रूप में रखते हुए सिर्फ गायब मॉड्यूल को ठीक करना ठीक समझते हैं?