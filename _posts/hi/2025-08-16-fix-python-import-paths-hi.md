---
audio: false
generated: true
image: false
lang: hi
layout: post
title: पायथन मॉड्यूल आयात पथ ठीक करें
translated: true
type: note
---

संक्षिप्त जवाब: आपको यहाँ `sys` की आवश्यकता है ताकि `from scripts.create.delete import delete_md` करने से पहले आप Python मॉड्यूल खोज पथ (`sys.path`) को संशोधित कर सकें।

---

### `sys` की आवश्यकता क्यों है

* Python, इम्पोर्ट किए जाने वाले मॉड्यूल/पैकेज `sys.path` में संग्रहीत निर्देशिकाओं की सूची में खोजता है।
* डिफ़ॉल्ट रूप से, इसमें शामिल हैं:

  * वर्तमान स्क्रिप्ट की निर्देशिका।
*   मानक लाइब्रेरी निर्देशिकाएँ।
*   इंस्टॉल की गई site-packages।
* यदि आपकी `scripts/create/delete.py` फ़ाइल उन डिफ़ॉल्ट स्थानों में से एक में नहीं है, तो इम्पोर्ट `ModuleNotFoundError` के साथ विफल हो जाएगा।

इसलिए कोड यह करता है:

```python
sys.path.append(
    os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
)
```

इसका मतलब है:

* वर्तमान फ़ाइल का पूर्ण पथ (`__file__`) लें।
* तीन निर्देशिकाएँ ऊपर जाएँ।
* उस निर्देशिका को `sys.path` में जोड़ें।

उसके बाद, Python `scripts.create.delete` को ऐसे रिज़ॉल्व कर सकता है जैसे कि वह इंस्टॉल किया गया हो या प्रोजेक्ट रूट में हो।

---

### `sys` के बिना

यदि आपके प्रोजेक्ट का लेआउट इस प्रकार है:

```
project/
├── scripts/
│   └── create/
│       └── delete.py
└── tools/
    └── myscript.py   <-- आप यह वाला चला रहे हैं
```

और आप `myscript.py` को सीधे चलाते हैं, तो Python स्वचालित रूप से यह नहीं जान पाएगा कि `project/` रूट है। जब तक कि आप निम्न में से एक न करें, तब तक इम्पोर्ट विफल रहेगा:

* इसे `PYTHONPATH=...` के साथ चलाएँ (जैसे, `PYTHONPATH=.. python myscript.py`), या
* प्रोजेक्ट को पैकेज करके इंस्टॉल करें (`pip install -e .`), या
* कोड में `sys.path.append(...)` के साथ गतिशील रूप से पथ जोड़ें (जैसा कि यहाँ किया गया है)।

---

👉 इसलिए `sys` की आवश्यकता विशुद्ध रूप से **रनटाइम पर इम्पोर्ट पथ में हेरफेर** करने के लिए है, जिससे आप अपने प्रोजेक्ट रूट के सापेक्ष मॉड्यूल इम्पोर्ट कर सकते हैं।

क्या आप चाहते हैं कि मैं आपको `sys.path` को मैन्युअल रूप से बदलने से बचने के लिए कुछ **साफ़-सुथरे विकल्प** भी दिखाऊँ?