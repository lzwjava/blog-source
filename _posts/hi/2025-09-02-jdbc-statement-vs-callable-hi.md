---
audio: false
generated: true
image: false
lang: hi
layout: post
title: जेडीबीसी में स्टेटमेंट बनाम कॉलेबल स्टेटमेंट
translated: true
type: note
---

Java के JDBC (और इस पर आधारित अन्य फ्रेमवर्क जैसे Hibernate या Spring) में, **`Statement`** और **`CallableStatement`** की अवधारणाएं दोनों ही इंटरफेस हैं जिनका उपयोग डेटाबेस को SQL कमांड भेजने के लिए किया जाता है, लेकिन वे अलग-अलग उद्देश्यों की पूर्ति करते हैं।

---

### 1. `Statement`

* **उद्देश्य**: बिना पैरामीटर वाले साधारण SQL क्वेरी को execute करने के लिए उपयोग किया जाता है।
* **उपयोग**: आप एक `Connection` से एक `Statement` ऑब्जेक्ट बनाते हैं और SQL स्ट्रिंग को execute करने के लिए इसका उपयोग करते हैं।
* **सामान्य मेथड्स**:

  * `executeQuery(sql)` → `SELECT` क्वेरीज़ के लिए, एक `ResultSet` रिटर्न करता है।
  * `executeUpdate(sql)` → `INSERT`, `UPDATE`, `DELETE` के लिए।
  * `execute(sql)` → जेनेरिक, `true` रिटर्न करता है अगर रिजल्ट एक `ResultSet` है।

**उदाहरण**:

```java
Connection conn = DriverManager.getConnection(url, user, pass);
Statement stmt = conn.createStatement();
ResultSet rs = stmt.executeQuery("SELECT id, name FROM employees");

while (rs.next()) {
    System.out.println(rs.getInt("id") + " " + rs.getString("name"));
}
```

* **कब उपयोग करें**: स्टेटिक SQL स्टेटमेंट के लिए जिनमें कोई पैरामीटर नहीं होते।
* **कमी**: डायनामिक यूजर इनपुट के साथ उपयोग करने पर SQL इंजेक्शन का कारण बन सकता है।

---

### 2. `CallableStatement`

* **उद्देश्य**: डेटाबेस में **stored procedures** को execute करने के लिए उपयोग किया जाता है।
* **उपयोग**: `Connection.prepareCall()` के साथ बनाया जाता है और **IN**, **OUT**, और **INOUT** पैरामीटर्स के साथ डेटाबेस रूटीन को कॉल करने की अनुमति देता है।
* **सामान्य मेथड्स**:

  * `setInt`, `setString`, आदि → इनपुट पैरामीटर्स सेट करने के लिए।
  * `registerOutParameter` → आउटपुट पैरामीटर्स रजिस्टर करने के लिए।
  * `execute()` या `executeQuery()` → stored procedure को रन करने के लिए।
  * `getInt`, `getString`, आदि → आउटपुट वैल्यू प्राप्त करने के लिए।

**उदाहरण**:

```java
Connection conn = DriverManager.getConnection(url, user, pass);
CallableStatement cstmt = conn.prepareCall("{call getEmployeeName(?, ?)}");

// IN पैरामीटर सेट करें
cstmt.setInt(1, 101);

// OUT पैरामीटर रजिस्टर करें
cstmt.registerOutParameter(2, java.sql.Types.VARCHAR);

// Execute करें
cstmt.execute();

// OUT पैरामीटर प्राप्त करें
String empName = cstmt.getString(2);
System.out.println("Employee name: " + empName);
```

* **कब उपयोग करें**: उन stored procedures/functions के लिए जो डेटाबेस में लॉजिक को encapsulate करते हैं।
* **फायदे**: कॉम्प्लेक्स लॉजिक, मल्टीपल रिजल्ट्स, इनपुट/आउटपुट पैरामीटर्स को सपोर्ट करता है।

---

### मुख्य अंतर (बिना टेबल के)

* `Statement` **स्टेटिक SQL** (ad-hoc queries) के लिए है।
* `CallableStatement` **stored procedures/functions** के लिए है।
* `Statement` सीधे SQL execute करता है, जबकि `CallableStatement` डेटाबेस रूटीन के साथ इंटरैक्ट करता है जो मल्टीपल रिजल्ट सेट और पैरामीटर्स रिटर्न कर सकती हैं।
* `Statement` सरल है लेकिन डायनामिक इनपुट के साथ कम सुरक्षित है, जबकि `CallableStatement` DB में स्टोर पुन: प्रयोज्य लॉजिक को कॉल करने का एक स्ट्रक्चर्ड तरीका प्रदान करता है।

---

👉 अगर आप चाहें, तो मैं यह भी समझा सकता हूं कि **`PreparedStatement`** इन दोनों के बीच कैसे फिट बैठता है (सुरक्षित, पैरामीटराइज्ड SQL, लेकिन stored procedures के लिए नहीं)। क्या आप चाहते हैं कि मैं इसे जोड़ूं?