---
audio: false
generated: true
image: false
lang: hi
layout: post
model: openai/gpt-5
title: लिनक्स फ़ायरवॉल सेटअप गाइड
translated: true
type: note
---

अच्छा सवाल है — Linux में फ़ायरवॉल सेटअप (जैसे **Red Hat** या **Ubuntu**) मुख्य रूप से आपके सिस्टम के आधार पर **`firewalld`** या **`ufw`** का उपयोग करके किया जाता है। नीचे दोनों परिवारों के लिए एक स्पष्ट गाइड दी गई है, ताकि आप सही विकल्प चुन सकें।

---

### 🔹 **Red Hat / CentOS / Fedora** पर (firewalld)

#### 1. जाँचें कि क्या `firewalld` इंस्टॉल और चल रहा है

```bash
sudo systemctl status firewalld
```

अगर यह नहीं चल रहा है:

```bash
sudo systemctl enable firewalld
sudo systemctl start firewalld
```

#### 2. बेसिक उपयोग

एक्टिव ज़ोन जाँचें (ज़ोन इंटरफेस और नियमों को समूहित करते हैं):

```bash
sudo firewall-cmd --get-active-zones
```

वर्तमान ज़ोन के सभी नियम सूचीबद्ध करें:

```bash
sudo firewall-cmd --list-all
```

#### 3. पोर्ट/सर्विसेज़ को अनुमति दें या ब्लॉक करें

एक पोर्ट की अनुमति दें (उदाहरण के लिए पोर्ट 22 पर SSH):

```bash
sudo firewall-cmd --permanent --add-port=22/tcp
```

एक ज्ञात सेवा की अनुमति दें (उदाहरण के लिए HTTP):

```bash
sudo firewall-cmd --permanent --add-service=http
```

किसी एक को ब्लॉक/हटाएँ:

```bash
sudo firewall-cmd --permanent --remove-service=http
```

लागू करने के लिए रीलोड करें:

```bash
sudo firewall-cmd --reload
```

#### 4. नियम सत्यापित करें

```bash
sudo firewall-cmd --list-all
```

---

### 🔹 **Ubuntu / Debian** पर (ufw)

Ubuntu **UFW** (Uncomplicated Firewall) के साथ आता है, जिसे प्रबंधित करना सरल है।

#### 1. UFW सक्षम करें

```bash
sudo ufw enable
```

#### 2. पोर्ट या सेवाओं को अनुमति दें/अस्वीकार करें

SSH की अनुमति दें:

```bash
sudo ufw allow ssh
```

HTTP (पोर्ट 80) की अनुमति दें:

```bash
sudo ufw allow 80/tcp
```

एक विशिष्ट पोर्ट रेंज की अनुमति दें:

```bash
sudo ufw allow 1000:2000/tcp
```

एक सेवा को अस्वीकार करें:

```bash
sudo ufw deny 23/tcp
```

#### 3. स्थिति जाँचें

```bash
sudo ufw status verbose
```

#### 4. आवश्यकता पड़ने पर नियम रीसेट करें

```bash
sudo ufw reset
```

---

### 🔹 दोनों सिस्टम के लिए

कुछ को छोड़कर **सभी आने वाले ट्रैफ़िक को ब्लॉक** करने के लिए, आप यह कर सकते हैं:

1. डिफ़ॉल्ट रूप से सभी आने वाले को अस्वीकार करें

   ```bash
   sudo ufw default deny incoming
   sudo ufw default allow outgoing
   ```

   *( `firewalld` के लिए, इसी तरह के व्यवहार के लिए ज़ोन या ड्रॉप ज़ोन का उपयोग करें)*

2. फिर चुनिंदा पोर्ट्स (जैसे, 22, 80, 443) की अनुमति दें।

---

### 🔹 सुरक्षा टिप

फ़ायरवॉल नियमों को रिमोटली सक्षम करने या बदलने से पहले हमेशा **पहले SSH की अनुमति दें** — नहीं तो आप खुद को लॉक आउट कर लेंगे।

---

क्या आप मुझे एक **उदाहरण मिनिमल फ़ायरवॉल सेटअप** दिखाना चाहेंगे (उदाहरण के लिए एक टिपिकल वेब सर्वर के लिए जो केवल SSH + HTTP + HTTPS की अनुमति देता है)?