---
audio: false
generated: true
lang: hi
layout: post
title: विजुअल स्टूडियो कोड में जावा कंपाइलर कॉन्फ़िगर करें
translated: true
type: note
---

नमस्ते! विंडोज़ पर Visual Studio Code (VSCode) संस्करण 1.96.4 में Java कंपाइलर को कॉन्फ़िगर करने के लिए, खासकर जब आपने पहले से ही Java Extension Pack इंस्टॉल कर लिया है, आपको Java Development Kit (JDK) पथ सेट करना होगा। Java कंपाइलर JDK का हिस्सा है, और VSCode को आपका Java कोड कंपाइल करने के लिए यह जानना ज़रूरी है कि इसे कहाँ ढूंढना है। आपको सेट अप करने में मदद के लिए यहां एक चरण-दर-चरण मार्गदर्शिका दी गई है:

---

### चरण 1: सुनिश्चित करें कि आपके पास JDK इंस्टॉल है
VSCode में कुछ भी कॉन्फ़िगर करने से पहले, सुनिश्चित करें कि आपके विंडोज़ मशीन पर एक JDK (सिर्फ JRE नहीं) इंस्टॉल है। JDK में Java कंपाइलर (`javac`) शामिल होता है। यदि आपने इसे अभी तक इंस्टॉल नहीं किया है, तो आप इसे Oracle, OpenJDK, या Adoptium जैसे प्रदाता से डाउनलोड कर सकते हैं (उदाहरण के लिए, JDK 17 या आपकी प्रोजेक्ट आवश्यकताओं के अनुकूल कोई अन्य संस्करण)। इंस्टॉल करने के बाद, इंस्टॉलेशन पथ नोट कर लें (उदाहरण के लिए, `C:\Program Files\Java\jdk-17.0.1`)।

---

### चरण 2: VSCode सेटिंग्स खोलें
VSCode को आपके JDK का स्थान बताने के लिए, आपको इसकी सेटिंग्स समायोजित करनी होंगी:

- **सेटिंग्स UI के माध्यम से:**
  - सेटिंग्स पैनल खोलने के लिए `Ctrl + ,` दबाएँ।
  - वैकल्पिक रूप से, `File > Preferences > Settings` पर जाएं।
- **settings.json के माध्यम से (वैकल्पिक):**
  - कमांड पैलेट खोलने के लिए `Ctrl + Shift + P` दबाएँ।
  - **"Open Settings (JSON)"** टाइप करें और `settings.json` फ़ाइल को सीधे संपादित करने के लिए इसे चुनें।

---

### चरण 3: `java.home` के साथ JDK पथ सेट करें
Java Extension Pack, कंपाइलेशन और भाषा सुविधाओं (जैसे IntelliSense) के लिए आपके JDK का पता लगाने हेतु `java.home` सेटिंग पर निर्भर करता है। इसे कॉन्फ़िगर करने का तरीका यहां बताया गया है:

- **सेटिंग्स UI में:**
  - सेटिंग्स पैनल में, **"java.home"** के लिए खोजें।
  - "Java: Home" फ़ील्ड में, अपने JDK इंस्टॉलेशन का पूरा पथ दर्ज करें। उदाहरण के लिए:
    ```
    C:\Program Files\Java\jdk-17.0.1
    ```
  - बैकस्लैश (`\`) का उपयोग करें क्योंकि आप विंडोज़ पर हैं, और सुनिश्चित करें कि पथ JDK रूट डायरेक्टरी की ओर इशारा करता हो (इसमें `javac.exe` के साथ एक `bin` फ़ोल्डर होना चाहिए)।

- **settings.json में:**
  - यदि आप `settings.json` संपादित कर रहे हैं, तो यह पंक्ति जोड़ें (पथ को अपने वास्तविक JDK स्थान से बदलें):
    ```json
    "java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
    ```
  - उदाहरण पूर्ण `settings.json`:
    ```json
    {
        "java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
    }
    ```
  - संपादन के बाद फ़ाइल सेव करें।

---

### चरण 4: पथ सत्यापित करें
दोबारा जांच लें कि:
- पथ एक JDK (JRE नहीं) की ओर इशारा करता है। JDK के `bin` फ़ोल्डर में `javac.exe` शामिल होना चाहिए।
- पथ में कोई टाइपो नहीं है, और यह आपके JDK इंस्टॉलेशन स्थान से मेल खाता है (उदाहरण के लिए, `C:\Program Files\Java\jdk-17.0.1`)।

यदि आपको यकीन नहीं है कि आपका JDK कहाँ इंस्टॉल है, तो आप इसे `C:\Program Files\Java` में या इंस्टॉलेशन के दौरान आपके द्वारा चुने गए स्थान पर ढूंढ सकते हैं।

---

### चरण 5: वैकल्पिक - एकाधिक JDK कॉन्फ़िगर करें
यदि आपके पास एकाधिक JDK संस्करण इंस्टॉल हैं और आप उनके बीच स्विच करना चाहते हैं (उदाहरण के लिए, एक प्रोजेक्ट के लिए JDK 8, दूसरे के लिए JDK 17), तो आप `java.configuration.runtimes` सेटिंग का उपयोग कर सकते हैं:

- इसे अपने `settings.json` में जोड़ें:
  ```json
  "java.configuration.runtimes": [
      {
          "name": "JavaSE-1.8",
          "path": "C:\\Program Files\\Java\\jdk1.8.0_291"
      },
      {
          "name": "JavaSE-17",
          "path": "C:\\Program Files\\Java\\jdk-17.0.1",
          "default": true
      }
  ]
  ```
- `default: true` विकल्प आपके कोड को चलाने के लिए डिफ़ॉल्ट रनटाइम सेट करता है। हालाँकि, कंपाइलेशन के लिए, Java एक्सटेंशन मुख्य रूप से `java.home` में निर्दिष्ट JDK का उपयोग करता है।

---

### चरण 6: VSCode को रीलोड या रीस्टार्ट करें
`java.home` सेट करने के बाद, आपको शायद यह करना पड़े:
- VSCode विंडो को रीलोड करें (`Ctrl + Shift + P` दबाएँ, **"Reload Window"** टाइप करें, और इसे चुनें)।
- या परिवर्तनों को प्रभावी होने के लिए VSCode को पूरी तरह से रीस्टार्ट करें।

---

### कंपाइलेशन पर नोट्स
- **एकल Java फ़ाइलें:** यदि आप अलग-अलग `.java` फ़ाइलों के साथ काम कर रहे हैं (बिल्ड टूल्स के बिना), Java Extension Pack का "Language Support for Java(TM) by Red Hat" आपके कोड को कंपाइल और रन करने के लिए `java.home` से JDK का उपयोग करता है। आप "Run" बटन या **"Java: Run"** कमांड का उपयोग कर सकते हैं।
- **बिल्ड टूल्स वाले प्रोजेक्ट्स:** यदि आप Maven, Gradle, या किसी अन्य बिल्ड टूल का उपयोग कर रहे हैं, तो कंपाइलर बिल्ड टूल के कॉन्फ़िगरेशन द्वारा प्रबंधित किया जाता है (उदाहरण के लिए, `JAVA_HOME` एनवायरनमेंट वेरिएबल, Maven के लिए `pom.xml`, या Gradle के लिए `build.gradle`)। हालाँकि, `java.home` सेट करना फिर भी यह सुनिश्चित करता है कि VSCode की भाषा सुविधाएँ सही ढंग से काम करती हैं।

---

### समस्या निवारण
- **पथ संबंधी समस्याएं:** यदि VSCode JDK नहीं ढूंढ पा रहा है, तो `java.home` में पथ को दोबारा जांचें। जब तक ज़रूरी न हो, अतिरिक्त स्पेस या कोट्स से बचें।
- **JDK इंस्टॉल नहीं है:** यदि आपको `javac` गुम होने की त्रुटियाँ मिलती हैं, तो सुनिश्चित करें कि एक JDK इंस्टॉल है।
- **संस्करण अनुकूलता:** सुनिश्चित करें कि आपका JDK संस्करण आपकी प्रोजेक्ट आवश्यकताओं से मेल खाता है (उदाहरण के लिए, JDK 8, 11, 17)।

---

बस! अब आपके पास VSCode में Java कंपाइलर कॉन्फ़िगर होना चाहिए। यदि आपको कोई समस्या आती है या आपके और प्रश्न हैं—जैसे किसी विशिष्ट बिल्ड टूल के साथ काम करना—तो बेझिझक मुझे बताएं, और मैं आगे सहायता करने में प्रसन्न रहूंगा! 😊

---

`maven.terminal.userJavaHome` को true पर सेट करें। आप टर्मिनल में Java संस्करण की जांच कर सकते हैं।