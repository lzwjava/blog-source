---
audio: false
generated: false
lang: hi
layout: post
title: Java Compiler को Visual Studio Code में सेट अप करें
translated: true
---

नमस्ते! विसुअल स्टूडियो कोड (VSCode) वर्जन 1.96.4 पर जावा कम्पाइलर को कन्फ़िगर करने के लिए, खासकर जब आपने पहले जावा एक्सटेंशन पैक इंस्टॉल कर लिया है, आपको जावा डेवलपमेंट किट (JDK) पथ सेट अप करना होगा। जावा कम्पाइलर JDK का हिस्सा है, और VSCode को अपने जावा कोड को कम्पाइल करने के लिए इसे कहाँ ढूंढना है, यह जानना होगा। यहां एक स्टेप-बाइ-स्टेप गाइड है जो आपको सेट अप करने में मदद करेगा:

---

### स्टेप 1: एक JDK इंस्टॉल होना सुनिश्चित करें
VSCode में कुछ भी कन्फ़िगर करने से पहले, सुनिश्चित करें कि आपके विंडोज मशीन पर एक JDK (जस्ट एक JRE नहीं) इंस्टॉल है। JDK में जावा कम्पाइलर (`javac`) शामिल है। अगर आपने अभी तक इसे इंस्टॉल नहीं किया है, तो आप इसे एक प्रोवाइडर जैसे कि ओराकल, ओपनJDK, या एडॉप्टियम (जैसे JDK 17 या आपके प्रोजेक्ट के साथ संगत कोई और वर्जन) से डाउनलोड कर सकते हैं। इंस्टॉल करने के बाद, इंस्टॉलेशन पथ (जैसे, `C:\Program Files\Java\jdk-17.0.1`) नोट करें।

---

### स्टेप 2: VSCode सेटिंग्स खोलें
VSCode को बताने के लिए कि आपके JDK कहाँ है, आपको इसके सेटिंग्स को अदल-बदल करना होगा:

- **सेटिंग्स UI के माध्यम से:**
  - सेटिंग्स पैनल खोलने के लिए `Ctrl + ,` दबाएं।
  - या `File > Preferences > Settings` पर जाएं।
- **settings.json (वैकल्पिक):**
  - कमांड पेलेट खोलने के लिए `Ctrl + Shift + P` दबाएं।
  - **"Open Settings (JSON)"** टाइप करें और इसे चुनें ताकि `settings.json` फ़ाइल को सीधे संपादित कर सकें।

---

### स्टेप 3: `java.home` के साथ JDK पथ सेट करें
जावा एक्सटेंशन पैक `java.home` सेटिंग पर निर्भर करता है ताकि कम्पाइलेशन और भाषा विशेषताओं (जैसे इंटेलिसेंस) के लिए आपके JDK को ढूंढ सके। इसे कन्फ़िगर करने के लिए:

- **सेटिंग्स UI में:**
  - सेटिंग्स पैनल में **"java.home"** खोजें।
  - "Java: Home" फ़ील्ड में, अपने JDK इंस्टॉलेशन का पूरा पथ दर्ज करें। उदाहरण के लिए:
    ```
    C:\Program Files\Java\jdk-17.0.1
    ```
  - आप विंडोज पर हैं, इसलिए बैकस्लैश (`\`) का उपयोग करें, और सुनिश्चित करें कि पथ JDK रूट डायरेक्टरी को इंगित करता है (इसमें `javac.exe` वाला एक `bin` फ़ोल्डर होना चाहिए).

- **settings.json में:**
  - अगर आप `settings.json` संपादित कर रहे हैं, तो इस लाइन को जोड़ें (पथ को अपने वास्तविक JDK स्थान से बदलें):
    ```json
    "java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
    ```
  - पूर्ण उदाहरण `settings.json`:
    ```json
    {
        "java.home": "C:\\Program Files\\Java\\jdk-17.0.1"
    }
    ```
  - संपादित करने के बाद फ़ाइल को सेइव करें।

---

### स्टेप 4: पथ की पुष्टि करें
दोबारा सुनिश्चित करें कि:
- पथ एक JDK (एक JRE नहीं) को इंगित करता है। JDK के `bin` फ़ोल्डर में `javac.exe` होना चाहिए।
- पथ में कोई टाइपोग्राफिकल त्रुटियाँ नहीं हैं, और यह आपके JDK इंस्टॉलेशन स्थान से मिलता है (जैसे, `C:\Program Files\Java\jdk-17.0.1`).

अगर आप अपने JDK को कहाँ इंस्टॉल किया है, यह नहीं जानते हैं, तो आप इसे `C:\Program Files\Java` या इंस्टॉलेशन के दौरान आपने चुना हुआ स्थान में ढूंढ सकते हैं।

---

### स्टेप 5: वैकल्पिक - बहु JDK कन्फ़िगर करें
अगर आपके पास कई JDK वर्जन इंस्टॉल हैं और आप उनमें से एक से दूसरे पर स्विच करना चाहते हैं (जैसे, एक प्रोजेक्ट के लिए JDK 8, दूसरे के लिए JDK 17), तो आप `java.configuration.runtimes` सेटिंग का उपयोग कर सकते हैं:

- अपने `settings.json` में यह जोड़ें:
  ```json
  "java.configuration.runtimes": [
      {
          "name": "JavaSE-1.8",
          "path": "C:\\Program Files\\Java\\jdk1.8.0_291"
      },
      {
          "name": "JavaSE-17",
          "path": "C:\\Program Files\\Java\\jdk-17.0.1",
          "default": true
      }
  ]
  ```
- `default: true` विकल्प कोड को चलाने के लिए डिफ़ॉल्ट रनटाइम सेट करता है। हालाँकि, कम्पाइलेशन के लिए, जावा एक्सटेंशन मुख्य रूप से `java.home` में निर्दिष्ट JDK का उपयोग करता है।

---

### स्टेप 6: VSCode को रिलोड या रीस्टार्ट करें
`java.home` सेट करने के बाद, आपको शायद:
- VSCode विंडो को रिलोड करना होगा (प्रेस `Ctrl + Shift + P`, **"Reload Window"** टाइप करें, और इसे चुनें).
- या VSCode को पूरी तरह से रीस्टार्ट करना होगा ताकि परिवर्तन प्रभावी हों।

---

### कम्पाइलेशन के बारे में नोट्स
- **एकल जावा फ़ाइलें:** अगर आप अलग-अलग `.java` फ़ाइलों (कोई बिल्ड टूल नहीं) के साथ काम कर रहे हैं, तो जावा एक्सटेंशन पैक के "Language Support for Java(TM) by Red Hat" का उपयोग करते हुए, `java.home` से JDK कोड को कम्पाइल और चलाने के लिए उपयोग करता है। आप "Run" बटन या **"Java: Run"** कमांड का उपयोग कर सकते हैं।
- **बिल्ड टूल्स के साथ प्रोजेक्ट:** अगर आप Maven, Gradle, या किसी और बिल्ड टूल का उपयोग कर रहे हैं, तो कम्पाइलर बिल्ड टूल के कन्फ़िगरेशन द्वारा प्रबंधित होता है (जैसे, `JAVA_HOME` पर्यावरण वरियबल, `pom.xml` के लिए Maven, या `build.gradle` के लिए Gradle)। हालाँकि, `java.home` सेट करने से VSCode के भाषा विशेषताओं का सही से काम करना सुनिश्चित होता है।

---

### ट्रबलशूटिंग
- **पथ समस्याएं:** अगर VSCode JDK को नहीं ढूंढ पाता है, तो `java.home` में पथ को दोबारा चेक करें। अनावश्यक स्पेस या क्वोटेशन को छोड़ दें जब तक कि आवश्यक नहीं हो।
- **JDK इंस्टॉल नहीं है:** अगर आपको `javac` के गायब होने के बारे में त्रुटियाँ मिलती हैं, तो सुनिश्चित करें कि एक JDK इंस्टॉल है।
- **वर्जन संगतता:** सुनिश्चित करें कि आपके JDK वर्जन आपके प्रोजेक्ट के आवश्यकताओं से मेल खाता है (जैसे, JDK 8, 11, 17).

---

यह सब है! अब आप VSCode में जावा कम्पाइलर को कन्फ़िगर कर चुके हैं। अगर आप किसी भी समस्या में फंस जाते हैं या और सवाल हैं—जैसे कि किसी खास बिल्ड टूल के साथ काम करना—तो मुझे बता सकते हैं, और मैं आपकी मदद करने के लिए खुशी से तैयार हूँ! 😊

---

`maven.terminal.userJavaHome` को `true` सेट करें। आप टर्मिनल में जावा वर्जन चेक कर सकते हैं।