---
audio: false
generated: true
image: false
lang: hi
layout: post
title: मिनटों में मार्कडाउन से स्लाइड्स
translated: true
type: note
---

Marp CLI आपको Markdown फाइलों को स्लाइड्स (HTML, PDF, PPTX) में बदलने देता है। यह विशेष रूप से तब उपयोगी होता है जब आप न्यूनतम फॉर्मेटिंग प्रयास के साथ सादे Markdown में प्रेजेंटेशन लिखना चाहते हैं।

यहां एक चरण-दर-चरण मार्गदर्शिका दी गई है:

---

### 1. Marp CLI इंस्टॉल करें

आपको पहले Node.js की आवश्यकता होगी।

```bash
npm install -g @marp-team/marp-cli
```

इंस्टॉलेशन जांचें:

```bash
marp --version
```

---

### 2. एक Markdown प्रेजेंटेशन लिखें

एक फाइल बनाएं, उदाहरण के लिए `slides.md`।

```markdown
---
marp: true
theme: default
paginate: true
---

# Hello, Marp!

---

## स्लाइड 2

- बुलेट 1
- बुलेट 2
```

मुख्य बिंदु:

* `---` (YAML फ्रंटमैटर) Marp और कॉन्फ़िगरेशन को सक्षम करता है।
* `---` (कॉन्टेंट के बीच) स्लाइड्स को अलग करता है।

---

### 3. ब्राउज़र में प्रीव्यू करें

ऑटो-रीलोड के साथ एक लोकल सर्वर चलाएं:

```bash
marp -p slides.md
```

अपने ब्राउज़र में [http://localhost:8080](http://localhost:8080) खोलें।

---

### 4. स्लाइड्स एक्सपोर्ट करें

विभिन्न फॉर्मेट जनरेट करें:

* HTML (सेल्फ-कंटेन्ड):

  ```bash
  marp slides.md -o slides.html
  ```

* PDF:

  ```bash
  marp slides.md -o slides.pdf
  ```

* PowerPoint (PPTX):

  ```bash
  marp slides.md -o slides.pptx
  ```

---

### 5. उपयोगी विकल्प

* **बदलावों के लिए वॉच करें** (ऑटो रीबिल्ड):

  ```bash
  marp -w slides.md
  ```

* **थीम सेट करें** (डिफ़ॉल्ट, gaia, uncover, या आपकी कस्टम CSS):

  ```bash
  marp --theme-set ./my-theme.css slides.md -o slides.html
  ```

* **स्पीकर नोट्स शामिल करें**:

  ```markdown
  ---
  marp: true
  ---

  # शीर्षक

  <!--
  ये स्पीकर नोट्स हैं
  -->
  ```

---

### 6. एडिटर इंटीग्रेशन

* VSCode में एक [Marp एक्सटेंशन](https://marketplace.visualstudio.com/items?itemName=marp-team.marp-vscode) है।
* यह Markdown एडिट करते समय लाइव प्रीव्यू दिखाता है।

---

👉 इस सेटअप के साथ, आप एक सादी `.md` फाइल से एक पॉलिश्ड प्रेजेंटेशन तक मिनटों में पहुंच सकते हैं।

क्या आप चाहते हैं कि मैं आपको एक **तैयार-से-उपयोग टेम्पलेट Markdown फाइल** (टाइटल स्लाइड, एजेंडा, कॉन्टेंट और क्लोजिंग स्लाइड के साथ) दूं ताकि आप तुरंत शुरुआत कर सकें?