---
audio: false
generated: true
image: false
lang: hi
layout: post
model: x-ai/grok-4-fast
title: एकाधिक परिभाषा लिंकिंग त्रुटियों को ठीक करना
translated: true
type: note
---

यह एक **मल्टीपल डेफिनिशन लिंकिंग एरर** है — एक क्लासिक C गलती जब **ग्लोबल वेरिएबल्स को हेडर फाइल (`variables.h`)** में डिफाइन किया जाता है, न कि सिर्फ वहाँ *डिक्लेयर* किया जाता है।

आइए समझते हैं कि क्या हो रहा है और इसे कैसे ठीक करें।

---

### 🔍 समस्या

आपकी एरर कहती है:

```
multiple definition of `curl' ...
multiple definition of `nec' ...
multiple definition of `contador' ...
multiple definition of `descargadas' ...
```

इसका मतलब है कि आपकी हेडर फाइल `variables.h` शायद ऐसी दिखती है:

```c
// variables.h (समस्या वाली)
#include <curl/curl.h>

CURL *curl;
int nec;
int contador;
int descargadas;
```

हर `.c` फाइल जो `variables.h` को इन्क्लूड करती है, उन वेरिएबल्स की अपनी **अपनी कॉपी** प्राप्त करती है।
लिंक टाइम पर, लिंकर को एक ही ग्लोबल सिंबल की *कई डेफिनिशन* मिलती हैं, हर ऑब्जेक्ट फाइल से एक।

---

### ✅ सही पैटर्न

आपको हेडर में ग्लोबल्स को **डिक्लेयर** करना होगा, और उन्हें केवल एक बार एक ही `.c` फाइल में **डिफाइन** करना होगा।

#### स्टेप 1: `variables.h` को ठीक करें

इसे इस तरह बदलें:

```c
// variables.h
#ifndef VARIABLES_H
#define VARIABLES_H

#include <curl/curl.h>

extern CURL *curl;
extern int nec;
extern int contador;
extern int descargadas;

#endif
```

#### स्टेप 2: उन्हें एक बार डिफाइन करें (जैसे, `variables.c` में)

`variables.c` बनाएं या अपडेट करें:

```c
#include "variables.h"

CURL *curl = NULL;
int nec = 0;
int contador = 0;
int descargadas = 0;
```

#### स्टेप 3: रीबिल्ड करें

क्लीन करें और रीबिल्ड करें:

```bash
make clean
make
```

---

### 🧠 यह क्यों काम करता है

* `extern` कंपाइलर को बताता है कि वेरिएबल *कहीं और मौजूद है*।
* केवल एक `.c` फाइल ही वास्तव में इसे डिफाइन करती है (इसके लिए मेमोरी अलोकेट करती है)।
* लिंकर के पास तब एक डेफिनिशन और कई रेफरेन्स होते हैं → कोई कॉन्फ्लिक्ट नहीं।

---

अगर आप चाहें, तो मैं आपको इसे और स्पष्ट करने के लिए बिफोर/आफ्टर कोड का एक मिनिमल उदाहरण दिखा सकता हूँ। क्या आप इसे चाहेंगे?