---
audio: false
date: 2025-09-09
generated: true
image: false
lang: hi
layout: post
model: openai/gpt-5
title: विंडोज़ पोर्ट और प्रक्रियाओं का प्रबंधन
translated: true
type: note
---

Windows पर, आप `netstat` का उपयोग करके यह जांच सकते हैं कि कौन से पोर्ट सुन रहे हैं — और किस प्रक्रिया (PID) द्वारा — और फिर उन प्रक्रियाओं को `taskkill` के साथ प्रबंधित या समाप्त कर सकते हैं। Java Spring Boot ऐप्स, Redis, या MySQL जैसे बैकएंड प्रोजेक्ट्स को डीबग करते समय अक्सर इसकी आवश्यकता होती है। यहां बताया गया है कैसे:

---

### 1. सुनने वाले पोर्ट्स की जांच करें

**कमांड प्रॉम्प्ट (cmd)** या **PowerShell** को व्यवस्थापक के रूप में चलाएं:

```bash
netstat -ano | findstr LISTENING
```

* `-a` → सभी कनेक्शन और सुनने वाले पोर्ट दिखाएं
* `-n` → पतों और पोर्ट्स को संख्यात्मक रूप में दिखाएं
* `-o` → PID (प्रक्रिया ID) शामिल करें

उदाहरण आउटपुट:

```
TCP    0.0.0.0:8080     0.0.0.0:0     LISTENING     12345
TCP    0.0.0.0:3306     0.0.0.0:0     LISTENING     6789
```

यहां:

* पोर्ट `8080` का उपयोग PID `12345` द्वारा किया जा रहा है (संभवतः आपका Spring Boot ऐप)।
* पोर्ट `3306` का उपयोग PID `6789` द्वारा किया जा रहा है (MySQL)।

---

### 2. पता लगाएं कि वह PID किस प्रक्रिया से संबंधित है

आप PID को एक प्रक्रिया नाम से मिला सकते हैं:

```bash
tasklist | findstr 12345
```

आउटपुट यह दिखा सकता है:

```
java.exe                  12345 Console                    1     512,000 K
```

इसका मतलब है कि आपका Spring Boot ऐप पोर्ट 8080 पर चल रहा है।

---

### 3. यदि आवश्यक हो तो प्रक्रिया को समाप्त करें

यदि आप उस प्रक्रिया को रोकना चाहते हैं:

```bash
taskkill /PID 12345 /F
```

* `/PID` → प्रक्रिया ID निर्दिष्ट करता है
* `/F` → फोर्स किल (जबरन समाप्त करें)

---

### 4. सामान्य बैकएंड सेवाओं के लिए विशिष्ट पोर्ट

* **Spring Boot (Java ऐप):** 8080, 8081, आदि।
* **MySQL:** 3306
* **Redis:** 6379
* **PostgreSQL:** 5432

इसलिए, यदि आप `netstat` आउटपुट में उन पोर्ट्स को देखते हैं, तो आप जान जाएंगे कि कौन सी सेवा चल रही है।

---

👉 प्रो टिप: आप इन्हें एक कमांड में जोड़ सकते हैं ताकि सीधे पता चल सके कि कोई दिया गया पोर्ट (जैसे, 8080) किस प्रक्रिया द्वारा उपयोग किया जा रहा है:

```bash
netstat -ano | findstr :8080
```

फिर PID लें और इसे `tasklist` के साथ जांचें।

---

क्या आप चाहते हैं कि मैं आपको एक **PowerShell वन-लाइनर** भी दिखाऊं जो सीधे पोर्ट्स को प्रक्रिया नामों से मैप कर दे, बिना अलग से `tasklist` के साथ जांच करने की आवश्यकता के?