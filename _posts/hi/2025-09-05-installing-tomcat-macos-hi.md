---
audio: false
generated: true
image: false
lang: hi
layout: post
title: मैकओएस पर अपाचे टोमकैट स्थापित करना
translated: true
type: note
---

यहाँ **Apache Tomcat को macOS पर इंस्टॉल और रन करने** के लिए एक चरण-दर-चरण गाइड दी गई है:

---

### 1. Java की जाँच करें

Tomcat को Java की आवश्यकता होती है। सत्यापित करें कि यह इंस्टॉल है:

```bash
java -version
```

यदि आपको Java वर्जन (जैसे `1.8` या `11` या उच्चतर) नहीं दिखाई देता है, तो एक इंस्टॉल करें। macOS पर सबसे आसान तरीका [Homebrew](https://brew.sh) के माध्यम से है:

```bash
brew install openjdk@11
```

फिर इसे लिंक करें:

```bash
brew link --force --overwrite openjdk@11
```

---

### 2. Tomcat इंस्टॉल करें (विकल्प A: Homebrew – सबसे आसान)

Homebrew के साथ आप Tomcat को जल्दी से इंस्टॉल कर सकते हैं:

```bash
brew install tomcat
```

यह आमतौर पर नवीनतम वर्जन (जैसे, Tomcat 10) इंस्टॉल करता है।

आप इसे यहाँ पाएँगे:

```
/usr/local/Cellar/tomcat/<version>/libexec
```

या यदि Apple Silicon का उपयोग कर रहे हैं:

```
/opt/homebrew/Cellar/tomcat/<version>/libexec
```

इसे रन करें:

```bash
catalina run
```

---

### 3. Tomcat इंस्टॉल करें (विकल्प B: मैन्युअल डाउनलोड – ऑफिशियल तरीका)

1. [Apache Tomcat वेबसाइट](https://tomcat.apache.org/download-10.cgi) पर जाएँ और नवीनतम स्टेबल रिलीज़ (`.tar.gz` वाली फाइल चुनें) डाउनलोड करें।

2. इसे एक डायरेक्टरी में एक्सट्रैक्ट करें, उदाहरण के लिए:

   ```bash
   tar xvf apache-tomcat-10.*.tar.gz
   mv apache-tomcat-10.* ~/tomcat
   ```

3. Tomcat स्टार्ट करें:

   ```bash
   cd ~/tomcat/bin
   ./startup.sh
   ```

4. इसे बंद करने के लिए:

   ```bash
   ./shutdown.sh
   ```

---

### 4. इंस्टॉलेशन सत्यापित करें

अपना ब्राउज़र खोलें और इस एड्रेस पर जाएँ:

```
http://localhost:8080
```

आपको Tomcat का वेलकम पेज दिखाई देना चाहिए।

---

### 5. (वैकल्पिक) PATH में एड करें

ताकि आप `startup.sh` को कहीं से भी रन कर सकें, इसे अपने `~/.zshrc` या `~/.bashrc` में एड करें:

```bash
export CATALINA_HOME=~/tomcat
export PATH=$CATALINA_HOME/bin:$PATH
```

रिलोड करें:

```bash
source ~/.zshrc
```

---

👉 बस हो गया। अब आपके पास macOS पर Tomcat लोकली रन कर रहा है।

क्या आप चाहते हैं कि मैं आपको सेटअप के बाद **Tomcat पर एक WAR फाइल डिप्लॉय कैसे करें** यह भी दिखाऊँ?