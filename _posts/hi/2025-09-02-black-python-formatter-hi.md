---
audio: false
generated: true
image: false
lang: hi
layout: post
title: ब्लैक पाइथन कोड फॉर्मेटर गाइड
translated: true
type: note
---

PyPI पर `black` पैकेज एक **Python कोड फॉर्मेटर** है। यह आपके Python कोड को स्वचालित रूप से सुसंगत स्टाइल नियमों (PEP 8-संगत, लेकिन अपनी स्वयं की सख्त राय के साथ) का पालन करने के लिए रीफॉर्मेट करता है। इसका उपयोग कैसे करें:

---

### 1. Black इंस्टॉल करें

```bash
pip install black
```

यदि आप सुनिश्चित करना चाहते हैं कि आपको हमेशा नवीनतम स्थिर संस्करण मिले:

```bash
pip install -U black
```

---

### 2. एक फ़ाइल या डायरेक्टरी फॉर्मेट करें

एक फ़ाइल पर Black चलाएँ:

```bash
black my_script.py
```

एक पूरी डायरेक्टरी (पुनरावर्ती रूप से) पर Black चलाएँ:

```bash
black src/
```

डिफ़ॉल्ट रूप से, यह फॉर्मेट किए गए कोड के साथ फाइलों को जगह-जगह लिख देगा।

---

### 3. बदलाव किए बिना फॉर्मेटिंग जांचें

यह देखने के लिए कि क्या आपकी फाइलों को रीफॉर्मेटिंग की आवश्यकता है, `--check` का उपयोग करें:

```bash
black --check my_script.py
```

स्टाइल स्थिरता सुनिश्चित करने के लिए यह CI पाइपलाइनों के लिए उपयोगी है।

---

### 4. सामान्य विकल्प

* `--line-length 88` → लाइन की लंबाई बदलें (डिफ़ॉल्ट: 88)।
* `--diff` → फ़ाइल को संशोधित किए बिना दिखाएँ कि क्या बदलेगा।
* `--skip-string-normalization` → स्ट्रिंग कोट्स न बदलें।
* `--exclude` → कुछ फाइलों/फोल्डरों को छोड़ दें।

उदाहरण:

```bash
black --line-length 100 --diff src/
```

---

### 5. एकीकरण

* **IDE सपोर्ट**: Black PyCharm, VS Code, Vim, Emacs, आदि में एक प्लगइन के रूप में उपलब्ध है।
* **Pre-commit hook**: `.pre-commit-config.yaml` में Black जोड़ें ताकि कमिट से पहले कोड फॉर्मेट हो जाए:

  ```yaml
  repos:
    - repo: https://github.com/psf/black
      rev: stable
      hooks:
        - id: black
  ```
* **CI/CD**: अपनी CI पाइपलाइन में `black --check .` जोड़ें।

---

👉 क्या आप चाहते हैं कि मैं **VSCode या PyCharm में स्वचालित फॉर्मेटिंग** सेटअप करने का तरीका भी दिखाऊँ, ताकि आपको मैन्युअल रूप से `black` चलाने की आवश्यकता न पड़े?