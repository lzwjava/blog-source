---
audio: false
generated: true
image: false
lang: hi
layout: post
title: जैकोको डेस्टफाइल उपयोग और विश्लेषण
translated: true
type: note
---

अच्छा सवाल — आइए समझते हैं कि JaCoCo का `destfile` कैसे काम करता है जब आप एक Java या Spring Boot एप्लिकेशन चलाते हैं।

### 1. `destfile` क्या है

`destfile` JaCoCo Java एजेंट (`jacocoagent.jar`) का एक पैरामीटर है जो एजेंट को बताता है कि **वह अपना एक्सेक्यूशन डेटा कहाँ लिखे**।
डिफ़ॉल्ट रूप से, यह `jacoco.exec` जैसा कुछ होता है, लेकिन आप इसे स्पष्ट रूप से सेट कर सकते हैं:

```bash
-javaagent:jacocoagent.jar=destfile=/path/to/jacoco.exec
```

### 2. क्या लिखा जाता है

जब आपका Spring Boot ऐप JaCoCo एजेंट के साथ चलता है, तो एजेंट आपकी क्लासेस के बाइटकोड को इंस्ट्रूमेंट करता है।
एक्सेक्यूशन के दौरान, JaCoCo कवरेज जानकारी रिकॉर्ड करता है, जैसे कि:

* कौन सी क्लासेस और मेथड्स लोड हुईं
* कौन सी लाइनें एक्सेक्यूट हुईं
* ब्रांच कवरेज (क्या `if`/`else` के दोनों रास्ते एक्सेक्यूट हुए, आदि)

यह रनटाइम कवरेज डेटा मेमोरी में स्टोर होता है और समय-समय पर `destfile` द्वारा परिभाषित फ़ाइल में लिखा जाता है।

तो हाँ — JVM, JaCoCo एजेंट के माध्यम से, फंक्शन और लाइन-लेवल एक्सेक्यूशन की जानकारी उस फ़ाइल में लिखता है।

### 3. विश्लेषण कैसे काम करता है

`.exec` फ़ाइल स्वयं **मानव-पठनीय नहीं होती**। यह सिर्फ बाइनरी कवरेज डेटा होता है।
आपके ऐप के समाप्त होने के बाद, आप आमतौर पर एक रिपोर्ट टास्क (जैसे, Maven `jacoco:report` या Gradle का `jacocoTestReport`) चलाते हैं जो:

* `destfile` डेटा को पढ़ता है
* आपकी कंपाइल की गई `.class` फ़ाइलों और सोर्स कोड को पढ़ता है
* एक HTML, XML, या CSV कवरेज रिपोर्ट जनरेट करता है

यह वह चरण है जहाँ आप प्रतिशत, रंगीन कोड लाइनें, आदि देखते हैं।

### 4. रनटाइम बनाम रिपोर्ट टाइम

* **रनटाइम:** एजेंट एक्सेक्यूशन को मेमोरी में रिकॉर्ड करता है और `destfile` में डंप करता है।
* **रिपोर्ट टाइम:** आप JaCoCo टूल्स (Maven, Gradle, CLI, या Ant) का उपयोग करके उस `.exec` फ़ाइल की व्याख्या करते हैं और पठनीय रिपोर्ट जनरेट करते हैं।

तो `destfile` स्वयं **सिर्फ कच्चा एक्सेक्यूशन डेटा** है — इसका विश्लेषण तब तक नहीं होता जब तक आप रिपोर्ट स्टेप नहीं चलाते।

---

👉 संक्षेप में: हाँ, जब आपका Spring Boot ऐप JaCoCo एजेंट के साथ चलता है, तो मेथड्स और लाइन्स के बारे में कवरेज जानकारी `destfile` में सेव हो जाती है। बाद में, JaCoCo रिपोर्ट टूल्स इसका उपयोग करते हैं (क्लास/सोर्स फ़ाइलों के साथ मिलाकर) मानव-अनुकूल रिपोर्ट बनाने के लिए।

क्या आप चाहते हैं कि मैं आपको यह भी दिखाऊं कि यह Spring Boot के साथ स्टेप-बाय-स्टेप कैसे काम करता है (जैसे, Maven कॉन्फ़िग + रन + रिपोर्ट जनरेशन)?