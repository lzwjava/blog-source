---
audio: false
date: 2025-09-11
generated: true
image: false
lang: hi
layout: post
model: openai/gpt-5
title: मेवेन लाइफसाइकिल और जैकोको को समझना
translated: true
type: note
---

अच्छा सवाल है — यह Maven के *lifecycle phases* और plugins के उनसे बंधने के तरीके पर निर्भर करता है।

### Lifecycle की मूल बातें

Maven के तीन मानक lifecycle हैं: `clean`, `default` (जिसे कभी-कभी *build* भी कहा जाता है), और `site`।
*default* lifecycle के अंदर, महत्वपूर्ण phases क्रम में इस प्रकार हैं:

* `validate`
* `compile`
* `test`
* `package`
* `verify`
* `install`
* `deploy`

प्रत्येक phase में उससे पहले की सभी phases शामिल होती हैं (यदि आप `mvn verify` चलाते हैं, तो Maven `validate` से लेकर `verify` तक सब कुछ execute करता है)। `mvn package` चलाने पर artifact (JAR/WAR) बनाने के बाद ही रुक जाएगा, और यह `verify` को नहीं चलाएगा।

### JaCoCo इसमें क्यों शामिल है

JaCoCo Maven plugin आमतौर पर अपने **`prepare-agent`** goal को configuration के आधार पर `initialize` या `verify` phase से बांधता है।

* यदि यह `verify` से बंधा हुआ है (coverage reports के लिए आम setup), तो `mvn verify` इसे trigger करेगा।
* `mvn package` lifecycle में उतना आगे नहीं जाता, इसलिए `verify` phase (और इस प्रकार JaCoCo का step) छूट जाती है।

इसीलिए जब आप `mvn verify` चलाते हैं तो JaCoCo execute होता दिखता है, लेकिन `mvn package` चलाने पर नहीं।

### व्यावहारिक निष्कर्ष

* **`mvn package`** का उपयोग तब करें जब आपको सिर्फ build artifact चाहिए (तेज़, कोई अतिरिक्त जांच नहीं)।
* **`mvn verify`** का उपयोग तब करें जब आप *integration tests, checks, coverage reports, या QA plugins* चलाना चाहते हैं जो बाद की phases से बंधे होते हैं।

👉 यदि आप चाहते हैं कि JaCoCo `mvn package` के साथ भी हमेशा चले, तो आप अपने `pom.xml` में `prepare-agent` goal को `test` या `package` जैसे किसी पहले phase से फिर से बांध सकते हैं।

क्या आप मुझे एक concrete `pom.xml` snippet दिखाना चाहेंगे जो JaCoCo के `prepare-agent` को `verify` के बजाय `test` पर ही चला दे?