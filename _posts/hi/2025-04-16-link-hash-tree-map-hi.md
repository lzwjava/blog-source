---
audio: false
generated: true
lang: hi
layout: post
title: लिंक्डहैशमैप बनाम ट्रीमैप
translated: true
type: note
---

बढ़िया सवाल! `LinkedHashMap` और `TreeMap` दोनों ही Java में `Map` इंटरफेस के implementations हैं, लेकिन इनमें **ऑर्डरिंग की अलग-अलग रणनीतियाँ** होती हैं और ये **अलग-अलग अंतर्निहित डेटा संरचनाओं** का उपयोग करते हैं।

### 🌳 `TreeMap`
- **ऑर्डरिंग**: कुंजियों के **प्राकृतिक क्रम (natural order)** के अनुसार क्रमबद्ध (या कोई कस्टम `Comparator` दिया गया हो तो उसके अनुसार)।
- **डेटा संरचना**: **रेड-ब्लैक ट्री** (एक प्रकार का सेल्फ-बैलेंसिंग बाइनरी सर्च ट्री) पर आधारित।
- **प्रदर्शन**:
  - get/put/remove के लिए `O(log n)`
  - सॉर्टेड ऑर्डर हमेशा बना रहता है
- **उपयोग का मामला**: जब आपको **कुंजियों का क्रमबद्ध होना** जरूरी हो।

### 🔗 `LinkedHashMap`
- **ऑर्डरिंग**: **डालने के क्रम (insertion order)** को बनाए रखता है (या वैकल्पिक रूप से एक्सेस ऑर्डर, अगर उस तरह कॉन्फ़िगर किया गया हो)।
- **डेटा संरचना**: **हैश टेबल + लिंक्ड लिस्ट** पर आधारित।
- **प्रदर्शन**:
  - get/put/remove के लिए `O(1)` (`HashMap` की तरह)
  - लिंक्ड लिस्ट की वजह से मेमोरी ओवरहेड थोड़ा ज्यादा
- **उपयोग का मामला**: जब आप **एंट्रीज को जिस क्रम में डाला गया है, उस क्रम** की परवाह करते हैं।

---

### ✅ Iterator का क्रम
हाँ, उनके **Iterator का क्रम अलग-अलग** होगा:

| Map का प्रकार    | Iterator का क्रम                     |
|------------------|--------------------------------------|
| `TreeMap`        | कुंजियों द्वारा क्रमबद्ध             |
| `LinkedHashMap`  | डालने का क्रम (या एक्सेस का क्रम)    |

---

### 🔍 उदाहरण

```java
Map<String, Integer> treeMap = new TreeMap<>();
Map<String, Integer> linkedMap = new LinkedHashMap<>();

treeMap.put("banana", 2);
treeMap.put("apple", 3);
treeMap.put("cherry", 1);

linkedMap.put("banana", 2);
linkedMap.put("apple", 3);
linkedMap.put("cherry", 1);

System.out.println("TreeMap:");
for (String key : treeMap.keySet()) {
    System.out.println(key);
}

System.out.println("LinkedHashMap:");
for (String key : linkedMap.keySet()) {
    System.out.println(key);
}
```

**आउटपुट:**
```
TreeMap:
apple
banana
cherry

LinkedHashMap:
banana
apple
cherry
```

अगर आप प्रदर्शन या मेमोरी उपयोग की और विस्तृत जानकारी चाहते हैं तो बताइए।