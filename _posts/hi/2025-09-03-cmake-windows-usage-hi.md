---
audio: false
generated: true
image: false
lang: hi
layout: post
model: openai/gpt-5
title: विंडोज़ पर सीमेक का उपयोग और व्याख्या
translated: true
type: note
---

बढ़िया सवाल! आइए समझते हैं कि **CMake** क्या है, इसे कैसे इस्तेमाल करते हैं—खासकर **Windows** पर, और `cmake --build` जैसे कमांड्स का क्या मतलब होता है।

---

### 🔹 CMake क्या है?
- **CMake** एक क्रॉस-प्लेटफॉर्म बिल्ड सिस्टम जनरेटर है।
- यह खुद प्रोजेक्ट को बिल्ड **नहीं** करता—यह आपके प्लेटफॉर्म और कंपाइलर के लिए उचित बिल्ड फाइल्स (जैसे Visual Studio सॉल्यूशंस, Makefiles, Ninja फाइल्स, आदि) बनाता है।
- फिर, आप उन जनरेट की गई फाइल्स (या CMake के अपने बिल्ड ड्राइवर) का उपयोग करके अपने प्रोजेक्ट को कंपाइल करते हैं।

---

### 🔹 Windows पर CMake इंस्टॉल करना
1. यहाँ से डाउनलोड करें: [https://cmake.org/download/](https://cmake.org/download/)
2. इंस्टॉल करें, और सुनिश्चित करें कि **"Add CMake to system PATH"** चेक किया गया है (या मैन्युअली ऐड करें)।
3. एक टर्मिनल (PowerShell या CMD) में वेरिफाई करें:
   ```sh
   cmake --version
   ```

---

### 🔹 Windows पर आम CMake वर्कफ़्लो

मान लीजिए आपके प्रोजेक्ट की संरचना कुछ ऐसी है:
```
project_root/
 ├─ CMakeLists.txt
 └─ src/
     └─ main.cpp
```

#### 1. एक टर्मिनल खोलें
(अगर MSVC इस्तेमाल कर रहे हैं तो **x64 Native Tools Command Prompt for VS** इस्तेमाल कर सकते हैं, या फिर `MinGW`/`Ninja` के लिए सामान्य PowerShell।)

#### 2. एक बिल्ड डायरेक्टरी बनाएँ
सोर्स और बिल्ड को अलग रखना बेहतर होता है:
```sh
cd project_root
mkdir build
cd build
```

#### 3. बिल्ड सिस्टम फाइल्स जनरेट करें
Visual Studio के साथ उदाहरण:
```sh
cmake .. -G "Visual Studio 17 2022" -A x64
```

Ninja के साथ उदाहरण:
```sh
cmake .. -G Ninja
```

यह स्टेप `../CMakeLists.txt` को पढ़ती है और ज़रूरी बिल्ड डेफिनिशंस जनरेट करती है।

#### 4. प्रोजेक्ट बिल्ड करें
अब आप इस्तेमाल कर सकते हैं:
```sh
cmake --build .
```

- Windows पर Visual Studio के साथ, यह अंदरूनी तौर पर `MSBuild` को कॉल करता है।
- Ninja के साथ, यह `ninja` टूल को कॉल करता है।

आप CMake को **Release** मोड में बिल्ड करने के लिए भी कह सकते हैं:
```sh
cmake --build . --config Release
```
⚠ ध्यान दें: `--config` का इस्तेमाल सिर्फ मल्टी-कॉन्फ़िग जनरेटर्स (Visual Studio, Xcode) के साथ होता है। अगर आप Ninja या Makefiles इस्तेमाल कर रहे हैं, तो आप कॉन्फ़िगरेशन के समय ही स्पेसिफाई करते हैं:
```sh
cmake .. -DCMAKE_BUILD_TYPE=Release
```

#### 5. अपनी एक्जीक्यूटेबल रन करें
बिल्ड करने के बाद, उचित डायरेक्टरी में देखें:
- **Visual Studio** के लिए: `build/Debug/` या `build/Release/`
- **Ninja** के लिए: सीधे `build/` के अंदर

---

### 🔹 आम कमांड्स चीट शीट
```sh
# प्रोजेक्ट कॉन्फ़िगर करें (बिल्ड फाइल्स जनरेट करें)
cmake -S . -B build

# Release बिल्ड टाइप के साथ कॉन्फ़िगर करें (सिंगल-कॉन्फ़िग जनरेटर)
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

# प्रोजेक्ट बिल्ड करें
cmake --build build

# स्पेसिफिक कॉन्फ़िग के साथ बिल्ड करें (Visual Studio/Xcode के लिए)
cmake --build build --config Release

# कंपाइल आउटपुट इंस्टॉल करें (अगर CMakeLists.txt में डिफाइन किया गया हो)
cmake --install build
```

---

✅ **सारांश**:  
- **`cmake ..`** → कॉन्फ़िगर करती है और प्लेटफॉर्म-स्पेसिफिक प्रोजेक्ट/बिल्ड फाइल्स जनरेट करती है।  
- **`cmake --build .`** → चुने गए बिल्ड टूल का इस्तेमाल करके असल में कंपाइल करती है।  
- Windows पर, आप अक्सर **Visual Studio जनरेटर्स** या बेहतर परफॉर्मेंस के लिए **Ninja** इस्तेमाल करेंगे।  

---

क्या आप चाहेंगे कि मैं आपको एक **मिनिमल उदाहरण CMakeLists.txt** दूँ और Windows पर एक साधारण C++ "Hello World" प्रोजेक्ट को बिल्ड करने के चरणों से कदम-दर-कदम गुज़ारूँ?