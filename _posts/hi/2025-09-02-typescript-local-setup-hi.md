---
audio: false
generated: true
image: false
lang: hi
layout: post
title: टाइपस्क्रिप्ट स्थानीय सेटअप गाइड चरण
translated: true
type: note
---

TypeScript को स्थानीय रूप से चलाने के लिए, आप आमतौर पर **npm** और TypeScript कंपाइलर (`tsc`) का उपयोग करेंगे। यहाँ एक चरण-दर-चरण मार्गदर्शिका है:

---

### 1. अपनी प्रोजेक्ट को इनिशियलाइज़ करें

यदि आपके पास पहले से `package.json` नहीं है:

```bash
npm init -y
```

यह एक `package.json` बनाता है ताकि आप dependencies को मैनेज कर सकें।

---

### 2. TypeScript इंस्टॉल करें

इसे एक dev dependency के रूप में इंस्टॉल करें:

```bash
npm install typescript --save-dev
```

यदि आप TypeScript कंपाइलर (`tsc`) को ग्लोबली भी उपलब्ध कराना चाहते हैं:

```bash
npm install -g typescript
```

---

### 3. एक tsconfig.json बनाएँ

एक डिफ़ॉल्ट कॉन्फ़िग फ़ाइल जनरेट करें:

```bash
npx tsc --init
```

यह `tsconfig.json` बनाता है जो नियंत्रित करता है कि TypeScript आपकी फ़ाइलों को कैसे कंपाइल करता है (टारगेट, मॉड्यूल सिस्टम, स्ट्रिक्ट मोड, आदि)।

---

### 4. एक TypeScript फ़ाइल लिखें

उदाहरण `index.ts`:

```ts
function greet(name: string): string {
  return `Hello, ${name}!`;
}

console.log(greet("World"));
```

---

### 5. TypeScript को कंपाइल करें

चलाएँ:

```bash
npx tsc
```

यह `tsconfig.json` के अनुसार सभी `.ts` फ़ाइलों को कंपाइल करता है।

या, केवल एक फ़ाइल को कंपाइल करने के लिए:

```bash
npx tsc index.ts
```

यह एक `.js` फ़ाइल जनरेट करता है (जैसे, `index.js`)।

---

### 6. कंपाइल की गई JavaScript को चलाएँ

Node.js का उपयोग करें:

```bash
node index.js
```

---

### 7. वैकल्पिक: सेव पर ऑटो-कंपाइल

`--watch` के साथ चलाएँ ताकि यह आपके द्वारा एडिट करने पर दोबारा कंपाइल हो:

```bash
npx tsc --watch
```

---

👉 यह है स्टैंडर्ड वर्कफ़्लो:
`npm install` → `tsc` (compile) → `node` (run compiled code).

---

क्या आप चाहते हैं कि मैं आपको यह भी दिखाऊँ कि `ts-node` के साथ TypeScript को सीधे चलाकर **मैन्युअल कंपाइलेशन को कैसे स्किप करें**?