---
audio: false
date: 2025-09-09
generated: true
image: false
lang: hi
layout: post
model: openai/gpt-5
title: जेकिल के लिए साइटमैप फ़ाइलें जनरेट करना
translated: true
type: note
---

एक **sitemap.xml** एक XML फ़ाइल है जो Google और Bing जैसे सर्च इंजनों को आपकी वेबसाइट के पेजों के बारे में बताती है। यह क्रॉलरों को आपकी साइट की संरचना को अधिक कुशलता से खोजने और इंडेक्स करने में मदद करती है। एक Jekyll ब्लॉग के लिए, इसका मतलब है कि सर्च इंजन आपकी सभी पोस्ट्स, पेजों और अनुवादों (जैसे आपका `index-zh.html`, `index-fr.html`, आदि) को ढूंढ सकते हैं, भले ही वे हर जगह लिंक न हों।

### एक साइटमैप क्या करता है

* आपकी साइट के सभी महत्वपूर्ण URL की सूची बनाता है।
* मेटाडेटा प्रदान करता है जैसे अंतिम संशोधित तिथि (`<lastmod>`), परिवर्तन आवृत्ति (`<changefreq>`), और प्राथमिकता (`<priority>`)।
* यह सुनिश्चित करके SEO में सुधार करता है कि क्रॉलर पेजों को न छोड़ें।

### Jekyll के लिए कैसे जनरेट करें

आपके पास कुछ विकल्प हैं:

#### 1. `jekyll-sitemap` प्लगइन का उपयोग करें (सुझाया गया)

1. अपने Jekyll प्रोजेक्ट की `Gemfile` में, जोड़ें:

   ```ruby
   gem 'jekyll-sitemap'
   ```
2. अपने `_config.yml` में, जोड़ें:

   ```yaml
   plugins:
     - jekyll-sitemap
   ```

   (यदि आप GitHub Pages पर हैं, तो `jekyll-sitemap` पहले से ही व्हाइटलिस्टेड है, इसलिए यह सीधे काम करता है।)
3. जब आप बिल्ड करते हैं (`jekyll build` या `make` के माध्यम से यदि आपने इसे सेट अप किया है), Jekyll आपके `_site/` फोल्डर में स्वचालित रूप से एक `sitemap.xml` जनरेट कर देगा।

#### 2. मैन्युअल रूप से एक साइटमैप टेम्पलेट बनाएं

यदि आप अधिक नियंत्रण पसंद करते हैं:

1. प्रोजेक्ट रूट पर एक फ़ाइल बनाएं: `/sitemap.xml`।
2. यह Liquid टेम्पलेट जोड़ें:

   ```xml
   ---
   layout: nil
   ---
   <?xml version="1.0" encoding="UTF-8"?>
   <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
     {% for page in site.pages %}
       {% if page.sitemap != false %}
         <url>
           <loc>{{ site.url }}{{ page.url | replace:'index.html','' }}</loc>
           <lastmod>{{ page.date | date_to_xmlschema }}</lastmod>
         </url>
       {% endif %}
     {% endfor %}
     {% for post in site.posts %}
       <url>
         <loc>{{ site.url }}{{ post.url }}</loc>
         <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
       </url>
     {% endfor %}
   </urlset>
   ```
3. सुनिश्चित करें कि आपके `_config.yml` में `url: "https://yourdomain.com"` है ताकि लिंक सही ढंग से जनरेट हों।

#### 3. सत्यापित करें

* बिल्ड करने के बाद, `_site/sitemap.xml` की जांच करें।
* बेहतर इंडेक्सिंग के लिए इसे Google Search Console में सबमिट करें।

---

👉 चूंकि आपके पास पहले से ही `_config.yml` और `_plugins/` है, सबसे आसान तरीका है `jekyll-sitemap` को सक्षम करना। इस तरह, आपको XML को मैन्युअल रूप से मेंटेन करने की आवश्यकता नहीं होगी।

क्या आप चाहते हैं कि मैं आपके रेपो (आपकी मौजूदा फाइलों के आधार पर) के लिए `_config.yml` में सटीक जोड़ तैयार करूं, ताकि आप इसे सीधे पेस्ट कर सकें?