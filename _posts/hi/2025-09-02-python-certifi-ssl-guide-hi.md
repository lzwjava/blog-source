---
audio: false
generated: true
image: false
lang: hi
layout: post
title: पायथन सर्टिफाई एसएसएल प्रमाणपत्र गाइड
translated: true
type: note
---

PyPI पर **`certifi`** पैकेज एक छोटी लाइब्रेरी है जो **Mozilla के विश्वसनीय रूट प्रमाणपत्रों (root certificates)** का एक बंडल प्रदान करती है। यह आमतौर पर Python लाइब्रेरीज जैसे `requests`, `urllib3`, या किसी भी HTTPS क्लाइंट द्वारा उपयोग की जाती है जिसे SSL/TLS प्रमाणपत्रों को validate करने की आवश्यकता होती है।

इसका उपयोग कैसे करें:

---

### 1. `certifi` इंस्टॉल करें

```bash
pip install certifi
```

---

### 2. CA बंडल का पथ प्राप्त करें

Python में:

```python
import certifi

print(certifi.where())
```

यह CA प्रमाणपत्र बंडल (एक `.pem` फ़ाइल) का पूरा पथ प्रिंट करेगा। उदाहरण के लिए:

```
/usr/local/lib/python3.11/site-packages/certifi/cacert.pem
```

---

### 3. `requests` या `urllib3` के साथ उपयोग करें

```python
import requests
import certifi

response = requests.get("https://example.com", verify=certifi.where())
print(response.status_code)
```

डिफ़ॉल्ट रूप से, `requests` आंतरिक रूप से पहले से ही `certifi` का उपयोग करता है, लेकिन स्पष्ट रूप से `verify=certifi.where()` पास करना यह सुनिश्चित करता है कि यह अपडेट किए गए बंडल का उपयोग करे।

---

### 4. अन्य HTTPS लाइब्रेरीज में उपयोग करें

यदि आप सीधे `urllib3` के साथ काम कर रहे हैं:

```python
import urllib3
import certifi

http = urllib3.PoolManager(
    cert_reqs="CERT_REQUIRED",
    ca_certs=certifi.where()
)

response = http.request("GET", "https://example.com")
print(response.status)
```

---

### 5. सिस्टम-व्यापी उपयोग (वैकल्पिक)

कभी-कभी आप चाह सकते हैं कि कोई ऐप certifi के CA बंडल का उपयोग करे:

```bash
export SSL_CERT_FILE=$(python -m certifi)
```

---

👉 संक्षेप में:

* `pip install certifi` के साथ इंस्टॉल करें
* प्रमाणपत्र बंडल पथ प्राप्त करने के लिए `certifi.where()` का उपयोग करें
* इसे उन लाइब्रेरीज को पास करें जिन्हें प्रमाणपत्र validation की आवश्यकता होती है

---

क्या आप चाहते हैं कि मैं आपको यह भी दिखाऊं **कि अपने संपूर्ण Python वातावरण को डिफ़ॉल्ट रूप से certifi के प्रमाणपत्रों का उपयोग कैसे कराएं**, ताकि आपको प्रत्येक लाइब्रेरी को अलग से कॉन्फ़िगर करने की आवश्यकता न पड़े?