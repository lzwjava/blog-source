---
audio: false
generated: true
image: false
lang: hi
layout: post
title: JMX पोर्ट प्रक्रिया खोजें और समाप्त करें
translated: true
type: note
---

ठीक है — आप जांचना चाहते हैं कि Windows पर किस Java प्रोग्राम ने **JMX remote agent** को पोर्ट `9091` पर खोला है, और फिर जरूरत पड़ने पर उसे बंद करना चाहते हैं। यहां बताया गया है कि आप इसे कदम दर कदम कैसे कर सकते हैं:

---

### 1. पोर्ट 9091 का उपयोग कर रही प्रक्रिया (Process) ढूंढें

**Command Prompt** खोलें (अगर संभव हो तो Administrator के रूप में), और चलाएँ:

```cmd
netstat -ano | findstr :9091
```

यह कुछ इस तरह दिखाएगा:

```
  TCP    0.0.0.0:9091    0.0.0.0:0    LISTENING    12345
```

* अंतिम कॉलम (`12345` यहाँ) **PID (Process ID)** है।

---

### 2. पहचानें कि वह PID किस प्रोग्राम से संबंधित है

अब PID को वास्तविक प्रोग्राम से मैप करें:

```cmd
tasklist /FI "PID eq 12345"
```

यह कुछ इस तरह दिखाएगा:

```
Image Name                     PID   Session Name        Mem Usage
========================= ======== ================ ============
java.exe                    12345 Console           200,000 K
```

अगर यह एक Java प्रक्रिया है, तो आप और भी अधिक विवरण प्राप्त कर सकते हैं:

```cmd
wmic process where processid=12345 get CommandLine
```

यह JVM को लॉन्च करने के लिए इस्तेमाल किए गए **command line** को प्रिंट करेगा — आपको JMX flags (`-Dcom.sun.management.jmxremote.port=9091`) दिखाई देंगे, जो पुष्टि करते हैं कि यह JMX remote agent ही है।

---

### 3. प्रक्रिया को बंद करें

अगर आपने पुष्टि कर ली है कि इसे रोकना सुरक्षित है, तो आप इसे बंद कर सकते हैं:

```cmd
taskkill /PID 12345 /F
```

* `/PID` प्रक्रिया ID निर्दिष्ट करता है।
* `/F` जबरन समाप्ति करता है।

---

### 4. PowerShell के साथ विकल्प

अगर आप PowerShell पसंद करते हैं, तो आप चला सकते हैं:

```powershell
Get-Process -Id (Get-NetTCPConnection -LocalPort 9091).OwningProcess
```

सीधे बंद करने के लिए:

```powershell
Stop-Process -Id (Get-NetTCPConnection -LocalPort 9091).OwningProcess -Force
```

---

🔎 सारांश:

1. `netstat -ano | findstr :9091` → PID प्राप्त करें
2. `tasklist /FI "PID eq <PID>"` → देखें कौन सा प्रोग्राम है
3. `wmic process where processid=<PID> get CommandLine` → JVM flags देखें
4. `taskkill /PID <PID> /F` → बंद करें

---

क्या आप चाहते हैं कि मैं आपको यह भी दिखाऊं कि JMX agent ports के लिए **एक साथ कई JVMs कैसे चेक करें** (ताकि आप देख सकें कि क्या अन्य पोर्ट्स जैसे 9090, 9999, आदि पर चल रही हैं)?