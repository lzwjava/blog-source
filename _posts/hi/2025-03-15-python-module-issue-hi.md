---
audio: false
generated: true
lang: hi
layout: post
title: पायथन मॉड्यूल आयात त्रुटि
translated: true
type: note
---

जब आप यह कमांड चलाकर अपनी Python स्क्रिप्ट रन करते हैं:

```bash
lzwjava@anonymous lzwjava.github.io % python scripts/nytimes/nytimes.py
```

आपको निम्न त्रुटि का सामना करना पड़ता है:

```
Traceback (most recent call last):
  File "/Users/lzwjava/projects/lzwjava.github.io/scripts/nytimes/nytimes.py", line 5, in <module>
    from ..llm.test_mistral import call_mistral_api
ImportError: attempted relative import with no known parent package
```

यह त्रुटि इसलिए होती है क्योंकि आप स्क्रिप्ट को कैसे चला रहे हैं और Python रिलेटिव इम्पोर्ट्स को कैसे हैंडल करता है। आइए इसे समझते हैं और एक समाधान प्रदान करते हैं।

---

#### समस्या को समझना

त्रुटि `ImportError: attempted relative import with no known parent package` तब उत्पन्न होती है जब आप किसी **रिलेटिव इम्पोर्ट** (जैसे, `from ..llm.test_mistral import call_mistral_api`) का उपयोग एक ऐसी स्क्रिप्ट में करते हैं जिसे Python एक पैकेज का हिस्सा नहीं मानता। Python में:

- **रिलेटिव इम्पोर्ट** डॉट नोटेशन (जैसे, `..`) का उपयोग करते हैं ताकि मॉड्यूल को करंट मॉड्यूल की पैकेज हायरार्की में स्थिति के सापेक्ष इम्पोर्ट किया जा सके। यहाँ, `..llm.test_mistral` का मतलब है "वर्तमान मॉड्यूल से दो स्तर ऊपर जाएं, फिर `llm` पैकेज में जाएं, और `test_mistral` से `call_mistral_api` को इम्पोर्ट करें।"
- जब आप सीधे `python scripts/nytimes/nytimes.py` का उपयोग करके कोई स्क्रिप्ट चलाते हैं, तो Python इसे **मुख्य मॉड्यूल** (जिसका `__name__ = "__main__"` होता है) के रूप में मानता है और इसे कोई पैकेज कॉन्टेक्स्ट नहीं देता। पैकेज कॉन्टेक्स्ट के बिना, Python रिलेटिव इम्पोर्ट्स को रिज़ॉल्व नहीं कर सकता क्योंकि उसे नहीं पता होता कि "पैरेंट पैकेज" क्या है।

आपके मामले में:
- स्क्रिप्ट `nytimes.py` का पथ `/Users/lzwjava/projects/lzwjava.github.io/scripts/nytimes/nytimes.py` है।
- रिलेटिव इम्पोर्ट `from ..llm.test_mistral import call_mistral_api` एक डायरेक्टरी स्ट्रक्चर का सुझाव देता है जो कुछ इस प्रकार है:

```
lzwjava.github.io/
    scripts/
        nytimes/
            nytimes.py
        llm/
            test_mistral.py
```

- हालाँकि, क्योंकि आप `nytimes.py` को सीधे चला रहे हैं, Python `scripts` या `nytimes` को पैकेज के रूप में नहीं पहचानता, जिसके कारण इम्पोर्ट फेल हो जाता है।

---

#### समाधान

इसे ठीक करने के लिए, आपको Python के `-m` फ्लैग का उपयोग करके स्क्रिप्ट को उसके पैकेज स्ट्रक्चर के भीतर एक मॉड्यूल के रूप में चलाना होगा। यह पैकेज हायरार्की को बनाए रखता है और रिलेटिव इम्पोर्ट्स को सही ढंग से काम करने की अनुमति देता है। यहाँ बताया गया है कि कैसे:

1. **`scripts` की पैरेंट डायरेक्टरी में नेविगेट करें**:
   - आपके द्वारा दिए गए कमांड के आधार पर, आप पहले से ही `lzwjava.github.io` डायरेक्टरी में हैं, जिसमें `scripts` फ़ोल्डर है।

2. **`-m` फ्लैग के साथ स्क्रिप्ट चलाएँ**:
   - निम्न कमांड का उपयोग करें:

   ```bash
   python -m scripts.nytimes.nytimes
   ```

   - **स्पष्टीकरण**:
     - `scripts.nytimes.nytimes` `scripts` पैकेज के अंदर `nytimes` सबपैकेज में स्थित `nytimes.py` मॉड्यूल को संदर्भित करता है।
     - `-m` फ्लैग Python को निर्देश देता है कि निर्दिष्ट मॉड्यूल को एक स्क्रिप्ट के रूप में execute करते हुए उसके पैकेज कॉन्टेक्स्ट को बनाए रखें।
     - यह रिलेटिव इम्पोर्ट `from ..llm.test_mistral import call_mistral_api` को सही ढंग से `scripts.llm.test_mistral` पर रिज़ॉल्व करने की अनुमति देता है।

---

#### आवश्यकताएँ

इस समाधान के काम करने के लिए, आपके डायरेक्टरी स्ट्रक्चर को कुछ इस तरह दिखना चाहिए:

```
lzwjava.github.io/
    scripts/
        __init__.py       # 'scripts' को एक पैकेज के रूप में चिह्नित करता है
        nytimes/
            __init__.py   # 'nytimes' को एक सबपैकेज के रूप में चिह्नित करता है
            nytimes.py    # आपकी स्क्रिप्ट
        llm/
            __init__.py   # 'llm' को एक सबपैकेज के रूप में चिह्नित करता है
            test_mistral.py  # call_mistral_api को contain करता है
```

- **`__init__.py` फाइलें**: ये फाइलें (जो खाली हो सकती हैं) Python में डायरेक्टरीज़ को पैकेज के रूप में परिभाषित करने के लिए आवश्यक हैं। यदि इनमें से कोई भी गायब है, तो उन्हें जोड़ें:
  - एक खाली `scripts/__init__.py` फ़ाइल बनाएँ
  - एक खाली `scripts/nytimes/__init__.py` फ़ाइल बनाएँ
  - एक खाली `scripts/llm/__init__.py` फ़ाइल बनाएँ

यदि ये फाइलें पहले से मौजूद हैं, तो कमांड `python -m scripts.nytimes.nytimes` समस्या को हल कर देनी चाहिए।

---

#### मूल कमांड क्यों विफल होती है

जब आप चलाते हैं:

```bash
python scripts/nytimes/nytimes.py
```

- Python `nytimes.py` को एक स्टैंडअलोन स्क्रिप्ट के रूप में execute करता है।
- डायरेक्टरी `scripts/nytimes` (जहाँ `nytimes.py` स्थित है) को `sys.path` में जोड़ दिया जाता है, लेकिन `scripts` को स्वयं एक पैकेज के रूप में नहीं माना जाता।
- परिणामस्वरूप, रिलेटिव इम्पोर्ट `from ..llm.test_mistral` विफल हो जाता है क्योंकि Python को नेविगेट करने के लिए कोई पैकेज हायरार्की नहीं मिलती।

`python -m` का उपयोग करके इसे ठीक किया जाता है क्योंकि यह स्पष्ट रूप से स्क्रिप्ट को `scripts` पैकेज स्ट्रक्चर के भीतर चलाता है।

---

#### वैकल्पिक वर्कअराउंड (अनुशंसित नहीं)

यदि आप किसी कारण से `-m` फ्लैग का उपयोग नहीं कर सकते, तो आप Python पथ को एडजस्ट करके **एब्सोल्यूट इम्पोर्ट्स** का उपयोग करने के लिए `nytimes.py` को संशोधित कर सकते हैं। `nytimes.py` के शीर्ष पर निम्न जोड़ें:

```python
import sys
import os
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..', '..')))
from scripts.llm.test_mistral import call_mistral_api
```

- यह `lzwjava.github.io` को `sys.path` में जोड़ता है, जिससे एब्सोल्यूट इम्पोर्ट काम कर पाता है।
- **नुकसान**: यह तरीका `-m` का उपयोग करने की तुलना में कम पोर्टेबल है और इसे Python में पैकेजों को हैंडल करने के मानक तरीके की तुलना में एक हैक माना जाता है।

---

#### प्रॉक्सी सेटिंग्स नोट

आपके आउटपुट में शामिल है:

```
🚀 **Proxy Settings Detected:**
   - HTTP_PROXY: http://127.0.0.1:7890
   - HTTPS_PROXY: http://127.0.0.1:7890
```

इससे पता चलता है कि आपकी स्क्रिप्ट या उसके द्वारा उपयोग किया जाने वाला कोई लाइब्रेरी HTTP अनुरोधों के लिए प्रॉक्सी सेटिंग्स का पता लगा रहा है। यह इम्पोर्ट त्रुटि से असंबंधित है और अभी के लिए इग्नोर किया जा सकता है। एक बार इम्पोर्ट समस्या ठीक हो जाने के बाद, स्क्रिप्ट को किसी भी नेटवर्क-संबंधित कोड पर आगे बढ़ना चाहिए।

---

#### अंतिम चरण

1. **समाधान आज़माएँ**:
   - `lzwjava.github.io` डायरेक्टरी से, यह चलाएँ:
     ```bash
     python -m scripts.nytimes.nytimes
     ```

2. **यदि यह विफल होता है**:
   - `scripts`, `nytimes`, और `llm` डायरेक्टरीज़ में गुम `__init__.py` फाइलों की जाँच करें और यदि आवश्यक हो तो उन्हें जोड़ें।
   - सत्यापित करें कि `test_mistral.py` फ़ाइल `scripts/llm/` में मौजूद है और इसमें `call_mistral_api` फ़ंक्शन है।

इससे `ImportError` हल हो जाना चाहिए और आपकी स्क्रिप्ट को सफलतापूर्वक चलने देना चाहिए।