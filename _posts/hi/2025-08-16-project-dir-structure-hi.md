---
audio: false
generated: true
image: false
lang: hi
layout: post
title: प्रोजेक्ट डायरेक्टरी संरचना की सर्वोत्तम प्रथाएँ
translated: true
type: note
---

अच्छा सवाल है।

सामान्य परंपरा यह है:

* **`scripts/`** आपके एप्लिकेशन कोड के लिए है (ऐसी चीज़ें जिन्हें आप प्रोडक्शन में या अपने प्रोजेक्ट के हिस्से के रूप में चलाना चाहते हैं)।
* **`tests/`** आपके टेस्ट कोड के लिए है (ऐसी चीज़ें जो आपकी स्क्रिप्ट्स/मॉड्यूल की शुद्धता को सत्यापित करती हैं)।

इसलिए, यदि आप एक नई टेस्ट फ़ाइल जोड़ रहे हैं, तो उसे सामान्य रूप से `scripts/` के बजाय **`tests/llm/`** के अंतर्गत रखा जाना चाहिए।

उदाहरण के लिए:

```
scripts/
  llm/
    main.py
    utils.py
tests/
  llm/
    test_main.py
    test_utils.py
```

इस तरह, आपके टेस्ट आपके इम्प्लीमेंटेशन से अलग रहते हैं, और `pytest` जैसे टूल आसानी से उन्हें खोज सकते हैं।

👉 अब आप कुछ इस तरह जोड़ सकते हैं:

```python
# tests/llm/test_basic.py
import pytest
from scripts.llm import utils

def test_addition():
    assert utils.add(2, 3) == 5
```

फिर चलाएं:

```bash
pytest tests/llm/
```

क्या आप चाहेंगे कि मैं आपको एक `conftest.py` सेटअप करने का तरीका भी दिखाऊं ताकि आप अपनी `tests/llm/` फ़ाइलों में फिक्सचर्स साझा कर सकें?