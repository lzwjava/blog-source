---
audio: false
generated: true
lang: hi
layout: post
title: विंडोज़ में प्रॉक्सी जाँच लागू करना
translated: true
type: note
---

Git-Bash और PowerShell में प्रॉक्सी चेक लागू करने के लिए, आपको प्रत्येक शेल में ऐसे तंत्र स्थापित करने होंगे जो नेटवर्क-संबंधी कमांड्स के निष्पादित होने का पता लगा सकें और कॉन्फ़िगर किए गए प्रॉक्सी सेटिंग्स दिखा सकें। नीचे Git-Bash और PowerShell दोनों के लिए चरण और कोड दिए गए हैं। "टर्मिनल" का उल्लेख संभवतः Windows Terminal को इंगित करता है जो इन शेल्स को होस्ट करता है, इसलिए हम Git-Bash और PowerShell के कार्यान्वयन पर ध्यान केंद्रित करेंगे।

---

### **Git-Bash के लिए**

Git-Bash, Windows पर Bash का एक एमुलेशन है, और हम प्रत्येक कमांड निष्पादन से पहले एक फ़ंक्शन चलाने के लिए `DEBUG` ट्रैप का उपयोग कर सकते हैं। लक्ष्य यह जांचना है कि क्या कमांड नेटवर्क-संबंधी है और यदि प्रॉक्सी सेटिंग्स सेट हैं, तो उन्हें प्रदर्शित करना है।

#### **चरण:**

1. **नेटवर्क-संबंधी कमांड्स की सूची परिभाषित करें।**
2. **प्रॉक्सी सेटिंग्स प्रदर्शित करने के लिए एक फ़ंक्शन बनाएँ।**
3. **कमांड और प्रॉक्सी सेटिंग्स की जांच करने के लिए एक फ़ंक्शन बनाएँ।**
4. **प्रत्येक कमांड से पहले जाँच चलाने के लिए `DEBUG` ट्रैप सेट करें।**
5. **मैन्युअल रूप से प्रॉक्सी सेटिंग्स प्रदर्शित करने के लिए एक `checkproxy` फ़ंक्शन परिभाषित करें।**
6. **सभी कॉन्फ़िगरेशन को `.bashrc` फ़ाइल में जोड़ें।**

#### **कार्यान्वयन:**

अपनी `~/.bashrc` फ़ाइल में निम्नलिखित कोड जोड़ें (यदि यह मौजूद नहीं है तो इसे बनाएँ):

```bash
# नेटवर्क-संबंधी कमांड्स की सूची
network_commands=(
    "gpa"
    "git"
    "ssh"
    "scp"
    "sftp"
    "rsync"
    "curl"
    "wget"
    "apt"
    "yum"
    "dnf"
    "npm"
    "yarn"
    "pip"
    "pip3"
    "gem"
    "cargo"
    "docker"
    "kubectl"
    "ping"
    "traceroute"
    "netstat"
    "ss"
    "ip"
    "ifconfig"
    "dig"
    "nslookup"
    "nmap"
    "telnet"
    "ftp"
    "nc"
    "tcpdump"
    "adb"
    "bundle"
    "brew"
    "cpanm"
    "bundle exec jekyll"
    "make"
    "python"
    "glcoud"
)

# प्रॉक्सी सेटिंग्स प्रदर्शित करने का फ़ंक्शन
display_proxy() {
    echo -e "🚀 **Proxy Settings Detected:**"
    [ -n "$HTTP_PROXY" ] && echo "   - HTTP_PROXY: $HTTP_PROXY"
    [ -n "$http_proxy" ] && echo "   - http_proxy: $http_proxy"
    [ -n "$HTTPS_PROXY" ] && echo "   - HTTPS_PROXY: $HTTPS_PROXY"
    [ -n "$https_proxy" ] && echo "   - https_proxy: $https_proxy"
    [ -n "$ALL_PROXY" ] && echo "   - ALL_PROXY: $ALL_PROXY"
    [ -n "$all_proxy" ] && echo "   - all_proxy: $all_proxy"
    echo ""
}

# यह जांचने का फ़ंक्शन कि कमांड नेटवर्क-संबंधी है और प्रॉक्सी सेट हैं
proxy_check() {
    local cmd
    # कमांड का पहला शब्द निकालें
    cmd=$(echo "$BASH_COMMAND" | awk '{print $1}')
    
    for network_cmd in "${network_commands[@]}"; do
        if [[ "$cmd" == "$network_cmd" ]]; then
            # जांचें कि क्या कोई प्रॉक्सी पर्यावरण चर सेट हैं
            if [ -n "$HTTP_PROXY" ] || [ -n "$http_proxy" ] || \
               [ -n "$HTTPS_PROXY" ] || [ -n "$https_proxy" ] || \
               [ -n "$ALL_PROXY" ] || [ -n "$all_proxy" ]; then
                display_proxy
            fi
            break
        fi
    done
}

# प्रत्येक कमांड से पहले proxy_check चलाने के लिए DEBUG ट्रैप सेट करें
trap 'proxy_check' DEBUG

# मैन्युअल रूप से प्रॉक्सी सेटिंग्स जांचने का फ़ंक्शन
checkproxy() {
    echo "HTTP_PROXY: $HTTP_PROXY"
    echo "HTTPS_PROXY: $HTTPS_PROXY"
    echo "Git HTTP Proxy:"
    git config --get http.proxy
    echo "Git HTTPS Proxy:"
    git config --get https.proxy
}
```

#### **यह कैसे काम करता है:**

- `network_commands` ऐरे उन कमांड्स को सूचीबद्ध करता है जो नेटवर्क-संबंधी हैं।
- `display_proxy` सभी प्रासंगिक प्रॉक्सी पर्यावरण चर दिखाता है यदि वे सेट हैं।
- `proxy_check` `BASH_COMMAND` (जो `DEBUG` ट्रैप में उपलब्ध है) का उपयोग करके निष्पादित हो रही कमांड प्राप्त करता है, पहला शब्द निकालता है, और जांचता है कि क्या यह किसी नेटवर्क कमांड से मेल खाता है। यदि प्रॉक्सी चर सेट हैं, तो यह उन्हें प्रदर्शित करता है।
- `trap 'proxy_check' DEBUG` लाइन सुनिश्चित करती है कि `proxy_check` प्रत्येक कमांड से पहले चले।
- `checkproxy` आपको मैन्युअल रूप से प्रॉक्सी सेटिंग्स देखने की अनुमति देता है, जिसमें Git-विशिष्ट प्रॉक्सी कॉन्फ़िगरेशन शामिल हैं।
- इसे `.bashrc` में जोड़ने के बाद, Git-Bash को पुनरारंभ करें या `source ~/.bashrc` चलाकर परिवर्तन लागू करें।

#### **उपयोग:**

- जब आप कोई नेटवर्क कमांड चलाते हैं (जैसे `git clone`, `curl`), यदि प्रॉक्सी सेटिंग्स कॉन्फ़िगर हैं, तो वे कमांड निष्पादन से पहले प्रदर्शित होंगी।
- प्रॉक्सी सेटिंग्स मैन्युअल रूप से देखने के लिए `checkproxy` चलाएँ।

---

### **PowerShell के लिए**

PowerShell में Bash के `DEBUG` ट्रैप का सीधा समकक्ष नहीं है, लेकिन हम समान कार्यक्षमता प्राप्त करने के लिए `PSReadLine` मॉड्यूल के `CommandValidationHandler` का उपयोग कर सकते हैं। यह हैंडलर प्रत्येक कमांड से पहले चलता है, जो हमें नेटवर्क कमांड्स और प्रॉक्सी सेटिंग्स की जांच करने की अनुमति देता है।

#### **चरण:**

1. **नेटवर्क-संबंधी कमांड्स की सूची परिभाषित करें।**
2. **प्रॉक्सी सेटिंग्स प्रदर्शित करने के लिए एक फ़ंक्शन बनाएँ।**
3. **कमांड्स और प्रॉक्सी सेटिंग्स की जांच करने के लिए `CommandValidationHandler` सेट करें।**
4. **मैन्युअल रूप से प्रॉक्सी सेटिंग्स प्रदर्शित करने के लिए एक `checkproxy` फ़ंक्शन परिभाषित करें।**
5. **सभी कॉन्फ़िगरेशन को अपने PowerShell प्रोफाइल में जोड़ें।**

#### **कार्यान्वयन:**

सबसे पहले, PowerShell में `$PROFILE` चलाकर अपनी PowerShell प्रोफाइल फ़ाइल का पता लगाएँ। यदि यह मौजूद नहीं है, तो इसे बनाएँ:

```powershell
New-Item -Type File -Force $PROFILE
```

निम्नलिखित कोड अपनी PowerShell प्रोफाइल (जैसे, `Microsoft.PowerShell_profile.ps1`) में जोड़ें:

```powershell
# नेटवर्क-संबंधी कमांड्स की सूची
$networkCommands = @(
    "gpa",
    "git",
    "ssh",
    "scp",
    "sftp",
    "rsync",
    "curl",
    "wget",
    "apt",
    "yum",
    "dnf",
    "npm",
    "yarn",
    "pip",
    "pip3",
    "gem",
    "cargo",
    "docker",
    "kubectl",
    "ping",
    "traceroute",
    "netstat",
    "ss",
    "ip",
    "ifconfig",
    "dig",
    "nslookup",
    "nmap",
    "telnet",
    "ftp",
    "nc",
    "tcpdump",
    "adb",
    "bundle",
    "brew",
    "cpanm",
    "bundle exec jekyll",
    "make",
    "python",
    "glcoud"
)

# प्रॉक्सी सेटिंग्स प्रदर्शित करने का फ़ंक्शन
function Display-Proxy {
    Write-Host "🚀 **Proxy Settings Detected:**"
    if ($env:HTTP_PROXY) { Write-Host "   - HTTP_PROXY: $env:HTTP_PROXY" }
    if ($env:http_proxy) { Write-Host "   - http_proxy: $env:http_proxy" }
    if ($env:HTTPS_PROXY) { Write-Host "   - HTTPS_PROXY: $env:HTTPS_PROXY" }
    if ($env:https_proxy) { Write-Host "   - https_proxy: $env:https_proxy" }
    if ($env:ALL_PROXY) { Write-Host "   - ALL_PROXY: $env:ALL_PROXY" }
    if ($env:all_proxy) { Write-Host "   - all_proxy: $env:all_proxy" }
    Write-Host ""
}

# निष्पादन से पहले कमांड्स की जांच करने के लिए CommandValidationHandler सेट करें
Set-PSReadLineOption -CommandValidationHandler {
    param($command)
    # कमांड का पहला शब्द निकालें
    $cmd = ($command -split ' ')[0]
    
    if ($networkCommands -contains $cmd) {
        # जांचें कि क्या कोई प्रॉक्सी पर्यावरण चर सेट हैं
        if ($env:HTTP_PROXY -or $env:http_proxy -or $env:HTTPS_PROXY -or $env:https_proxy -or $env:ALL_PROXY -or $env:all_proxy) {
            Display-Proxy
        }
    }
    # कमांड निष्पादन की अनुमति देने के लिए हमेशा true वापस करें
    return $true
}

# मैन्युअल रूप से प्रॉक्सी सेटिंग्स जांचने का फ़ंक्शन
function checkproxy {
    Write-Host "HTTP_PROXY: $env:HTTP_PROXY"
    Write-Host "HTTPS_PROXY: $env:HTTPS_PROXY"
    Write-Host "Git HTTP Proxy:"
    git config --get http.proxy
    Write-Host "Git HTTPS Proxy:"
    git config --get https.proxy
}
```

#### **यह कैसे काम करता है:**

- `$networkCommands` नेटवर्क-संबंधी कमांड्स की एक सरणी है।
- `Display-Proxy` सभी प्रासंगिक प्रॉक्सी पर्यावरण चर प्रदर्शित करता है यदि वे सेट हैं।
- `Set-PSReadLineOption -CommandValidationHandler` एक स्क्रिप्ट ब्लॉक को परिभाषित करता है जो प्रत्येक कमांड से पहले चलता है:
  - यह कमांड का पहला शब्द निकालता है।
  - जांचता है कि क्या यह `$networkCommands` में है।
  - यदि प्रॉक्सी चर सेट हैं, तो यह `Display-Proxy` को कॉल करता है।
  - कमांड के निष्पादन को सुनिश्चित करने के लिए `$true` लौटाता है।
- `checkproxy` प्रॉक्सी सेटिंग्स को मैन्युअल रूप से देखने की अनुमति देता है, जिसमें Git-विशिष्ट प्रॉक्सी शामिल हैं।
- अपने प्रोफाइल में जोड़ने के बाद, PowerShell को पुनरारंभ करें या `. $PROFILE` चलाकर परिवर्तन लागू करें।

#### **आवश्यकताएँ:**

- `PSReadLine` मॉड्यूल आवश्यक है, जो PowerShell 5.1 और बाद के संस्करणों में डिफ़ॉल्ट रूप से शामिल है।
- यदि पुराने संस्करण का उपयोग कर रहे हैं, तो आपको PowerShell को अपग्रेड करना होगा या कोई वैकल्पिक विधि ढूंढनी होगी (यहाँ शामिल नहीं, क्योंकि अधिकांश सिस्टम नए संस्करणों का उपयोग करते हैं)।

#### **उपयोग:**

- जब आप कोई नेटवर्क कमांड चलाते हैं (जैसे `git pull`, `curl`), यदि प्रॉक्सी सेटिंग्स कॉन्फ़िगर हैं, तो वे कमांड निष्पादन से पहले प्रदर्शित होंगी।
- प्रॉक्सी सेटिंग्स मैन्युअल रूप से देखने के लिए `checkproxy` चलाएँ।

---

### **"टर्मिनल" पर नोट्स**

- यदि "टर्मिनल" Windows Terminal को संदर्भित करता है, तो यह Git-Bash, PowerShell, या Command Prompt (cmd.exe) जैसे शेल्स के लिए केवल एक होस्ट है।
- उपरोक्त कार्यान्वयन Windows Terminal में Git-Bash या PowerShell सत्रों के भीतर काम करते हैं।
- Command Prompt (cmd.exe) में समान कार्यक्षमता लागू करना इसकी सीमित स्क्रिप्टिंग क्षमताओं के कारण व्यावहारिक नहीं है। इसके बजाय Git-Bash या PowerShell का उपयोग करने की सिफारिश की जाती है।

---

### **अतिरिक्त विचार**

- **कमांड पार्सिंग:**
  - दोनों कार्यान्वयन केवल कमांड के पहले शब्द की नेटवर्क कमांड्स सूची के विरुद्ध जांच करते हैं। उदाहरण के लिए, `git clone` ट्रिगर करता है क्योंकि `git` सूची में है।
  - `bundle exec jekyll` जैसे मल्टी-वर्ड कमांड्स ट्रिगर करेंगे यदि `bundle` सूची में है, जो अधिकांश मामलों के लिए पर्याप्त है।
  - यदि आवश्यक हो, तो आप कमांड के सभी शब्दों की जांच करने के लिए कोड संशोधित कर सकते हैं, लेकिन इससे गलत सकारात्मक परिणाम हो सकते हैं और आम तौर पर यह अनावश्यक है।

- **प्रॉक्सी चर:**
  - दोनों कार्यान्वयन सामान्य विविधताओं को कवर करने के लिए `HTTP_PROXY`, `http_proxy`, `HTTPS_PROXY`, `https_proxy`, `ALL_PROXY`, और `all_proxy` की जांच करते हैं।
  - Windows में, पर्यावरण चर केस-इनसेंसिटिव होते हैं, लेकिन हम Unix-जैसे व्यवहार (विशेष रूप से Git-Bash में) के साथ स्थिरता के लिए दोनों केस की जांच करते हैं।

- **Git प्रॉक्सी सेटिंग्स:**
  - दोनों शेल्स में `checkproxy` फ़ंक्शन `git config --get` का उपयोग करके Git-विशिष्ट प्रॉक्सी सेटिंग्स प्रदर्शित करता है।

---

### **परीक्षण**

- **प्रॉक्सी चर सेट करें:**
  - Git-Bash में: `export HTTP_PROXY=http://proxy.example.com:8080`
  - PowerShell में: `$env:HTTP_PROXY = "http://proxy.example.com:8080"`
- **नेटवर्क कमांड्स चलाएँ:**
  - `git --version`, `curl -V`, आदि जैसे कमांड्स आज़माएँ।
  - कमांड आउटपुट से पहले प्रॉक्सी सेटिंग्स प्रदर्शित होनी चाहिए।
- **`checkproxy` का उपयोग करें:**
  - प्रॉक्सी सेटिंग्स मैन्युअल रूप से देखने के लिए किसी भी शेल में `checkproxy` चलाएँ।

---

यह कार्यान्वयन Git-Bash और PowerShell दोनों में एक मजबूत प्रॉक्सी चेक प्रदान करता है, जो Windows Terminal या स्टैंडअलोन के भीतर उपयोग के लिए उपयुक्त है।