---
audio: false
generated: true
image: false
lang: hi
layout: post
model: x-ai/grok-4-fast
title: लिबकर्ल कॉन्फ़िगरेशन त्रुटि को ठीक करना
translated: true
type: note
---

यह त्रुटि इस बात का संकेत है कि `configure` स्क्रिप्ट **libcurl** (HTTP/HTTPS अनुरोधों के लिए एक लाइब्रेरी) को ढूंढने का प्रयास कर रही है, लेकिन उसे `curl-config` — एक सहायक बाइनरी जो बताती है कि libcurl कहाँ इंस्टॉल है — का पता नहीं चल पा रहा है।

आपके सिस्टम के आधार पर इसे ठीक करने के तरीके यहाँ दिए गए हैं:

---

### 🧩 Ubuntu / Debian के लिए

यह चलाएँ:

```bash
sudo apt update
sudo apt install libcurl4-openssl-dev
```

फिर पुनः प्रयास करें:

```bash
./configure
```

---

### 🧩 Fedora / CentOS / RHEL के लिए

यह चलाएँ:

```bash
sudo dnf install libcurl-devel
```

---

### 🧩 Arch / Manjaro के लिए

यह चलाएँ:

```bash
sudo pacman -S curl
```

---

### 🔍 इंस्टॉलेशन सत्यापित करने के लिए

इंस्टॉलेशन के बाद, सुनिश्चित करें कि `curl-config` उपलब्ध है:

```bash
which curl-config
```

यदि यह `/usr/bin/curl-config` जैसा कोई पथ दिखाता है, तो सब ठीक है।

फिर दोबारा चलाएँ:

```bash
./configure
make
sudo make install
```

---

क्या आप चाहेंगे कि मैं समझाऊँ कि `curl-config` वास्तव में क्या करता है और `configure` स्क्रिप्ट को इसकी आवश्यकता क्यों होती है?