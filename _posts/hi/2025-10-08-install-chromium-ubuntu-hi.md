---
audio: false
generated: true
image: false
lang: hi
layout: post
model: x-ai/grok-4-fast
title: उबुन्टू 24.04 पर क्रोमियम इंस्टॉल करना
translated: true
type: note
---

**Chromium** को **Ubuntu 24.04** पर इंस्टॉल करने के लिए, आपके पास कुछ अच्छे विकल्प हैं — यह इस बात पर निर्भर करता है कि आप **ऑफिशियल Snap वर्जन**, **APT (Debian पैकेज)** वर्जन, या **मैन्युअल डाउनलोड** चाहते हैं। यहाँ प्रत्येक के लिए एक स्पष्ट गाइड दी गई है:

---

### 🧩 विकल्प 1: Snap के माध्यम से इंस्टॉल करें (ऑफिशियल Ubuntu तरीका)

यह **डिफ़ॉल्ट** और सबसे स्थिर तरीका है।

```bash
sudo snap install chromium
```

फिर इसे इससे रन करें:

```bash
chromium
```

✅ *फायदे:*

* Canonical द्वारा ऑफिशियल रूप से मेंटेन किया गया।
* अपने-आप अपडेट हो जाता है।

⚠️ *नुकसान:*

* स्टार्टअप धीमा हो सकता है।
* Snap सैंडबॉक्सिंग सिस्टम प्रॉक्सी या फ़ाइल एक्सेस में समस्याएँ पैदा कर सकता है।

---

### ⚙️ विकल्प 2: APT के माध्यम से इंस्टॉल करें (Debian पैकेज, Non-Snap)

यदि आपको **Snap पसंद नहीं है**, तो आप Chromium को **थर्ड-पार्टी रेपो** से इंस्टॉल कर सकते हैं, जैसे कि **ungoogled-chromium** या **OBS** द्वारा प्रदान किया गया ऑफिशियल Debian बिल्ड।

1. OBS रेपो को एड करें:

   ```bash
   sudo apt install wget gnupg
   wget -qO - https://download.opensuse.org/repositories/home:/ungoogled_chromium/Ubuntu_24.04/Release.key | sudo gpg --dearmor -o /usr/share/keyrings/ungoogled-chromium.gpg
   ```

2. रिपॉजिटरी को एड करें:

   ```bash
   echo "deb [signed-by=/usr/share/keyrings/ungoogled-chromium.gpg] https://download.opensuse.org/repositories/home:/ungoogled_chromium/Ubuntu_24.04/ /" | sudo tee /etc/apt/sources.list.d/ungoogled-chromium.list
   ```

3. Chromium इंस्टॉल करें:

   ```bash
   sudo apt update
   sudo apt install ungoogled-chromium
   ```

फिर इसे इससे रन करें:

```bash
ungoogled-chromium
```

✅ *फायदे:*

* कोई Snap डिपेंडेंसी नहीं।
* प्राइवेसी-हार्डेंड वर्जन उपलब्ध।

⚠️ *नुकसान:*

* अपडेट Snap की तुलना में धीमे आते हैं।
* कुछ फीचर्स (जैसे Google Sync) डिसेबल होते हैं।

---

### 🧰 विकल्प 3: Chromium के ऑफिशियल बिल्ड से इंस्टॉल करें (मैन्युअल)

यदि आप **नवीनतम डेवलपर वर्जन** चाहते हैं:

```bash
sudo apt install chromium-browser
```

अगर यह Snap पर रीडायरेक्ट करता है और आप उसे अवॉइड करना चाहते हैं:

```bash
sudo apt purge chromium-browser
sudo apt install chromium
```

या, मैन्युअली एक `.deb` फ़ाइल डाउनलोड करें:

```bash
wget https://commondatastorage.googleapis.com/chromium-browser-snapshots/Linux_x64/LAST_CHANGE -O /tmp/LAST_CHANGE
LATEST=$(cat /tmp/LAST_CHANGE)
wget "https://commondatastorage.googleapis.com/chromium-browser-snapshots/Linux_x64/${LATEST}/chrome-linux.zip"
unzip chrome-linux.zip
./chrome-linux/chrome
```

---

### 🌍 प्रॉक्सी सेटिंग्स पर नोट

चूंकि आपका सिस्टम इस्तेमाल कर रहा है:

```
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
```

सुनिश्चित करें कि Snap और APT आपकी प्रॉक्सी का सम्मान करें इन्हें `/etc/environment` या आपके शेल प्रोफाइल में एक्सपोर्ट करके:

```bash
export http_proxy=http://127.0.0.1:7890
export https_proxy=http://127.0.0.1:7890
```

---

क्या आप मुझे यह दिखाना चाहेंगे कि Chromium को स्वचालित रूप से उस प्रॉक्सी का उपयोग कैसे करवाएं (ताकि वह हमेशा उसके माध्यम से कनेक्ट हो)?