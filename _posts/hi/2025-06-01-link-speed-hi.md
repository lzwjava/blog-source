---
audio: false
generated: true
lang: hi
layout: post
title: उबुन्टु में ईथरनेट लिंक स्पीड जांचें
translated: true
type: note
---

Ubuntu में, आप Ethernet लिंक स्पीड की जांच कई कमांड-लाइन टूल्स का उपयोग करके कर सकते हैं। यहां सबसे आम तरीके दिए गए हैं:

### **1. `ethtool` का उपयोग करना (अनुशंसित)**
`ethtool` आपके Ethernet इंटरफेस की वर्तमान लिंक स्पीड की जांच करने का सबसे विश्वसनीय तरीका है।

#### `ethtool` इंस्टॉल करें (अगर पहले से इंस्टॉल नहीं है):
```bash
sudo apt install ethtool
```

#### लिंक स्पीड की जांच करें:
```bash
sudo ethtool <interface_name>
```
`<interface_name>` को अपने Ethernet इंटरफेस (जैसे `eth0`, `enp3s0`, `ens33`) से बदलें।

#### उदाहरण:
```bash
sudo ethtool enp3s0
```
आउटपुट में `Speed` और `Duplex` फ़ील्ड्स को देखें:
```
Settings for enp3s0:
    Supported ports: [ TP ]
    Supported link modes:   10baseT/Half 10baseT/Full 
                            100baseT/Half 100baseT/Full 
                            1000baseT/Full 
    Supported pause frame use: No
    Supports auto-negotiation: Yes
    Supported FEC modes: Not reported
    Advertised link modes:  10baseT/Half 10baseT/Full 
                            100baseT/Half 100baseT/Full 
                            1000baseT/Full 
    Advertised pause frame use: No
    Advertised auto-negotiation: Yes
    Advertised FEC modes: Not reported
    Speed: 1000Mb/s   # <--- यह आपकी वर्तमान लिंक स्पीड है
    Duplex: Full      # <--- फुल या हाफ डुप्लेक्स
    Port: Twisted Pair
    PHYAD: 1
    Transceiver: internal
    Auto-negotiation: on
    MDI-X: on (auto)
    Supports Wake-on: pumbg
    Wake-on: d
    Current message level: 0x00000007 (7)
                           drv probe link
    Link detected: yes      # <--- इंगित करता है कि केबल जुड़ा हुआ है या नहीं
```

---

### **2. `ip` या `ifconfig` का उपयोग करना (बेसिक जानकारी)**
ये कमांड दिखाते हैं कि इंटरफेस चालू है या नहीं, लेकिन सीधे लिंक स्पीड प्रदर्शित नहीं करते हैं।

#### `ip` का उपयोग करना:
```bash
ip link show <interface_name>
```
उदाहरण:
```bash
ip link show enp3s0
```
(स्पीड नहीं दिखाता, सिर्फ लिंक स्टेटस दिखाता है।)

#### `ifconfig` का उपयोग करना (पुराना है लेकिन अभी भी प्रयोग किया जाता है):
```bash
ifconfig <interface_name>
```
उदाहरण:
```bash
ifconfig enp3s0
```
(दिखाता है कि इंटरफेस चालू है या नहीं, लेकिन स्पीड नहीं दिखाता।)

---

### **3. `dmesg` का उपयोग करना (ड्राइवर लॉग्स की जांच)**
कभी-कभी, Ethernet ड्राइवर प्रारंभीकरण के दौरान परकालित स्पीड को लॉग करता है।
```bash
dmesg | grep <interface_name>
```
उदाहरण:
```bash
dmesg | grep enp3s0
```
इस तरह की लाइनों को देखें:
```
[    5.123456] e1000e: enp3s0 NIC Link is Up 1000 Mbps Full Duplex
```

---

### **4. `mii-tool` का उपयोग करना (पुराने सिस्टम)**
बहुत पुराने सिस्टम के लिए, `mii-tool` काम कर सकता है:
```bash
sudo apt install net-tools
sudo mii-tool -v <interface_name>
```
उदाहरण:
```bash
sudo mii-tool -v eth0
```
(आधुनिक Gigabit Ethernet के लिए अनुशंसित नहीं है।)

---

### **सारांश**
- **सबसे अच्छा तरीका:** `sudo ethtool <interface>` (स्पीड, डुप्लेक्स, ऑटो-निगोशिएशन दिखाता है)।
- **वैकल्पिक:** अगर `ethtool` उपलब्ध नहीं है तो `dmesg` लॉग्स की जांच करें।
- **बेसिक स्टेटस:** `ip link show` या `ifconfig` (स्पीड नहीं दिखाता है)।

अगर आपको और स्पष्टीकरण की आवश्यकता हो तो बताएं! 🚀