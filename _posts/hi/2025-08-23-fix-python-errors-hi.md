---
audio: false
generated: true
image: false
lang: hi
layout: post
title: पायथन नॉट फाउंड एरर को आसानी से ठीक करें
translated: true
type: note
---

हाँ — `"python not found"` त्रुटि इसलिए होती है क्योंकि आपके लॉगिन शेल का `PATH` वातावरण उस इंटरएक्टिव शेल से भिन्न हो सकता है जहाँ आप आमतौर पर `python` टाइप करते हैं। जब आप `subprocess.run(..., shell=True)` कॉल करते हैं, तो सबशेल `python` को नहीं देख पाता यदि वह `$PATH` पर नहीं है या यदि आपके सिस्टम पर केवल `python3` है।

इसे ठीक करने के विश्वसनीय तरीके यहाँ दिए गए हैं:

---

### 1. `sys.executable` का उपयोग करें

`python` के मिलने पर निर्भर रहने के बजाय, हमेशा वर्तमान में चल रहे Python इंटरप्रेटर को कॉल करें:

```python
import os
import subprocess
import platform
import sys

def gpa():
    python_exec = sys.executable  # वर्तमान Python इंटरप्रेटर का पूरा पथ

    system = platform.system()
    if system == "Linux":
        shell_command = f"bash -l -c '{python_exec} ~/bin/gitmessageai.py --api deepseek --allow-pull-push'"
    elif system == "Darwin":
        shell_command = f"zsh -l -c '{python_exec} ~/bin/gitmessageai.py --api deepseek --allow-pull-push'"
    else:  # Windows
        shell_command = f'cmd.exe /c "{python_exec} %USERPROFILE%\\bin\\gitmessageai.py --api deepseek --allow-pull-push"'

    subprocess.run(shell_command, shell=True)
```

यह गारंटी देता है कि वही Python जिससे आपने अपनी स्क्रिप्ट चलाई थी, वह `gitmessageai.py` को भी चलाएगा।

---

### 2. `python` और `python3` दोनों को आज़माएँ

कुछ सिस्टम पर केवल `python3` होता है। आप उपलब्धता का पता लगा सकते हैं:

```python
import shutil

python_exec = shutil.which("python") or shutil.which("python3")
```

फिर उसे अपनी कमांड स्ट्रिंग में प्रतिस्थापित करें।

---

### 3. एक ज्ञात पथ को हार्डकोड करें

यदि आप पूर्ण पथ जानते हैं (जैसे `/usr/bin/python3` या `/opt/homebrew/bin/python3`), तो आप सीधे उसे अपनी कमांड में उपयोग कर सकते हैं।

---

👉 सबसे मजबूत समाधान **विकल्प 1 (`sys.executable`)** है, क्योंकि यह आपकी स्क्रिप्ट चला रहे इंटरप्रेटर का ही उपयोग करता है। इस तरह आप कभी भी `$PATH` पर निर्भर नहीं रहते।

क्या आप चाहते हैं कि मैं आपके फ़ंक्शन को `sys.executable` के साथ पूरी तरह से एकीकृत करके पुनर्गठित कर दूँ ताकि आपको फिर कभी `"python not found"` त्रुटि न दिखे?