---
audio: false
date: 2025-09-09
generated: true
image: false
lang: hi
layout: post
model: openai/gpt-5
title: लिबरचैट में OPENAI_REVERSE_PROXY सेट करना
translated: true
type: note
---

ठीक है 👍 आप LibreChat के `.env` को एडिट कर रहे हैं।
यह फ़ाइल वही जगह है जहाँ आप `OPENAI_REVERSE_PROXY` सेट करते हैं।

---

### 1. वेरिएबल क्या करता है

`OPENAI_REVERSE_PROXY` का उपयोग तब किया जाता है यदि आप चाहते हैं कि LibreChat, OpenAI API रिक्वेस्ट्स को एक प्रॉक्सी सर्वर के माध्यम से रूट करे (उदाहरण के लिए, यदि आप लॉगिंग, कैशिंग, रीजनल रूटिंग जोड़ने या प्रतिबंधों को बायपास करने के लिए एक सेल्फ-होस्टेड रिवर्स प्रॉक्सी चलाते हैं)।

---

### 2. इसे कैसे सेट करें

अपने `.env` फ़ाइल में, बस इसे अपने रिवर्स प्रॉक्सी का बेस URL असाइन करें। उदाहरण के लिए:

```ini
OPENAI_REVERSE_PROXY=https://your-proxy.example.com/v1
```

नोट्स:

* URL को आपके प्रॉक्सी एंडपॉइंट की ओर इशारा करना चाहिए जो OpenAI के `/v1` API की नकल करता हो।
* अंत में `/` शामिल न करें, जब तक कि आपके प्रॉक्सी की इसकी आवश्यकता न हो।
* यदि आप प्रॉक्सी का उपयोग नहीं कर रहे हैं तो इसे खाली रखें (`OPENAI_REVERSE_PROXY=`)।

---

### 3. बदलावों को लागू करें

`.env` को एडिट करने के बाद, अपने LibreChat कंटेनर्स को रीस्टार्ट करें ताकि वे नए वैल्यू को पिकअप कर सकें:

```bash
docker compose down
docker compose up -d --force-recreate
```

---

### 4. वेरिफाई करें

आप चल रहे कंटेनर के अंदर चेक कर सकते हैं:

```bash
docker compose exec librechat env | grep OPENAI_REVERSE_PROXY
```

इसे आपके द्वारा सेट किया गया वैल्यू दिखाना चाहिए।

---

क्या आपके पास पहले से ही कोई रिवर्स प्रॉक्सी URL है (जैसे `https://openrouter.ai/api/v1` या आपका अपना Nginx इंस्टेंस), या क्या आप चाहते हैं कि मैं आपको दिखाऊं कि LibreChat के लिए इसे **कैसे सेट अप करें**?