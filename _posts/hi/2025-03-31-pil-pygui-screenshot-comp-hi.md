---
audio: false
generated: true
image: false
lang: hi
layout: post
title: पीआईएल बनाम पायऑटोजीयूआई स्क्रीनशॉट तुलना
translated: true
type: note
---

दोनों **PIL का `ImageGrab`** और **PyAutoGUI के स्क्रीनशॉट टूल्स** आपको स्क्रीन कैप्चर करने देते हैं, लेकिन वे उपयोग, सुविधाओं और क्रॉस-प्लेटफ़ॉर्म सपोर्ट में भिन्न हैं।

---

## 1. PIL (Pillow) – `ImageGrab`

`ImageGrab` **Pillow (आधुनिक PIL फोर्क)** का हिस्सा है। यह आपको स्क्रीन या उसके एक हिस्से को कैप्चर करने की अनुमति देता है।

### यह कैसे काम करता है

* **Windows और macOS** पर, यह पिक्सेल कैप्चर करने के लिए नेटिव APIs (Windows पर GDI, macOS पर Quartz) का उपयोग करता है।
* **Linux** पर, यह डिफ़ॉल्ट रूप से सपोर्टेड नहीं है (इसे `xwd` जैसी अतिरिक्त लाइब्रेरीज़ की आवश्यकता होती है)।

### उदाहरण उपयोग

```python
from PIL import ImageGrab

# पूरी स्क्रीन कैप्चर करें
screenshot = ImageGrab.grab()
screenshot.save("screenshot.png")

# एक क्षेत्र कैप्चर करें (बाएं, ऊपर, दाएं, नीचे)
region = (100, 100, 500, 500)
cropped_screenshot = ImageGrab.grab(bbox=region)
cropped_screenshot.show()
```

### फायदे

* सरल, Pillow का हिस्सा (कोई अतिरिक्त डिपेंडेंसी नहीं)।
* अच्छा जब आप पहले से ही इमेज प्रोसेसिंग के लिए Pillow के साथ काम कर रहे हैं।

### नुकसान

* Linux पर डिफ़ॉल्ट रूप से कोई सपोर्ट नहीं।
* कम लचीलापन (मल्टी-मॉनिटर जागरूकता नहीं)।

---

## 2. PyAutoGUI – `screenshot()`

PyAutoGUI ऑटोमेशन (माउस/कीबोर्ड + स्क्रीन कैप्चर) के लिए अधिक सामान्य-उद्देश्य वाला है।

### यह कैसे काम करता है

* **आंतरिक रूप से Pillow को रैप करता है**।
* OS APIs का उपयोग करता है (क्रॉस-प्लेटफ़ॉर्म: Windows, macOS, Linux)।
* सीधे एक Pillow `Image` वापस कर सकता है या फ़ाइल में सेव कर सकता है।

### उदाहरण उपयोग

```python
import pyautogui

# पूरी स्क्रीन कैप्चर करें
screenshot = pyautogui.screenshot()
screenshot.save("screenshot.png")

# एक क्षेत्र कैप्चर करें (बाएं, ऊपर, चौड़ाई, ऊँचाई)
region_screenshot = pyautogui.screenshot(region=(100, 100, 400, 400))
region_screenshot.show()
```

### फायदे

* क्रॉस-प्लेटफ़ॉर्म (Windows, macOS, Linux)।
* ऑटोमेशन स्क्रिप्ट (माउस/कीबोर्ड कंट्रोल) के साथ एकीकृत करने में आसान।
* सीधे विशिष्ट क्षेत्रों को कैप्चर कर सकता है।

### नुकसान

* `ImageGrab` की तुलना में थोड़ा धीमा क्योंकि यह अधिक एब्स्ट्रैक्ट करता है।
* अतिरिक्त डिपेंडेंसी (`pip install pyautogui`)।

---

## आपको किसका उपयोग करना चाहिए?

* **यदि आपको केवल स्क्रीनशॉट की आवश्यकता है और आप Windows/macOS पर हैं** → `ImageGrab` हल्का और तेज़ है।
* **यदि आपको ऑटोमेशन या Linux सपोर्ट की भी आवश्यकता है** → `pyautogui` बेहतर है।

---

👉 क्या आप चाहते हैं कि मैं आपको Python कोड के साथ `ImageGrab` और `pyautogui.screenshot()` के बीच प्रदर्शन अंतर का **बेंचमार्क कैसे करें** यह भी दिखाऊं?