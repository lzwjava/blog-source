---
audio: false
generated: true
image: false
lang: hi
layout: post
title: सोर्सग्राफ क्वेरी भाषा गाइड
translated: true
type: note
---

Sourcegraph खोज की अपनी एक क्वेरी भाषा है जो लचीली और शक्तिशाली है। यहां आमतौर पर इस्तेमाल होने वाले सिंटैक्स का विवरण दिया गया है:

---

### 1. **बुनियादी कीवर्ड खोज**

* बस शब्द टाइप करें:

  ```
  http client
  ```

  दोनों शब्दों वाली फाइलें या कोड ढूंढता है।

---

### 2. **लॉजिकल ऑपरेटर्स**

* `AND`, `OR`, `NOT` (अपरकेस में होने चाहिए):

  ```
  React AND useEffect
  React OR Vue
  logger NOT debug
  ```

---

### 3. **फ़िल्टर्स**

* `repo:` → रिपॉजिटरी के अनुसार सीमित करें

  ```
  repo:github.com/myorg/myrepo
  ```
* `file:` → फाइल पथ से मिलान करें (regex समर्थित)

  ```
  file:\.java$
  ```
* `lang:` → एक प्रोग्रामिंग भाषा तक सीमित करें

  ```
  lang:python requests
  ```
* `content:` → टेक्स्ट खोज को बाध्य करें (संरचनात्मक मिलानों को नज़रअंदाज करता है)

  ```
  content:"SELECT * FROM"
  ```
* `case:yes` → खोज को केस-सेंसिटिव बनाएं

---

### 4. **रेगेक्स मिलान**

* डिफ़ॉल्ट रूप से, Sourcegraph regex का समर्थन करता है।

  ```
  foo(bar|baz)
  ^class\s+\w+
  ```

---

### 5. **संरचनात्मक खोज**

कोड संरचनाओं से मिलान करने के लिए `:[var]` जैसे पैटर्न होल्स का उपयोग करें:

```
if (:[cond]) { :[body] }
```

---

### 6. **रिविज़न / ब्रांच खोज**

* `@branch`, `@commit`, या `@tag` जोड़ें:

  ```
  repo:myrepo@main lang:go fmt.Println
  ```

---

### 7. **सिंबल्स और परिभाषाएँ**

* `type:symbol` → सिंबल नामों की खोज करें
* `type:commit` → कमिट संदेशों की खोज करें
* `type:diff` → कोड डिफ्स की खोज करें

---

### 8. **उदाहरण**

* Go फाइलों में सभी `TODO` कमेंट्स ढूंढें:

  ```
  TODO lang:go
  ```
* Java मेथड्स के लिए केवल `src/` फोल्डर में खोजें:

  ```
  file:^src/ lang:java "public static"
  ```
* एकाधिक रिपॉजिटरीज में खोजें:

  ```
  repo:github.com/org/.* file:\.ts$ "useState"
  ```

---

👉 क्या आप चाहते हैं कि मैं **सबसे अधिक इस्तेमाल होने वाले Sourcegraph क्वेरीज़ की एक त्वरित चीट शीट** तैयार करूं (जैसे कि एक-लाइनर्स जिन्हें आप दैनिक पुन: उपयोग कर सकते हैं), या **उन्नत पैटर्न के साथ एक गहन जानकारी**?