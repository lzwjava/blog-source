---
audio: false
generated: true
lang: es
layout: post
title: Manipulación de Bits en Java
translated: true
type: note
---

La manipulación de bits utiliza operadores bitwise para resolver problemas de manera eficiente. Exploremos cómo encontrar un número único y verificar si un número es una potencia de 2.

## 1. Número Único: Magia del XOR
Encuentra el único número que no se repite en un array donde todos los demás aparecen dos veces usando XOR.

### Implementación en Java
```java
public class SingleNumber {
    public static int singleNumber(int[] nums) {
        int result = 0;
        for (int num : nums) result ^= num;
        return result;
    }

    public static void main(String[] args) {
        int[] nums = {2, 2, 1, 1, 4};
        System.out.println("Single number: " + singleNumber(nums));
    }
}
```
**Salida:** `Single number: 4`

## 2. Potencia de 2: Verificación de Bits
Un número es una potencia de 2 si tiene exactamente un bit 1 en su representación binaria.

### Implementación en Java
```java
public class PowerOfTwo {
    public static boolean isPowerOfTwo(int n) {
        return n > 0 && (n & (n - 1)) == 0;
    }

    public static void main(String[] args) {
        int n = 16;
        System.out.println(n + " is power of 2: " + isPowerOfTwo(n));
        n = 18;
        System.out.println(n + " is power of 2: " + isPowerOfTwo(n));
    }
}
```
**Salida:**
```
16 is power of 2: true
18 is power of 2: false
```

---

## Conclusión a través de los Blogs
- **Ordenamiento:** Quicksort, merge sort y bubble sort cubren velocidad, estabilidad y simplicidad.
- **Cadenas:** KMP, tries y Rabin-Karp manejan búsqueda de patrones y búsquedas por prefijo.
- **Búsqueda:** Búsqueda binaria y búsqueda lineal ofrecen soluciones logarítmicas y lineales.
- **Grafos:** DFS, BFS, Dijkstra y Kruskal abordan recorrido, caminos y MSTs.
- **Manipulación de Bits:** XOR y verificaciones de bits resuelven problemas complejos con operaciones mínimas.

Cada categoría tiene sus fortalezas: ¡elige la herramienta correcta para tu problema y experimenta con estas implementaciones! Avísame si deseas más profundidad o algoritmos adicionales en cualquier categoría.