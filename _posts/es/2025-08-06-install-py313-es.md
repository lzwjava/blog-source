---
audio: false
generated: true
image: false
lang: es
layout: post
title: Instalar los Encabezados de Desarrollo de Python 3.13
translated: true
type: note
---

Este nuevo error revela el último obstáculo para compilar `sentencepiece`: **faltan los headers de desarrollo de Python**.

---

### ❌ Error:

```
fatal error: Python.h: No such file or directory
```

Esto significa que el compilador no puede encontrar `Python.h`, que es necesario para compilar extensiones C de Python.

---

### ✅ Solución

Necesitas instalar los headers de desarrollo de Python 3.13:

#### Para Ubuntu/Debian (Python 3.13):

Dado que Python 3.13 es muy nuevo, si estás usando una compilación personalizada o `pyenv`, es posible que los headers no estén instalados por defecto. Prueba una de las siguientes opciones:

1. **Si estás usando Python 3.13 a nivel del sistema** (instalado desde deadsnakes o desde el código fuente):

```bash
sudo apt install python3.13-dev
```

2. **Si estás usando `pyenv`:**

```bash
pyenv install 3.13.0 --with-openssl --enable-shared
```

Y asegúrate de que tu entorno virtual esté usando esta versión.

---

### 🔁 Luego Reintenta:

Una vez que los headers estén instalados, intenta de nuevo:

```bash
pip install sentencepiece --use-pep517
```

---

Avísame si estás usando una instalación personalizada de Python (por ejemplo, compilada manualmente, mediante `pyenv`, `conda`, etc.), y puedo ofrecerte una guía más específica.