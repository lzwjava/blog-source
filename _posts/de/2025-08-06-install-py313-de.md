---
audio: false
generated: true
image: false
lang: de
layout: post
title: Installieren Sie die Python 3.13 Entwicklungsheader
translated: true
type: note
---

Dieser neue Fehler zeigt das letzte Hindernis beim Bau von `sentencepiece`: **Fehlende Python-Entwicklungsheader**.

---

### ❌ Fehler:

```
fatal error: Python.h: No such file or directory
```

Dies bedeutet, dass der Compiler `Python.h` nicht finden kann, welche zum Kompilieren von Python-C-Erweiterungen notwendig ist.

---

### ✅ Lösung

Sie müssen die Python 3.13 Entwicklungsheader installieren:

#### Für Ubuntu/Debian (Python 3.13):

Da Python 3.13 sehr neu ist, sind die Header bei einer benutzerdefinierten Installation oder bei Verwendung von `pyenv` möglicherweise nicht standardmäßig installiert. Versuchen Sie eines der folgenden Befehle:

1. **Falls Sie eine systemweite Python 3.13 Installation verwenden** (installiert über deadsnakes oder aus den Quellen):

```bash
sudo apt install python3.13-dev
```

2. **Falls Sie `pyenv` verwenden:**

```bash
pyenv install 3.13.0 --with-openssl --enable-shared
```

Stellen Sie sicher, dass Ihre virtuelle Umgebung diese Version verwendet.

---

### 🔁 Danach erneut versuchen:

Sobald die Header installiert sind, versuchen Sie es erneut:

```bash
pip install sentencepiece --use-pep517
```

---

Lassen Sie mich wissen, falls Sie eine benutzerdefinierte Python-Installation verwenden (z.B. manuell kompiliert, via `pyenv`, `conda`, etc.), dann kann ich spezifischere Anleitung geben.