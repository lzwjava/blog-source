---
audio: false
generated: true
image: false
lang: fr
layout: post
title: En‐têtes de développement Python 3.13
translated: true
type: note
---

Cette nouvelle erreur révèle le dernier obstacle à la compilation de `sentencepiece` : **l'absence des en-têtes de développement Python**.

---

### ❌ Erreur :

```
fatal error: Python.h: No such file or directory
```

Cela signifie que le compilateur ne trouve pas `Python.h`, qui est nécessaire pour compiler les extensions C de Python.

---

### ✅ Solution

Vous devez installer les en-têtes de développement pour Python 3.13 :

#### Pour Ubuntu/Debian (Python 3.13) :

Étant donné que Python 3.13 est très récent, si vous utilisez une compilation personnalisée ou `pyenv`, les en-têtes peuvent ne pas être installés par défaut. Essayez l'une des solutions suivantes :

1. **Si vous utilisez Python 3.13 système** (installé depuis deadsnakes ou depuis les sources) :

```bash
sudo apt install python3.13-dev
```

2. **Si vous utilisez `pyenv` :**

```bash
pyenv install 3.13.0 --with-openssl --enable-shared
```

Et assurez-vous que votre environnement virtuel utilise cette version.

---

### 🔁 Puis Réessayez :

Une fois les en-têtes installés, réessayez :

```bash
pip install sentencepiece --use-pep517
```

---

Faites-moi savoir si vous utilisez une installation Python personnalisée (par exemple, compilée manuellement, via `pyenv`, `conda`, etc.), et je pourrai vous donner des instructions plus spécifiques.